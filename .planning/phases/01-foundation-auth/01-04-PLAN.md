---
phase: 01-foundation-auth
plan: "04"
type: execute
wave: 2
depends_on:
  - 01-02
files_modified:
  - backend/prisma/schema.prisma
  - backend/prisma/migrations/*
  - backend/src/prisma/prisma.module.ts
  - backend/src/prisma/prisma.service.ts
  - backend/src/users/users.module.ts
  - backend/src/users/users.service.ts
  - backend/src/auth/auth.module.ts
  - backend/src/app.module.ts
autonomous: true
requirements:
  - AUTH-01
must_haves:
  truths:
    - "New accounts have a persistent user record in SQLite ready for signup and login."
    - "Backend modules expose typed user lookup and creation contracts before endpoint work begins."
    - "The Nest app knows about Prisma, users, and auth modules as first-class dependencies."
  artifacts:
    - path: backend/prisma/schema.prisma
      provides: "SQLite `User` model for auth persistence."
    - path: backend/src/users/users.service.ts
      provides: "Typed `createUser`, `findByEmail`, and `findById` contracts."
    - path: backend/src/prisma/prisma.service.ts
      provides: "Nest DI wrapper over Prisma client."
  key_links:
    - from: backend/src/users/users.service.ts
      to: backend/prisma/schema.prisma
      via: "Prisma user persistence"
      pattern: "prisma.user"
    - from: backend/src/app.module.ts
      to: backend/src/auth/auth.module.ts
      via: "module registration"
      pattern: "AuthModule"
---

<objective>
Create the backend persistence and module contracts that auth endpoints will build on.

Purpose: Separate database/module wiring from endpoint implementation so backend verification can fail earlier and more locally.
Output: Prisma SQLite schema, Prisma module/service, Users module/service, and auth module registration stub.
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@.planning/phases/01-foundation-auth/01-02-PLAN.md

<interfaces>
Create these contracts exactly so Plan 01-05 can implement against them:

```ts
createUser(email: string, passwordHash: string)
findByEmail(email: string)
findById(id: string)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the SQLite auth persistence layer</name>
  <files>backend/prisma/schema.prisma, backend/prisma/migrations/*, backend/src/prisma/prisma.module.ts, backend/src/prisma/prisma.service.ts</files>
  <action>Create Prisma SQLite persistence with `provider = "sqlite"` and `url = env("DATABASE_URL")`. Define a `User` model with `id`, unique `email`, `passwordHash`, `createdAt`, and `updatedAt`. Generate the initial migration under `backend/prisma/migrations/`. Create `PrismaModule` and `PrismaService` for Nest DI. Do not add conversation or message tables in this phase.</action>
  <verify>
    <automated>pnpm install && pnpm --filter backend prisma generate && pnpm --filter backend prisma migrate dev --name init-auth --skip-seed</automated>
  </verify>
  <done>The backend has a typed SQLite user store and a checked-in initial migration dedicated to auth persistence.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the users module contracts and auth module stub into Nest</name>
  <files>backend/src/users/users.module.ts, backend/src/users/users.service.ts, backend/src/auth/auth.module.ts, backend/src/app.module.ts</files>
  <action>Create `UsersService` methods `createUser`, `findByEmail`, and `findById` backed by Prisma. Add `UsersModule` and a minimal `AuthModule` stub so `backend/src/app.module.ts` can register `PrismaModule`, `UsersModule`, and `AuthModule` without waiting for endpoint code. Keep this plan focused on contracts and DI wiring only; controller and strategy code belongs to Plan 01-05.</action>
  <verify>
    <automated>pnpm install && pnpm --filter backend build && rg -n 'createUser|findByEmail|findById|AuthModule|UsersModule|PrismaModule' backend/src/users/users.service.ts backend/src/app.module.ts backend/src/auth/auth.module.ts</automated>
  </verify>
  <done>The Nest app compiles with persistence and user contracts in place, enabling the auth feature plan to stay scoped to auth behavior.</done>
</task>

</tasks>

<verification>
Generate Prisma client, apply the initial migration, and confirm the backend still builds after module registration.
</verification>

<success_criteria>
Backend persistence is ready when the `User` schema, migration, and typed user service contracts exist and the Nest app compiles with them registered.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-04-SUMMARY.md`
</output>
