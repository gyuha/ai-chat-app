---
phase: 01-foundation-auth
plan: "03"
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - frontend/src/app/router.tsx
  - frontend/src/app/providers.tsx
  - frontend/src/app/query-client.ts
  - frontend/src/lib/api/client.ts
  - frontend/src/features/auth/api.ts
  - frontend/src/features/auth/auth.store.ts
  - frontend/src/features/auth/session.ts
  - frontend/src/routes/__root.tsx
  - frontend/src/routes/login.tsx
  - frontend/src/routes/signup.tsx
  - frontend/src/routes/index.tsx
  - frontend/src/components/auth/auth-card.tsx
  - frontend/src/components/auth/login-form.tsx
  - frontend/src/components/auth/signup-form.tsx
  - frontend/src/components/auth/session-loader.tsx
  - frontend/src/components/ui/*
  - frontend/src/styles.css
  - frontend/src/main.tsx
  - frontend/src/routeTree.gen.ts
  - frontend/tests/auth-routing.test.tsx
  - frontend/tests/auth-forms.test.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
must_haves:
  truths:
    - "User can reach dedicated `/login` and `/signup` screens with only email/password auth affordances per D-01 and D-03."
    - "Successful signup or login transitions directly into authenticated app bootstrap with no extra interstitial."
    - "Refreshing the app restores session once via the backend session endpoint per D-05."
    - "Expired or invalid sessions immediately clear client auth state and redirect to `/login` per D-04."
  artifacts:
    - path: frontend/src/routes/login.tsx
      provides: "Login route and invalid-credential UI state."
    - path: frontend/src/routes/signup.tsx
      provides: "Signup route and duplicate-email / validation UI state."
    - path: frontend/src/features/auth/session.ts
      provides: "Single-source session query and unauthorized handling."
    - path: frontend/tests/auth-routing.test.tsx
      provides: "Automated verification for bootstrap, redirects, and session restore."
  key_links:
    - from: frontend/src/components/auth/login-form.tsx
      to: frontend/src/features/auth/api.ts
      via: "form submit mutation calling /auth/login"
      pattern: "/auth/login"
    - from: frontend/src/features/auth/session.ts
      to: backend GET /auth/session
      via: "query function with credentials include"
      pattern: "/auth/session"
    - from: frontend/src/routes/index.tsx
      to: frontend/src/features/auth/session.ts
      via: "beforeLoad or route guard redirect"
      pattern: "/login"
---

<objective>
Wire the React auth experience: login/signup screens, one-time session restoration, and redirect behavior for protected app entry.

Purpose: Finish the user-visible side of AUTH-01, AUTH-02, and AUTH-03 using the approved UI spec and backend auth contract.
Output: Auth routes, API client, session query/store wiring, auth forms, and frontend tests.
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@.planning/phases/01-foundation-auth/01-UI-SPEC.md
@.planning/phases/01-foundation-auth/01-02-PLAN.md
@backend/src/auth/auth.controller.ts
@backend/src/auth/dto/login.dto.ts
@backend/src/auth/dto/signup.dto.ts
@backend/src/auth/dto/session-user.dto.ts

<interfaces>
Frontend must consume these payload shapes exactly:

```ts
type AuthUser = { id: string; email: string }
type AuthResponse = { user: AuthUser }
type LogoutResponse = { ok: true }
type LoginPayload = { email: string; password: string }
type SignupPayload = { email: string; password: string }
```

All auth fetches must send `credentials: 'include'`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the frontend auth API client, query bootstrap, and protected routing</name>
  <files>frontend/src/app/router.tsx, frontend/src/app/providers.tsx, frontend/src/app/query-client.ts, frontend/src/lib/api/client.ts, frontend/src/features/auth/api.ts, frontend/src/features/auth/auth.store.ts, frontend/src/features/auth/session.ts, frontend/src/routes/__root.tsx, frontend/src/routes/index.tsx, frontend/src/main.tsx</files>
  <read_first>.planning/phases/01-foundation-auth/01-CONTEXT.md, .planning/phases/01-foundation-auth/01-RESEARCH.md, .planning/phases/01-foundation-auth/01-UI-SPEC.md, frontend/src/main.tsx, frontend/src/app/router.tsx, frontend/src/lib/api/client.ts, frontend/src/features/auth/api.ts, frontend/src/features/auth/session.ts, frontend/src/routes/__root.tsx, frontend/src/routes/index.tsx, backend/src/auth/auth.controller.ts</read_first>
  <behavior>
    - Test 1: App boot calls `/auth/session` once with `credentials: 'include'`.
    - Test 2: An unauthenticated visit to the protected index route redirects to `/login`.
    - Test 3: A `401` from session restore clears auth state and routes to `/login` immediately.
  </behavior>
  <action>Create a frontend fetch client in `frontend/src/lib/api/client.ts` that prefixes requests with `import.meta.env.VITE_API_BASE_URL` and always sets `credentials: 'include'`. Create auth API helpers for `signup`, `login`, `logout`, `getSession`, and `refreshSession` hitting `/auth/signup`, `/auth/login`, `/auth/logout`, `/auth/session`, and `/auth/refresh`. Build a single TanStack Query session query in `frontend/src/features/auth/session.ts` with `retry: false` and unauthorized handling that clears the auth store and redirects to `/login`. Create router/provider wiring so the protected root/index route uses TanStack Router redirect logic before loading protected content. Preserve D-04 by redirecting immediately on `401`, not by showing a modal or retry loop.</action>
  <acceptance_criteria>`frontend/src/lib/api/client.ts` contains the exact string `credentials: 'include'`; `frontend/src/features/auth/api.ts` contains the exact endpoint strings `/auth/signup`, `/auth/login`, `/auth/logout`, `/auth/session`, and `/auth/refresh`; `frontend/src/routes/index.tsx` contains a redirect target string `/login`; `frontend/src/features/auth/session.ts` contains `retry: false`.</acceptance_criteria>
  <verify>
    <automated>pnpm --dir frontend test -- auth-routing.test.tsx && pnpm --dir frontend build</automated>
  </verify>
  <done>The SPA has a single auth bootstrap path and a protected entry route that behaves correctly for authenticated and unauthenticated users.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement login and signup screens that match the approved auth contract</name>
  <files>frontend/src/routes/login.tsx, frontend/src/routes/signup.tsx, frontend/src/components/auth/auth-card.tsx, frontend/src/components/auth/login-form.tsx, frontend/src/components/auth/signup-form.tsx, frontend/src/components/auth/session-loader.tsx, frontend/src/components/ui/*, frontend/src/styles.css</files>
  <read_first>.planning/phases/01-foundation-auth/01-UI-SPEC.md, .planning/phases/01-foundation-auth/01-CONTEXT.md, frontend/src/routes/login.tsx, frontend/src/routes/signup.tsx, frontend/src/components/auth/auth-card.tsx, frontend/src/components/auth/login-form.tsx, frontend/src/components/auth/signup-form.tsx, frontend/src/components/auth/session-loader.tsx, frontend/src/styles.css</read_first>
  <behavior>
    - Test 1: `/login` renders `로그인`, `이메일`, `비밀번호`, and the primary CTA text `로그인하기`.
    - Test 2: `/signup` renders only email/password auth fields and no social-login or password-reset affordances.
    - Test 3: Invalid email shows a field-level inline error on blur and submit.
    - Test 4: Pending submit swaps the button label to a loading state and shows an accent-colored spinner.
  </behavior>
  <action>Build a single centered auth card on desktop and full-width stacked layout on mobile using the UI spec values from `01-UI-SPEC.md`. Implement only two modes: `로그인` and `회원가입`. Required fields are `이메일` and `비밀번호`; add `비밀번호 확인` on signup if client-side confirmation is implemented, otherwise do not invent extra fields. Use the exact copy `로그인하기`, `로그인이 필요합니다`, `이메일과 비밀번호로 로그인하면 채팅을 시작할 수 있습니다.`, and `로그인 정보를 확인하지 못했습니다. 다시 로그인해 주세요.`. Validate email format on blur and submit, validate password presence on submit, and render one inline error per field below the field. Keep the submit button enabled until the request starts, then replace the label with a short loading state and show a spinner colored `#C26A3D`. Do not add password reset links, social login buttons, OAuth buttons, celebratory success screens, or logout confirmation modal per D-03 and the UI spec.</action>
  <acceptance_criteria>`frontend/src/routes/login.tsx` contains the exact strings `로그인`, `이메일`, `비밀번호`, and `로그인하기`; `frontend/src/routes/signup.tsx` contains `회원가입`; `rg -n "OAuth|Google|GitHub|비밀번호 재설정|Forgot password" frontend/src/routes frontend/src/components/auth` returns no matches; `frontend/src/styles.css` or auth components contain the exact accent color `#C26A3D` for the loading indicator.</acceptance_criteria>
  <verify>
    <automated>pnpm --dir frontend test -- auth-forms.test.tsx && pnpm --dir frontend build</automated>
  </verify>
  <done>Users can complete login and signup flows through explicit email/password-only screens that match the approved copy and interaction contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add frontend tests for auth routing, form states, and session expiry behavior</name>
  <files>frontend/tests/auth-routing.test.tsx, frontend/tests/auth-forms.test.tsx</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/01-foundation-auth/01-UI-SPEC.md, frontend/src/features/auth/session.ts, frontend/src/routes/login.tsx, frontend/src/routes/signup.tsx, frontend/tests/auth-routing.test.tsx, frontend/tests/auth-forms.test.tsx</read_first>
  <behavior>
    - Test 1: Visiting the protected route while unauthenticated redirects to `/login`.
    - Test 2: Successful mocked session restore lands on the protected app route.
    - Test 3: Invalid credentials show the form-level error message from the UI spec.
    - Test 4: Expired session `401` clears auth state and returns the app to `/login`.
  </behavior>
  <action>Create Vitest + Testing Library coverage in `frontend/tests/auth-routing.test.tsx` and `frontend/tests/auth-forms.test.tsx`. Mock the auth API layer, not DOM globals, so route and form tests exercise real component wiring. Assert the protected route redirect path `/login`, the session endpoint path `/auth/session`, the CTA `로그인하기`, and the form-level error text `로그인 정보를 확인하지 못했습니다. 다시 로그인해 주세요.`. Add a test that ensures auth fetches use `credentials: 'include'` either by inspecting the client helper or mocking fetch arguments. Ensure tests do not rely on backend availability.</action>
  <acceptance_criteria>`frontend/tests/auth-routing.test.tsx` contains the exact strings `/login` and `/auth/session`; `frontend/tests/auth-forms.test.tsx` contains the exact CTA `로그인하기` and error text `로그인 정보를 확인하지 못했습니다. 다시 로그인해 주세요.`; at least one frontend test asserts `credentials: 'include'`.</acceptance_criteria>
  <verify>
    <automated>pnpm --dir frontend test && pnpm --dir frontend build</automated>
  </verify>
  <done>Frontend auth behavior is locked by tests for route protection, session restoration, pending states, and error handling.</done>
</task>

</tasks>

<verification>
Run the full frontend test suite and production build. Confirm `/login` and `/signup` are the only public auth entry points, session restore happens once, and any `401` path returns the user to `/login`.
</verification>

<success_criteria>
Phase 1 frontend auth is complete when a user can sign up or log in through the specified screens, refresh into a restored session, and be redirected back to `/login` immediately when the session becomes invalid.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-03-SUMMARY.md`
</output>
