---
phase: 01-foundation-auth
plan: "03"
type: execute
wave: 1
depends_on:
  - 01-01
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/tsconfig.app.json
  - frontend/tsconfig.node.json
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/styles.css
  - frontend/components.json
  - frontend/.env.example
autonomous: true
requirements:
  - CHAT-04
must_haves:
  truths:
    - "Users will enter a dedicated frontend app shell that carries the approved auth visual tokens."
    - "The frontend exposes only `VITE_API_BASE_URL` as public configuration."
    - "No OpenRouter or JWT secret identifiers appear in frontend app files per CHAT-04."
  artifacts:
    - path: frontend/package.json
      provides: "Frontend SPA package definition with build and test scripts."
    - path: frontend/src/styles.css
      provides: "Auth token foundation matching the approved UI spec."
    - path: frontend/.env.example
      provides: "Frontend public env template limited to `VITE_API_BASE_URL`."
  key_links:
    - from: frontend/src/main.tsx
      to: frontend/src/styles.css
      via: "root stylesheet import"
      pattern: "styles.css"
    - from: frontend/.env.example
      to: frontend/package.json
      via: "frontend-only runtime contract"
      pattern: "VITE_API_BASE_URL"
---

<objective>
Bootstrap the standalone React/Vite frontend shell and approved auth token contract.

Purpose: Give later auth-routing and auth-screen plans a stable SPA shell without mixing in backend secrets or backend wiring.
Output: Buildable frontend package, base app entry, auth design tokens, and frontend env boundary.
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@.planning/phases/01-foundation-auth/01-UI-SPEC.md
@.planning/phases/01-foundation-auth/01-01-PLAN.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade the frontend package shell into a buildable Vite React app package</name>
  <files>frontend/package.json, frontend/vite.config.ts, frontend/tsconfig.json, frontend/tsconfig.app.json, frontend/tsconfig.node.json, frontend/index.html</files>
  <action>Replace the Wave 0 frontend package shell with the actual Vite/React package definition from the research stack. Include React 19, Vite, Vitest, TanStack Query, TanStack Router dependencies, and stable scripts `dev`, `build`, and `test` without requiring auth route files yet. Configure Vite and TypeScript for a standalone app in `frontend/`. Keep shadcn-compatible aliases ready, but do not wire auth routing in this plan.</action>
  <verify>
    <automated>pnpm install && pnpm --filter frontend exec node -e "const p=require('./package.json'); if(!p.scripts.build||!p.scripts.test){process.exit(1)}"</automated>
  </verify>
  <done>The frontend package can be installed and exposes stable build/test scripts for later auth infrastructure work.</done>
</task>

<task type="auto">
  <name>Task 2: Create the frontend shell entry and auth design token contract</name>
  <files>frontend/src/main.tsx, frontend/src/styles.css, frontend/components.json, frontend/.env.example</files>
  <action>Create a minimal React app entry in `frontend/src/main.tsx` that imports `./styles.css` and renders a placeholder shell. Encode the approved UI contract in `frontend/src/styles.css`: font `Geist Sans`, colors `#F7F7F2`, `#E8E4D8`, `#C26A3D`, `#B42318`, spacing tokens `4/8/16/24/32/48/64`, and `44px` control heights. Write `frontend/components.json` with preset `b1xS4xAK3M`. Create `frontend/.env.example` with only `VITE_API_BASE_URL=http://localhost:3000`. Do not add any `OPENROUTER_` or `JWT_` variables to frontend files per CHAT-04.</action>
  <verify>
    <automated>pnpm install && pnpm --filter frontend build && rg -n '#F7F7F2|#E8E4D8|#C26A3D|#B42318|VITE_API_BASE_URL' frontend/src/styles.css frontend/.env.example frontend/components.json && ! rg -n 'OPENROUTER_|JWT_(ACCESS|REFRESH)_SECRET' frontend --glob '!**/*.md'</automated>
  </verify>
  <done>The frontend builds as a standalone SPA shell, carries the approved auth tokens, and exposes only the public API base URL.</done>
</task>

</tasks>

<verification>
Build the frontend package and confirm its env/example and source files contain no server-secret identifiers.
</verification>

<success_criteria>
The frontend shell is ready when Vite builds, the approved auth token system exists in CSS, and public configuration is limited to `VITE_API_BASE_URL`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-03-SUMMARY.md`
</output>
