---
phase: 01-foundation-auth
plan: "02"
type: execute
wave: 1
depends_on:
  - 01-01
files_modified:
  - backend/package.json
  - backend/tsconfig.json
  - backend/tsconfig.build.json
  - backend/nest-cli.json
  - backend/src/main.ts
  - backend/src/app.module.ts
  - backend/src/config/env.schema.ts
  - backend/.env.example
autonomous: true
requirements:
  - CHAT-04
must_haves:
  truths:
    - "Invalid backend configuration fails before any auth request is served."
    - "Server-only secrets remain declared only in backend files per CHAT-04."
    - "The backend app can boot as an isolated service inside the `backend` workspace package."
  artifacts:
    - path: backend/src/config/env.schema.ts
      provides: "Validated backend env contract for database, JWT, OpenRouter, and CORS keys."
    - path: backend/src/main.ts
      provides: "Nest bootstrap with cookie parsing, validation pipe, and credentialed CORS."
    - path: backend/.env.example
      provides: "Backend-only env template for required secret and origin keys."
  key_links:
    - from: backend/src/app.module.ts
      to: backend/src/config/env.schema.ts
      via: "ConfigModule validation schema"
      pattern: "validationSchema"
    - from: backend/src/main.ts
      to: backend/.env.example
      via: "boot keys documented in env example"
      pattern: "FRONTEND_ORIGIN"
---

<objective>
Bootstrap the standalone NestJS backend shell and its backend-only env boundary.

Purpose: Establish the backend runtime and secret validation surface before database and auth feature work starts.
Output: Buildable Nest app shell, validated env schema, and backend `.env.example`.
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@.planning/phases/01-foundation-auth/01-01-PLAN.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade the backend package shell into a buildable NestJS app package</name>
  <files>backend/package.json, backend/tsconfig.json, backend/tsconfig.build.json, backend/nest-cli.json</files>
  <action>Replace the Wave 0 backend package shell with the actual NestJS package definition from the research stack: include Nest core/common/config, validation, auth prerequisites, Jest tooling, and scripts `build`, `start:dev`, `test`, and `test:e2e`. Keep the package name `backend` from D-06 and reuse the Wave 0 script names instead of renaming them. Configure the TypeScript and Nest CLI files for a standalone app in `backend/`, not a Nest monorepo.</action>
  <verify>
    <automated>pnpm install && pnpm --filter backend exec node -e "const p=require('./package.json'); if(!p.scripts.build||!p.scripts.test){process.exit(1)}"</automated>
  </verify>
  <done>The backend workspace package has the required runtime and test dependencies plus stable scripts for later auth work.</done>
</task>

<task type="auto">
  <name>Task 2: Create the Nest bootstrap and backend-only env validation layer</name>
  <files>backend/src/main.ts, backend/src/app.module.ts, backend/src/config/env.schema.ts, backend/.env.example</files>
  <action>Create a Nest bootstrap that registers `cookieParser()`, a global `ValidationPipe` with `whitelist`, `forbidNonWhitelisted`, and `transform`, and credentialed CORS sourced from `FRONTEND_ORIGIN`. Register `ConfigModule.forRoot({ isGlobal: true, envFilePath: '.env', validationSchema })` in `backend/src/app.module.ts`. The Joi schema in `backend/src/config/env.schema.ts` must require `DATABASE_URL`, `JWT_ACCESS_SECRET`, `JWT_REFRESH_SECRET`, `JWT_ACCESS_TTL`, `JWT_REFRESH_TTL`, `OPENROUTER_API_KEY`, `OPENROUTER_MODEL`, `FRONTEND_ORIGIN`, and `PORT`. Keep every OpenRouter and JWT key backend-only per CHAT-04.</action>
  <verify>
    <automated>pnpm install && pnpm --filter backend build && rg -n 'DATABASE_URL|JWT_ACCESS_SECRET|JWT_REFRESH_SECRET|OPENROUTER_API_KEY|OPENROUTER_MODEL|FRONTEND_ORIGIN' backend/src/config/env.schema.ts backend/.env.example</automated>
  </verify>
  <done>The backend can compile with a strict env contract and does not leak any server-only config into frontend surfaces.</done>
</task>

</tasks>

<verification>
Run a backend build after install and confirm the required env keys exist only in backend files.
</verification>

<success_criteria>
The backend shell is ready when Nest compiles, boot config is validated up front, and every server-only secret name is confined to backend files.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-02-SUMMARY.md`
</output>
