---
phase: 01-foundation-auth
plan: "06"
type: execute
wave: 2
depends_on:
  - 01-03
files_modified:
  - frontend/vite.config.ts
  - frontend/src/app/router.tsx
  - frontend/src/app/providers.tsx
  - frontend/src/app/query-client.ts
  - frontend/src/lib/api/client.ts
  - frontend/src/features/auth/api.ts
  - frontend/src/features/auth/auth.store.ts
  - frontend/src/features/auth/session.ts
  - frontend/src/features/auth/session.test.tsx
  - frontend/src/routes/__root.tsx
  - frontend/src/routes/index.tsx
  - frontend/src/routes/login.tsx
  - frontend/src/routes/signup.tsx
  - frontend/src/main.tsx
autonomous: true
requirements:
  - AUTH-03
  - CHAT-04
must_haves:
  truths:
    - "Refreshing the app triggers one session-restore path instead of ad hoc client token handling."
    - "Unauthenticated access to the protected app entry redirects to `/login` per D-04."
    - "Frontend auth API calls always use cookies and never read server-only secret env values."
  artifacts:
    - path: frontend/src/lib/api/client.ts
      provides: "Credentialed API client rooted at `VITE_API_BASE_URL`."
    - path: frontend/src/features/auth/session.ts
      provides: "Single session bootstrap query and unauthorized-state handling."
    - path: frontend/src/routes/index.tsx
      provides: "Protected app entry route."
  key_links:
    - from: frontend/src/lib/api/client.ts
      to: frontend/src/features/auth/api.ts
      via: "shared fetch wrapper"
      pattern: "credentials: 'include'"
    - from: frontend/src/features/auth/session.ts
      to: frontend/src/routes/index.tsx
      via: "redirect on unauthenticated session"
      pattern: "/login"
---

<objective>
Implement the frontend auth infrastructure: API client, session bootstrap, and protected routing skeleton.

Purpose: Isolate auth/session plumbing from visual auth screens so routing and bootstrap behavior can be verified earlier.
Output: Credentialed frontend API layer, auth store/query wiring, protected routes, and a real session test.
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@.planning/phases/01-foundation-auth/01-UI-SPEC.md
@.planning/phases/01-foundation-auth/01-03-PLAN.md

<interfaces>
Consume these payloads exactly:

```ts
type AuthUser = { id: string; email: string }
type AuthResponse = { user: AuthUser }
type LogoutResponse = { ok: true }
type LoginPayload = { email: string; password: string }
type SignupPayload = { email: string; password: string }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the frontend auth API layer and session bootstrap state</name>
  <files>frontend/src/app/query-client.ts, frontend/src/lib/api/client.ts, frontend/src/features/auth/api.ts, frontend/src/features/auth/auth.store.ts, frontend/src/features/auth/session.ts, frontend/src/features/auth/session.test.tsx</files>
  <behavior>
    - Test 1: The session bootstrap path calls `/auth/session` with `credentials: 'include'`.
    - Test 2: A `401` session response clears auth state immediately.
  </behavior>
  <action>Create a fetch client rooted at `import.meta.env.VITE_API_BASE_URL` that always sets `credentials: 'include'`. Add auth API helpers for `signup`, `login`, `logout`, `getSession`, and `refreshSession` hitting the backend auth routes. Implement one TanStack Query session bootstrap flow and a small auth store for current user/authenticated status. Replace the Wave 0 `frontend/src/features/auth/session.test.tsx` placeholder with real tests for one-time session restore and unauthorized-state clearing. Do not read or mirror any `OPENROUTER_*` or `JWT_*` env keys in frontend code.</action>
  <verify>
    <automated>pnpm install && pnpm --filter frontend test -- src/features/auth/session.test.tsx && pnpm --filter frontend build</automated>
  </verify>
  <done>The frontend has one credentialed auth bootstrap path backed by real tests and no client-side secret handling.</done>
</task>

<task type="auto">
  <name>Task 2: Wire protected routes and placeholder auth entry routes around the session bootstrap</name>
  <files>frontend/vite.config.ts, frontend/src/app/router.tsx, frontend/src/app/providers.tsx, frontend/src/routes/__root.tsx, frontend/src/routes/index.tsx, frontend/src/routes/login.tsx, frontend/src/routes/signup.tsx, frontend/src/main.tsx</files>
  <action>Add TanStack Router wiring, providers, and route files so the app boots through a single root router. Create a protected index route that redirects to `/login` when the bootstrap state is unauthenticated, matching D-04. Create minimal `login` and `signup` route placeholders now so the router and route generation can build before the visual auth components land in Plan 01-07. Keep this plan focused on routing/state infrastructure; actual auth screen content belongs to Plan 01-07.</action>
  <verify>
    <automated>pnpm install && pnpm --filter frontend build && rg -n "/login|credentials: 'include'|retry: false" frontend/src/routes/index.tsx frontend/src/lib/api/client.ts frontend/src/features/auth/session.ts</automated>
  </verify>
  <done>The SPA compiles with protected routing and placeholder auth routes, allowing later screen work to focus on form behavior instead of router plumbing.</done>
</task>

</tasks>

<verification>
Run the targeted frontend session test and production build. Confirm the protected route depends on the shared session bootstrap and redirects to `/login` when unauthenticated.
</verification>

<success_criteria>
Frontend auth infrastructure is complete when session bootstrap, unauthorized redirect behavior, and cookie-backed API calls are implemented and verified without auth screen UI coupling.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-06-SUMMARY.md`
</output>
