---
phase: 01-foundation-auth
plan: "05"
type: execute
wave: 3
depends_on:
  - 01-04
files_modified:
  - backend/src/auth/auth.module.ts
  - backend/src/auth/auth.controller.ts
  - backend/src/auth/auth.service.ts
  - backend/src/auth/auth.constants.ts
  - backend/src/auth/dto/signup.dto.ts
  - backend/src/auth/dto/login.dto.ts
  - backend/src/auth/dto/session-user.dto.ts
  - backend/src/auth/strategies/local.strategy.ts
  - backend/src/auth/strategies/jwt.strategy.ts
  - backend/src/auth/guards/local-auth.guard.ts
  - backend/src/auth/guards/jwt-auth.guard.ts
  - backend/src/auth/decorators/current-user.decorator.ts
  - backend/test/auth.e2e-spec.ts
  - backend/src/auth/auth.service.spec.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - CHAT-04
must_haves:
  truths:
    - "A user can sign up with email and password per D-01."
    - "A user can log in and receive cookie-backed JWT session state that survives refresh per D-02 and D-05."
    - "An expired or invalid session returns `401`, enabling immediate frontend redirect to `/login` per D-04."
    - "Auth responses never expose password hashes or OpenRouter configuration per CHAT-04."
  artifacts:
    - path: backend/src/auth/auth.controller.ts
      provides: "HTTP endpoints for signup, login, logout, refresh, and session."
    - path: backend/src/auth/auth.service.ts
      provides: "Password hashing, JWT issuance, and cookie/session behavior."
    - path: backend/test/auth.e2e-spec.ts
      provides: "Automated auth contract coverage."
  key_links:
    - from: backend/src/auth/strategies/local.strategy.ts
      to: backend/src/auth/auth.service.ts
      via: "email/password validation"
      pattern: "usernameField: 'email'"
    - from: backend/src/auth/strategies/jwt.strategy.ts
      to: backend/src/auth/auth.constants.ts
      via: "access cookie extraction"
      pattern: "access_token"
    - from: backend/src/auth/auth.controller.ts
      to: backend/test/auth.e2e-spec.ts
      via: "HTTP contract coverage"
      pattern: "/auth/session"
---

<objective>
Implement the backend auth behavior and the tests that prove it.

Purpose: Deliver the full backend slice for AUTH-01, AUTH-02, AUTH-03, and the backend side of CHAT-04 without mixing it with persistence wiring.
Output: Cookie-backed auth endpoints, DTOs/guards/strategies, and backend auth test coverage.
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@.planning/phases/01-foundation-auth/01-04-PLAN.md

<interfaces>
Implement these HTTP contracts exactly:

```http
POST /auth/signup -> 201 { "user": { "id": string, "email": string } }
POST /auth/login -> 200 { "user": { "id": string, "email": string } } + access_token/refresh_token cookies
GET /auth/session -> 200 { "user": { "id": string, "email": string } } | 401
POST /auth/logout -> 200 { "ok": true } + cleared cookies
POST /auth/refresh -> 200 { "user": { "id": string, "email": string } }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement auth service, DTOs, guards, and controllers against the SQLite user store</name>
  <files>backend/src/auth/auth.module.ts, backend/src/auth/auth.controller.ts, backend/src/auth/auth.service.ts, backend/src/auth/auth.constants.ts, backend/src/auth/dto/signup.dto.ts, backend/src/auth/dto/login.dto.ts, backend/src/auth/dto/session-user.dto.ts, backend/src/auth/strategies/local.strategy.ts, backend/src/auth/strategies/jwt.strategy.ts, backend/src/auth/guards/local-auth.guard.ts, backend/src/auth/guards/jwt-auth.guard.ts, backend/src/auth/decorators/current-user.decorator.ts</files>
  <behavior>
    - Test 1: `POST /auth/signup` with a new email returns `201` and `{ user: { id, email } }`.
    - Test 2: `POST /auth/login` with valid credentials returns `200` and sets `access_token` plus `refresh_token` httpOnly cookies.
    - Test 3: `GET /auth/session` with a valid access cookie returns `200`, while invalid or missing cookies produce `401`.
  </behavior>
  <action>Implement D-01, D-02, D-04, and D-05 with endpoints `POST /auth/signup`, `POST /auth/login`, `POST /auth/logout`, `POST /auth/refresh`, and `GET /auth/session`. Use `class-validator` DTOs with `email` and `password`, `bcrypt.hash`/`bcrypt.compare`, `@nestjs/jwt`, `LocalStrategy` with `usernameField: 'email'`, and `JwtStrategy` extracting `access_token` from cookies. Issue short-lived access and longer-lived refresh cookies named `access_token` and `refresh_token`, both `httpOnly`, `sameSite: 'lax'`, `path: '/'`, and `secure` only in production. Never return JWT strings, password hashes, or OpenRouter values in JSON responses.</action>
  <verify>
    <automated>pnpm install && pnpm --filter backend test -- auth.service.spec.ts && pnpm --filter backend build</automated>
  </verify>
  <done>The backend auth module implements the required HTTP and cookie behavior against the real user store.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the Wave 0 stubs with backend auth contract tests</name>
  <files>backend/test/auth.e2e-spec.ts, backend/src/auth/auth.service.spec.ts</files>
  <behavior>
    - Test 1: Duplicate signup fails with `409`.
    - Test 2: Session restore succeeds after login and fails with `401` after logout.
    - Test 3: Auth payloads never contain `passwordHash`, `OPENROUTER_API_KEY`, or `OPENROUTER_MODEL`.
  </behavior>
  <action>Replace the Wave 0 placeholders in `backend/test/auth.e2e-spec.ts` and `backend/src/auth/auth.service.spec.ts` with real tests using `@nestjs/testing` and `supertest`. Cover signup, duplicate-email failure, login cookie issuance, session restore, refresh, and logout cookie clearing. Add assertions that auth JSON exposes only user-safe payloads and that server-only env identifiers are absent from response bodies. Keep tests fast enough to satisfy the validation latency target.</action>
  <verify>
    <automated>pnpm install && pnpm --filter backend test -- auth.e2e-spec.ts && pnpm --filter backend exec rg -n 'OPENROUTER_(API_KEY|MODEL)' src .env.example</automated>
  </verify>
  <done>The backend auth contract is enforced by automated tests and preserves the secret boundary required by CHAT-04.</done>
</task>

</tasks>

<verification>
Run the backend auth tests and confirm signup, login, session restore, refresh, and logout behaviors all pass without leaking server-only fields.
</verification>

<success_criteria>
Backend auth is complete when users can sign up, log in, restore session, refresh, and log out through tested endpoints and every auth response remains secret-safe.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-05-SUMMARY.md`
</output>
