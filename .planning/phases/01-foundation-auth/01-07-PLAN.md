---
phase: 01-foundation-auth
plan: "07"
type: execute
wave: 3
depends_on:
  - 01-06
files_modified:
  - frontend/src/routes/login.tsx
  - frontend/src/routes/signup.tsx
  - frontend/src/components/auth/auth-card.tsx
  - frontend/src/components/auth/login-form.tsx
  - frontend/src/components/auth/signup-form.tsx
  - frontend/src/components/auth/session-loader.tsx
  - frontend/src/components/ui/*
  - frontend/src/styles.css
  - frontend/tests/auth-routing.test.tsx
  - frontend/tests/auth-forms.test.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
must_haves:
  truths:
    - "A user can reach dedicated `/login` and `/signup` screens with email/password-only affordances per D-01 and D-03."
    - "Successful login or signup transitions directly into app bootstrap with no interstitial."
    - "Session expiry sends the user back to `/login` immediately per D-04."
  artifacts:
    - path: frontend/src/routes/login.tsx
      provides: "Login screen and invalid-credential state."
    - path: frontend/src/routes/signup.tsx
      provides: "Signup screen and duplicate-email state."
    - path: frontend/tests/auth-routing.test.tsx
      provides: "Frontend redirect and session-expiry coverage."
  key_links:
    - from: frontend/src/components/auth/login-form.tsx
      to: frontend/src/features/auth/api.ts
      via: "login mutation"
      pattern: "/auth/login"
    - from: frontend/src/components/auth/signup-form.tsx
      to: frontend/src/features/auth/api.ts
      via: "signup mutation"
      pattern: "/auth/signup"
    - from: frontend/tests/auth-routing.test.tsx
      to: frontend/src/routes/index.tsx
      via: "route-guard verification"
      pattern: "/login"
---

<objective>
Implement the user-facing auth screens and the frontend tests that prove their behavior.

Purpose: Finish the visible side of AUTH-01, AUTH-02, and AUTH-03 using the approved UI spec and the routing/state infrastructure from Plan 01-06.
Output: Login and signup screens, auth UI components, session loader, and frontend auth tests.
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-UI-SPEC.md
@.planning/phases/01-foundation-auth/01-06-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement login and signup screens that match the approved auth contract</name>
  <files>frontend/src/routes/login.tsx, frontend/src/routes/signup.tsx, frontend/src/components/auth/auth-card.tsx, frontend/src/components/auth/login-form.tsx, frontend/src/components/auth/signup-form.tsx, frontend/src/components/auth/session-loader.tsx, frontend/src/components/ui/*, frontend/src/styles.css</files>
  <action>Replace the placeholder auth routes with the approved centered-card auth experience. Implement only two modes, `로그인` and `회원가입`, with required fields `이메일` and `비밀번호`; add `비밀번호 확인` only if you intentionally implement client-side confirmation. Use the exact approved copy for the primary CTA and auth empty/error states. Validate email on blur and submit, validate password presence on submit, show one inline field error per field, and keep the submit button enabled until request start. While pending, swap the button label to a short loading state and show a `#C26A3D` spinner. Do not add password reset, social login, OAuth, celebratory success screens, or logout confirmation modal per D-03.</action>
  <verify>
    <automated>pnpm install && pnpm --filter frontend build && ! rg -n 'OAuth|Google|GitHub|Forgot password|비밀번호 재설정' frontend/src/routes frontend/src/components/auth && rg -n '로그인하기|회원가입|#C26A3D' frontend/src/routes frontend/src/components/auth frontend/src/styles.css</automated>
  </verify>
  <done>The frontend exposes the approved email/password auth screens and no out-of-scope auth affordances.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the frontend auth test placeholders with routing and form coverage</name>
  <files>frontend/tests/auth-routing.test.tsx, frontend/tests/auth-forms.test.tsx</files>
  <behavior>
    - Test 1: Unauthenticated visits to the protected route redirect to `/login`.
    - Test 2: Successful mocked session restore lands on the protected app route.
    - Test 3: Invalid credentials show the approved form-level error copy.
    - Test 4: Expired session `401` clears auth state and returns the app to `/login`.
  </behavior>
  <action>Create Vitest + Testing Library coverage in `frontend/tests/auth-routing.test.tsx` and `frontend/tests/auth-forms.test.tsx`. Mock the auth API layer so tests exercise the real routing and form wiring from the app, not DOM-global fetch assumptions. Assert the path `/login`, the session endpoint `/auth/session`, the CTA `로그인하기`, and the form-level error text `로그인 정보를 확인하지 못했습니다. 다시 로그인해 주세요.`. Include at least one assertion that auth requests use `credentials: 'include'` through the shared client helper.</action>
  <verify>
    <automated>pnpm install && pnpm --filter frontend test -- frontend/tests/auth-routing.test.tsx frontend/tests/auth-forms.test.tsx && pnpm --filter frontend build</automated>
  </verify>
  <done>The frontend auth UX is pinned by automated tests for routing, session expiry, pending states, and form error handling.</done>
</task>

</tasks>

<verification>
Run the targeted frontend auth tests and a production build. Confirm `/login` and `/signup` are the only public auth entry points and that session-expiry behavior returns the user to `/login`.
</verification>

<success_criteria>
Frontend auth is complete when users can sign up or log in through the approved screens, refresh into a restored session, and get redirected back to `/login` immediately when session state becomes invalid.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-07-SUMMARY.md`
</output>
