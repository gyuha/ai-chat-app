---
phase: 01-foundation-auth
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - biome.json
  - tsconfig.base.json
  - .gitignore
  - backend/package.json
  - frontend/package.json
  - backend/test/jest-e2e.json
  - backend/test/auth.e2e-spec.ts
  - backend/src/auth/auth.service.spec.ts
  - frontend/vitest.config.ts
  - frontend/src/features/auth/session.test.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - CHAT-04
must_haves:
  truths:
    - "Auth-specific automated checks can run before backend and frontend feature work begins."
    - "The repo exposes only two app packages, `backend` and `frontend`, per D-06 and D-07."
    - "Frontend test scaffolding includes a guardrail against server-only secret names appearing in client code."
  artifacts:
    - path: package.json
      provides: "Root workspace scripts for install, lint, typecheck, and test orchestration."
    - path: backend/test/auth.e2e-spec.ts
      provides: "Backend auth contract stub required by VALIDATION.md Wave 0."
    - path: frontend/src/features/auth/session.test.tsx
      provides: "Frontend session bootstrap test stub required by VALIDATION.md Wave 0."
  key_links:
    - from: pnpm-workspace.yaml
      to: backend/package.json
      via: "workspace package inclusion"
      pattern: "backend"
    - from: pnpm-workspace.yaml
      to: frontend/package.json
      via: "workspace package inclusion"
      pattern: "frontend"
    - from: package.json
      to: backend/package.json
      via: "workspace test/build scripts"
      pattern: "backend"
---

<objective>
Create the Wave 0 scaffold required by `01-VALIDATION.md`: root workspace metadata, package shells, and auth test/config assets.

Purpose: Remove missing-test blockers before app bootstrap work starts and keep the repo aligned to the locked two-app structure from D-06 and D-07.
Output: Root workspace files, minimal `backend` and `frontend` package manifests, and Wave 0 auth test/config stubs.
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@.planning/phases/01-foundation-auth/01-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the root workspace and package shells for Wave 0</name>
  <files>package.json, pnpm-workspace.yaml, biome.json, tsconfig.base.json, .gitignore, backend/package.json, frontend/package.json</files>
  <action>Create the root pnpm workspace for D-06 and D-07 with exactly two packages named `backend` and `frontend`. Add root scripts `build`, `lint`, `typecheck`, and `test` that delegate into workspace packages, but do not require working app builds yet. Create minimal `backend/package.json` and `frontend/package.json` shells with stable `build` and `test` script names so later plans can extend them without renaming commands. Keep `packageManager` set to `pnpm@10.33.0` and do not introduce a third app or shared runtime package.</action>
  <verify>
    <automated>test -f package.json && test -f pnpm-workspace.yaml && test -f backend/package.json && test -f frontend/package.json && rg -n '"(build|lint|typecheck|test)"' package.json backend/package.json frontend/package.json</automated>
  </verify>
  <done>The repository has a valid two-app workspace shell and stable script names without assuming backend/frontend feature files already exist.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Wave 0 auth test and config assets from VALIDATION.md</name>
  <files>backend/test/jest-e2e.json, backend/test/auth.e2e-spec.ts, backend/src/auth/auth.service.spec.ts, frontend/vitest.config.ts, frontend/src/features/auth/session.test.tsx</files>
  <action>Create the exact Wave 0 assets required by `01-VALIDATION.md`: backend Jest e2e config, backend auth e2e stub, backend auth service spec stub, frontend Vitest config, and frontend session test stub. Make each stub explicitly reference the planned auth contracts so later plans can replace placeholders instead of inventing new test surfaces. Include at least one frontend static assertion or TODO comment guarding against `OPENROUTER_*` or `JWT_*_SECRET` names in client code per CHAT-04.</action>
  <verify>
    <automated>test -f backend/test/jest-e2e.json && test -f backend/test/auth.e2e-spec.ts && test -f backend/src/auth/auth.service.spec.ts && test -f frontend/vitest.config.ts && test -f frontend/src/features/auth/session.test.tsx && rg -n 'OPENROUTER_|JWT_(ACCESS|REFRESH)_SECRET|auth/session|auth/login' backend/test/auth.e2e-spec.ts backend/src/auth/auth.service.spec.ts frontend/src/features/auth/session.test.tsx</automated>
  </verify>
  <done>All Wave 0 validation assets exist, giving later plans concrete test/config files to extend instead of missing references.</done>
</task>

</tasks>

<verification>
Confirm the root workspace manifests and every Wave 0 asset exist before moving to app bootstrap work. No task in later plans should reference a non-existent test/config file after this plan completes.
</verification>

<success_criteria>
Wave 0 is complete when `01-VALIDATION.md` no longer has missing test/config assets and the repo exposes stable root, backend, and frontend script surfaces for later plans.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-01-SUMMARY.md`
</output>
