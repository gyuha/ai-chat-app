---
phase: 01-foundation-auth
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - biome.json
  - tsconfig.base.json
  - .gitignore
  - backend/package.json
  - backend/tsconfig.json
  - backend/tsconfig.build.json
  - backend/nest-cli.json
  - backend/src/main.ts
  - backend/src/app.module.ts
  - backend/src/config/env.schema.ts
  - backend/.env.example
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/tsconfig.app.json
  - frontend/tsconfig.node.json
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/styles.css
  - frontend/src/routeTree.gen.ts
  - frontend/components.json
  - frontend/.env.example
autonomous: true
requirements:
  - CHAT-04
must_haves:
  truths:
    - "Developer can install one pnpm workspace that contains separate frontend and backend apps per D-06 and D-07."
    - "Backend-only secrets are declared and validated without any OpenRouter or JWT secret names appearing in frontend source."
    - "Frontend bootstrap and auth visual tokens exist so later auth flows can be wired without redesigning the shell."
  artifacts:
    - path: package.json
      provides: "Root workspace scripts for install, lint, typecheck, backend build, and frontend build."
    - path: backend/src/config/env.schema.ts
      provides: "Validated backend env contract for DATABASE_URL, JWT secrets, OpenRouter keys, and frontend origin."
    - path: frontend/src/styles.css
      provides: "Auth UI token foundation matching the approved UI spec colors, spacing, and typography."
  key_links:
    - from: pnpm-workspace.yaml
      to: backend/package.json
      via: "workspace package inclusion"
      pattern: "backend"
    - from: pnpm-workspace.yaml
      to: frontend/package.json
      via: "workspace package inclusion"
      pattern: "frontend"
    - from: backend/src/app.module.ts
      to: backend/src/config/env.schema.ts
      via: "ConfigModule validationSchema import"
      pattern: "validationSchema"
---

<objective>
Create the Phase 1 execution base: root workspace files, backend boot shell with secret validation, and frontend boot shell with the approved auth design tokens.

Purpose: Lock the `frontend` / `backend` boundary from D-06 and D-07 before auth logic is added.
Output: Installable pnpm workspace, backend env boundary, frontend SPA shell, and UI token contract.
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@.planning/phases/01-foundation-auth/01-UI-SPEC.md
@CLAUDE.md
@PROMPT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the root pnpm workspace and shared toolchain shell</name>
  <files>package.json, pnpm-workspace.yaml, biome.json, tsconfig.base.json, .gitignore</files>
  <read_first>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/01-foundation-auth/01-CONTEXT.md, .planning/phases/01-foundation-auth/01-RESEARCH.md, package.json, pnpm-workspace.yaml, biome.json, tsconfig.base.json, .gitignore</read_first>
  <action>Create the root workspace for D-06 and D-07 with exactly two packages named `frontend` and `backend`. Add a root `package.json` with scripts `dev:frontend`, `dev:backend`, `build`, `lint`, `format`, and `typecheck`, and set `packageManager` to `pnpm@10.33.0`. Create `pnpm-workspace.yaml` with `packages:` entries `frontend` and `backend`. Create `biome.json` with formatter and linter enabled for TypeScript/JSON. Create `tsconfig.base.json` with shared `target` set to `ES2022`, `moduleResolution` set to `Bundler`, `strict` set to `true`, and path aliases rooted per app instead of a shared runtime package. Add `.gitignore` entries for `node_modules`, `dist`, `.turbo`, `.env`, `.env.*`, `coverage`, and `frontend/src/routeTree.gen.ts` if the router plugin regenerates it.</action>
  <acceptance_criteria>`pnpm-workspace.yaml` contains the exact lines `- frontend` and `- backend`; `package.json` contains the exact script keys `dev:frontend`, `dev:backend`, `build`, `lint`, `format`, and `typecheck`; `package.json` contains `pnpm@10.33.0`; `.gitignore` contains the exact strings `node_modules`, `.env`, and `coverage`.</acceptance_criteria>
  <verify>
    <automated>pnpm install && pnpm lint && pnpm typecheck</automated>
  </verify>
  <done>The repository installs as one pnpm workspace and exposes shared commands without introducing any third top-level app or shared server/client secret file.</done>
</task>

<task type="auto">
  <name>Task 2: Bootstrap the NestJS backend shell with backend-only env validation</name>
  <files>backend/package.json, backend/tsconfig.json, backend/tsconfig.build.json, backend/nest-cli.json, backend/src/main.ts, backend/src/app.module.ts, backend/src/config/env.schema.ts, backend/.env.example</files>
  <read_first>.planning/phases/01-foundation-auth/01-CONTEXT.md, .planning/phases/01-foundation-auth/01-RESEARCH.md, CLAUDE.md, backend/package.json, backend/src/main.ts, backend/src/app.module.ts, backend/src/config/env.schema.ts, backend/.env.example</read_first>
  <action>Create a standalone NestJS app shell under `backend/` with `@nestjs/common`, `@nestjs/core`, `@nestjs/config`, `joi`, `cookie-parser`, and TypeScript build scripts. In `backend/src/app.module.ts`, register `ConfigModule.forRoot({ isGlobal: true, envFilePath: '.env', validationSchema })` using a Joi schema from `backend/src/config/env.schema.ts`. The schema must require exactly these keys: `DATABASE_URL`, `JWT_ACCESS_SECRET`, `JWT_REFRESH_SECRET`, `JWT_ACCESS_TTL`, `JWT_REFRESH_TTL`, `OPENROUTER_API_KEY`, `OPENROUTER_MODEL`, `FRONTEND_ORIGIN`, and `PORT`. In `backend/src/main.ts`, enable global `ValidationPipe` with `whitelist: true`, `forbidNonWhitelisted: true`, and `transform: true`, register `cookieParser()`, and set CORS to `origin: config.getOrThrow('FRONTEND_ORIGIN')` with `credentials: true`. Create `backend/.env.example` with placeholder values for every required key and keep all OpenRouter and JWT secrets backend-only per CHAT-04.</action>
  <acceptance_criteria>`backend/src/config/env.schema.ts` contains the exact keys `DATABASE_URL`, `JWT_ACCESS_SECRET`, `JWT_REFRESH_SECRET`, `OPENROUTER_API_KEY`, `OPENROUTER_MODEL`, and `FRONTEND_ORIGIN`; `backend/src/main.ts` contains `cookieParser()` and `credentials: true`; `backend/.env.example` contains one line starting with `OPENROUTER_API_KEY=` and one line starting with `OPENROUTER_MODEL=`.</acceptance_criteria>
  <verify>
    <automated>pnpm --dir backend build && rg -n "OPENROUTER_API_KEY|OPENROUTER_MODEL|JWT_ACCESS_SECRET|JWT_REFRESH_SECRET" backend/src backend/.env.example</automated>
  </verify>
  <done>The backend can boot with validated env rules, CORS credentials enabled, and secret names confined to the backend app.</done>
</task>

<task type="auto">
  <name>Task 3: Bootstrap the React frontend shell and auth design token contract</name>
  <files>frontend/package.json, frontend/vite.config.ts, frontend/tsconfig.json, frontend/tsconfig.app.json, frontend/tsconfig.node.json, frontend/index.html, frontend/src/main.tsx, frontend/src/styles.css, frontend/src/routeTree.gen.ts, frontend/components.json, frontend/.env.example</files>
  <read_first>.planning/phases/01-foundation-auth/01-CONTEXT.md, .planning/phases/01-foundation-auth/01-RESEARCH.md, .planning/phases/01-foundation-auth/01-UI-SPEC.md, frontend/package.json, frontend/vite.config.ts, frontend/src/main.tsx, frontend/src/styles.css, frontend/components.json, frontend/.env.example</read_first>
  <action>Create the `frontend/` Vite React shell with React 19, TanStack Router plugin, TanStack Query, Zustand, shadcn-compatible aliases, and a single public env key `VITE_API_BASE_URL`. In `frontend/src/main.tsx`, mount the app root and import `./styles.css`. In `frontend/src/styles.css`, encode the approved UI contract values: font family `Geist Sans`, background `#F7F7F2`, card tint `#E8E4D8`, accent `#C26A3D`, destructive `#B42318`, spacing tokens `4/8/16/24/32/48/64`, body `16px`, label `14px`, heading `20px`, display `28px`, and touch target/input/button height `44px`. Create `frontend/components.json` using the captured shadcn preset `b1xS4xAK3M`. Create `frontend/.env.example` with only `VITE_API_BASE_URL=http://localhost:3000` and do not add any `OPENROUTER_` or `JWT_` prefixed variables.</action>
  <acceptance_criteria>`frontend/.env.example` contains the exact line `VITE_API_BASE_URL=http://localhost:3000`; `frontend/components.json` contains the exact string `b1xS4xAK3M`; `frontend/src/styles.css` contains the exact color values `#F7F7F2`, `#E8E4D8`, `#C26A3D`, and `#B42318`; `rg -n "OPENROUTER|JWT_.*SECRET" frontend` returns no matches outside documentation.</acceptance_criteria>
  <verify>
    <automated>pnpm --dir frontend build && rg -n "OPENROUTER|JWT_.*SECRET" frontend --glob '!**/*.md'</automated>
  </verify>
  <done>The frontend boots as a standalone SPA, exposes only `VITE_API_BASE_URL`, and carries the approved auth visual tokens forward to later UI work.</done>
</task>

</tasks>

<verification>
Run the root install/lint/typecheck flow, then run isolated backend and frontend builds. Confirm the workspace still contains only `frontend/` and `backend/` as top-level apps and that secret identifiers exist only inside backend files.
</verification>

<success_criteria>
Phase 1 foundation is ready when the repository installs with pnpm, the backend validates secret env keys at boot, and the frontend contains the approved auth token contract without exposing OpenRouter or JWT secrets.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-01-SUMMARY.md`
</output>
