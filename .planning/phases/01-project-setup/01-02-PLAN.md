---
phase: 01-project-setup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/prisma/schema.prisma
  - backend/src/main.ts
  - backend/src/app.module.ts
  - biome.json
  - .env.example
  - backend/.env.example
autonomous: true
requirements:
  - BACK-04
must_haves:
  truths:
    - "Prisma 마이그레이션으로 SQLite 데이터베이스가 생성된다"
    - "백엔드 개발 서버가 port 3000에서 실행된다"
    - "ValidationPipe가 모든 요청을 검증하도록 설정된다 (BACK-04)"
    - "Biome으로 코드 린트/포맷팅이 실행된다"
  artifacts:
    - path: "backend/prisma/schema.prisma"
      provides: "Prisma 데이터베이스 스키마"
      contains: "model User", "model Chat", "model Message"
    - path: "backend/src/main.ts"
      provides: "NestJS 부트스트랩 및 ValidationPipe 설정"
      contains: "ValidationPipe", "useGlobalPipes"
    - path: "backend/src/app.module.ts"
      provides: "NestJS 루트 모듈"
      contains: "@Module({ imports: [ConfigModule]"
    - path: "biome.json"
      provides: "Biome 린트/포맷 설정"
      contains: "\"linter\": { \"enabled\": true"
    - path: ".env.example"
      provides: "환경변수 템플릿"
      contains: "DATABASE_URL", "JWT_SECRET", "OPENROUTER_API_KEY"
  key_links:
    - from: "backend/src/main.ts"
      to: "class-validator"
      via: "ValidationPipe configuration"
      pattern: "ValidationPipe.*whitelist.*true"
    - from: "backend/src/main.ts"
      to: "@nestjs/config"
      via: "ConfigModule.forRoot()"
      pattern: "ConfigModule"
    - from: "backend/prisma/schema.prisma"
      to: "SQLite database"
      via: "prisma migrate dev"
      pattern: "provider = \"sqlite\""
    - from: "biome.json"
      to: "TypeScript/JSON files"
      via: "pnpm lint command"
      pattern: "\"recommended\": true"
---

<objective>
NestJS 백엔드 어플리케이션을 구성하고 Prisma 데이터베이스 스키마를 정의하며, Biome으로 코드 품질 도구를 설정한다.

Purpose: DTO 기반 입력 검증(BACK-04)을 위한 ValidationPipe를 구성하고, User/Chat/Message 모델을 정의하여 데이터베이스 기반을 마련한다. 또한 Biome으로 일관된 코드 스타일을 강제한다.
Output: NestJS 소스 파일, Prisma 스키마, Biome 설정, 환경변수 템플릿
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app.worktrees/gsd-glm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app.worktrees/gsd-glm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-project-setup/01-RESEARCH.md
@.planning/research/ARCHITECTURE.md

## Prisma 스키마 (01-RESEARCH.md)

```prisma
datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id        String   @id @default(uuid())
  email     String   @unique
  password  String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model Chat {
  id          String    @id @default(uuid())
  title       String?
  userId      String
  user        User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  messages    Message[]
  systemPrompt String?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
}

model Message {
  id        String   @id @default(uuid())
  content   String
  role      String
  chatId    String
  chat      Chat     @relation(fields: [chatId], references: [id], onDelete: Cascade)
  createdAt DateTime @default(now())
}
```

## NestJS ValidationPipe 설정 (01-RESEARCH.md)

```typescript
app.useGlobalPipes(
  new ValidationPipe({
    whitelist: true,           // DTO에 정의되지 않은 속성 제거
    forbidNonWhitelisted: true, // 정의되지 않은 속성 있으면 에러
    transform: true,           // 타입 변환 활성화
  }),
);
```

## Biome 설정 (01-RESEARCH.md)

```json
{
  "linter": {
    "enabled": true,
    "rules": {
      "recommended": true,
      "suspicious": {
        "noExplicitAny": "warn"
      }
    }
  },
  "formatter": {
    "enabled": true,
    "formatWithErrors": false,
    "indentStyle": "space",
    "lineWidth": 80
  }
}
```

## 프로젝트 구조 (ARCHITECTURE.md)

```
backend/
├── prisma/
│   ├── schema.prisma
│   └── migrations/
├── src/
│   ├── main.ts          # 부트스트랩, ValidationPipe, CORS
│   └── app.module.ts    # 루트 모듈, ConfigModule
└── .env                 # 환경변수 (로컬)
```

## 환경변수 (CLAUDE.md)

- DATABASE_URL: SQLite 데이터베이스 경로
- JWT_SECRET: JWT 토큰 서명 키
- OPENROUTER_API_KEY: OpenRouter API 키
- FRONTEND_URL: 프론트엔드 CORS 허용 origin
</context>

<tasks>

<task type="auto">
  <name>Prisma 스키마 및 데이터베이스 설정</name>
  <files>backend/prisma/schema.prisma, backend/.env.example</files>
  <read_first>
  - .planning/phases/01-project-setup/01-RESEARCH.md - Prisma 스키마 예시 확인
  - backend/package.json - @nestjs/config 사용 확인
  </read_first>
  <action>
  Prisma 스키마와 환경변수 템플릿을 생성한다:

  **1) backend/prisma/schema.prisma**
  ```prisma
  datasource db {
    provider = "sqlite"
    url      = env("DATABASE_URL")
  }

  generator client {
    provider = "prisma-client-js"
  }

  model User {
    id        String   @id @default(uuid())
    email     String   @unique
    password  String
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
    chats     Chat[]
  }

  model Chat {
    id           String    @id @default(uuid())
    title        String?
    userId       String
    user         User      @relation(fields: [userId], references: [id], onDelete: Cascade)
    messages     Message[]
    systemPrompt String?
    createdAt    DateTime   @default(now())
    updatedAt    DateTime   @updatedAt
  }

  model Message {
    id        String   @id @default(uuid())
    content   String
    role      String
    chatId    String
    chat      Chat     @relation(fields: [chatId], references: [id], onDelete: Cascade)
    createdAt DateTime @default(now())
  }
  ```

  **2) backend/.env.example**
  ```env
  # Database
  DATABASE_URL="file:./dev.db"

  # JWT
  JWT_SECRET="your-secret-key-change-in-production"
  JWT_EXPIRES_IN="15m"
  JWT_REFRESH_EXPIRES_IN="7d"

  # OpenRouter API
  OPENROUTER_API_KEY="your-openrouter-api-key"
  OPENROUTER_API_URL="https://openrouter.ai/api/v1"

  # Server
  PORT=3000
  FRONTEND_URL="http://localhost:5173"

  # Node
  NODE_ENV="development"
  ```
  </action>
  <verify>
    <automated>grep -q "model User" backend/prisma/schema.prisma && grep -q "model Chat" backend/prisma/schema.prisma && grep -q "model Message" backend/prisma/schema.prisma && grep -q "DATABASE_URL" backend/.env.example</automated>
  </verify>
  <done>
  - Prisma 스키마가 User, Chat, Message 모델을 정의한다
  - User 모델이 email(unique), password, timestamp 필드를 포함한다
  - Chat 모델이 User와 다대일 관계, Message와 일대다 관계를 맺는다
  - Message 모델이 content, role, chatId 필드를 포함한다
  - onDelete: Cascade가 연관 레코드 자동 삭제를 설정한다
  - .env.example이 필수 환경변수를 모두 포함한다
  </done>
</task>

<task type="auto">
  <name>NestJS 애플리케이션 설정</name>
  <files>backend/src/main.ts, backend/src/app.module.ts</files>
  <read_first>
  - backend/prisma/schema.prisma - 데이터베이스 모델 확인
  - backend/.env.example - 환경변수 확인
  - backend/package.json - @nestjs/config 의존성 확인
  </read_first>
  <action>
  NestJS 부트스트랩과 루트 모듈을 생성한다. ValidationPipe를 전역으로 설정하여 BACK-04 요구사항을 충족한다:

  **1) backend/src/main.ts**
  ```typescript
  import { NestFactory } from '@nestjs/core';
  import { ValidationPipe } from '@nestjs/common';
  import { AppModule } from './app.module';

  async function bootstrap() {
    const app = await NestFactory.create(AppModule);

    // BACK-04: DTO 기반 검증 활성화
    app.useGlobalPipes(
      new ValidationPipe({
        whitelist: true,             // DTO에 정의되지 않은 속성 제거
        forbidNonWhitelisted: true,  // 정의되지 않은 속성 있으면 에러
        transform: true,             // 타입 변환 활성화
      }),
    );

    // CORS 설정
    app.enableCors({
      origin: process.env.FRONTEND_URL || 'http://localhost:5173',
      credentials: true,
    });

    const port = process.env.PORT || 3000;
    await app.listen(port);
    console.log(`Application is running on: http://localhost:${port}`);
  }
  bootstrap();
  ```

  **2) backend/src/app.module.ts**
  ```typescript
  import { Module } from '@nestjs/common';
  import { ConfigModule } from '@nestjs/config';

  @Module({
    imports: [
      ConfigModule.forRoot({
        isGlobal: true,  // 모든 모듈에서 ConfigModule 접근 가능
        envFilePath: '.env',
      }),
    ],
    controllers: [],
    providers: [],
  })
  export class AppModule {}
  ```
  </action>
  <verify>
    <automated>grep -q "ValidationPipe" backend/src/main.ts && grep -q "whitelist: true" backend/src/main.ts && grep -q "ConfigModule.forRoot" backend/src/app.module.ts && grep -q "isGlobal: true" backend/src/app.module.ts</automated>
  </verify>
  <done>
  - main.ts가 ValidationPipe를 전역 파이프로 설정한다
  - ValidationPipe가 whitelist, forbidNonWhitelisted, transform 옵션을 활성화한다 (BACK-04)
  - main.ts가 CORS를 설정하여 프론트엔드에서 요청을 허용한다
  - app.module.ts가 ConfigModule을 isGlobal: true로 가져온다
  - 애플리케이션이 port 3000에서 실행된다
  </done>
</task>

<task type="auto">
  <name>Biome 설정 및 루트 환경변수</name>
  <files>biome.json, .env.example</files>
  <read_first>
  - package.json (루트) - Biome 의존성 확인
  - backend/.env.example - 백엔드 환경변수 확인
  </read_first>
  <action>
  Biome 린터/포매터 설정과 루트 환경변수 템플릿을 생성한다:

  **1) biome.json** (루트)
  ```json
  {
    "linter": {
      "enabled": true,
      "rules": {
        "recommended": true,
        "suspicious": {
          "noExplicitAny": "warn"
        },
        "style": {
          "noVar": "error"
        },
        "correctness": {
          "noUnusedVariables": "warn"
        }
      }
    },
    "formatter": {
      "enabled": true,
      "formatWithErrors": false,
      "indentStyle": "space",
      "indentWidth": 2,
      "lineWidth": 80,
      "lineEnding": "lf"
    },
    "javascript": {
      "formatter": {
        "jsxQuoteStyle": "double",
        "quoteProperties": "asNeeded",
        "trailingCommas": "es5",
        "semicolons": "always",
        "arrowParentheses": "always",
        "bracketSpacing": true,
        "bracketSameLine": false
      }
    },
    "json": {
      "formatter": {
        "enabled": true,
        "indentStyle": "space",
        "trailingCommas": "none"
      }
    },
    "files": {
      "ignoreUnknown": false,
      "ignore": [
        "node_modules",
        "dist",
        ".next",
        "coverage"
      ]
    }
  }
  ```

  **2) .env.example** (루트)
  ```env
  # Database
  DATABASE_URL="file:./backend/prisma/dev.db"

  # JWT
  JWT_SECRET="your-secret-key-change-in-production"
  JWT_EXPIRES_IN="15m"
  JWT_REFRESH_EXPIRES_IN="7d"

  # OpenRouter API
  OPENROUTER_API_KEY="your-openrouter-api-key"
  OPENROUTER_API_URL="https://openrouter.ai/api/v1"

  # Server
  PORT=3000
  FRONTEND_URL="http://localhost:5173"

  # Node
  NODE_ENV="development"
  ```
  </action>
  <verify>
    <automated>grep -q "\"linter\": { \"enabled\": true" biome.json && grep -q "\"formatter\": { \"enabled\": true" biome.json && grep -q "DATABASE_URL" .env.example && grep -q "OPENROUTER_API_KEY" .env.example</automated>
  </verify>
  <done>
  - biome.json가 linter와 formatter를 활성화한다
  - linter가 recommended 규칙을 사용하고 noExplicitAny를 경고한다
  - formatter가 space indent, 80자 line width, semicolons always를 사용한다
  - javascript 포매터가 JSX double quote, trailing commas es5를 사용한다
  - files.ignore가 node_modules, dist를 제외한다
  - .env.example이 모든 필수 환경변수를 포함한다
  </done>
</task>

</tasks>

<verification>
## 전체 단계 검증

### 1단계: Prisma 초기화
```bash
cd backend && npx prisma generate
```
**예상 결과:** Prisma Client가 생성되고 타입 정의가 설치된다

### 2단계: 데이터베이스 마이그레이션
```bash
cd backend && npx prisma migrate dev --name init
```
**예상 결과:** `backend/prisma/dev.db` 파일이 생성되고 마이그레이션 폴더에 마이그레이션 파일이 생성된다

### 3단계: 백엔드 서버 실행
```bash
cd backend && cp .env.example .env
pnpm --filter backend dev
```
**예상 결과:** "Application is running on: http://localhost:3000" 메시지가 표시되고 서버가 실행된다

### 4단계: Biome 린트 실행
```bash
pnpm lint
```
**예상 결과:** 에러 없이 완료되거나 권장사항(warn)만 표시된다

### 5단계: ValidationPipe 설정 확인
```bash
curl -X POST http://localhost:3000/api/test -H "Content-Type: application/json" -d '{"unknown":"field"}'
```
**예상 결과:** 400 Bad Request 에러 (ValidationPipe가 정의되지 않은 필드를 거부)

### 6단계: 환경변수 설정
```bash
cp .env.example .env
# .env 파일에 실제 값 입력 (JWT_SECRET, OPENROUTER_API_KEY 등)
```
</verification>

<success_criteria>
**완료 기준:**
1. `npx prisma migrate dev --name init`로 dev.db가 생성되고 마이그레이션이 성공한다
2. `pnpm --filter backend dev`로 백엔드 서버가 port 3000에서 실행된다
3. `pnpm lint`로 Biome이 에러 없이 실행된다
4. ValidationPipe가 whitelist: true, forbidNonWhitelisted: true로 설정된다 (BACK-04)
5. ConfigModule이 isGlobal: true로 설정되어 모든 모듈에서 환경변수를 접근할 수 있다
6. .env.example이 모든 필수 환경변수를 포함한다
7. CORS가 프론트엔드(origin: localhost:5173)에서 요청을 허용하도록 설정된다
</success_criteria>

<output>
완료 후 다음 파일을 생성한다:
- `.planning/phases/01-project-setup/01-02-SUMMARY.md`
</output>
