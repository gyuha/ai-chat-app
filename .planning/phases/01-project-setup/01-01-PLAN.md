---
phase: 01-project-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - package.json
  - backend/package.json
  - frontend/package.json
  - backend/tsconfig.json
  - frontend/tsconfig.json
  - backend/nest-cli.json
  - frontend/vite.config.ts
  - frontend/index.html
  - frontend/src/main.tsx
  - .gitignore
autonomous: true
requirements:
  - BACK-04
must_haves:
  truths:
    - "개발자가 pnpm install로 모든 의존성을 설치할 수 있다"
    - "개발자가 pnpm --filter backend dev로 백엔드 개발 서버를 실행할 수 있다"
    - "개발자가 pnpm --filter frontend dev로 프론트엔드 개발 서버를 실행할 수 있다"
    - "TypeScript가 두 패키지에서 모두 컴파일된다"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "pnpm workspace 구성"
      contains: "packages: - 'backend' - 'frontend'"
    - path: "package.json"
      provides: "루트 공통 의존성 및 스크립트"
      contains: "\"name\": \"gsd-glm\""
    - path: "backend/package.json"
      provides: "NestJS 백엔드 의존성"
      contains: "@nestjs/common"
    - path: "frontend/package.json"
      provides: "React + Vite 프론트엔드 의존성"
      contains: "react"
    - path: "backend/tsconfig.json"
      provides: "백엔드 TypeScript 설정"
      contains: "\"strict\": true"
    - path: "frontend/tsconfig.json"
      provides: "프론트엔드 TypeScript 설정"
      contains: "\"strict\": true"
    - path: "frontend/vite.config.ts"
      provides: "Vite 빌드 설정 및 API 프록시"
      contains: "proxy: { '/api': 'http://localhost:3000' }"
  key_links:
    - from: "pnpm-workspace.yaml"
      to: "backend/package.json, frontend/package.json"
      via: "workspace protocol"
      pattern: "workspace:\\*"
    - from: "package.json (root)"
      to: "backend/, frontend/"
      via: "pnpm --filter"
      pattern: "--filter"
    - from: "frontend/vite.config.ts"
      to: "backend (port 3000)"
      via: "proxy configuration"
      pattern: "proxy.*3000"
---

<objective>
pnpm workspace 기반 모노레포 구조를 생성하고 NestJS 백엔드와 React 프론트엔드의 기본 패키지 설정을 완료한다.

Purpose: 프론트엔드와 백엔드를 별도 패키지로 관리하면서 공통 의존성을 공유하고, 일관된 개발 환경을 구축한다.
Output: pnpm workspace 설정 파일들, package.json 파일들, TypeScript 설정, Vite 설정
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app.worktrees/gsd-glm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app.worktrees/gsd-glm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-project-setup/01-RESEARCH.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md

## 핵심 기술 스택 (01-RESEARCH.md)

pnpm workspace 모노레포 구조:
- 루트에 공통 의존성 선언으로 디스크 공간 절약
- workspace:* 프로토콜로 패키지 간 링크
- 의존성 호이스팅 충돌 방지

백엔드 (NestJS 11.1.17):
- @nestjs/common, @nestjs/core, @nestjs/platform-express
- @nestjs/config (환경변수 관리)
- TypeScript 6.0.2

프론트엔드 (React 19.2.4 + Vite 8.0.3):
- react, react-dom
- vite, @vitejs/plugin-react
- TypeScript 6.0.2

## 프로젝트 구조 (ARCHITECTURE.md)

```
gsd-glm/
├── backend/
│   ├── src/
│   ├── package.json
│   ├── tsconfig.json
│   └── nest-cli.json
├── frontend/
│   ├── src/
│   ├── package.json
│   ├── tsconfig.json
│   └── vite.config.ts
├── pnpm-workspace.yaml
└── package.json
```

## 설치 버전 정보 (STACK.md)

pnpm: 9.x
TypeScript: 6.0.2
NestJS: 11.1.17
React: 19.2.4
Vite: 8.0.3
</context>

<tasks>

<task type="auto">
  <name>Workspace 및 패키지 기본 구조 생성</name>
  <files>pnpm-workspace.yaml, package.json, backend/package.json, frontend/package.json</files>
  <read_first>
  - 없음 (greenfield 프로젝트)
  </read_first>
  <action>
  다음 파일들을 생성하여 pnpm workspace 모노레포 구조를 설정한다:

  **1) pnpm-workspace.yaml** (루트)
  ```yaml
  packages:
    - 'backend'
    - 'frontend'
  ```

  **2) package.json** (루트)
  ```json
  {
    "name": "gsd-glm",
    "private": true,
    "version": "0.1.0",
    "description": "OpenRouter Free Chat Web App",
    "scripts": {
      "dev": "concurrently \"pnpm --filter backend dev\" \"pnpm --filter frontend dev\"",
      "lint": "biome check .",
      "format": "biome format --write .",
      "install:all": "pnpm install"
    },
    "devDependencies": {
      "@biomejs/biome": "^2.4.0",
      "typescript": "^6.0.0",
      "concurrently": "^9.1.2"
    }
  }
  ```

  **3) backend/package.json**
  ```json
  {
    "name": "backend",
    "version": "0.1.0",
    "description": "OpenRouter Chat Backend",
    "scripts": {
      "dev": "nest start --watch",
      "build": "nest build",
      "start": "node dist/main"
    },
    "dependencies": {
      "@nestjs/common": "^11.0.0",
      "@nestjs/core": "^11.0.0",
      "@nestjs/platform-express": "^11.0.0",
      "@nestjs/config": "^4.0.0",
      "reflect-metadata": "^0.2.2"
    },
    "devDependencies": {
      "@nestjs/cli": "^11.0.0",
      "@types/node": "^22.0.0"
    }
  }
  ```

  **4) frontend/package.json**
  ```json
  {
    "name": "frontend",
    "version": "0.1.0",
    "description": "OpenRouter Chat Frontend",
    "type": "module",
    "scripts": {
      "dev": "vite",
      "build": "tsc && vite build",
      "preview": "vite preview"
    },
    "dependencies": {
      "react": "^19.0.0",
      "react-dom": "^19.0.0"
    },
    "devDependencies": {
      "@vitejs/plugin-react": "^6.0.0",
      "vite": "^8.0.0"
    }
  }
  ```
  </action>
  <verify>
    <automated>grep -q "packages:" pnpm-workspace.yaml && grep -q '"name": "gsd-glm"' package.json && grep -q "@nestjs/common" backend/package.json && grep -q "react" frontend/package.json</automated>
  </verify>
  <done>
  - pnpm-workspace.yaml이 backend와 frontend 패키지를 정의한다
  - 루트 package.json이 공통 의존성(TypeScript, Biome)을 선언한다
  - backend/package.json이 NestJS 핵심 의존성을 포함한다
  - frontend/package.json이 React와 Vite 의존성을 포함한다
  </done>
</task>

<task type="auto">
  <name>TypeScript 설정 및 빌드 도구 구성</name>
  <files>backend/tsconfig.json, frontend/tsconfig.json, backend/nest-cli.json, frontend/vite.config.ts</files>
  <read_first>
  - package.json (루트) - TypeScript 버전 확인
  - backend/package.json - NestJS 사용 확인
  - frontend/package.json - Vite 사용 확인
  </read_first>
  <action>
  다음 설정 파일들을 생성하여 TypeScript 컴파일과 빌드 환경을 구성한다:

  **1) tsconfig.json** (루트 - 공통 설정)
  ```json
  {
    "compilerOptions": {
      "target": "ES2022",
      "module": "commonjs",
      "lib": ["ES2022"],
      "strict": true,
      "skipLibCheck": true,
      "esModuleInterop": true,
      "allowSyntheticDefaultImports": true,
      "forceConsistentCasingInFileNames": true
    }
  }
  ```

  **2) backend/tsconfig.json** (루트 설정 확장)
  ```json
  {
    "extends": "../tsconfig.json",
    "compilerOptions": {
      "module": "commonjs",
      "declaration": true,
      "removeComments": true,
      "emitDecoratorMetadata": true,
      "experimentalDecorators": true,
      "allowSyntheticDefaultImports": true,
      "target": "ES2022",
      "sourceMap": true,
      "outDir": "./dist",
      "baseUrl": "./",
      "incremental": true,
      "skipLibCheck": true,
      "strictNullChecks": false,
      "noImplicitAny": false,
      "strictBindCallApply": false,
      "forceConsistentCasingInFileNames": false,
      "noFallthroughCasesInSwitch": false
    },
    "include": ["src/**/*"],
    "exclude": ["node_modules", "dist", "test"]
  }
  ```

  **3) frontend/tsconfig.json** (루트 설정 확장, Vite용)
  ```json
  {
    "extends": "../tsconfig.json",
    "compilerOptions": {
      "target": "ES2020",
      "useDefineForClassFields": true,
      "lib": ["ES2020", "DOM", "DOM.Iterable"],
      "module": "ESNext",
      "skipLibCheck": true,
      "moduleResolution": "bundler",
      "allowImportingTsExtensions": true,
      "resolveJsonModule": true,
      "isolatedModules": true,
      "noEmit": true,
      "jsx": "react-jsx",
      "strict": true,
      "noUnusedLocals": true,
      "noUnusedParameters": true,
      "noFallthroughCasesInSwitch": true
    },
    "include": ["src"],
    "references": [{ "path": "./tsconfig.node.json" }]
  }
  ```

  **4) frontend/tsconfig.node.json** (Vite 설정 파일용)
  ```json
  {
    "compilerOptions": {
      "composite": true,
      "skipLibCheck": true,
      "module": "ESNext",
      "moduleResolution": "bundler",
      "allowSyntheticDefaultImports": true
    },
    "include": ["vite.config.ts"]
  }
  ```

  **5) backend/nest-cli.json** (NestJS CLI 설정)
  ```json
  {
    "$schema": "https://json.schemastore.org/nest-cli",
    "collection": "@nestjs/schematics",
    "sourceRoot": "src",
    "compilerOptions": {
      "deleteOutDir": true
    }
  }
  ```

  **6) frontend/vite.config.ts** (Vite 설정 + API 프록시)
  ```typescript
  import { defineConfig } from 'vite';
  import react from '@vitejs/plugin-react';

  export default defineConfig({
    plugins: [react()],
    server: {
      port: 5173,
      proxy: {
        '/api': {
          target: 'http://localhost:3000',
          changeOrigin: true,
        },
      },
    },
  });
  ```
  </action>
  <verify>
    <automated>grep -q "\"strict\": true" tsconfig.json && grep -q "experimentalDecorators": true" backend/tsconfig.json && grep -q "jsx": \"react-jsx\"" frontend/tsconfig.json && grep -q "proxy:" frontend/vite.config.ts</automated>
  </verify>
  <done>
  - 루트 tsconfig.json이 공통 TypeScript 옵션을 정의한다
  - backend/tsconfig.json이 NestJS 데코레이터와 컴파일 옵션을 설정한다
  - frontend/tsconfig.json이 Vite용 ESNext 모듈과 React JSX를 설정한다
  - frontend/vite.config.ts가 /api 요청을 백엔드(port 3000)로 프록시한다
  - nest-cli.json이 NestJS CLI 소스 경로를 설정한다
  </done>
</task>

<task type="auto">
  <name>프론트엔드 엔트리 포인트 및 .gitignore 생성</name>
  <files>frontend/index.html, frontend/src/main.tsx, .gitignore</files>
  <read_first>
  - frontend/package.json - Vite 엔트리 포인트 확인
  - frontend/vite.config.ts - Vite 설정 확인
  </read_first>
  <action>
  다음 파일들을 생성하여 프론트엔드 기본 구조를 완성하고 Git 설정을 추가한다:

  **1) frontend/index.html** (Vite 엔트리 HTML)
  ```html
  <!doctype html>
  <html lang="ko">
    <head>
      <meta charset="UTF-8" />
      <meta name="viewport" content="width=device-width, initial-scale=1.0" />
      <title>OpenRouter Chat</title>
    </head>
    <body>
      <div id="root"></div>
      <script type="module" src="/src/main.tsx"></script>
    </body>
  </html>
  ```

  **2) frontend/src/main.tsx** (React 엔트리)
  ```typescript
  import React from 'react';
  import ReactDOM from 'react-dom/client';
  import './index.css';

  ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
      <div>
        <h1>OpenRouter Chat</h1>
        <p>개발 서버가 정상적으로 실행 중입니다.</p>
      </div>
    </React.StrictMode>,
  );
  ```

  **3) frontend/src/index.css** (기본 스타일)
  ```css
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  body {
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', sans-serif;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }

  #root {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
  }
  ```

  **4) .gitignore** (루트)
  ```
  # Dependencies
  node_modules/
  .pnp/
  .pnp.js

  # Environment variables
  .env
  .env.local
  .env.*.local

  # Build outputs
  dist/
  dist-ssr/
  *.local

  # Database
  *.db
  *.db-journal
  backend/prisma/migrations/

  # Editor
  .vscode/*
  !.vscode/extensions.json
  .idea/
  .DS_Store
  *.suo
  *.ntvs*
  *.njsproj
  *.sln
  *.sw?

  # Logs
  logs
  *.log
  npm-debug.log*
  pnpm-debug.log*
  yarn-debug.log*
  yarn-error.log*
  lerna-debug.log*
  ```
  </action>
  <verify>
    <automated>test -f frontend/index.html && test -f frontend/src/main.tsx && test -f .gitignore && grep -q "node_modules" .gitignore</automated>
  </verify>
  <done>
  - frontend/index.html이 root div와 main.tsx 스크립트를 포함한다
  - frontend/src/main.tsx가 React 앱을 마운트한다
  - frontend/src/index.css가 기본 리셋 스타일을 적용한다
  - .gitignore가 node_modules, .env, build output, 데이터베이스 파일을 제외한다
  </done>
</task>

</tasks>

<verification>
## 전체 단계 검증

### 1단계: 패키지 설치
```bash
pnpm install
```
**예상 결과:** 에러 없이 모든 의존성이 설치되고 node_modules/가 생성된다

### 2단계: 백엔드 개발 서버 (뼈대만)
```bash
cd backend && npx nest new . --skip-git --package-manager pnpm
```
**참고:** 이 태스크에서는 패키지 설정만 생성. 실제 NestJS 소스 코드는 다음 계획(01-02)에서 생성

### 3단계: 프론트엔드 개발 서버
```bash
pnpm --filter frontend dev
```
**예상 결과:** Vite dev server가 port 5173에서 시작되고 브라우저에 "OpenRouter Chat" 표시

### 4단계: TypeScript 컴파일 검증
```bash
cd backend && npx tsc --noEmit
cd frontend && npx tsc --noEmit
```
**예상 결과:** 두 패키지 모두 TypeScript 컴파일 에러 없음
</verification>

<success_criteria>
**완료 기준:**
1. `pnpm install` 실행 시 에러 없이 완료된다
2. `pnpm --filter frontend dev`로 프론트엔드 서버가 port 5173에서 시작된다
3. TypeScript가 두 패키지에서 컴파일 에러 없이 빌드된다
4. pnpm workspace 구조가 backend와 frontend 패키지를 올바르게 인식한다
5. Vite가 /api 요청을 localhost:3000으로 프록시하도록 설정된다
6. .gitignore가 환경변수, 데이터베이스, 빌드 출력을 올바르게 제외한다
</success_criteria>

<output>
완료 후 다음 파일을 생성한다:
- `.planning/phases/01-project-setup/01-01-SUMMARY.md`
</output>
