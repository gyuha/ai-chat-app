---
phase: 02-core-chat-loop
plan: '03'
type: execute
wave: 3
depends_on:
  - '02'
files_modified:
  - src/components/chat/ChatInput.tsx
  - src/components/layout/ChatArea.tsx
  - src/components/chat/MessageList.tsx
autonomous: true
requirements:
  - MSG-05
  - MSG-06
  - MSG-07
must_haves:
  truths:
    - AI 응답 완료 전 취소 버튼으로 중단 가능
    - 메시지 전송 중 입력창 비활성화 (중복 전송 방지)
    - 메시지 영역은 자동 스크롤로 최신 메시지 위치
  artifacts:
    - path: src/components/chat/ChatInput.tsx
      provides: 취소 버튼, 입력창 잠금
      contains: cancelStream, disabled={isStreaming}
    - path: src/components/layout/ChatArea.tsx
      provides: MessageList 통합
      contains: MessageList
    - path: src/components/chat/MessageList.tsx
      provides: 자동 스크롤
      contains: scrollIntoView
  key_links:
    - from: MessageList.tsx
      to: ChatArea.tsx
      via: render
      pattern: MessageList
    - from: ChatArea.tsx
      to: ChatContext
      via: useChat
      pattern: useChat
---

<objective>
취소 버튼, 입력창 잠금, 자동 스크롤 기능을 구현한다.
MSG-05, MSG-06, MSG-07을 충족한다.
</objective>

<context>
@src/components/chat/ChatInput.tsx
@src/components/layout/ChatArea.tsx
@src/context/ChatContext.tsx

From src/components/chat/ChatInput.tsx (after 02-02):
- has isStreaming state
- has abortControllerRef
- has handleSubmit with streaming logic
- needs: cancel button and input disable

From src/components/layout/ChatArea.tsx (after 02-01):
- has messages rendering
- needs: auto-scroll and MessageList refactor

**Research Pattern 3 - Auto-scroll (from RESEARCH.md):**
```typescript
const bottomRef = useRef<HTMLDivElement>(null);

useEffect(() => {
  bottomRef.current?.scrollIntoView({ behavior: 'smooth' });
}, [messages]);
```

**Research Pattern 5 - Cancel (from RESEARCH.md):**
```typescript
const cancelStream = () => {
  if (abortControllerRef.current) {
    abortControllerRef.current.abort();
    abortControllerRef.current = null;
  }
};
```

**Pitfall 5 Warning (auto-scroll):**
Only auto-scroll if user is near bottom (~100px threshold). If user scrolled up to read history, don't interrupt.
</context>

<tasks>

<task type="auto">
<name>Task 1: Add cancel button and input disable to ChatInput</name>
<files>src/components/chat/ChatInput.tsx</files>
<action>
1. Read existing ChatInput.tsx

2. Add cancelStream function:
   - Check if abortControllerRef.current exists
   - Call abort() on it
   - Set abortControllerRef.current = null
   - This will cause fetch to throw AbortError

3. Add cancel button next to textarea (or modify send button):
   - Conditionally render:
     - If isStreaming: show cancel button (red bg, "Cancel" text)
     - Else: show send button (disabled if input.trim() is empty)
   - onClick: call cancelStream

4. Disable textarea during streaming:
   - Add disabled={isStreaming} to textarea
   - Update placeholder: isStreaming ? 'Waiting for response...' : 'Type a message...'

5. Style changes for disabled state:
   - Add conditional class for disabled textarea:
     - isStreaming ? 'bg-gray-100 cursor-not-allowed' : 'bg-white'

**MSG-06 Verification:** textarea has disabled={isStreaming}

**MSG-05 Verification:** Cancel button calls abort() on AbortController
</action>
<verify>
<automated>grep -n "disabled={isStreaming}" src/components/chat/ChatInput.tsx && grep -n "cancelStream" src/components/chat/ChatInput.tsx</automated>
</verify>
<done>
Cancel button appears during streaming and aborts the request (MSG-05). Input textarea is disabled during streaming (MSG-06). Duplicate submissions prevented.
</done>
</task>

<task type="auto">
<name>Task 2: Create MessageList component with auto-scroll</name>
<files>src/components/chat/MessageList.tsx</files>
<action>
1. Create src/components/chat/MessageList.tsx

2. Component receives messages and isStreaming as props:
```typescript
interface MessageListProps {
  messages: Message[];
  isStreaming: boolean;
}
```

3. Add auto-scroll logic:
   - const bottomRef = useRef<HTMLDivElement>(null)
   - useEffect that:
     - Only scrolls if user is near bottom (within 100px)
     - Check scrollHeight - scrollTop - clientHeight < 100
     - If near bottom: bottomRef.current?.scrollIntoView({ behavior: 'smooth' })
   - Dependency: messages.length and isStreaming

4. Render messages:
   - Map over messages
   - Each message: flex justify-end for user, justify-start for assistant
   - Message bubble styling same as existing ChatArea

5. Place div ref at bottom:
   - <div ref={bottomRef} />

**Pitfall 5 mitigation:** Check if user has scrolled up before auto-scrolling

```typescript
const shouldScroll = useCallback(() => {
  const container = bottomRef.current?.parentElement;
  if (!container) return true;
  const distanceFromBottom = container.scrollHeight - container.scrollTop - container.clientHeight;
  return distanceFromBottom < 100;
}, []);
```
</action>
<verify>
<automated>grep -n "scrollIntoView" src/components/chat/MessageList.tsx && grep -n "bottomRef" src/components/chat/MessageList.tsx</automated>
</verify>
<done>
Messages render in list. When new messages arrive and user is near bottom, auto-scrolls to show latest (MSG-07). Does not interrupt if user scrolled up.
</done>
</task>

<task type="auto">
<name>Task 3: Integrate MessageList into ChatArea</name>
<files>src/components/layout/ChatArea.tsx</files>
<action>
1. Read existing ChatArea.tsx

2. Replace inline messages rendering with MessageList component:
   - Import MessageList from '../chat/MessageList'
   - Import useState, useRef from react (for isStreaming state)
   - Note: isStreaming state needs to come from ChatInput context or lifted state

3. Lifting isStreaming state issue:
   - Currently isStreaming is local to ChatInput
   - Need to lift it to ChatArea or use Context

**Solution - Use Context for streaming state:**
1. Add isStreaming and streamingMessageId to ChatState:
   - Add to ChatState interface: isStreaming: boolean
   - Add action types: START_STREAMING, FINISH_STREAMING, CANCEL_STREAMING
   - Add dispatch actions for these

2. In ChatArea:
   - Get isStreaming from useChat().state
   - Pass to MessageList

3. In ChatInput:
   - Get dispatch from useChat
   - On streaming start: dispatch({ type: 'START_STREAMING' })
   - On streaming end/cancel: dispatch({ type: 'FINISH_STREAMING' })

4. Update ChatArea messages rendering:
   - Replace inline map with <MessageList messages={selectedConversation.messages} isStreaming={state.isStreaming} />
</action>
<verify>
<automated>grep -n "MessageList" src/components/layout/ChatArea.tsx && grep -n "isStreaming" src/components/layout/ChatArea.tsx</automated>
</verify>
<done>
MessageList integrated into ChatArea. Auto-scroll works on new messages. isStreaming state shared across components via Context.
</done>
</task>

</tasks>

<verification>
1. grep -n "disabled={isStreaming}" src/components/chat/ChatInput.tsx - input disabled during streaming
2. grep -n "cancelStream" src/components/chat/ChatInput.tsx - cancel function exists
3. grep -n "scrollIntoView" src/components/chat/MessageList.tsx - auto-scroll exists
4. grep -n "MessageList" src/components/layout/ChatArea.tsx - integration verified
5. npm run build passes
</verification>

<success_criteria>
- Cancel button appears during streaming, stops AI response (MSG-05)
- Input textarea disabled during streaming (MSG-06)
- Messages auto-scroll to bottom when new content arrives (MSG-07)
- npm run build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-chat-loop/02-03-SUMMARY.md`
</output>
