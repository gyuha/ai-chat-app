---
phase: 03-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Toast.tsx
  - src/context/ChatContext.tsx
  - src/hooks/useLocalStorage.ts
autonomous: false
requirements:
  - STORE-01
  - STORE-02
  - STORE-03
user_setup: []

must_haves:
  truths:
    - "사용자가 localStorage 실패 시 토스트 알림을 받을 수 있다 (D-02)"
    - "localStorage 용량 초과 시 적절한 오류 처리가 된다 (D-03, STORE-03)"
    - "새로고침 후 대화 데이터가 복원된다 (STORE-02)"
    - "대화방 데이터가 localStorage에 자동 저장된다 (STORE-01)"
  artifacts:
    - path: "src/components/Toast.tsx"
      provides: "토스트 알림 UI 컴포넌트"
      contains: "ToastProvider, useToast hook, toast variants (error/success)"
    - path: "src/context/ChatContext.tsx"
      provides: "localStorage 연동 및 오류 처리"
      modifies: "useEffect에서 setSavedState 호출, try-catch로 quota exceeded 처리"
    - path: "src/hooks/useLocalStorage.ts"
      provides: "localStorage hook - quota exceeded 감지"
      modifies: "QuotaExceededError 감지 후 콜백으로 에러 전달"
  key_links:
    - from: "ChatContext.tsx"
      to: "Toast.tsx"
      via: "useToast hook"
      pattern: "useToast().error"
    - from: "ChatContext.tsx"
      to: "useLocalStorage.ts"
      via: "setSavedState 호출"
      pattern: "setSavedState.* quota exceeded"
---

<objective>
localStorage를 통한 데이터 영속성 구현. localStorage 저장/복원 로직 및 용량 초과 시 오류 처리를 포함.
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app.worktrees/simple/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app.worktrees/simple/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/hooks/useLocalStorage.ts
@src/context/ChatContext.tsx
@src/types/chat.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Toast component</name>
  <files>src/components/Toast.tsx</files>
  <read_first>
    - src/hooks/useLocalStorage.ts
    - src/types/chat.ts
  </read_first>
  <action>
    Create a Toast notification component system with:
    - ToastProvider context and hook (useToast)
    - toast function: `{ message: string, type?: 'error' | 'success' }`
    - Auto-dismiss after 4000ms for success, 6000ms for error
    - Position: bottom-right corner
    - Tailwind CSS for styling
    - Z-index: 50

    The toast state should be managed via useState + useEffect for auto-dismiss timer.
    Use React portals if needed for proper stacking.
  </action>
  <verify>
    <automated>grep -l "ToastProvider\|useToast" src/components/Toast.tsx && grep -c "toast" src/components/Toast.tsx</automated>
  </verify>
  <done>Toast.tsx created with ToastProvider, useToast hook, and toast function. Auto-dismiss works.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Toast into ChatProvider for localStorage errors</name>
  <files>src/context/ChatContext.tsx</files>
  <read_first>
    - src/context/ChatContext.tsx
    - src/components/Toast.tsx
    - src/hooks/useLocalStorage.ts
  </read_first>
  <action>
    Modify ChatContext.tsx to:
    1. Import and wrap children with ToastProvider
    2. In the useEffect that calls setSavedState, wrap in try-catch
    3. On QuotaExceededError, call toast({ message: 'localStorage 용량이 부족합니다. 대화를 삭제하여 공간을 확보해주세요.', type: 'error' })
    4. On other errors, log to console (don't crash)

    Per D-02: Use toast for user notification
    Per D-03: No auto-deletion - just notify user
  </action>
  <verify>
    <automated>grep -n "QuotaExceededError\|toast\|ToastProvider" src/context/ChatContext.tsx</automated>
  </verify>
  <done>ChatProvider catches localStorage errors and shows toast notification to user.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Verify persistence works</name>
  <files></files>
  <read_first>
    - src/components/Toast.tsx
    - src/context/ChatContext.tsx
  </read_first>
  <action>Human verification of localStorage persistence</action>
  <how-to-verify>
    1. Open browser devtools -> Application -> Local Storage
    2. Clear any existing data
    3. Refresh page
    4. Create a new chat and send a message
    5. Refresh page again
    6. Verify conversation persists across refresh
    7. To test quota exceeded error (if possible):
       - Fill localStorage with large data until quota exceeded
       - Create new conversation
       - Verify toast appears with error message
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <done>All conversations persist across page refresh. Toast notifications appear on localStorage errors.</done>
</task>

</tasks>

<verification>
- Toast component renders without crashing
- localStorage persistence works (conversations survive refresh)
- QuotaExceededError triggers toast notification (not app crash)
</verification>

<success_criteria>
1. 새로고침 후 대화 데이터가 복원된다 (STORE-02)
2. 모든 대화방 데이터가 localStorage에 자동 저장된다 (STORE-01)
3. localStorage 용량 초과 시 토스트 알림으로 사용자에게 알려진다 (STORE-03, D-02, D-03)
</success_criteria>

<output>
After completion, create `.planning/phases/03-persistence/03-01-SUMMARY.md`
</output>
