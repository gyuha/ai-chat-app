---
wave: 3
depends_on:
  - 03-02
files_modified:
  - frontend/src/stores/chat.ts
  - frontend/src/stores/chatList.ts
  - frontend/src/routes/chat.tsx
  - frontend/src/routes/index.tsx
  - frontend/src/components/chat/ChatLayout.tsx
  - frontend/src/components/chat/ChatSidebar.tsx
  - frontend/src/components/chat/ChatMain.tsx
  - frontend/src/components/chat/MessageItem.tsx
  - frontend/src/components/chat/ChatInput.tsx
  - frontend/src/lib/api/chat.ts
  - frontend/src/lib/api/message.ts
autonomous: true
---

# PLAN 03-03: 프론트엔드 채팅 UI 구현

**목표:** ChatGPT 스타일의 2단 레이아웃(사이드바 + 메인 영역)을 구현하고, Zustand 스토어로 채팅 상태를 관리하며, 실시간 스트리밍 메시지를 표시한다.

## 성공 기준

1. 사이드바에 대화 목록이 표시되고 새 대화 버튼이 동작한다
2. 메인 영역에 메시지가 사용자/AI 구분되어 표시된다
3. 메시지 전송 시 스트리밍 응답이 실시간으로 렌더링된다
4. Stop generating 버튼이 스트리밍 중에 표시되고 동작한다

## 작업

### Task 0: shadcn/ui 초기화 및 필수 컴포넌트 설치

<read_first>
- frontend/package.json
- CLAUDE.md (프로젝트 설정 참조)
</read_first>

<acceptance_criteria>
- shadcn/ui가 초기화되어야 한다 (components.json 생성)
- Button 컴포넌트가 설치되어야 한다
- Textarea 컴포넌트가 설치되어야 한다
- ScrollArea 컴포넌트가 설치되어야 한다
- Card 컴포넌트가 설치되어야 한다
- frontend/src/components/ui/ 디렉토리가 생성되어야 한다
</acceptance_criteria>

<action>
터미널에서 다음 명령을 순서대로 실행한다:

1. shadcn/ui 초기화:
```bash
cd frontend && npx shadcn@latest init -y
```

2. 필수 컴포넌트 설치:
```bash
cd frontend && npx shadcn@latest add button textarea scroll-area card -y
```

3. 설치 확인:
```bash
ls frontend/src/components/ui/
```

예상 출력: button.tsx, textarea.tsx, scroll-area.tsx, card.tsx, utils.ts 등
</action>

---

### Task 1: Zustand 스토어 생성

<read_first>
- frontend/src/stores/auth.ts (persist 패턴 참조)
- frontend/src/lib/api/types.ts (타입 정의)
</read_first>

<acceptance_criteria>
- frontend/src/stores/chat.ts 파일이 존재해야 한다
- frontend/src/stores/chatList.ts 파일이 존재해야 한다
- chatStore가 현재 대화, 메시지 목록, 스트리밍 상태를 관리해야 한다
- chatListStore가 대화 목록을 관리해야 한다
- persist 미들웨어로 상태가 유지되어야 한다
</acceptance_criteria>

<action>
**frontend/src/stores/chat.ts 생성**
```typescript
import { create } from 'zustand';
import { persist } from 'zustand/middleware';
import type { Chat, Message } from '../lib/api/types';
import { streamMessage } from '../lib/api/streaming';

interface ChatState {
  // 상태
  currentChat: Chat | null;
  messages: Message[];
  streamingContent: string;
  isStreaming: boolean;
  error: string | null;
  abortController: AbortController | null;

  // 액션
  setCurrentChat: (chat: Chat | null) => void;
  setMessages: (messages: Message[]) => void;
  addMessage: (message: Message) => void;
  startStreaming: () => void;
  updateStreamingContent: (content: string) => void;
  stopStreaming: () => void;
  clearStreaming: () => void;
  setError: (error: string | null) => void;
  sendMessage: (content: string) => Promise<void>;
  regenerateLast: () => Promise<void>;
}

export const useChatStore = create<ChatState>()(
  persist(
    (set, get) => ({
      // 초기 상태
      currentChat: null,
      messages: [],
      streamingContent: '',
      isStreaming: false,
      error: null,
      abortController: null,

      // 액션
      setCurrentChat: (chat) => set({ currentChat: chat }),

      setMessages: (messages) => set({ messages }),

      addMessage: (message) =>
        set((state) => ({
          messages: [...state.messages, message],
        })),

      startStreaming: () =>
        set({
          isStreaming: true,
          streamingContent: '',
          error: null,
          abortController: new AbortController(),
        }),

      updateStreamingContent: (content) =>
        set({ streamingContent: content }),

      stopStreaming: () => {
        const controller = get().abortController;
        if (controller) {
          controller.abort();
        }
        set({
          isStreaming: false,
          abortController: null,
        });
      },

      clearStreaming: () =>
        set({
          streamingContent: '',
          isStreaming: false,
          abortController: null,
          error: null,
        }),

      setError: (error) => set({ error }),

      sendMessage: async (content: string) => {
        const { currentChat, startStreaming, updateStreamingContent, clearStreaming, setError, abortController } = get();

        if (!currentChat) {
          setError('No active chat');
          return;
        }

        startStreaming();

        try {
          await streamMessage(currentChat.id, content, {
            signal: abortController?.signal,
            onChunk: (chunk) => {
              set((state) => ({
                streamingContent: state.streamingContent + chunk,
              }));
            },
            onComplete: () => {
              clearStreaming();
              // 메시지 목록 새로고침 트리거
              window.dispatchEvent(new CustomEvent('messages-refresh'));
            },
            onError: (error) => {
              setError(error);
              clearStreaming();
            },
          });
        } catch (error) {
          setError(error.message);
          clearStreaming();
        }
      },

      regenerateLast: async () => {
        const { messages, currentChat, startStreaming, clearStreaming, setError } = get();

        if (!currentChat || messages.length === 0) {
          setError('Cannot regenerate: no messages');
          return;
        }

        // 마지막 사용자 메시지 찾기
        const lastUserMessage = [...messages].reverse().find((m) => m.role === 'user');

        if (!lastUserMessage) {
          setError('Cannot regenerate: no user message');
          return;
        }

        startStreaming();

        try {
          await streamMessage(currentChat.id, lastUserMessage.content, {
            onChunk: (chunk) => {
              set((state) => ({
                streamingContent: state.streamingContent + chunk,
              }));
            },
            onComplete: () => {
              clearStreaming();
              window.dispatchEvent(new CustomEvent('messages-refresh'));
            },
            onError: (error) => {
              setError(error);
              clearStreaming();
            },
          });
        } catch (error) {
          setError(error.message);
          clearStreaming();
        }
      },
    }),
    {
      name: 'chat-storage',
      partialize: (state) => ({
        currentChat: state.currentChat,
        // messages는 persist에서 제외 (너무 큼)
        // streaming 상태는 persist에서 제외
      }),
    },
  ),
);
```

**frontend/src/stores/chatList.ts 생성**
```typescript
import { create } from 'zustand';
import { persist } from 'zustand/middleware';
import type { Chat } from '../lib/api/types';
import { chatApi } from '../lib/api/chat';

interface ChatListState {
  // 상태
  chats: Chat[];
  isLoading: boolean;
  error: string | null;

  // 액션
  loadChats: () => Promise<void>;
  createChat: (systemPrompt?: string) => Promise<Chat>;
  deleteChat: (id: string) => Promise<void>;
  setError: (error: string | null) => void;
}

export const useChatListStore = create<ChatListState>()(
  persist(
    (set, get) => ({
      // 초기 상태
      chats: [],
      isLoading: false,
      error: null,

      // 액션
      loadChats: async () => {
        set({ isLoading: true, error: null });
        try {
          const { data } = await chatApi.getChats();
          set({ chats: data, isLoading: false });
        } catch (error) {
          set({ error: error.message, isLoading: false });
        }
      },

      createChat: async (systemPrompt?: string) => {
        set({ isLoading: true, error: null });
        try {
          const { data } = await chatApi.createChat(systemPrompt);
          set((state) => ({
            chats: [data, ...state.chats],
            isLoading: false,
          }));
          return data;
        } catch (error) {
          set({ error: error.message, isLoading: false });
          throw error;
        }
      },

      deleteChat: async (id: string) => {
        set({ isLoading: true, error: null });
        try {
          await chatApi.deleteChat(id);
          set((state) => ({
            chats: state.chats.filter((c) => c.id !== id),
            isLoading: false,
          }));
        } catch (error) {
          set({ error: error.message, isLoading: false });
          throw error;
        }
      },

      setError: (error) => set({ error }),
    }),
    {
      name: 'chatlist-storage',
    },
  ),
);
```
</action>

---

### Task 2: 채팅 레이아웃 컴포넌트 구현

<read_first>
- frontend/src/routes/__root.tsx (라우트 구조 참조)
- frontend/src/components/ui/ (shadcn/ui 컴포넌트 확인)
</read_first>

<acceptance_criteria>
- frontend/src/components/chat/ChatLayout.tsx 파일이 존재해야 한다
- frontend/src/components/chat/ChatSidebar.tsx 파일이 존재해야 한다
- frontend/src/components/chat/ChatMain.tsx 파일이 존재해야 한다
- frontend/src/components/chat/MessageItem.tsx 파일이 존재해야 한다
- frontend/src/components/chat/ChatInput.tsx 파일이 존재해야 한다
- 사이드바 너비가 260px로 고정되어야 한다
- 메시지가 role별로 좌/우 정렬되어야 한다 (user: 우측, assistant: 좌측)
</acceptance_criteria>

<action>
먼저 기존 라우팅 구조를 확인한다:

**기존 __root.tsx 확인**
```bash
cat frontend/src/routes/__root.tsx
```

beforeLoad에서 인증 가드가 이미 설정되어 있는지 확인하고, 이를 ChatLayout에서 참고한다.

**frontend/src/components/chat/ChatLayout.tsx 생성**
```typescript
import { ReactNode } from 'react';
import { ChatSidebar } from './ChatSidebar';

interface ChatLayoutProps {
  children: ReactNode;
}

export function ChatLayout({ children }: ChatLayoutProps) {
  return (
    <div className="flex h-screen bg-white">
      <ChatSidebar />
      <main className="flex-1 overflow-hidden">
        {children}
      </main>
    </div>
  );
}
```

**frontend/src/components/chat/ChatSidebar.tsx 생성**
```typescript
import { useEffect } from 'react';
import { useNavigate } from '@tanstack/react-router';
import { useChatListStore } from '../../stores/chatList';
import { useAuthStore } from '../../stores/auth';
import { Button } from '../ui/button';

export function ChatSidebar() {
  const navigate = useNavigate();
  const { chats, isLoading, loadChats, createChat, deleteChat } = useChatListStore();
  const { user } = useAuthStore();

  useEffect(() => {
    if (user) {
      loadChats();
    }
  }, [user, loadChats]);

  const handleNewChat = async () => {
    try {
      const chat = await createChat();
      navigate({ to: '/chat/$id', params: { id: chat.id } });
    } catch (error) {
      console.error('Failed to create chat:', error);
    }
  };

  const handleDeleteChat = async (id: string) => {
    if (confirm('Are you sure you want to delete this chat?')) {
      try {
        await deleteChat(id);
        // 현재 대화를 삭제한 경우 홈으로 이동
        navigate({ to: '/' });
      } catch (error) {
        console.error('Failed to delete chat:', error);
      }
    }
  };

  const handleLogout = () => {
    // 로그아웃 로직 (authStore의 clearAuth 사용)
    navigate({ to: '/login' });
  };

  return (
    <aside className="w-[260px] bg-gray-50 border-r border-gray-200 flex flex-col">
      <div className="p-4">
        <Button onClick={handleNewChat} className="w-full">
          New Chat
        </Button>
      </div>

      <div className="flex-1 overflow-y-auto">
        {isLoading ? (
          <div className="p-4 text-sm text-gray-500">Loading...</div>
        ) : chats.length === 0 ? (
          <div className="p-4 text-sm text-gray-500">No chats yet</div>
        ) : (
          <ul className="divide-y divide-gray-200">
            {chats.map((chat) => (
              <li key={chat.id} className="group relative">
                <button
                  onClick={() => navigate({ to: '/chat/$id', params: { id: chat.id } })}
                  className="w-full text-left px-4 py-3 hover:bg-gray-100 transition-colors"
                >
                  <div className="font-medium text-sm truncate">
                    {chat.title || 'New Chat'}
                  </div>
                  <div className="text-xs text-gray-500 mt-1">
                    {new Date(chat.updatedAt).toLocaleDateString()}
                  </div>
                </button>
                <button
                  onClick={() => handleDeleteChat(chat.id)}
                  className="absolute right-2 top-1/2 -translate-y-1/2 opacity-0 group-hover:opacity-100 p-2 text-gray-400 hover:text-red-500 transition-opacity"
                >
                  ×
                </button>
              </li>
            ))}
          </ul>
        )}
      </div>

      <div className="p-4 border-t border-gray-200">
        <Button variant="ghost" onClick={handleLogout} className="w-full">
          Logout
        </Button>
      </div>
    </aside>
  );
}
```

**frontend/src/components/chat/ChatMain.tsx 생성**
```typescript
import { useEffect, useState } from 'react';
import { useChatStore } from '../../stores/chat';
import { MessageItem } from './MessageItem';
import { ChatInput } from './ChatInput';
import { messageApi } from '../../lib/api/message';

export function ChatMain() {
  const { currentChat, messages, setMessages, streamingContent, isStreaming, stopStreaming, sendMessage, regenerateLast, error } = useChatStore();
  const [isLoading, setIsLoading] = useState(true);

  useEffect(() => {
    if (currentChat) {
      loadMessages();
    }

    function loadMessages() {
      setIsLoading(true);
      messageApi.getMessages(currentChat.id)
        .then(({ data }) => {
          setMessages(data);
        })
        .catch(console.error)
        .finally(() => setIsLoading(false));
    }

    // 메시지 새로고침 이벤트 리스너
    const handleRefresh = () => loadMessages();
    window.addEventListener('messages-refresh', handleRefresh);

    return () => {
      window.removeEventListener('messages-refresh', handleRefresh);
    };
  }, [currentChat, setMessages]);

  if (!currentChat) {
    return (
      <div className="h-full flex items-center justify-center text-gray-500">
        Select or create a chat to start messaging
      </div>
    );
  }

  return (
    <div className="h-full flex flex-col">
      <div className="flex-1 overflow-y-auto p-4">
        {isLoading ? (
          <div className="flex justify-center items-center h-full">
            <div className="text-gray-500">Loading messages...</div>
          </div>
        ) : messages.length === 0 ? (
          <div className="flex justify-center items-center h-full">
            <div className="text-gray-500">Start a conversation</div>
          </div>
        ) : (
          <div className="max-w-3xl mx-auto space-y-4">
            {messages.map((message) => (
              <MessageItem key={message.id} message={message} />
            ))}
            {isStreaming && streamingContent && (
              <MessageItem
                message={{
                  id: 'streaming',
                  content: streamingContent,
                  role: 'assistant',
                  status: 'streaming',
                  createdAt: new Date().toISOString(),
                }}
              />
            )}
          </div>
        )}
      </div>

      <div className="border-t border-gray-200 p-4">
        {error && (
          <div className="mb-4 p-3 bg-red-50 text-red-600 rounded-md text-sm">
            {error}
          </div>
        )}
        {isStreaming ? (
          <div className="max-w-3xl mx-auto">
            <button
              onClick={stopStreaming}
              className="w-full px-4 py-2 bg-gray-100 hover:bg-gray-200 text-gray-700 rounded-md transition-colors"
            >
              Stop generating
            </button>
          </div>
        ) : (
          <ChatInput onSendMessage={sendMessage} onRegenerate={messages.length > 0 ? regenerateLast : undefined} />
        )}
      </div>
    </div>
  );
}
```

**frontend/src/components/chat/MessageItem.tsx 생성**
```typescript
import type { Message } from '../../lib/api/types';

interface MessageItemProps {
  message: Message;
}

export function MessageItem({ message }: MessageItemProps) {
  const isUser = message.role === 'user';

  return (
    <div className={`flex ${isUser ? 'justify-end' : 'justify-start'}`}>
      <div
        className={`max-w-[80%] rounded-lg px-4 py-2 ${
          isUser
            ? 'bg-blue-500 text-white'
            : 'bg-gray-100 text-gray-900'
        }`}
      >
        <p className="whitespace-pre-wrap break-words">{message.content}</p>
        {message.status === 'streaming' && (
          <span className="inline-block w-2 h-4 bg-current animate-pulse ml-1" />
        )}
      </div>
    </div>
  );
}
```

**frontend/src/components/chat/ChatInput.tsx 생성**
```typescript
import { useState, FormEvent } from 'react';
import { Button } from '../ui/button';

interface ChatInputProps {
  onSendMessage: (content: string) => Promise<void>;
  onRegenerate?: () => Promise<void>;
}

export function ChatInput({ onSendMessage, onRegenerate }: ChatInputProps) {
  const [content, setContent] = useState('');

  const handleSubmit = async (e: FormEvent) => {
    e.preventDefault();
    if (!content.trim()) return;

    const messageContent = content;
    setContent('');

    try {
      await onSendMessage(messageContent);
    } catch (error) {
      // 에러 발생 시 입력 내용 복원
      setContent(messageContent);
      console.error('Failed to send message:', error);
    }
  };

  const handleRegenerate = async () => {
    if (onRegenerate) {
      try {
        await onRegenerate();
      } catch (error) {
        console.error('Failed to regenerate:', error);
      }
    }
  };

  return (
    <form onSubmit={handleSubmit} className="max-w-3xl mx-auto">
      <div className="flex gap-2">
        {onRegenerate && (
          <Button
            type="button"
            variant="outline"
            onClick={handleRegenerate}
            className="shrink-0"
          >
            Regenerate
          </Button>
        )}
        <div className="flex-1 relative">
          <textarea
            value={content}
            onChange={(e) => setContent(e.target.value)}
            placeholder="Type your message..."
            rows={1}
            className="w-full px-4 py-2 border border-gray-300 rounded-md resize-none focus:outline-none focus:ring-2 focus:ring-blue-500"
            onKeyDown={(e) => {
              if (e.key === 'Enter' && !e.shiftKey) {
                e.preventDefault();
                handleSubmit(e);
              }
            }}
          />
        </div>
        <Button type="submit" disabled={!content.trim()} className="shrink-0">
          Send
        </Button>
      </div>
    </form>
  );
}
```
</action>

---

### Task 3: 채팅 라우트 구현

<read_first>
- frontend/src/routes/__root.tsx (라우트 구조 참조)
- frontend/src/routes/index.tsx (기존 인덱스 라우트 확인)
</read_first>

<acceptance_criteria>
- frontend/src/routes/chat.tsx 파일이 존재해야 한다
- /chat/:id 라우트가 ChatLayout을 렌더링해야 한다
- 라우트 파라미터에서 chat id를 추출하여 chatStore에 설정해야 한다
- / 라우트가 최근 대화 또는 빈 화면을 표시해야 한다
</acceptance_criteria>

<action>
**frontend/src/routes/chat.tsx 생성**
```typescript
import { createFileRoute, useNavigate } from '@tanstack/react-router';
import { useEffect } from 'react';
import { ChatLayout } from '../components/chat/ChatLayout';
import { ChatMain } from '../components/chat/ChatMain';
import { useChatStore } from '../stores/chat';
import { chatApi } from '../lib/api/chat';

export const Route = createFileRoute('/chat/$id')({
  component: ChatPage,
});

function ChatPage() {
  const { id } = Route.useParams();
  const navigate = useNavigate();
  const { setCurrentChat } = useChatStore();

  useEffect(() => {
    // 대화 정보 로드
    chatApi.getChat(id)
      .then(({ data }) => {
        setCurrentChat(data);
      })
      .catch((error) => {
        console.error('Failed to load chat:', error);
        navigate({ to: '/' });
      });
  }, [id, setCurrentChat, navigate]);

  return (
    <ChatLayout>
      <ChatMain />
    </ChatLayout>
  );
}
```

**frontend/src/routes/index.tsx 수정 (또는 생성)**
```typescript
import { createFileRoute, useNavigate } from '@tanstack/react-router';
import { useEffect } from 'react';
import { ChatLayout } from '../components/chat/ChatLayout';
import { ChatMain } from '../components/chat/ChatMain';
import { useChatStore } from '../stores/chat';
import { useChatListStore } from '../stores/chatList';

export const Route = createFileRoute('/')({
  component: IndexPage,
});

function IndexPage() {
  const navigate = useNavigate();
  const { currentChat, setCurrentChat } = useChatStore();
  const { chats } = useChatListStore();

  useEffect(() => {
    // 현재 대화가 없으면 최근 대화로 이동
    if (!currentChat && chats.length > 0) {
      navigate({ to: '/chat/$id', params: { id: chats[0].id } });
    } else if (currentChat) {
      navigate({ to: '/chat/$id', params: { id: currentChat.id } });
    }
  }, [currentChat, chats, navigate]);

  return (
    <ChatLayout>
      <ChatMain />
    </ChatLayout>
  );
}
```
</action>

---

## 검증

### 기능적 요구사항
- [ ] CHAT-01: 사용자는 메시지를 전송하고 SSE 기반 토큰 단위 스트리밍 응답을 받을 수 있다
- [ ] CHAT-02: 사용자는 AI 응답 생성 중 "Stop generating"으로 중단할 수 있다
- [ ] CHAT-03: 사용자는 마지막 AI 응답을 재생성(Regenerate)할 수 있다
- [ ] CONV-01: 사용자는 새 대화를 생성할 수 있다
- [ ] CONV-02: 사용자는 사이드바에서 대화 목록을 최근 순으로 조회할 수 있다
- [ ] CONV-03: 사용자는 대화를 삭제할 수 있다

### 기술적 요구사항
- [ ] shadcn/ui가 초기화되어야 한다 (components.json 생성)
- [ ] Button, Textarea, ScrollArea, Card 컴포넌트가 설치되어야 한다
- [ ] chatStore와 chatListStore가 생성되어야 한다
- [ ] ChatLayout, ChatSidebar, ChatMain, MessageItem, ChatInput 컴포넌트가 생성되어야 한다
- [ ] /chat/:id 라우트가 동작해야 한다
- [ ] 스트리밍 중 메시지가 실시간으로 렌더링되어야 한다

## 검증 명령어

```bash
# 1. 프론트엔드 개발 서버 시작
cd frontend && pnpm dev

# 2. 브라우저에서 http://localhost:5173 접속

# 3. 다음 기능 테스트:
# - 새 대화 생성 버튼 클릭
# - 메시지 전송 (Enter 키)
# - 스트리밍 중단 (Stop generating 버튼)
# - 메시지 재생성 (Regenerate 버튼)
# - 대화 삭제 (사이드바에서 × 버튼)
# - 사이드바에서 다른 대화 선택

# 4. 빌드 테스트
cd frontend && pnpm build
```

## 검증 기준

1. **UI 레이아웃 검증**
   - 사이드바 너비가 260px여야 함
   - 사이드바가 좌측에 고정되어야야 함
   - 메인 영역이 나머지 공간을 차지해야 함

2. **메시지 표시 검증**
   - 사용자 메시지가 우측 정렬, 파란색 배경이어야 함
   - AI 메시지가 좌측 정렬, 회색 배경이어야야 함
   - 스트리밍 중 커서가 깜빡여야야 함

3. **상호작용 검증**
   - New Chat 버튼으로 새 대화 생성 가능해야야 함
   - Enter 키로 메시지 전송 가능해야야야 함
   - Shift+Enter로 줄바꿈 가능해야야야 함
   - Stop generating 버튼으로 스트리밍 중단 가능해야야야 함
   - Regenerate 버튼으로 마지막 응답 재생성 가능해야야야 함

4. **상태 관리 검증**
   - 페이지 새로고침 후에도 대화 목록 유지되어야야 함
   - 스트리밍 상태가 실시간으로 업데이트되어야야 함
   - 에러 발생 시 에러 메시지가 표시되어야야야 함
