---
wave: 1
depends_on: []
files_modified:
  - backend/prisma/schema.prisma
  - backend/src/migrations/{migration}/migration.sql
  - backend/src/chat/chat.module.ts
  - backend/src/chat/chat.controller.ts
  - backend/src/chat/chat.service.ts
  - backend/src/chat/dto/*.dto.ts
  - backend/src/openrouter/openrouter.module.ts
  - backend/src/openrouter/openrouter.service.ts
  - backend/src/openrouter/openrouter.controller.ts
  - backend/src/app.module.ts
autonomous: true
---

# PLAN 03-01: 백엔드 채팅 기초 및 OpenRouter 통합

**목표:** 대화 CRUD API와 OpenRouter 프록시 서비스를 구축하여 채팅 기능의 백엔드 기초를 마련한다.

## 성공 기준

1. Prisma 마이그레이션으로 Message 모델에 status 필드가 추가된다
2. ChatModule이 생성되어 대화 생성, 조회, 삭제 API가 동작한다
3. OpenRouterService가 생성되어 OpenAI SDK를 통해 OpenRouter API와 통신한다
4. 모든 채팅 라우트가 JwtAuthGuard로 보호된다
5. 모델 allowlist가 환경변수로 관리된다

## 작업

### Task 0: 백엔드 의존성 설치

<read_first>
- backend/package.json
</read_first>

<acceptance_criteria>
- openai@4.77.3 패키지가 설치되어야 한다
- package.json에 openai 의존성이 추가되어야 한다
</acceptance_criteria>

<action>
터미널에서 다음 명령을 실행한다:
```bash
cd backend && pnpm add openai@4.77.3
```
</action>

---

### Task 1: Prisma 스키마 수정 및 마이그레이션

<read_first>
- backend/prisma/schema.prisma
</read_first>

<acceptance_criteria>
- Message 모델에 status 필드가 추가되어야 한다 (String, default: "completed")
- 마이그레이션 파일이 생성되어야 한다
- npx prisma migrate dev 명령이 성공적으로 실행되어야 한다
</acceptance_criteria>

<action>
backend/prisma/schema.prisma의 Message 모델을 수정한다:

```prisma
model Message {
  id        String   @id @default(uuid())
  content   String
  role      String
  chatId    String
  chat      Chat     @relation(fields: [chatId], references: [id], onDelete: Cascade)
  status    String   @default("completed") // streaming | completed | stopped | error
  createdAt DateTime @default(now())
}
```

그 후 터미널에서 다음 명령을 실행한다:
```bash
cd backend && npx prisma migrate dev --name add_message_status
```
</action>

---

### Task 2: ChatModule 생성

<read_first>
- backend/src/auth/auth.module.ts (모듈 패턴 참조)
- backend/src/users/users.module.ts (CRUD 패턴 참조)
- backend/prisma/schema.prisma (데이터 모델 확인)
</read_first>

<acceptance_criteria>
- backend/src/chat/chat.module.ts 파일이 존재해야 한다
- backend/src/chat/chat.controller.ts 파일이 존재해야 한다
- backend/src/chat/chat.service.ts 파일이 존재해야 한다
- ChatModule이 AppModule에 imports되어야 한다
- POST /api/chats 엔드포인트가 대화를 생성해야 한다
- GET /api/chats 엔드포인트가 사용자의 대화 목록을 반환해야 한다
- GET /api/chats/:id 엔드포인트가 특정 대화를 반환해야 한다
- DELETE /api/chats/:id 엔드포인트가 대화를 삭제해야 한다
- 모든 엔드포인트가 @UseGuards(JwtAuthGuard)로 보호되어야 한다
</acceptance_criteria>

<action>
다음 파일들을 생성한다:

**backend/src/chat/dto/create-chat.dto.ts**
```typescript
import { IsString, IsOptional, MaxLength } from 'class-validator';

export class CreateChatDto {
  @IsOptional()
  @IsString()
  @MaxLength(2000)
  systemPrompt?: string;
}
```

**backend/src/chat/dto/update-chat.dto.ts**
```typescript
import { IsString, IsOptional, MaxLength } from 'class-validator';

export class UpdateChatDto {
  @IsOptional()
  @IsString()
  @MaxLength(200)
  title?: string;

  @IsOptional()
  @IsString()
  @MaxLength(2000)
  systemPrompt?: string;
}
```

**backend/src/chat/chat.service.ts**
```typescript
import { Injectable, NotFoundException, ForbiddenException } from '@nestjs/common';
import { PrismaService } from '../prisma/prisma.service';
import { CreateChatDto, UpdateChatDto } from './dto';

@Injectable()
export class ChatService {
  constructor(private prisma: PrismaService) {}

  async createChat(userId: string, dto: CreateChatDto) {
    return this.prisma.chat.create({
      data: {
        userId,
        systemPrompt: dto.systemPrompt,
      },
    });
  }

  async getChats(userId: string) {
    return this.prisma.chat.findMany({
      where: { userId },
      orderBy: { updatedAt: 'desc' },
    });
  }

  async getChatById(chatId: string, userId: string) {
    const chat = await this.prisma.chat.findUnique({
      where: { id: chatId },
    });

    if (!chat) {
      throw new NotFoundException('Chat not found');
    }

    if (chat.userId !== userId) {
      throw new ForbiddenException('Access denied');
    }

    return chat;
  }

  async deleteChat(chatId: string, userId: string) {
    const chat = await this.getChatById(chatId, userId);

    await this.prisma.chat.delete({
      where: { id: chatId },
    });

    return { message: 'Chat deleted successfully' };
  }

  async updateChat(chatId: string, userId: string, dto: UpdateChatDto) {
    await this.getChatById(chatId, userId);

    return this.prisma.chat.update({
      where: { id: chatId },
      data: dto,
    });
  }
}
```

**backend/src/chat/chat.controller.ts**
```typescript
import { Controller, Get, Post, Delete, Put, Body, Param, UseGuards, Req } from '@nestjs/common';
import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';
import { ChatService } from './chat.service';
import { CreateChatDto, UpdateChatDto } from './dto';

@Controller('chats')
@UseGuards(JwtAuthGuard)
export class ChatController {
  constructor(private chatService: ChatService) {}

  @Post()
  createChat(@Req() req, @Body() dto: CreateChatDto) {
    return this.chatService.createChat(req.user.userId, dto);
  }

  @Get()
  getChats(@Req() req) {
    return this.chatService.getChats(req.user.userId);
  }

  @Get(':id')
  getChat(@Param('id') id: string, @Req() req) {
    return this.chatService.getChatById(id, req.user.userId);
  }

  @Delete(':id')
  deleteChat(@Param('id') id: string, @Req() req) {
    return this.chatService.deleteChat(id, req.user.userId);
  }

  @Put(':id')
  updateChat(@Param('id') id: string, @Req() req, @Body() dto: UpdateChatDto) {
    return this.chatService.updateChat(id, req.user.userId, dto);
  }
}
```

**backend/src/chat/chat.module.ts**
```typescript
import { Module } from '@nestjs/common';
import { ChatController } from './chat.controller';
import { ChatService } from './chat.service';

@Module({
  controllers: [ChatController],
  providers: [ChatService],
  exports: [ChatService],
})
export class ChatModule {}
```

**backend/src/app.module.ts에 ChatModule import 추가**
```typescript
import { ChatModule } from './chat/chat.module';

@Module({
  imports: [
    // ... 기존 imports
    ChatModule,
  ],
})
```
</action>

---

### Task 3: OpenRouterService 생성

<read_first>
- backend/src/app.module.ts (ConfigModule 패턴 참조)
- backend/.env.example (환경변수 패턴 참조)
</read_first>

<acceptance_criteria>
- backend/src/openrouter/openrouter.module.ts 파일이 존재해야 한다
- backend/src/openrouter/openrouter.service.ts 파일이 존재해야 한다
- backend/src/openrouter/openrouter.controller.ts 파일이 존재해야 한다
- OpenAI SDK가 설치되어야 한다 (openai 패키지)
- OPENROUTER_API_KEY 환경변수가 .env.example에 추가되어야 한다
- OpenRouterService가 OpenAI 클라이언트를 초기화해야 한다
- GET /api/openrouter/models 엔드포인트가 사용 가능한 모델 목록을 반환해야 한다
</acceptance_criteria>

<action>
먼저 openai 패키지를 설치한다:
```bash
cd backend && pnpm add openai@4.77.3
```

**backend/.env.example에 환경변수 추가**
```env
OPENROUTER_API_KEY=your_openrouter_api_key_here
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
ALLOWED_MODELS=google/gemma-2-9b-it:free,meta-llama/llama-3-8b-instruct:free
```

**backend/src/openrouter/openrouter.service.ts**
```typescript
import { Injectable, Logger } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
import OpenAI from 'openai';

@Injectable()
export class OpenRouterService {
  private readonly logger = new Logger(OpenRouterService.name);
  private openai: OpenAI;
  private allowedModels: string[];

  constructor(private configService: ConfigService) {
    this.openai = new OpenAI({
      apiKey: this.configService.get<string>('OPENROUTER_API_KEY'),
      baseURL: this.configService.get<string>('OPENROUTER_BASE_URL', 'https://openrouter.ai/api/v1'),
    });

    const models = this.configService.get<string>('ALLOWED_MODELS', 'google/gemma-2-9b-it:free');
    this.allowedModels = models.split(',').map((m) => m.trim());
  }

  getAllowedModels(): string[] {
    return this.allowedModels;
  }

  isModelAllowed(model: string): boolean {
    return this.allowedModels.includes(model);
  }

  getDefaultModel(): string {
    return this.allowedModels[0] || 'google/gemma-2-9b-it:free';
  }

  async streamChat(messages: Array<{ role: string; content: string }>, model?: string, abortSignal?: AbortSignal) {
    const selectedModel = model || this.getDefaultModel();

    if (!this.isModelAllowed(selectedModel)) {
      throw new Error(`Model ${selectedModel} is not allowed`);
    }

    this.logger.log(`Streaming chat with model: ${selectedModel}`);

    return this.openai.chat.completions.create({
      model: selectedModel,
      messages,
      stream: true,
    }, {
      signal: abortSignal,
    });
  }

  async generateChatTitle(firstMessage: string): Promise<string> {
    try {
      const response = await this.openai.chat.completions.create({
        model: this.getDefaultModel(),
        messages: [
          {
            role: 'system',
            content: 'Generate a concise title (max 5 words) for the following user message. Return only the title, no quotes.',
          },
          {
            role: 'user',
            content: firstMessage,
          },
        ],
        max_tokens: 30,
      });

      return response.choices[0]?.message?.content?.trim() || 'New Chat';
    } catch (error) {
      this.logger.error('Failed to generate chat title:', error);
      return 'New Chat';
    }
  }
}
```

**backend/src/openrouter/openrouter.controller.ts**
```typescript
import { Controller, Get } from '@nestjs/common';
import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';
import { OpenRouterService } from './openrouter.service';

@Controller('openrouter')
@UseGuards(JwtAuthGuard)
export class OpenRouterController {
  constructor(private openRouterService: OpenRouterService) {}

  @Get('models')
  getModels() {
    return {
      models: this.openRouterService.getAllowedModels(),
      default: this.openRouterService.getDefaultModel(),
    };
  }
}
```

**backend/src/openrouter/openrouter.module.ts**
```typescript
import { Module } from '@nestjs/common';
import { OpenRouterController } from './openrouter.controller';
import { OpenRouterService } from './openrouter.service';

@Module({
  controllers: [OpenRouterController],
  providers: [OpenRouterService],
  exports: [OpenRouterService],
})
export class OpenRouterModule {}
```

**backend/src/app.module.ts에 OpenRouterModule import 추가**
```typescript
import { OpenRouterModule } from './openrouter/openrouter.module';

@Module({
  imports: [
    // ... 기존 imports
    OpenRouterModule,
  ],
})
```
</action>

---

### Task 4: DTO 유효성 검증 테스트

<read_first>
- backend/src/chat/dto/*.dto.ts
- backend/src/main.ts (ValidationPipe 설정 확인)
</read_first>

<acceptance_criteria>
- 잘못된 데이터로 POST /api/chats 요청 시 400 에러가 반환되어야 한다
- systemPrompt가 2000자를 초과하면 400 에러가 반환되어야 한다
- title이 200자를 초과하면 400 에러가 반환되어야 한다
</acceptance_criteria>

<action>
백엔드 서버를 실행하고 다음 테스트를 수행한다:

```bash
# 백엔드 서버 시작
cd backend && pnpm dev

# 별도 터미널에서 테스트
# 1. 정상 요청 (로그인 후 access token 필요)
curl -X POST http://localhost:3000/api/chats \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -d '{"systemPrompt":"You are a helpful assistant"}'

# 2. systemPrompt 길이 초과 (2001자)
curl -X POST http://localhost:3000/api/chats \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -d '{"systemPrompt":"'$(python3 -c 'print("a"*2001)'"'"}'

# 3. 잘못된 JSON
curl -X POST http://localhost:3000/api/chats \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -d '{"systemPrompt":123}'
```

예상 결과:
- 정상 요청: 201 Created + 대화 객체
- 길이 초과: 400 Bad Request + "systemPrompt must be shorter than or equal to 2000 characters"
- 잘못된 타입: 400 Bad Request + 유효성 검증 에러
</action>

---

## 검증

### 기능적 요구사항
- [ ] BACK-01: 백엔드가 OpenRouter API를 프록시한다 (OpenRouterService 생성)
- [ ] BACK-02: 서버에서 사용 가능 모델 allowlist를 관리한다 (ALLOWED_MODELS 환경변수)
- [ ] CONV-01: 사용자는 새 대화를 생성할 수 있다 (POST /api/chats)
- [ ] CONV-02: 사용자는 사이드바에서 대화 목록을 최근 순으로 조회할 수 있다 (GET /api/chats)
- [ ] CONV-03: 사용자는 대화를 삭제할 수 있다 (DELETE /api/chats/:id)

### 기술적 요구사항
- [ ] openai@4.77.3 패키지가 설치됨
- [ ] Prisma 마이그레이션 성공 (status 필드 추가)
- [ ] ChatModule이 AppModule에 등록됨
- [ ] OpenRouterModule이 AppModule에 등록됨
- [ ] 모든 채팅 라우트가 JwtAuthGuard로 보호됨
- [ ] 환경변수가 .env.example에 추가됨

## 검증 명령어

```bash
# 1. Prisma 마이그레이션 확인
cd backend && npx prisma migrate status

# 2. 의존성 확인
cd backend && pnpm list openai

# 3. 백엔드 서버 시작
cd backend && pnpm dev

# 4. API 테스트 (access token 필요)
curl -X GET http://localhost:3000/api/openrouter/models \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN"

curl -X POST http://localhost:3000/api/chats \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -d '{}'
```
