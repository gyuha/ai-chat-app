---
wave: 4
depends_on:
  - 03-03
files_modified:
  - backend/src/chat/chat.service.ts
  - backend/src/chat/chat.controller.ts
  - frontend/src/components/chat/ChatSidebar.tsx
  - frontend/src/components/chat/ChatSettings.tsx
  - frontend/src/lib/api/chat.ts
autonomous: true
---

# PLAN 03-04: 고급 기능 구현 (제목 생성, 시스템 프롬프트, Rate Limit 처리)

**목표:** 대화 제목 자동 생성, 시스템 프롬프트 설정, OpenRouter Rate Limit 에러 처리 등 고급 기능을 구현하여 사용자 경험을 완성한다.

## 성공 기준

1. 첫 메시지 전송 후 대화 제목이 자동 생성된다
2. 사용자가 대화별 시스템 프롬프트를 설정할 수 있다
3. OpenRouter Rate Limit(429) 발생 시 사용자 친화적 메시지와 재시도 버튼이 표시된다
4. API 에러가 사용자 친화적 메시지로 변환된다

## 작업

### Task 1: 대화 제목 자동 생성

<read_first>
- backend/src/openrouter/openrouter.service.ts (generateChatTitle 메서드)
- backend/src/chat/chat.service.ts
- backend/src/chat/chat.controller.ts
</read_first>

<acceptance_criteria>
- 첫 번째 사용자 메시지 전송 후 백그라운드로 제목 생성이 시작되어야 한다
- 제목 생성 성공 시 Chat.title이 업데이트되어야 한다
- 제목 생성 실패 시 기본값 "New Chat"이 유지되어야 한다
- 프론트엔드에서 제목 변경이 실시간으로 반영되어야 한다
</acceptance_criteria>

<action>
**backend/src/chat/chat.service.ts에 generateTitleAsync 메서드 추가**
```typescript
import { Logger } from '@nestjs/common';
import { OpenRouterService } from '../openrouter/openrouter.service';

@Injectable()
export class ChatService {
  private logger = new Logger(ChatService.name);

  constructor(
    private prisma: PrismaService,
    private openRouterService: OpenRouterService,
  ) {}

  // ... 기존 메서드

  async generateTitleAsync(chatId: string, firstMessage: string) {
    // 비동기로 제목 생성 (사용자 요청을 차단하지 않음)
    setImmediate(async () => {
      try {
        this.logger.log(`Generating title for chat ${chatId}`);
        const title = await this.openRouterService.generateChatTitle(firstMessage);

        await this.prisma.chat.update({
          where: { id: chatId },
          data: { title },
        });

        this.logger.log(`Title generated for chat ${chatId}: ${title}`);
      } catch (error) {
        this.logger.error(`Failed to generate title for chat ${chatId}:`, error);
      }
    });
  }
}
```

**backend/src/chat/chat.controller.ts의 sendMessage 수정**
```typescript
@Post(':id/messages')
async sendMessage(
  @Param('id') chatId: string,
  @Req() req,
  @Body() dto: CreateMessageDto,
  @Res() res: Response,
) {
  const chat = await this.chatService.getChatById(chatId, req.user.userId);

  // 사용자 메시지 저장
  await this.messageService.createMessage(chatId, dto.content, 'user', 'completed');

  // 첫 번째 메시지인 경우 제목 생성 트리거
  const messages = await this.messageService.getMessages(chatId);
  if (messages.length === 1) {
    this.chatService.generateTitleAsync(chatId, dto.content);
  }

  // ... 기존 스트리밍 로직
}
```
</action>

---

### Task 2: 시스템 프롬프트 설정 UI

<read_first>
- frontend/src/components/chat/ChatSidebar.tsx
- frontend/src/components/ui/ (shadcn/ui 컴포넌트)
- frontend/src/lib/api/chat.ts
</read_first>

<acceptance_criteria>
- 대화 설정 UI에서 시스템 프롬프트를 수정할 수 있어야 한다
- 시스템 프롬프트가 2000자로 제한되어야야 한다
- 설정 변경 후 즉시 적용되어야야 한다
</acceptance_criteria>

<action>
**frontend/src/components/chat/ChatSettings.tsx 생성**
```typescript
import { useState } from 'react';
import { useChatStore } from '../../stores/chat';
import { chatApi } from '../../lib/api/chat';
import { Button } from '../ui/button';

export function ChatSettings() {
  const { currentChat, setCurrentChat } = useChatStore();
  const [systemPrompt, setSystemPrompt] = useState(currentChat?.systemPrompt || '');
  const [isEditing, setIsEditing] = useState(false);
  const [isSaving, setIsSaving] = useState(false);

  const handleSave = async () => {
    if (!currentChat) return;

    setIsSaving(true);
    try {
      const { data } = await chatApi.updateChat(currentChat.id, { systemPrompt });
      setCurrentChat(data);
      setIsEditing(false);
    } catch (error) {
      console.error('Failed to update system prompt:', error);
    } finally {
      setIsSaving(false);
    }
  };

  const handleCancel = () => {
    setSystemPrompt(currentChat?.systemPrompt || '');
    setIsEditing(false);
  };

  if (!currentChat) return null;

  return (
    <div className="p-4 border-b border-gray-200">
      <button
        onClick={() => setIsEditing(!isEditing)}
        className="text-sm font-medium text-gray-700 hover:text-gray-900"
      >
        System Prompt {isEditing ? '▼' : '▶'}
      </button>

      {isEditing && (
        <div className="mt-3 space-y-2">
          <textarea
            value={systemPrompt}
            onChange={(e) => setSystemPrompt(e.target.value)}
            placeholder="Set a custom system prompt for this conversation..."
            rows={4}
            maxLength={2000}
            className="w-full px-3 py-2 border border-gray-300 rounded-md text-sm resize-none focus:outline-none focus:ring-2 focus:ring-blue-500"
          />
          <div className="flex justify-between items-center">
            <span className="text-xs text-gray-500">
              {systemPrompt.length}/2000
            </span>
            <div className="flex gap-2">
              <Button size="sm" variant="outline" onClick={handleCancel}>
                Cancel
              </Button>
              <Button
                size="sm"
                onClick={handleSave}
                disabled={isSaving}
              >
                {isSaving ? 'Saving...' : 'Save'}
              </Button>
            </div>
          </div>
        </div>
      )}
    </div>
  );
}
```

**frontend/src/components/chat/ChatMain.tsx에 ChatSettings 추가**
```typescript
import { ChatSettings } from './ChatSettings';

export function ChatMain() {
  // ... 기존 코드

  return (
    <div className="h-full flex flex-col">
      <ChatSettings />
      {/* ... 기존 레이아웃 */}
    </div>
  );
}
```
</action>

---

### Task 3: Rate Limit 에러 처리

<read_first>
- backend/src/openrouter/openrouter.service.ts
- backend/src/chat/chat.controller.ts
- frontend/src/lib/api/streaming.ts
</read_first>

<acceptance_criteria>
- OpenRouter 429 에러 시 사용자 친화적 메시지가 표시되어야 한다
- Retry-After 헤더가 있으면 대기 시간이 표시되어야 한다
- 프론트엔드에서 재시도 버튼이 제공되어야 한다
- 에러 코드별로 다른 메시지가 표시되어야야 한다
</acceptance_criteria>

<action>
**backend/src/openrouter/openrouter.service.ts에 에러 처리 메서드 추가**
```typescript
private handleOpenRouterError(error: any): string {
  if (error.status === 429) {
    const retryAfter = error.headers?.['retry-after'];
    const waitTime = retryAfter ? `${retryAfter}초` : '잠시';
    return `요청이 너무 많습니다. ${waitTime} 후 다시 시도해주세요.`;
  }

  if (error.status === 401) {
    return 'API 인증에 실패했습니다. 관리자에게 문의해주세요.';
  }

  if (error.status === 500) {
    return '서버 오류가 발생했습니다. 잠시 후 다시 시도해주세요.';
  }

  return error.message || '알 수 없는 오류가 발생했습니다.';
}

async streamChatWithErrorHandling(messages: Array<{ role: string; content: string }>, model?: string, abortSignal?: AbortSignal) {
  try {
    return await this.streamChat(messages, model, abortSignal);
  } catch (error) {
    const userMessage = this.handleOpenRouterError(error);
    this.logger.error(`OpenRouter error: ${userMessage}`, error);
    throw new Error(userMessage);
  }
}
```

**frontend/src/lib/api/streaming.ts에 재시도 기능 추가**
```typescript
export interface StreamMessageOptions {
  onChunk: (content: string) => void;
  onComplete: () => void;
  onError: (error: string, retry?: () => void) => void;
  signal?: AbortSignal;
}

export async function streamMessage(
  chatId: string,
  content: string,
  options: StreamMessageOptions,
) {
  // ... 기존 구현

  const executeRequest = async (attemptNumber: number = 0) => {
    try {
      // ... 기존 fetch 로직
    } catch (error) {
      if (error.message.includes('요청이 너무 많습니다') && attemptNumber < 3) {
        // Rate Limit 에러: 재시도 옵션 제공
        options.onError(error.message, () => executeRequest(attemptNumber + 1));
      } else {
        options.onError(error.message);
      }
    }
  };

  return executeRequest();
}
```

**frontend/src/components/chat/ChatMain.tsx에 에러 UI 개선**
```typescript
const [retryFunction, setRetryFunction] = useState<(() => void) | null>(null);

// streaming.ts의 onError 수정
onError: (error, retry) => {
  setError(error);
  if (retry) {
    setRetryFunction(() => retry);
  }
  clearStreaming();
}

// JSX에 재시도 버튼 추가
{error && (
  <div className="mb-4 p-3 bg-red-50 text-red-600 rounded-md text-sm flex justify-between items-center">
    <span>{error}</span>
    {retryFunction && (
      <Button
        size="sm"
        variant="outline"
        onClick={() => {
          retryFunction();
          setRetryFunction(null);
        }}
      >
        Retry
      </Button>
    )}
  </div>
)}
```
</action>

---

### Task 4: 사용자 친화적 에러 메시지

<read_first>
- backend/src/chat/chat.controller.ts
- backend/src/main.ts (ExceptionFilter 패턴)
</read_first>

<acceptance_criteria>
- 모든 API 에러가 사용자 친화적 메시지로 변환되어야 한다
- 에러 응답 형식이 통일되어야야야 한다 ({ message: string, code?: string })
- 개발 환경에서는 스택 트레이스가 로깅되어야 한다
</acceptance_criteria>

<action>
**backend/src/common/filters/all-exceptions.filter.ts 생성**
```typescript
import { ExceptionFilter, Catch, ArgumentsHost, HttpException, Logger } from '@nestjs/common';
import { Response } from 'express';

@Catch()
export class AllExceptionsFilter implements ExceptionFilter {
  private readonly logger = new Logger(AllExceptionsFilter.name);

  catch(exception: unknown, host: ArgumentsHost) {
    const ctx = host.switchToHttp();
    const response = ctx.getResponse<Response>();

    let status = 500;
    let message = '서버 오류가 발생했습니다. 잠시 후 다시 시도해주세요.';

    if (exception instanceof HttpException) {
      status = exception.getStatus();
      const exceptionResponse = exception.getResponse();

      if (typeof exceptionResponse === 'string') {
        message = exceptionResponse;
      } else if (typeof exceptionResponse === 'object') {
        message = (exceptionResponse as any).message || message;
      }
    }

    // 개발 환경에서는 상세 에러 로깅
    if (process.env.NODE_ENV !== 'production') {
      this.logger.error(`Exception: ${message}`, exception);
    } else {
      this.logger.error(`Exception: ${message}`);
    }

    response.status(status).json({
      message,
      ...(process.env.NODE_ENV !== 'production' && { stack: exception instanceof Error ? exception.stack : undefined }),
    });
  }
}
```

**backend/src/main.ts에 ExceptionFilter 등록**
```typescript
import { AllExceptionsFilter } from './common/filters/all-exceptions.filter';

async function bootstrap() {
  const app = await NestFactory.create(AppModule);

  // 전역 Exception Filter
  app.useGlobalFilters(new AllExceptionsFilter());

  // ... 기존 설정
}
```
</action>

---

## 검증

### 기능적 요구사항
- [ ] CHAT-04: 대화별 시스템 프롬프트를 설정할 수 있다
- [ ] CONV-04: 대화 제목이 첫 메시지를 기반으로 자동 생성된다
- [ ] BACK-03: API rate limit 에러(429)를 사용자 친화적으로 처리한다
- [ ] BACK-06: 에러 메시지가 사용자 친화적으로 변환된다

### 기술적 요구사항
- [ ] generateTitleAsync가 비동기로 제목 생성해야 한다
- [ ] ChatSettings 컴포넌트가 시스템 프롬프트 수정 UI를 제공해야 한다
- [ ] Rate Limit 에러 시 재시도 버튼이 표시되어야 한다
- [ ] AllExceptionsFilter가 모든 에러를 처리해야 한다

## 검증 명령어

```bash
# 1. 백엔드 서버 시작
cd backend && pnpm dev

# 2. 프론트엔드 서버 시작
cd frontend && pnpm dev

# 3. 제목 자동 생성 테스트
# - 새 대화 생성
# - 첫 메시지 전송
# - 잠시 후 사이드바에서 제목 변경 확인

# 4. 시스템 프롬프트 테스트
# - 대화 설정 열기
# - 시스템 프롬프트 입력
# - 저장 후 메시지 전송
# - 시스템 프롬프트가 적용되는지 확인

# 5. Rate Limit 에러 테스트 (개발 환경에서 시뮬레이션 필요)
# - 짧은 시간 동안 여러 메시지 전송
# - 또는 백엔드에서 일부러 429 에러 반환
# - 재시도 버튼 표시 확인

# 6. 에러 처리 테스트
# - 네트워크 연결 해제
# - 메시지 전송 시도
# - 에러 메시지 확인
```

## 검증 기준

1. **제목 자동 생성 검증**
   - 첫 메시지 전송 후 5-10초 내에 제목이 생성되어야 한다
   - 제목이 5단어 이내여야 한다
   - 제목 생성 실패 시 "New Chat"이 표시되어야 한다

2. **시스템 프롬프트 검증**
   - 시스템 프롬프트 설정 UI가 열려야 한다
   - 2000자 제한이 적용되어야 한다
   - 저장 후 즉시 적용되어야 한다

3. **Rate Limit 처리 검증**
   - 429 에러 시 사용자 친화적 메시지가 표시되어야 한다
   - Retry-After 헤더가 있으면 대기 시간이 표시되어야 한다
   - 재시도 버튼이 작동해야 한다

4. **에러 메시지 검증**
   - 모든 에러가 한글로 표시되어야 한다
   - 에러 코드가 일관된 형식이어야 한다 ({ message: string })
   - 개발 환경에서만 스택 트레이스가 표시되어야 한다
