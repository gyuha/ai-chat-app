---
phase: 03-streaming-chat-experience
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - apps/web/src/routes/chat/$chatId.tsx
  - apps/web/src/features/chats
  - apps/web/src/features/composer
  - apps/web/src/store/ui
  - apps/web/src/components/chat
autonomous: true
requirements:
  - MSG-02
  - MSG-03
  - MSG-05
user_setup: []
must_haves:
  truths:
    - Sending a prompt immediately shows the user turn and an in-flight assistant state.
    - Users can stop an active generation from the composer without losing already streamed text.
    - Stream completion reconciles optimistic UI with persisted chat data cleanly.
  artifacts:
    - apps/web/src/routes/chat/$chatId.tsx
    - apps/web/src/store/ui
    - apps/web/src/components/chat
  key_links:
    - Route param remains the source of truth for the active conversation.
    - Query cache owns persisted chat detail while Zustand owns only in-flight UI session state.
---

<objective>
Implement the streaming message lifecycle, optimistic rendering, and abort flow in the chat screen.

Purpose: Turn the current static/local-preview chat route into a real conversational surface that streams assistant output and lets the user stop generation safely.
Output: In-flight message lifecycle, optimistic user append, assistant placeholder/delta updates, stop action wiring, and scroll/status behavior tied to stream state.
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app.worktrees/codex/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app.worktrees/codex/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PRD.md
@.planning/phases/03-streaming-chat-experience/03-CONTEXT.md
@.planning/phases/03-streaming-chat-experience/03-RESEARCH.md
@apps/web/src/routes/chat/$chatId.tsx
@apps/web/src/store/ui/chat-shell-store.ts
@apps/web/src/features/composer/prompt-composer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace local preview flow with optimistic stream lifecycle</name>
  <files>apps/web/src/routes/chat/$chatId.tsx, apps/web/src/features/chats, apps/web/src/store/ui, apps/web/src/components/chat</files>
  <read_first>.planning/phases/03-streaming-chat-experience/03-CONTEXT.md, .planning/phases/03-streaming-chat-experience/03-RESEARCH.md, apps/web/src/routes/chat/$chatId.tsx</read_first>
  <action>Refactor the chat route so prompt submission appends an optimistic user message, creates an assistant placeholder, then patches the in-flight assistant content as stream deltas arrive. Reconcile completion back into query-backed chat detail and remove the old Phase 2 local-preview-only behavior.</action>
  <verify>test -f apps/web/src/routes/chat/\$chatId.tsx</verify>
  <acceptance_criteria>
    - user turn appears immediately on submit
    - assistant content updates incrementally during streaming
    - duplicate local/persisted messages are not left behind after completion
  </acceptance_criteria>
  <done>The chat route behaves like a real streaming conversation instead of a static preview surface.</done>
</task>

<task type="auto">
  <name>Task 2: Add stop-generation control, status feedback, and scroll rules</name>
  <files>apps/web/src/features/composer, apps/web/src/store/ui, apps/web/src/components/chat, apps/web/src/routes/chat/$chatId.tsx</files>
  <read_first>.planning/PRD.md, .planning/phases/03-streaming-chat-experience/03-RESEARCH.md, apps/web/src/features/composer/prompt-composer.tsx</read_first>
  <action>Switch the composer primary action from send to stop while generation is active, wire it to abort the current stream, and keep already received assistant text visible as a partial answer. Add concise status feedback, `aria-live` summaries, and near-bottom-only auto-scroll behavior that respects user reading position.</action>
  <verify>test -f apps/web/src/features/composer/prompt-composer.tsx</verify>
  <acceptance_criteria>
    - active generation shows stop affordance in the composer action slot
    - abort cancels the live stream and leaves partial assistant text visible
    - auto-scroll follows deltas only when the user is near the bottom
    - status updates are concise and screen-reader friendly
  </acceptance_criteria>
  <done>Users can read, interrupt, and resume control of a live generation without the UI fighting them.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm lint && pnpm typecheck && pnpm test` exits 0
- [ ] Manual smoke confirms optimistic send, token streaming, stop, and partial retention
- [ ] No duplicate messages remain after stream completion
</verification>

<success_criteria>

- All tasks completed
- Streaming feels immediate and controllable
- Phase 3 now satisfies the core interaction value of the product
</success_criteria>

<output>
After completion, create `.planning/phases/03-streaming-chat-experience/03-02-SUMMARY.md`
</output>
