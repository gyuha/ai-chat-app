---
phase: 03-streaming-chat-experience
plan: 03
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - apps/web/src/components/chat
  - apps/web/src/lib/markdown
  - apps/web/src/lib/api
  - apps/web/src/routes/chat/$chatId.tsx
  - apps/web/src/features/chats
  - apps/web/src/store/ui
autonomous: true
requirements:
  - MSG-04
  - REND-01
  - REND-02
user_setup: []
must_haves:
  truths:
    - Assistant markdown is readable in dark mode across long prose, lists, tables, and fenced code.
    - Code blocks expose a clear copy action with success feedback.
    - Users can regenerate the latest assistant turn without leaving the current chat flow.
  artifacts:
    - apps/web/src/components/chat
    - apps/web/src/lib/markdown
    - apps/web/src/routes/chat/$chatId.tsx
  key_links:
    - Renderer work builds on the stream lifecycle from 03-02 rather than creating a parallel message path.
    - Regenerate reuses the normalized stream transport from 03-01.
---

<objective>
Implement markdown/code rendering and the regenerate flow for the latest assistant response.

Purpose: Finish the Phase 3 reading experience so streamed assistant messages are not only live, but also easy to read, copy, and retry.
Output: Markdown renderer, custom code block component with copy action, regenerate stream wiring, and final message-action polish for the latest assistant turn.
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app.worktrees/codex/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app.worktrees/codex/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PRD.md
@.planning/TECH-DESIGN.md
@.planning/phases/03-streaming-chat-experience/03-CONTEXT.md
@.planning/phases/03-streaming-chat-experience/03-RESEARCH.md
@apps/web/src/components/chat/message-bubble.tsx
@apps/web/src/routes/chat/$chatId.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add assistant markdown renderer and custom code block copy UI</name>
  <files>apps/web/src/components/chat, apps/web/src/lib/markdown, apps/web/src/routes/chat/$chatId.tsx</files>
  <read_first>.planning/phases/03-streaming-chat-experience/03-CONTEXT.md, .planning/PRD.md, apps/web/src/components/chat/message-bubble.tsx</read_first>
  <action>Introduce a markdown rendering layer for assistant messages using the project-approved stack. Style headings, lists, tables, blockquotes, inline code, and fenced code for the dark chat surface. Add a custom code block header with language label and copy action, and make sure long content scrolls internally instead of breaking the page layout.</action>
  <verify>test -d apps/web/src/lib/markdown</verify>
  <acceptance_criteria>
    - assistant markdown renders correctly with GFM basics
    - code blocks have a visible copy affordance and success feedback
    - long code and tables do not introduce page-level horizontal scroll
  </acceptance_criteria>
  <done>Assistant responses are readable as real markdown documents instead of plain text blobs.</done>
</task>

<task type="auto">
  <name>Task 2: Implement regenerate for the latest assistant turn</name>
  <files>apps/web/src/routes/chat/$chatId.tsx, apps/web/src/features/chats, apps/web/src/lib/api, apps/web/src/store/ui</files>
  <read_first>.planning/phases/03-streaming-chat-experience/03-RESEARCH.md, apps/web/src/routes/chat/$chatId.tsx, apps/web/src/lib/api/index.ts</read_first>
  <action>Add a regenerate action for the latest assistant message only. Reuse the existing stream lifecycle and normalized endpoint contract so regenerate behaves like a controlled re-run of the last assistant turn, including loading state, stop support, completion reconciliation, and error handling consistent with standard submission.</action>
  <verify>test -f apps/web/src/routes/chat/\$chatId.tsx</verify>
  <acceptance_criteria>
    - latest assistant turn exposes regenerate affordance when appropriate
    - regenerate triggers the dedicated stream endpoint and replaces the latest assistant response flow cleanly
    - regenerate and stop do not conflict with each other
  </acceptance_criteria>
  <done>The core chat experience includes both readable output and a practical retry path.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm lint && pnpm typecheck && pnpm test && pnpm build` exits 0
- [ ] Manual smoke confirms markdown/code rendering, copy, and regenerate flow
- [ ] latest assistant response can be regenerated and then stopped if needed
</verification>

<success_criteria>

- All tasks completed
- Phase 3 requirement set for streaming and rendering is fully covered
- Phase 4 can focus on settings/title/delete without revisiting chat reading basics
</success_criteria>

<output>
After completion, create `.planning/phases/03-streaming-chat-experience/03-03-SUMMARY.md`
</output>
