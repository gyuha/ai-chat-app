---
phase: 03-streaming-chat-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/server/src/modules/streaming
  - apps/server/src/infrastructure/openrouter
  - apps/server/src/modules/chats
  - apps/web/src/lib/api
  - apps/web/src/lib/stream
  - packages/contracts/src/index.ts
autonomous: true
requirements:
  - MSG-02
user_setup: []
must_haves:
  truths:
    - Server streaming endpoints emit a stable internal event contract instead of skeleton heartbeats only.
    - Web has a reusable POST SSE parser that can consume provider-normalized stream events.
    - No browser code reaches OpenRouter directly.
  artifacts:
    - apps/server/src/modules/streaming
    - apps/web/src/lib/stream
    - packages/contracts/src/index.ts
  key_links:
    - Streaming transport stays aligned with the Phase 1 proxy boundary.
    - Query/UI lifecycle work in later plans can rely on one normalized event shape.
---

<objective>
Implement the real streaming transport layer on both server and client.

Purpose: Replace the Phase 1 stream skeleton with a provider-normalized SSE contract and give the web app a parser utility that can read POST-based stream events reliably.
Output: Stream event contracts, server streaming service/controller wiring, OpenRouter stream normalization, and a web parser/helper for POST SSE responses.
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app.worktrees/codex/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app.worktrees/codex/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/TECH-DESIGN.md
@.planning/phases/03-streaming-chat-experience/03-CONTEXT.md
@.planning/phases/03-streaming-chat-experience/03-RESEARCH.md
@apps/server/src/modules/streaming/streaming.controller.ts
@apps/web/src/lib/api/index.ts
@packages/contracts/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace SSE skeleton with normalized streaming service on the server</name>
  <files>apps/server/src/modules/streaming, apps/server/src/infrastructure/openrouter, apps/server/src/modules/chats, packages/contracts/src/index.ts</files>
  <read_first>.planning/phases/03-streaming-chat-experience/03-CONTEXT.md, .planning/TECH-DESIGN.md, apps/server/src/modules/streaming/streaming.controller.ts</read_first>
  <action>Create or expand the server streaming service so `messages/stream` and `regenerate/stream` emit the normalized internal stream events defined for this project. Connect provider streaming, DTO validation, error mapping, and persistence hooks without leaking raw OpenRouter chunk formats to the client.</action>
  <verify>test -f apps/server/src/modules/streaming/streaming.controller.ts</verify>
  <acceptance_criteria>
    - server emits `meta`, `message:start`, `message:delta`, `message:done`, `error`, and `heartbeat` as needed
    - provider errors are converted into user-safe stream error payloads
    - stream endpoints still live under `/api/v1/chats/:chatId/*`
  </acceptance_criteria>
  <done>Streaming endpoints are real transport primitives instead of placeholders.</done>
</task>

<task type="auto">
  <name>Task 2: Add reusable POST SSE parser and stream API helpers on the web</name>
  <files>apps/web/src/lib/api, apps/web/src/lib/stream, packages/contracts/src/index.ts</files>
  <read_first>.planning/phases/03-streaming-chat-experience/03-RESEARCH.md, apps/web/src/lib/api/index.ts, packages/contracts/src/index.ts</read_first>
  <action>Add a stream-aware API helper and parser utility that can consume POST-based SSE responses through `fetch` and `ReadableStream`. Make event parsing robust to arbitrary chunk boundaries so later UI plans can focus only on lifecycle and rendering.</action>
  <verify>test -d apps/web/src/lib/stream</verify>
  <acceptance_criteria>
    - parser handles event name + data frames across chunk boundaries
    - helper exposes a clear callback or async iterator API for later UI integration
    - no OpenRouter URL or key logic appears in web code
  </acceptance_criteria>
  <done>Web has a stable transport primitive for streaming chat events.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm lint && pnpm typecheck && pnpm test` exits 0
- [ ] server stream endpoints produce the normalized event sequence in tests or smoke
- [ ] parser handles chunk-split event frames
</verification>

<success_criteria>

- All tasks completed
- Phase 3 has a reliable transport foundation for optimistic chat UX
- Follow-up plans can implement lifecycle and rendering without redefining the stream contract
</success_criteria>

<output>
After completion, create `.planning/phases/03-streaming-chat-experience/03-01-SUMMARY.md`
</output>
