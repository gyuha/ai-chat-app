---
phase: 03-free-model-selection-and-conversation-bootstrap
plan: 03
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - src/components/layout/app-header.tsx
  - src/components/layout/app-shell.tsx
  - src/routes/__root.tsx
  - src/components/chat/conversation-model-selector.tsx
autonomous: true
requirements:
  - MODL-01
  - MODL-02
  - MODL-03
  - DATA-03
must_haves:
  truths:
    - 현재 대화의 활성 모델을 헤더에서 확인하고 바꿀 수 있어야 한다.
    - 모델 목록은 저장된 API 키 기반의 무료 모델만 보여야 한다.
    - header selector는 ChatGPT 유사 구조를 유지하되 draft 상태와 loading 상태를 분명히 드러내야 한다.
  artifacts:
    - src/components/layout/app-header.tsx
    - src/components/chat/conversation-model-selector.tsx
    - src/routes/__root.tsx
  key_links:
    - src/components/chat/conversation-model-selector.tsx가 active conversation과 free models query를 묶는다.
    - src/components/layout/app-header.tsx가 static `무료 모델 기준` 버튼 대신 현재 모델 selector를 렌더링한다.
    - src/routes/__root.tsx가 active conversation title과 selector에 필요한 route metadata를 shell로 전달한다.
---

<objective>
헤더 기반 무료 모델 탐색 및 현재 대화 모델 선택 UX를 구현한다.

Purpose: Phase 3의 주 모델 선택 surface를 헤더에 배치해, 사용자가 현재 활성 모델을 확인하고 무료 모델 목록 안에서 즉시 전환할 수 있게 한다.
Output: header model selector, active model label, free-model-only browsing surface.
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app.worktrees/frontend-codex/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app.worktrees/frontend-codex/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-free-model-selection-and-conversation-bootstrap/03-CONTEXT.md
@.planning/phases/01-app-shell-and-interface-foundation/01-UI-SPEC.md
@src/components/layout/app-header.tsx
@src/routes/__root.tsx
@src/hooks/use-free-models-query.ts
@src/hooks/use-conversations-query.ts
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 헤더용 conversation model selector를 reusable component로 만든다</name>
  <files>src/components/chat/conversation-model-selector.tsx, src/components/layout/app-header.tsx</files>
  <read_first>src/components/layout/app-header.tsx, src/hooks/use-free-models-query.ts, src/hooks/use-conversations-query.ts, .planning/phases/03-free-model-selection-and-conversation-bootstrap/03-CONTEXT.md</read_first>
  <action>헤더의 기존 정적 보조 버튼을 현재 대화용 모델 selector로 교체한다. selector는 저장된 API 키 기반의 무료 모델 목록만 보여야 하고, 현재 활성 모델 이름 또는 fallback label을 노출해야 한다. 구현 방식은 헤더 anchored dropdown/popup여야 하며, `ui-ux-pro-max`와 shadcn 가이드에 맞게 absolute div를 직접 짜지 말고 Popover/Command 또는 동등한 shadcn primitive를 사용한다. 모델명, id 일부 또는 최소 메타데이터를 보여 `MODL-01`의 탐색 요구를 만족하되 과도한 정보 밀도는 피한다.</action>
  <verify>pnpm build</verify>
  <acceptance_criteria>
    - src/components/chat/conversation-model-selector.tsx contains `Popover` or `Select`
    - src/components/chat/conversation-model-selector.tsx contains `freeModels`
    - src/components/layout/app-header.tsx does not contain `무료 모델 기준`
  </acceptance_criteria>
  <done>사용자는 헤더에서 현재 활성 모델을 확인하고 무료 모델 목록을 열 수 있다.</done>
</task>

<task type="auto">
  <name>Task 2: 모델 변경을 현재 conversation metadata에 즉시 반영한다</name>
  <files>src/components/layout/app-shell.tsx, src/routes/__root.tsx, src/components/chat/conversation-model-selector.tsx</files>
  <read_first>src/components/layout/app-shell.tsx, src/routes/__root.tsx, src/hooks/use-conversations-query.ts</read_first>
  <action>active conversation id와 title 흐름을 shell에서 안정적으로 읽을 수 있게 정리하고, selector가 선택 즉시 해당 conversation의 `modelId`를 저장하도록 연결한다. 별도 저장 버튼은 두지 말고, cache/UI가 즉시 갱신되어 같은 대화를 다시 열어도 활성 모델이 유지되게 한다. loading, empty free-models, invalid active conversation 같은 경계 상태를 한국어 문구로 처리한다.</action>
  <verify>pnpm build</verify>
  <acceptance_criteria>
    - src/routes/__root.tsx contains `activeConversationId`
    - src/components/chat/conversation-model-selector.tsx contains `updateConversationModel`
    - src/components/chat/conversation-model-selector.tsx contains `aria`
  </acceptance_criteria>
  <done>헤더에서 선택한 모델이 현재 대화 메타데이터에 즉시 반영되고 다시 열어도 유지된다.</done>
</task>

</tasks>

<verification>
- `pnpm build`가 헤더 selector와 shell metadata wiring 추가 후 통과한다.
- 헤더에서 무료 모델 목록을 보고 현재 모델을 즉시 바꿀 수 있다.
</verification>

<success_criteria>
- `MODL-01`과 `MODL-03`의 핵심 surface가 헤더에서 충족된다.
- current conversation의 활성 모델이 UI와 persistence 양쪽에서 일치한다.
</success_criteria>

<output>
After completion, create `.planning/phases/03-free-model-selection-and-conversation-bootstrap/03-03-SUMMARY.md`
</output>
