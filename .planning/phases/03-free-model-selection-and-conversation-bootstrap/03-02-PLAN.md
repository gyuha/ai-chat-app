---
phase: 03-free-model-selection-and-conversation-bootstrap
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/components/layout/conversation-list.tsx
  - src/components/layout/conversation-list-item.tsx
  - src/components/layout/new-chat-button.tsx
  - src/components/chat/chat-empty-state.tsx
  - src/lib/conversation-service.ts
autonomous: true
requirements:
  - CONV-01
  - DATA-03
must_haves:
  truths:
    - 사이드바와 빈 상태의 `새 대화` 액션이 실제 conversation 생성/재사용과 route 이동으로 연결되어야 한다.
    - mock conversation 목록은 제거되고, 실제 Dexie conversation 목록이 최신 `updatedAt` 기준으로 보여야 한다.
    - draft conversation은 목록에서도 식별 가능해야 하지만 미완성 draft가 여러 개 누적되면 안 된다.
  artifacts:
    - src/components/layout/conversation-list.tsx
    - src/components/layout/conversation-list-item.tsx
    - src/components/layout/new-chat-button.tsx
    - src/components/chat/chat-empty-state.tsx
  key_links:
    - src/components/layout/new-chat-button.tsx가 create/reuse draft action 후 `/chat/$conversationId`로 이동한다.
    - src/components/layout/conversation-list.tsx가 실제 conversations query를 사용해 목록을 렌더링한다.
    - src/components/chat/chat-empty-state.tsx가 home empty state에서도 같은 새 대화 action을 재사용한다.
---

<objective>
새 대화 진입점과 사이드바 목록을 실제 데이터 흐름에 연결한다.

Purpose: 사용자가 어디에서 `새 대화 시작`을 눌러도 동일한 draft 생성 규칙을 따르고, 사이드바에서 실제 대화 메타데이터를 볼 수 있게 한다.
Output: connected conversation list, reusable start-new-conversation action, real route navigation.
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app.worktrees/frontend-codex/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app.worktrees/frontend-codex/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-free-model-selection-and-conversation-bootstrap/03-CONTEXT.md
@src/components/layout/conversation-list.tsx
@src/components/layout/new-chat-button.tsx
@src/components/chat/chat-empty-state.tsx
@src/hooks/use-conversations-query.ts
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 새 대화 액션을 공용 bootstrap 흐름으로 연결한다</name>
  <files>src/components/layout/new-chat-button.tsx, src/components/chat/chat-empty-state.tsx, src/lib/conversation-service.ts</files>
  <read_first>src/components/layout/new-chat-button.tsx, src/components/chat/chat-empty-state.tsx, src/hooks/use-conversations-query.ts, .planning/phases/03-free-model-selection-and-conversation-bootstrap/03-CONTEXT.md</read_first>
  <action>사이드바 버튼, 모바일 헤더 버튼, home empty state CTA가 모두 같은 새 대화 bootstrap action을 사용하도록 바꾼다. 이 action은 Phase 3 결정대로 draft conversation을 즉시 생성하거나 기존 미선택 draft를 재사용한 뒤 `/chat/$conversationId`로 이동해야 한다. 버튼/CTA copy는 한국어로 유지하고, 기존 shell layout이나 mobile close behavior는 깨지지 않게 한다.</action>
  <verify>pnpm build</verify>
  <acceptance_criteria>
    - src/components/layout/new-chat-button.tsx contains `/chat/`
    - src/components/chat/chat-empty-state.tsx contains `새 대화 시작`
    - src/components/chat/chat-empty-state.tsx does not contain `to=\"/\"`
  </acceptance_criteria>
  <done>모든 새 대화 진입점이 실제 draft conversation route로 연결된다.</done>
</task>

<task type="auto">
  <name>Task 2: 사이드바 목록을 실제 conversation metadata 기반으로 렌더링한다</name>
  <files>src/components/layout/conversation-list.tsx, src/components/layout/conversation-list-item.tsx</files>
  <read_first>src/components/layout/conversation-list.tsx, src/components/layout/conversation-list-item.tsx, src/hooks/use-conversations-query.ts</read_first>
  <action>mock conversation 배열을 제거하고 실제 conversation list query로 교체한다. 목록은 `updatedAt` 최신순으로 보여야 하고, title이 비어 있거나 placeholder인 draft conversation도 적절한 fallback title을 가져야 한다. draft conversation은 미세한 label 또는 status text로 구분하되, Phase 1에서 잠근 ChatGPT 근접형의 거의 평면적인 sidebar tone을 유지한다.</action>
  <verify>pnpm build</verify>
  <acceptance_criteria>
    - src/components/layout/conversation-list.tsx does not contain `mockConversations`
    - src/components/layout/conversation-list.tsx contains `updatedAt`
    - src/components/layout/conversation-list-item.tsx contains `draft`
  </acceptance_criteria>
  <done>사이드바가 실제 저장된 대화 메타데이터를 기준으로 갱신된다.</done>
</task>

</tasks>

<verification>
- `pnpm build`가 실제 conversation list와 새 대화 navigation 연결 후 통과한다.
- 새 대화 버튼과 home empty state CTA가 모두 draft creation flow를 공유한다.
</verification>

<success_criteria>
- 사용자는 사이드바나 빈 상태에서 실제 대화를 시작할 수 있다.
- 사이드바 목록이 mock이 아닌 실제 conversation metadata를 보여준다.
</success_criteria>

<output>
After completion, create `.planning/phases/03-free-model-selection-and-conversation-bootstrap/03-02-SUMMARY.md`
</output>
