---
phase: 03-free-model-selection-and-conversation-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/app-db.ts
  - src/lib/conversation-service.ts
  - src/hooks/use-conversations-query.ts
autonomous: true
requirements:
  - CONV-01
  - DATA-03
must_haves:
  truths:
    - Dexie가 conversation 메타데이터를 로컬에 저장하고 다시 읽을 수 있어야 한다.
    - 새 대화 draft 재사용, 대화 목록 정렬, modelId/systemPrompt 갱신 규칙이 공용 service와 query action으로 고정되어야 한다.
    - 이후 UI plan들이 mock conversation 배열 대신 실제 persistence 계층을 재사용해야 한다.
  artifacts:
    - src/lib/app-db.ts
    - src/lib/conversation-service.ts
    - src/hooks/use-conversations-query.ts
  key_links:
    - src/lib/app-db.ts가 conversations store와 필요한 인덱스를 정의한다.
    - src/lib/conversation-service.ts가 draft singleton, metadata patch, sorted list read를 제공한다.
    - src/hooks/use-conversations-query.ts가 TanStack Query 기반 read/write entrypoint를 제공한다.
---

<objective>
Phase 3의 conversation persistence 기반을 만든다.

Purpose: 새 대화 생성과 대화별 모델 메타데이터를 UI보다 먼저 안정적인 로컬 저장 계층으로 고정한다.
Output: conversations Dexie schema, conversation service, query hooks/actions.
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app.worktrees/frontend-codex/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app.worktrees/frontend-codex/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-free-model-selection-and-conversation-bootstrap/03-CONTEXT.md
@src/lib/app-db.ts
@src/hooks/use-settings-query.ts
@src/lib/settings-service.ts
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: conversations store와 typed metadata helper를 추가한다</name>
  <files>src/lib/app-db.ts, src/lib/conversation-service.ts</files>
  <read_first>src/lib/app-db.ts, .planning/phases/03-free-model-selection-and-conversation-bootstrap/03-CONTEXT.md, .planning/REQUIREMENTS.md</read_first>
  <action>Dexie schema를 확장해 conversation metadata를 저장할 `conversations` store를 추가한다. record에는 최소한 `id`, `title`, `modelId`, `systemPrompt`, `createdAt`, `updatedAt`가 포함되어야 하고, 목록 정렬을 위해 `updatedAt` 인덱스를 둔다. `src/lib/conversation-service.ts`에는 get/list/create/update helper를 만들되, Phase 3 결정대로 model 미선택 draft conversation은 하나만 유지하고, 새 대화 생성 시 `defaultModelId`와 `defaultSystemPrompt`를 settings store에서 읽어 초기 metadata에 반영한다. mock data fallback이나 localStorage 이중 저장은 추가하지 않는다.</action>
  <verify>pnpm build</verify>
  <acceptance_criteria>
    - src/lib/app-db.ts contains `conversations`
    - src/lib/app-db.ts contains `updatedAt`
    - src/lib/conversation-service.ts contains `createOrReuseDraftConversation`
    - src/lib/conversation-service.ts contains `defaultModelId`
    - src/lib/conversation-service.ts contains `defaultSystemPrompt`
  </acceptance_criteria>
  <done>대화 메타데이터가 로컬 DB에 저장되고, draft singleton 정책을 service 레벨에서 재사용할 수 있다.</done>
</task>

<task type="auto">
  <name>Task 2: conversation query/hooks를 만들어 UI에서 동일한 persistence 규칙을 사용하게 한다</name>
  <files>src/hooks/use-conversations-query.ts, src/lib/conversation-service.ts</files>
  <read_first>src/hooks/use-settings-query.ts, src/hooks/use-free-models-query.ts, src/lib/conversation-service.ts</read_first>
  <action>`src/hooks/use-conversations-query.ts`를 추가해 conversation list, active conversation, 새 대화 생성, modelId 갱신, metadata refresh action을 제공한다. query key와 cache update 방식은 Phase 2 settings 패턴과 맞추고, 새 대화 생성 또는 모델 변경 시 optimistic cache update 또는 invalidate 규칙을 분명히 둔다. 이후 plan들이 직접 Dexie를 호출하지 않도록 read/write entrypoint를 이 hook 계층으로 모은다.</action>
  <verify>pnpm build</verify>
  <acceptance_criteria>
    - src/hooks/use-conversations-query.ts contains `useQuery`
    - src/hooks/use-conversations-query.ts contains `useQueryClient`
    - src/hooks/use-conversations-query.ts contains `createOrReuseDraftConversation`
    - src/hooks/use-conversations-query.ts contains `updateConversationModel`
  </acceptance_criteria>
  <done>UI는 conversation persistence 규칙을 query action 하나로 재사용할 수 있다.</done>
</task>

</tasks>

<verification>
- `pnpm build`가 Dexie schema와 conversation hooks 추가 후 통과한다.
- conversations persistence와 draft singleton 규칙이 코드 레벨에서 명시된다.
</verification>

<success_criteria>
- executor는 이후 UI plan에서 mock conversation 배열을 제거하고 실제 데이터 계층을 바로 사용할 수 있다.
- `DATA-03`의 기반이 되는 conversation metadata 저장 구조가 먼저 고정된다.
</success_criteria>

<output>
After completion, create `.planning/phases/03-free-model-selection-and-conversation-bootstrap/03-01-SUMMARY.md`
</output>
