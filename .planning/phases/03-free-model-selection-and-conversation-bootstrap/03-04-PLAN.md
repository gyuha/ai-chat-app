---
phase: 03-free-model-selection-and-conversation-bootstrap
plan: 04
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - src/routes/chat.$conversationId.tsx
  - src/components/chat/message-pane-placeholder.tsx
  - src/components/chat/chat-composer.tsx
autonomous: true
requirements:
  - MODL-02
  - MODL-03
  - DATA-03
must_haves:
  truths:
    - `modelId`가 없는 draft conversation은 입력보다 모델 선택을 먼저 요구해야 한다.
    - composer는 draft 상태와 selected 상태에 따라 disabled/active가 명확히 갈려야 한다.
    - chat route 본문은 현재 대화의 title, 활성 모델 상태, 다음 행동을 한국어로 설명해야 한다.
  artifacts:
    - src/routes/chat.$conversationId.tsx
    - src/components/chat/message-pane-placeholder.tsx
    - src/components/chat/chat-composer.tsx
  key_links:
    - src/routes/chat.$conversationId.tsx가 active conversation query를 읽어 draft/selected 상태를 route body에 전달한다.
    - src/components/chat/message-pane-placeholder.tsx가 draft 배너와 metadata summary를 렌더링한다.
    - src/components/chat/chat-composer.tsx가 model 선택 여부에 따라 비활성화된다.
---

<objective>
draft conversation의 본문 상태와 composer gating을 구현한다.

Purpose: model 미선택 draft에서는 행동 유도를 명확히 하고, 모델이 정해진 뒤에만 채팅 입력이 가능하도록 route body를 정리한다.
Output: draft banner, composer disabled state, conversation metadata-aware placeholder surface.
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app.worktrees/frontend-codex/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app.worktrees/frontend-codex/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-free-model-selection-and-conversation-bootstrap/03-CONTEXT.md
@.planning/phases/01-app-shell-and-interface-foundation/01-UI-SPEC.md
@src/routes/chat.$conversationId.tsx
@src/components/chat/message-pane-placeholder.tsx
@src/components/chat/chat-composer.tsx
@src/hooks/use-conversations-query.ts
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: chat route body를 active conversation metadata 기반으로 전환한다</name>
  <files>src/routes/chat.$conversationId.tsx, src/components/chat/message-pane-placeholder.tsx</files>
  <read_first>src/routes/chat.$conversationId.tsx, src/components/chat/message-pane-placeholder.tsx, src/hooks/use-conversations-query.ts</read_first>
  <action>route param으로 받은 conversation id를 실제 conversation query와 연결해 title, updated state, model selection state를 본문에 반영한다. `modelId`가 없는 draft conversation에서는 설명 배너와 `모델 선택` CTA를 보여 주고, CTA는 헤더 selector를 사용해야 한다는 점을 명확히 안내한다. `modelId`가 있는 대화에서는 현재 활성 모델과 빈 메시지 상태를 자연스럽게 보여 주되, Phase 4의 실제 메시지 스트리밍 구현 범위는 침범하지 않는다.</action>
  <verify>pnpm build</verify>
  <acceptance_criteria>
    - src/components/chat/message-pane-placeholder.tsx contains `모델 선택`
    - src/components/chat/message-pane-placeholder.tsx contains `활성 모델`
    - src/routes/chat.$conversationId.tsx contains `use`
  </acceptance_criteria>
  <done>chat route 본문이 실제 conversation metadata와 draft 상태를 반영한다.</done>
</task>

<task type="auto">
  <name>Task 2: composer를 모델 선택 상태에 따라 비활성화한다</name>
  <files>src/components/chat/chat-composer.tsx, src/routes/chat.$conversationId.tsx</files>
  <read_first>src/components/chat/chat-composer.tsx, src/routes/chat.$conversationId.tsx, .planning/phases/03-free-model-selection-and-conversation-bootstrap/03-CONTEXT.md</read_first>
  <action>composer가 `modelId` 없는 draft conversation에서는 입력과 전송 버튼 모두 비활성화되도록 수정한다. disabled copy는 한국어로 명확히 보여 주고, 이미 모델이 있는 대화에서는 기존 2~4행 입력 경험을 유지한다. 실제 message submit 로직은 Phase 4에서 구현할 것이므로, 이번 phase에서는 disabled gating과 placeholder/assistive text 정리에 집중한다.</action>
  <verify>pnpm build</verify>
  <acceptance_criteria>
    - src/components/chat/chat-composer.tsx contains `disabled`
    - src/components/chat/chat-composer.tsx contains `모델`
    - src/components/chat/chat-composer.tsx contains `placeholder`
  </acceptance_criteria>
  <done>모델 미선택 상태에서는 입력을 시작할 수 없고, 다음 행동이 분명해진다.</done>
</task>

</tasks>

<verification>
- `pnpm build`가 draft banner와 composer gating 추가 후 통과한다.
- model 미선택 draft에서는 입력이 비활성화되고, 모델 선택 후에는 활성화될 준비가 된다.
</verification>

<success_criteria>
- 새 대화 draft 흐름이 UI에서 명확하게 드러난다.
- 사용자는 현재 대화의 활성 모델 여부를 본문과 헤더에서 일관되게 이해할 수 있다.
</success_criteria>

<output>
After completion, create `.planning/phases/03-free-model-selection-and-conversation-bootstrap/03-04-SUMMARY.md`
</output>
