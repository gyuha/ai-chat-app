---
phase: 03-free-model-selection-and-conversation-bootstrap
plan: 05
type: execute
wave: 4
depends_on:
  - "03"
  - "04"
files_modified:
  - src/routes/index.test.tsx
  - src/routes/chat.$conversationId.test.tsx
  - src/components/layout/conversation-list.test.tsx
  - src/test/test-utils.ts
autonomous: true
requirements:
  - MODL-01
  - MODL-02
  - MODL-03
  - CONV-01
  - DATA-03
must_haves:
  truths:
    - 새 대화 생성, draft 재사용, 모델 선택, composer gating, metadata persistence가 테스트로 고정되어야 한다.
    - Phase 3 구현은 test/build/lint를 함께 통과해야 한다.
    - free models fetch와 fake IndexedDB 기반 검증이 deterministic해야 한다.
  artifacts:
    - src/routes/index.test.tsx
    - src/routes/chat.$conversationId.test.tsx
    - src/components/layout/conversation-list.test.tsx
    - src/test/test-utils.ts
  key_links:
    - route tests가 새 대화 bootstrap과 draft reuse를 검증한다.
    - chat route tests가 header/body 상태와 composer disabled rule을 검증한다.
    - test utils가 conversation DB reset과 free model fetch mock helper를 제공한다.
---

<objective>
Phase 3 정책을 자동 검증으로 고정한다.

Purpose: conversation bootstrap과 모델 선택 흐름이 이후 스트리밍/chat phase에서 회귀하지 않게 한다.
Output: route-level integration tests, reusable conversation test helpers, passing verification commands.
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app.worktrees/frontend-codex/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app.worktrees/frontend-codex/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-free-model-selection-and-conversation-bootstrap/03-CONTEXT.md
@src/test/test-utils.ts
@src/routes/index.tsx
@src/routes/chat.$conversationId.tsx
@src/components/layout/conversation-list.tsx
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 새 대화와 모델 선택 핵심 흐름을 integration test로 추가한다</name>
  <files>src/routes/index.test.tsx, src/routes/chat.$conversationId.test.tsx, src/components/layout/conversation-list.test.tsx, src/test/test-utils.ts</files>
  <read_first>src/test/test-utils.ts, src/routes/index.test.tsx, src/routes/chat.$conversationId.tsx, .planning/phases/03-free-model-selection-and-conversation-bootstrap/03-CONTEXT.md</read_first>
  <action>fake IndexedDB와 fetch mock을 사용해 Phase 3 정책을 route-level integration tests로 고정한다. 최소한 home empty state에서 새 대화를 시작하면 `/chat/$conversationId`로 이동하는지, default model이 없으면 draft composer가 disabled인지, 같은 미선택 draft를 다시 재사용하는지, header/body에서 현재 active model이 표시되는지, 모델 변경이 다시 열어도 유지되는지를 검증한다. conversation list 테스트는 최신순 정렬과 draft 시각 구분을 검증한다.</action>
  <verify>pnpm test</verify>
  <acceptance_criteria>
    - src/routes/index.test.tsx contains `/chat/`
    - src/routes/chat.$conversationId.test.tsx contains `모델 선택`
    - src/routes/chat.$conversationId.test.tsx contains `disabled`
    - src/components/layout/conversation-list.test.tsx contains `draft`
    - src/test/test-utils.ts contains `conversation`
  </acceptance_criteria>
  <done>Phase 3의 주요 UX 정책이 자동 테스트로 반복 검증 가능해진다.</done>
</task>

<task type="auto">
  <name>Task 2: verification command를 묶어 Phase 3 완료 조건을 닫는다</name>
  <files>src/routes/index.test.tsx, src/routes/chat.$conversationId.test.tsx, src/components/layout/conversation-list.test.tsx</files>
  <read_first>package.json, src/routes/index.test.tsx, src/routes/chat.$conversationId.test.tsx</read_first>
  <action>verification command는 정확히 `pnpm test`, `pnpm build`, `pnpm biome check .` 세 가지를 사용한다. 테스트는 free models success path와 no-default draft path를 모두 포함해야 하고, flaky timing 없이 deterministic mock fetch를 유지한다. lint/build가 통과하지 않으면 구현을 조정하되 Phase 1/2에서 이미 허용된 upstream warning 외 새 warning/error는 남기지 않는다.</action>
  <verify>pnpm test && pnpm build && pnpm biome check .</verify>
  <acceptance_criteria>
    - src/routes/chat.$conversationId.test.tsx contains `userEvent`
    - src/routes/chat.$conversationId.test.tsx contains `waitFor`
    - src/components/layout/conversation-list.test.tsx contains `render`
  </acceptance_criteria>
  <done>Phase 3의 요구사항 충족 여부를 test/build/lint 명령으로 반복 확인할 수 있다.</done>
</task>

</tasks>

<verification>
- `pnpm test`가 draft bootstrap, model selection, conversation list 흐름을 포함해 통과한다.
- `pnpm build`가 conversation metadata 연결 후 통과한다.
- `pnpm biome check .`가 새 route/test code까지 포함해 통과한다.
</verification>

<success_criteria>
- Phase 3 요구사항 전체가 자동 테스트와 build/lint로 다시 검증 가능하다.
- Phase 4에서 실제 메시지 전송을 붙여도 conversation bootstrap과 model metadata 정책이 쉽게 깨지지 않는다.
</success_criteria>

<output>
After completion, create `.planning/phases/03-free-model-selection-and-conversation-bootstrap/03-05-SUMMARY.md`
</output>
