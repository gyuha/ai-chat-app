---
phase: 01-데이터-상태-기반
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - biome.json
  - index.html
  - src/index.css
  - src/main.tsx
  - src/router.ts
  - src/routeTree.gen.ts
  - src/lib/utils.ts
  - src/db/index.ts
  - src/stores/ui-store.ts
  - src/routes/__root.tsx
  - src/routes/index.tsx
  - src/routes/chat/$conversationId.tsx
  - src/routes/settings.tsx
  - components.json
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-05
  - DATA-06
  - DATA-07

must_haves:
  truths:
    - "Vite 개발 서버가 정상 실행된다"
    - "TanStack Router가 /, /chat/:id, /settings 라우트를 인식한다"
    - "Dexie.js DB가 openrouter-chat-db 이름으로 3개 테이블(settings, conversations, messages)을 생성한다"
    - "shadcn/ui Button 컴포넌트가 정상 렌더링된다"
    - "Zustand 스토어가 사이드바 상태를 관리한다"
  artifacts:
    - path: "package.json"
      provides: "모든 프로젝트 의존성"
      contains: "dexie"
    - path: "src/db/index.ts"
      provides: "Dexie DB 스키마 + 타입 정의"
      contains: "EntityTable"
    - path: "src/stores/ui-store.ts"
      provides: "Zustand UI 상태 스토어"
      contains: "create"
    - path: "src/router.ts"
      provides: "TanStack Router 인스턴스"
      contains: "createRouter"
    - path: "src/main.tsx"
      provides: "앱 진입점 (QueryClient + RouterProvider)"
      contains: "QueryClientProvider"
    - path: "vite.config.ts"
      provides: "Vite 설정 (Tailwind + Router 플러그인)"
      contains: "TanStackRouterVite"
  key_links:
    - from: "src/main.tsx"
      to: "src/router.ts"
      via: "import { router }"
      pattern: "import.*router.*from"
    - from: "src/main.tsx"
      to: "@tanstack/react-query"
      via: "QueryClientProvider"
      pattern: "QueryClientProvider"
    - from: "src/routes/__root.tsx"
      to: "src/router.ts"
      via: "route tree"
      pattern: "createRootRoute"
---

<objective>
Vite + React 19 + TypeScript 프로젝트를 생성하고, 모든 의존성을 설치하며, 데이터 계층(Dexie.js DB 스키마), 상태 관리(Zustand + TanStack Query), 라우팅(TanStack Router)의 기반을 구축한다.

Purpose: Phase 1의 모든 후속 작업이 의존하는 인프라를 한 번에 세팅한다. 이 플랜 없이는 어떤 UI/데이터 작업도 불가능하다.
Output: 실행 가능한 Vite 개발 서버, DB 스키마, 라우트 파일, Zustand 스토어, TanStack Query 설정
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app.worktrees/frontend-glm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app.worktrees/frontend-glm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-데이터-상태-기반/01-RESEARCH.md
@.planning/phases/01-데이터-상태-기반/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vite 프로젝트 초기화 + 의존성 설치 + 설정 파일 구성</name>
  <files>package.json, vite.config.ts, tsconfig.json, tsconfig.app.json, tsconfig.node.json, biome.json, index.html, src/index.css, components.json</files>
  <read_first>
    - RESEARCH.md의 Standard Stack 섹션 (버전 번호)
    - UI-SPEC.md의 Color 섹션 (다크모드 CSS 변수)
    - CLAUDE.md의 Commands 섹션 (빌드/린트 명령어)
  </read_first>
  <action>
1. Vite 프로젝트를 현재 디렉토리에 직접 생성 (`pnpm create vite . --template react-ts`). 이미 .planning 등 기존 파일이 있으므로 충돌 시 건너뛰기.

2. package.json에 명시된 대로 핵심 의존성 설치:
```bash
pnpm add react@19 react-dom@19 dexie dexie-react-hooks @tanstack/react-query @tanstack/react-router zustand lucide-react
pnpm add -D typescript @types/react @types/react-dom vite @vitejs/plugin-react tailwindcss @tailwindcss/vite @tanstack/router-plugin @biomejs/biome vitest
```
RESEARCH.md에 명시된 버전: react@19.2.4, react-dom@19.2.4, dexie@4.4.1, dexie-react-hooks@4.4.0, @tanstack/react-query@5.95.2, @tanstack/react-router@1.168.8, zustand@5.0.12, lucide-react@1.7.0, tailwindcss@4.2.2, @tailwindcss/vite@4.2.2, @tanstack/router-plugin@1.167.9, @biomejs/biome@2.4.9, vitest@4.1.2

3. `vite.config.ts` 작성:
```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import { TanStackRouterVite } from '@tanstack/router-plugin/vite';
import tailwindcss from '@tailwindcss/vite';
import path from 'path';

export default defineConfig({
  plugins: [
    TanStackRouterVite({
      routesDirectory: './src/routes',
      generatedRouteTree: './src/routeTree.gen.ts',
    }),
    tailwindcss(),
    react(),
  ],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
});
```

4. `tsconfig.json` 및 `tsconfig.app.json`에서 strict 모드 활성화, `@` 경로 alias 설정 (`paths: { "@/*": ["./src/*"] }`).

5. `biome.json` 설정 파일 생성 (formatter: indent 2, line width 100, linter 활성).

6. `index.html`에 `<div id="root"></div>` 확인, `<html lang="ko">` 설정.

7. `src/index.css`에 Tailwind v4 설정:
```css
@import "tailwindcss";

@theme {
  --color-background: hsl(0 0% 3.9%);
  --color-foreground: hsl(0 0% 98%);
  --color-card: hsl(0 0% 14.9%);
  --color-card-foreground: hsl(0 0% 98%);
  --color-primary: hsl(0 0% 98%);
  --color-primary-foreground: hsl(0 0% 9%);
  --color-destructive: hsl(0 84.2% 60.2%);
  --color-muted: hsl(0 0% 14.9%);
  --color-muted-foreground: hsl(0 0% 63.9%);
  --color-border: hsl(0 0% 14.9%);
  --color-input: hsl(0 0% 14.9%);
  --color-ring: hsl(0 0% 83.1%);
  --color-accent: hsl(0 0% 14.9%);
  --color-accent-foreground: hsl(0 0% 98%);
  --radius: 0.5rem;
}
```

8. `components.json` (shadcn/ui 설정) 작성:
```json
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "new-york",
  "rsc": false,
  "tsx": true,
  "tailwind": {
    "config": "",
    "css": "src/index.css",
    "baseColor": "zinc",
    "cssVariables": true
  },
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils",
    "ui": "@/components/ui",
    "lib": "@/lib"
  }
}
```

9. shadcn/ui 초기화: `pnpm dlx shadcn@latest init` (위 components.json이 있으면 건너뛰고 `pnpm dlx shadcn@latest add button` 으로 Button 컴포넌트만 추가하여 설치 검증).

10. `src/lib/utils.ts`에 cn() 유틸리티 작성:
```typescript
import { type ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

11. `pnpm build`로 타입체크 + 빌드 성공 확인.
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/ai-chat-app.worktrees/frontend-glm && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - package.json에 react, dexie, zustand, @tanstack/react-query, @tanstack/react-router, tailwindcss, @biomejs/biome 모두 포함
    - vite.config.ts에 TanStackRouterVite, tailwindcss, react 플러그인 순서대로 등록
    - tsconfig.app.json에 strict: true, paths에 "@/*" alias 설정
    - src/index.css에 @import "tailwindcss" 와 @theme 블록 존재
    - pnpm build 성공 (exit code 0)
  </done>
</task>

<task type="auto">
  <name>Task 2: Dexie.js DB 스키마 + 타입 + Zustand 스토어 + TanStack Query/Router 설정</name>
  <files>src/db/index.ts, src/stores/ui-store.ts, src/main.tsx, src/router.ts, src/routes/__root.tsx, src/routes/index.tsx, src/routes/chat/$conversationId.tsx, src/routes/settings.tsx</files>
  <read_first>
    - RESEARCH.md의 Pattern 1 (Dexie DB Schema)
    - RESEARCH.md의 Pattern 4 (Zustand UI Store)
    - RESEARCH.md의 Pattern 5 (TanStack Router)
    - RESEARCH.md의 Code Examples (main.tsx, router setup)
    - CLAUDE.md의 라우트 구조 섹션
  </read_first>
  <action>
1. `src/db/index.ts` 작성 - Dexie.js v4 데이터베이스 정의:
```typescript
import Dexie, { type EntityTable } from 'dexie';

export interface Setting {
  key: string;
  value: string;
}

export interface Conversation {
  id: string;
  title: string;
  modelId: string;
  systemPrompt?: string;
  createdAt: Date;
  updatedAt: Date;
}

export interface Message {
  id: string;
  conversationId: string;
  role: 'user' | 'assistant' | 'system';
  content: string;
  createdAt: Date;
}

class OpenRouterChatDB extends Dexie {
  settings!: EntityTable<Setting, 'key'>;
  conversations!: EntityTable<Conversation, 'id'>;
  messages!: EntityTable<Message, 'id'>;

  constructor() {
    super('openrouter-chat-db');
    this.version(1).stores({
      settings: 'key',
      conversations: 'id, updatedAt',
      messages: 'id, conversationId, createdAt',
    });
  }
}

export const db = new OpenRouterChatDB();
```
주의: Dexie v4에서는 `Table<T>`가 아닌 `EntityTable<T, KeyType>` 제네릭을 사용. 모든 인터페이스를 export.

2. `src/stores/ui-store.ts` 작성 - Zustand UI 상태 스토어:
```typescript
import { create } from 'zustand';

interface UIState {
  sidebarOpen: boolean;
  toggleSidebar: () => void;
  setSidebarOpen: (open: boolean) => void;
}

export const useUIStore = create<UIState>((set) => ({
  sidebarOpen: true,
  toggleSidebar: () => set((state) => ({ sidebarOpen: !state.sidebarOpen })),
  setSidebarOpen: (open) => set({ sidebarOpen: open }),
}));
```

3. `src/routes/` 디렉토리 생성 후 라우트 파일 작성:

`src/routes/__root.tsx`:
```typescript
import { createRootRoute, Outlet } from '@tanstack/react-router';

export const Route = createRootRoute({
  component: () => (
    <div className="flex h-screen bg-background text-foreground">
      <Outlet />
    </div>
  ),
});
```

`src/routes/index.tsx`:
```typescript
import { createFileRoute } from '@tanstack/react-router';

export const Route = createFileRoute('/')({
  component: () => <div className="flex-1 flex items-center justify-center">대화를 선택하거나 새 대화를 시작하세요.</div>,
});
```

`src/routes/chat/$conversationId.tsx`:
```typescript
import { createFileRoute } from '@tanstack/react-router';

export const Route = createFileRoute('/chat/$conversationId')({
  component: () => <div className="flex-1 flex items-center justify-center">채팅 영역 (Phase 2 구현)</div>,
});
```

`src/routes/settings.tsx`:
```typescript
import { createFileRoute } from '@tanstack/react-router';

export const Route = createFileRoute('/settings')({
  component: () => <div className="flex-1 flex items-center justify-center">설정 페이지 (Phase 1 Plan 03 구현)</div>,
});
```

4. `src/router.ts` 작성:
```typescript
import { createRouter } from '@tanstack/react-router';
import { routeTree } from './routeTree.gen';

export const router = createRouter({ routeTree });

declare module '@tanstack/react-router' {
  interface Register {
    router: typeof router;
  }
}
```

5. `src/main.tsx` 작성:
```typescript
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import { RouterProvider } from '@tanstack/react-router';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import { router } from './router';
import './index.css';

const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 1000 * 60 * 5,
      gcTime: 1000 * 60 * 30,
    },
  },
});

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <QueryClientProvider client={queryClient}>
      <RouterProvider router={router} />
    </QueryClientProvider>
  </StrictMode>,
);
```

6. TanStack Router 플러그인이 routeTree.gen.ts를 자동 생성하는지 `pnpm dev` 실행 후 확인 (Ctrl+C로 종료).

7. `pnpm build`로 전체 빌드 성공 확인.
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/ai-chat-app.worktrees/frontend-glm && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - src/db/index.ts에 Setting, Conversation, Message 인터페이스 export, EntityTable 사용
    - src/stores/ui-store.ts에 useUIStore export (sidebarOpen, toggleSidebar, setSidebarOpen)
    - src/main.tsx에 QueryClientProvider + RouterProvider 중첩 렌더링
    - src/router.ts에 createRouter + Register 타입 선언
    - src/routes/에 __root.tsx, index.tsx, chat/$conversationId.tsx, settings.tsx 존재
    - routeTree.gen.ts 자동 생성 확인
    - pnpm build 성공 (exit code 0)
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` 성공 (타입체크 + 번들링)
2. `pnpm dev` 실행 후 브라우저에서 http://localhost:5173 접속 시 빈 페이지 렌더링
3. 개발자 도구 > Application > IndexedDB에 `openrouter-chat-db` DB 생성 확인
4. /settings 경로로 직접 이동 시 placeholder 텍스트 표시
5. /chat/test-id 경로로 이동 시 placeholder 텍스트 표시
</verification>

<success_criteria>
- Vite 개발 서버가 정상 실행됨 (pnpm dev)
- 빌드가 성공함 (pnpm build)
- IndexedDB에 3개 테이블(settings, conversations, messages)이 정의됨
- 3개 라우트(/, /chat/:id, /settings)가 인식됨
- Zustand 스토어가 sidebarOpen 상태를 관리함
- TanStack Query가 QueryClientProvider로 설정됨
</success_criteria>

<output>
After completion, create `.planning/phases/01-데이터-상태-기반/01-01-SUMMARY.md`
</output>
