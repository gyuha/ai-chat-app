---
phase: 01-app-shell-and-interface-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - components.json
  - src/lib/utils.ts
  - src/components/ui/button.tsx
  - src/components/ui/input.tsx
  - src/components/ui/textarea.tsx
  - src/components/ui/dropdown-menu.tsx
  - src/components/ui/sheet.tsx
  - src/components/ui/sidebar.tsx
  - src/components/ui/sonner.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
must_haves:
  truths:
    - 이후 shell과 상태 화면이 official shadcn/ui Vite primitives 위에서 구현될 수 있다.
    - sidebar, sheet, toast, form control primitives가 준비되어 모바일/데스크톱 shell과 오류 피드백 구현 기반이 생긴다.
  artifacts:
    - components.json
    - src/lib/utils.ts
    - src/components/ui/button.tsx
    - src/components/ui/input.tsx
    - src/components/ui/textarea.tsx
    - src/components/ui/dropdown-menu.tsx
    - src/components/ui/sheet.tsx
    - src/components/ui/sidebar.tsx
    - src/components/ui/sonner.tsx
  key_links:
    - components.json alias 설정이 src/lib/utils.ts와 src/components/ui/* 경로를 연결한다.
    - shell plan들이 hand-rolled nav/drawer 대신 official shadcn sidebar/sheet/sonner를 직접 가져다 쓸 수 있다.
---

<objective>
shadcn/ui Vite 초기화와 shell 구현에 필요한 공용 UI primitives를 추가한다.

Purpose: Phase 1의 레이아웃과 상태 화면을 custom primitive 없이, UI-SPEC에서 잠근 shadcn 패턴 위에 구현하게 한다.
Output: components.json, `cn` util, shell/feedback/form용 official UI primitives.
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app.worktrees/frontend-codex/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app.worktrees/frontend-codex/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-app-shell-and-interface-foundation/01-CONTEXT.md
@.planning/phases/01-app-shell-and-interface-foundation/01-UI-SPEC.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: shadcn Vite 설정과 alias 기반 공용 util을 초기화한다</name>
  <files>components.json, src/lib/utils.ts</files>
  <action>current official shadcn Vite CLI 흐름에 맞춰 기존 Vite 프로젝트를 초기화하되, template은 `vite`, component library는 `radix`, icon library는 `lucide-react` 기준으로 맞춘다. components.json의 alias는 `@/components`, `@/components/ui`, `@/lib`, `@/hooks` 형태로 정리하고, src/lib/utils.ts에는 shadcn 표준 `cn` util을 둔다. UI-SPEC에서 잠근 font/color/spacing 방향을 깨는 preset이나 외부 registry는 사용하지 않는다.</action>
  <verify>
    <automated>pnpm dlx shadcn@latest init -t vite -d</automated>
  </verify>
  <done>components.json과 `cn` util이 생기고, 이후 `shadcn add`가 현재 프로젝트 alias와 Vite 구조를 기준으로 동작한다.</done>
</task>

<task type="auto">
  <name>Task 2: shell과 상태 화면에 필요한 official shadcn primitives를 추가한다</name>
  <files>src/components/ui/button.tsx, src/components/ui/input.tsx, src/components/ui/textarea.tsx, src/components/ui/dropdown-menu.tsx, src/components/ui/sheet.tsx, src/components/ui/sidebar.tsx, src/components/ui/sonner.tsx</files>
  <action>official registry만 사용해서 `button`, `input`, `textarea`, `dropdown-menu`, `sheet`, `sidebar`, `sonner`를 추가한다. custom sidebar/drawer/toast를 직접 만들지 않는다. 가져온 코드는 Biome로 정리하고, import alias가 `@/` 기준으로 정리되도록 맞춘다. plan 이후 shell implementation이 바로 이어질 수 있도록 unused demo snippets는 제거하고 component exports는 유지한다.</action>
  <verify>
    <automated>pnpm dlx shadcn@latest add button input textarea dropdown-menu sheet sidebar sonner && pnpm biome check . && pnpm build</automated>
  </verify>
  <done>필수 primitives가 `src/components/ui/` 아래에 추가되고 프로젝트가 여전히 build/lint 가능하다.</done>
</task>

</tasks>

<verification>
- shadcn CLI 초기화가 현재 Vite 프로젝트에서 정상 동작한다.
- official registry primitives만 추가되고 custom 대체 구현이 생기지 않는다.
- build와 biome 검증이 계속 통과한다.
</verification>

<success_criteria>
- shell plan이 `SidebarProvider`, `Sidebar`, `Sheet`, `Sonner` 패턴을 바로 사용할 수 있다.
- mobile nav, theme actions, toast feedback을 위한 공용 컴포넌트 기반이 준비된다.
- UI 구현이 shadcn/Vite 표준 구조를 따르므로 이후 확장이 단순하다.
</success_criteria>

<output>
After completion, create `.planning/phases/01-app-shell-and-interface-foundation/01-02-SUMMARY.md`
</output>
