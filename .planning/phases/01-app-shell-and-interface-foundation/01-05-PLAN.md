---
phase: 01-app-shell-and-interface-foundation
plan: 05
type: execute
wave: 5
depends_on:
  - "04"
files_modified:
  - src/components/chat/api-key-onboarding-card.tsx
  - src/components/chat/chat-empty-state.tsx
  - src/components/chat/message-pane-placeholder.tsx
  - src/components/chat/chat-composer.tsx
  - src/components/settings/settings-panel-placeholder.tsx
  - src/routes/index.tsx
  - src/routes/chat.$conversationId.tsx
  - src/routes/settings.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
must_haves:
  truths:
    - `/` 경로는 API 키 미등록 상태의 중앙 온보딩 카드를 한국어로 보여준다.
    - `/chat/$conversationId` 경로는 대화가 없는 상태의 메시지 영역과 2~4행 자동 확장 composer shell을 보여준다.
    - `/settings` 경로는 theme/API key/default model/system prompt를 위한 한국어 placeholder panel을 shell 안에서 일관되게 보여준다.
  artifacts:
    - src/components/chat/api-key-onboarding-card.tsx
    - src/components/chat/chat-empty-state.tsx
    - src/components/chat/message-pane-placeholder.tsx
    - src/components/chat/chat-composer.tsx
    - src/components/settings/settings-panel-placeholder.tsx
    - src/routes/index.tsx
    - src/routes/chat.$conversationId.tsx
    - src/routes/settings.tsx
  key_links:
    - index/chat/settings route leaves가 각각 대응하는 state component를 직접 렌더링한다.
    - chat route가 message pane placeholder와 composer를 같은 content column에 연결한다.
    - onboarding/error/empty copy가 01-UI-SPEC.md의 한글 문구 규칙과 직접 대응된다.
---

<objective>
한국어 온보딩, 빈 상태, route placeholder 화면을 shell 안에 완성해 Phase 1의 사용자 가시 화면을 마무리한다.

Purpose: shell chrome만 있는 상태에서 끝내지 않고, 사용자가 실제 제품 구조를 이해할 수 있는 한국어 state surfaces를 route별로 제공한다.
Output: API key onboarding card, empty chat state, placeholder message pane, auto-growing composer shell, settings placeholder panels.
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app.worktrees/frontend-codex/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app.worktrees/frontend-codex/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-app-shell-and-interface-foundation/01-CONTEXT.md
@.planning/phases/01-app-shell-and-interface-foundation/01-UI-SPEC.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 한국어 온보딩 카드, 빈 상태, composer shell 컴포넌트를 만든다</name>
  <files>src/components/chat/api-key-onboarding-card.tsx, src/components/chat/chat-empty-state.tsx, src/components/chat/message-pane-placeholder.tsx, src/components/chat/chat-composer.tsx</files>
  <action>UI-SPEC copywriting contract를 그대로 적용한다. API 키 미등록 상태는 중앙 카드가 첫 시선을 가져야 하고, 인라인 오류 슬롯과 상단 토스트를 수용할 구조를 포함해야 한다. chat empty state는 예시 프롬프트 없이 제목/설명/행동 유도에 집중한다. composer는 실제 전송 로직 없이도 2~4행 자동 확장 layout, 버튼 자리, keyboard-friendly spacing을 가져야 한다. reduced motion과 mobile 320px 폭에서의 overflow 금지 규칙을 지킨다.</action>
  <verify>
    <automated>pnpm biome check . && pnpm build</automated>
  </verify>
  <done>온보딩/빈 상태/composer 컴포넌트가 한국어 카피와 Phase 1 인터랙션 규칙을 반영한 상태로 생성된다.</done>
</task>

<task type="auto">
  <name>Task 2: route leaves를 실제 상태 화면과 settings placeholder에 연결한다</name>
  <files>src/routes/index.tsx, src/routes/chat.$conversationId.tsx, src/routes/settings.tsx, src/components/settings/settings-panel-placeholder.tsx</files>
  <action>`/`는 API 키 미등록 상태를 보여주는 index route로 연결하고, `/chat/$conversationId`는 빈 대화 message pane + composer shell을 렌더링한다. `/settings`는 API key, 기본 모델, 시스템 프롬프트, theme 섹션을 한국어 placeholder panel로 노출한다. settings는 아직 Dexie 저장/검증 로직을 넣지 말고 Phase 2를 위한 구조와 copy만 제공한다. 모든 route leaf는 root shell 안에서 자연스럽게 보이고, desktop/mobile에서 horizontal scroll 없이 동작해야 한다.</action>
  <verify>
    <automated>pnpm biome check . && pnpm build</automated>
  </verify>
  <done>세 route가 모두 shell 내부에서 한국어 상태 화면을 렌더링하고, Phase 1 성공 기준의 빈 상태/설정 구조를 충족한다.</done>
</task>

</tasks>

<verification>
- `/`, `/chat/$conversationId`, `/settings`가 서로 다른 한국어 state surface를 렌더링한다.
- 온보딩/빈 상태/composer/settings placeholder가 UI-SPEC 문구와 반응형 규칙을 따른다.
- build/lint가 마지막까지 통과한다.
</verification>

<success_criteria>
- 사용자는 API 키 미등록 상태, 빈 대화 상태, 설정 구조를 실제 shell 안에서 확인할 수 있다.
- 주요 내비게이션과 상태 UI가 한국어로 제공된다.
- Phase 2 이후 실제 데이터/로직을 얹을 수 있는 UI 골격이 완성된다.
</success_criteria>

<output>
After completion, create `.planning/phases/01-app-shell-and-interface-foundation/01-05-SUMMARY.md`
</output>
