---
phase: 01-app-shell-and-interface-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - package.json
  - pnpm-lock.yaml
  - vite.config.ts
  - src/App.tsx
  - src/providers/theme-provider.tsx
  - src/stores/ui-store.ts
  - src/router.tsx
  - src/routes/__root.tsx
  - src/routes/index.tsx
  - src/routes/chat.$conversationId.tsx
  - src/routes/settings.tsx
  - src/routeTree.gen.ts
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
must_haves:
  truths:
    - 앱이 TanStack Router file-based routing으로 `/`, `/chat/$conversationId`, `/settings` 세 경로를 인식한다.
    - theme는 dark 기본값을 유지하면서 system preference를 읽고 수동 전환 상태를 유지할 수 있다.
    - root route가 toast/theme/UI state를 공통으로 제공해 이후 shell과 route 화면을 감싼다.
  artifacts:
    - vite.config.ts
    - src/providers/theme-provider.tsx
    - src/stores/ui-store.ts
    - src/router.tsx
    - src/routes/__root.tsx
    - src/routes/index.tsx
    - src/routes/chat.$conversationId.tsx
    - src/routes/settings.tsx
    - src/routeTree.gen.ts
  key_links:
    - vite.config.ts가 TanStack Router Vite plugin을 통해 src/routes/*에서 routeTree를 생성한다.
    - src/router.tsx가 generated routeTree와 root route를 연결하고 src/App.tsx가 RouterProvider를 마운트한다.
    - src/providers/theme-provider.tsx와 src/stores/ui-store.ts가 root route 전체에서 theme/sidebar 상태를 공유한다.
---

<objective>
TanStack Router file-based routing, theme provider, UI store를 추가해 app shell이 실제 route 구조 위에서 렌더링될 기반을 만든다.

Purpose: 단순 placeholder 앱에서 벗어나, 이후 shell과 상태 화면이 실제 route tree와 app-wide providers 위에 구현되게 한다.
Output: router bootstrap, generated route tree, dark-default theme provider, shared UI store, route skeleton files.
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app.worktrees/frontend-codex/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app.worktrees/frontend-codex/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-app-shell-and-interface-foundation/01-CONTEXT.md
@.planning/phases/01-app-shell-and-interface-foundation/01-UI-SPEC.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TanStack Router file-based routing과 route skeleton을 추가한다</name>
  <files>package.json, pnpm-lock.yaml, vite.config.ts, src/router.tsx, src/routes/__root.tsx, src/routes/index.tsx, src/routes/chat.$conversationId.tsx, src/routes/settings.tsx, src/routeTree.gen.ts</files>
  <action>current official TanStack Router Vite plugin 흐름에 맞춰 `@tanstack/react-router`와 `@tanstack/router-plugin`을 추가하고, vite.config.ts에 router plugin을 연결한다. src/routes에는 file-based 규칙으로 `__root.tsx`, `index.tsx`, `chat.$conversationId.tsx`, `settings.tsx`를 만든다. 이 단계의 route 컴포넌트는 아직 최종 shell UI를 넣지 말고, 한국어 placeholder 제목과 안정적인 layout slot만 제공한다. routeTree는 build 과정에서 생성되므로 `src/routeTree.gen.ts`가 만들어지도록 plugin 출력까지 확인한다.</action>
  <verify>
    <automated>pnpm install && pnpm build && test -f src/routeTree.gen.ts</automated>
  </verify>
  <done>세 개의 핵심 route가 생성되고 build 후 generated route tree 파일이 생긴다.</done>
</task>

<task type="auto">
  <name>Task 2: dark 기본 theme provider와 공유 UI store를 app root에 연결한다</name>
  <files>src/App.tsx, src/providers/theme-provider.tsx, src/stores/ui-store.ts, src/routes/__root.tsx</files>
  <action>next-themes 같은 추가 테마 라이브러리를 끌어오기보다, Phase 1 요구사항에 필요한 최소 custom theme provider를 작성한다. provider는 `dark`를 기본으로 하되 system theme를 읽고 수동 전환 상태를 localStorage에 유지해야 한다. Zustand 기반 src/stores/ui-store.ts에는 모바일 sidebar open state와 theme preference를 담고, root route 또는 App.tsx에서 RouterProvider, theme provider, toaster mount를 함께 연결한다. 아직 실제 shell 레이아웃은 만들지 말고 provider + route outlet 기반만 안정화한다.</action>
  <verify>
    <automated>pnpm biome check . && pnpm build</automated>
  </verify>
  <done>앱이 router + theme provider를 통해 부팅되고, dark 기본값 및 system-aware theme 전환 기반이 root에서 공유된다.</done>
</task>

</tasks>

<verification>
- routeTree 생성이 build 과정에서 실제로 일어난다.
- `RouterProvider`와 theme provider가 함께 마운트된 상태로 앱이 빌드된다.
- shared UI store가 이후 shell plans에서 sidebar/theme action source로 재사용 가능하다.
</verification>

<success_criteria>
- `/`, `/chat/$conversationId`, `/settings` route skeleton이 존재한다.
- dark default + system-aware theme 로직이 앱 루트에 연결된다.
- 이후 shell plan은 provider/router를 새로 만들지 않고 UI만 얹으면 된다.
</success_criteria>

<output>
After completion, create `.planning/phases/01-app-shell-and-interface-foundation/01-03-SUMMARY.md`
</output>
