---
phase: 01-app-shell-and-interface-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - "03"
files_modified:
  - src/components/layout/app-shell.tsx
  - src/components/layout/app-sidebar.tsx
  - src/components/layout/app-header.tsx
  - src/components/layout/theme-toggle.tsx
  - src/components/layout/new-chat-button.tsx
  - src/components/layout/conversation-list.tsx
  - src/components/layout/conversation-list-item.tsx
  - src/routes/__root.tsx
  - src/stores/ui-store.ts
autonomous: true
requirements:
  - UI-01
  - UI-03
  - UI-04
must_haves:
  truths:
    - 데스크톱에서 약 280px sidebar와 표준형 header를 가진 ChatGPT 유사 shell이 렌더링된다.
    - 모바일에서 sidebar는 좌측 Sheet 오버레이로 열리고 기본은 닫힘이며, 햄버거와 우측 새 대화 액션이 동작한다.
    - active conversation, focus ring, theme toggle이 제한된 accent 규칙 안에서 표시된다.
  artifacts:
    - src/components/layout/app-shell.tsx
    - src/components/layout/app-sidebar.tsx
    - src/components/layout/app-header.tsx
    - src/components/layout/theme-toggle.tsx
    - src/components/layout/new-chat-button.tsx
    - src/components/layout/conversation-list.tsx
    - src/components/layout/conversation-list-item.tsx
    - src/routes/__root.tsx
  key_links:
    - src/routes/__root.tsx가 AppShell을 route outlet과 연결한다.
    - AppSidebar가 official shadcn sidebar/sheet primitives와 src/stores/ui-store.ts를 함께 사용한다.
    - AppHeader가 theme toggle과 모바일 새 대화 액션을 shell chrome에 연결한다.
---

<objective>
ChatGPT 유사 앱 셸, 반응형 sidebar, 표준형 header를 실제 root layout로 구현한다.

Purpose: Phase 1의 핵심 요구인 shell 구조와 mobile/desktop navigation behavior를 실제 앱 chrome으로 고정한다.
Output: AppShell, AppSidebar, AppHeader, theme toggle, mock conversation list, root route shell wiring.
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app.worktrees/frontend-codex/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app.worktrees/frontend-codex/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-app-shell-and-interface-foundation/01-CONTEXT.md
@.planning/phases/01-app-shell-and-interface-foundation/01-UI-SPEC.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: desktop/mobile 공용 shell chrome 컴포넌트를 만든다</name>
  <files>src/components/layout/app-shell.tsx, src/components/layout/app-sidebar.tsx, src/components/layout/app-header.tsx, src/components/layout/new-chat-button.tsx, src/components/layout/conversation-list.tsx, src/components/layout/conversation-list-item.tsx</files>
  <action>D-01~D-16과 UI-SPEC layout contract를 그대로 반영한다. desktop은 약 280px sidebar, standard header, main content, bottom composer slot 구조를 유지한다. mobile은 custom drawer를 만들지 말고 official `Sheet`/`Sidebar` primitives를 사용한다. conversation list는 실제 IndexedDB 연결 대신 deterministic mock items를 사용하되, active item 표시와 최신순 느낌이 드는 더미 데이터를 넣는다. 카드 그림자나 과한 motion을 넣지 말고 거의 평면형 surface, 150-200ms transition, 제한적 accent만 사용한다.</action>
  <verify>
    <automated>pnpm biome check . && pnpm build</automated>
  </verify>
  <done>독립 layout 컴포넌트들이 생성되고 mobile/desktop shell 구조가 코드로 분리되어 재사용 가능하다.</done>
</task>

<task type="auto">
  <name>Task 2: root route에 shell을 연결하고 responsive navigation interaction을 완성한다</name>
  <files>src/routes/__root.tsx, src/stores/ui-store.ts, src/components/layout/theme-toggle.tsx</files>
  <action>root route는 현재 pathname을 바탕으로 header title과 활성 sidebar 항목을 계산하고 AppShell에 전달한다. src/stores/ui-store.ts를 확장해 mobile sheet open/close, theme toggle, 새 대화 버튼 클릭 핸들러에 필요한 상태를 넣는다. 모바일에서는 햄버거 버튼만 sidebar open trigger가 되고, 새 대화 액션은 header 우측에 별도로 둔다. keyboard focus ring, active state, theme toggle aria-label은 UI-SPEC 문구를 따른다. horizontal scroll이 생기지 않게 320px 폭까지 확인 가능한 CSS 구조를 유지한다.</action>
  <verify>
    <automated>pnpm biome check . && pnpm build</automated>
  </verify>
  <done>root route에서 실제 shell chrome이 렌더링되고, mobile/desktop navigation behavior가 Phase 1 결정과 일치한다.</done>
</task>

</tasks>

<verification>
- desktop과 mobile 모두 같은 route tree 위에서 shell이 렌더링된다.
- mobile sidebar는 default closed left Sheet로 동작한다.
- header/theme/new chat/sidebar active state가 shell 전체에서 일관되게 보인다.
</verification>

<success_criteria>
- 사용자는 ChatGPT 유사 구조의 sidebar + header + content shell을 볼 수 있다.
- 사용자는 모바일에서 햄버거 기반 sidebar와 우측 새 대화 액션을 사용할 수 있다.
- theme toggle과 accent 사용이 UI-SPEC/CONTEXT 결정과 충돌하지 않는다.
</success_criteria>

<output>
After completion, create `.planning/phases/01-app-shell-and-interface-foundation/01-04-SUMMARY.md`
</output>
