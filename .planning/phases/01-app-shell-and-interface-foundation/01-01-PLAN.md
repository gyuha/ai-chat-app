---
phase: 01-app-shell-and-interface-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - index.html
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - vite.config.ts
  - biome.json
  - src/vite-env.d.ts
  - src/main.tsx
  - src/App.tsx
  - src/styles/globals.css
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-04
must_haves:
  truths:
    - 현재 저장소 루트에서 pnpm 기반 React 19 + Vite + TypeScript strict 앱이 바로 설치, 실행, build 가능한 상태가 된다.
    - 앱 엔트리가 한국어 메타데이터와 다크 기본 배경을 가진 최소 부트스트랩 화면을 렌더링한다.
  artifacts:
    - package.json
    - vite.config.ts
    - biome.json
    - src/main.tsx
    - src/App.tsx
    - src/styles/globals.css
  key_links:
    - package.json scripts가 vite와 biome를 호출하고 생성된 앱 엔트리를 대상으로 동작한다.
    - src/main.tsx가 src/styles/globals.css와 src/App.tsx를 함께 마운트한다.
---

<objective>
React 19 + Vite + TypeScript strict + Biome + Tailwind CSS v4 기반의 통제된 앱 부트스트랩을 수동으로 만든다.

Purpose: 이후 plan들이 uncontrolled scaffold diff 없이 동일한 파일 구조 위에서 shell, router, shadcn 컴포넌트를 누적 구현할 수 있게 한다.
Output: 설치 가능한 package/toolchain 설정, 다크 기본 전역 스타일, 최소 앱 엔트리.
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app.worktrees/frontend-codex/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app.worktrees/frontend-codex/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-app-shell-and-interface-foundation/01-CONTEXT.md
@.planning/phases/01-app-shell-and-interface-foundation/01-UI-SPEC.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 수동 Vite 앱 골격과 toolchain 설정 파일을 만든다</name>
  <files>package.json, pnpm-lock.yaml, index.html, tsconfig.json, tsconfig.app.json, tsconfig.node.json, vite.config.ts, biome.json, src/vite-env.d.ts</files>
  <action>자동 scaffold CLI를 그대로 실행하지 말고, 현재 빈 저장소에 맞는 최소 파일만 수동으로 작성한다. package.json에는 React 19, React DOM, Vite, TypeScript, @vitejs/plugin-react, tailwindcss, @tailwindcss/vite, @biomejs/biome를 추가하고 pnpm scripts는 dev/build/preview/lint/format으로 정리한다. TypeScript는 strict mode를 유지하고 `@/*` alias가 이후 shadcn/TanStack Router에서 재사용 가능하도록 vite.config.ts와 tsconfig 계열 파일을 맞춘다. Biome는 formatter+linter 기본값만 두고 ESLint/Prettier는 추가하지 않는다. 서버, OpenRouter 호출, IndexedDB 관련 의존성은 이 plan에서 넣지 않는다.</action>
  <verify>
    <automated>pnpm install</automated>
  </verify>
  <done>루트에서 `pnpm install`이 성공하고 Vite/TypeScript/Biome 기반 파일들이 이후 UI 작업을 받을 수 있는 상태다.</done>
</task>

<task type="auto">
  <name>Task 2: 최소 앱 엔트리와 다크 기본 글로벌 스타일을 만든다</name>
  <files>src/main.tsx, src/App.tsx, src/styles/globals.css, index.html</files>
  <action>src/main.tsx에서 글로벌 CSS를 불러오고 src/App.tsx를 마운트한다. src/App.tsx는 Phase 3에서 router로 대체될 임시 부트스트랩 화면이지만, 지금도 한국어 앱 이름과 다크 기본 배경을 보여야 한다. src/styles/globals.css에는 Tailwind CSS v4 방식의 `@import "tailwindcss";`를 사용하고, UI-SPEC의 dominant/secondary/text 색을 반영한 root/body/#root 기본 규칙을 둔다. index.html은 `lang="ko"`와 모바일 viewport를 포함하고 title은 `OpenRouter Chat`으로 둔다. 과한 데모 UI나 불필요한 샘플 코드는 넣지 않는다.</action>
  <verify>
    <automated>pnpm biome check . && pnpm build</automated>
  </verify>
  <done>브라우저에서 앱이 다크 기본 배경의 최소 화면으로 부팅되고, `pnpm build`와 `pnpm biome check .`가 모두 통과한다.</done>
</task>

</tasks>

<verification>
- `pnpm install`이 루트에서 오류 없이 완료된다.
- `pnpm biome check .`가 baseline 위반 없이 통과한다.
- `pnpm build`가 빈 shell 앱을 정상 빌드한다.
</verification>

<success_criteria>
- 사용자는 비어 있는 저장소가 아니라 실행 가능한 Vite 앱 엔트리를 받는다.
- 이후 plan에서 router, shadcn, shell 레이아웃을 같은 파일 구조 위에 누적할 수 있다.
- 다크 기본값과 한국어 앱 식별자가 초기 엔트리부터 반영된다.
</success_criteria>

<output>
After completion, create `.planning/phases/01-app-shell-and-interface-foundation/01-01-SUMMARY.md`
</output>
