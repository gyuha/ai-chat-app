---
phase: 02-auth-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/prisma/schema.prisma
  - backend/src/auth/auth.module.ts
  - backend/src/auth/auth.controller.ts
  - backend/src/auth/auth.service.ts
  - backend/src/users/users.module.ts
  - backend/src/users/users.service.ts
  - backend/src/prisma/prisma.module.ts
  - backend/src/prisma/prisma.service.ts
  - backend/src/auth/strategies/local.strategy.ts
  - backend/src/auth/strategies/jwt.strategy.ts
  - backend/src/auth/guards/jwt-auth.guard.ts
  - backend/src/auth/dto/register.dto.ts
  - backend/src/auth/dto/login.dto.ts
  - backend/src/auth/dto/auth-response.dto.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - SEC-02
user_setup: []

must_haves:
  truths:
    - "사용자가 이메일과 비밀번호로 회원가입할 수 있다"
    - "사용자가 이메일과 비밀번호로 로그인할 수 있다"
    - "비밀번호가 bcrypt로 안전하게 해싱되어 저장된다"
    - "JWT 토큰이 발급되어 인증 상태를 유지한다"
  artifacts:
    - path: "backend/prisma/schema.prisma"
      provides: "User 모델 (refreshToken 필드 포함)"
      contains: "refreshToken String?"
    - path: "backend/src/auth/auth.service.ts"
      provides: "인증 로직 (회원가입, 로그인, 토큰 발급)"
      exports: ["register", "login", "refreshToken"]
    - path: "backend/src/auth/auth.controller.ts"
      provides: "인증 API 엔드포인트"
      exports: ["POST /api/auth/register", "POST /api/auth/login", "POST /api/auth/refresh", "POST /api/auth/logout", "GET /api/auth/me"]
    - path: "backend/src/auth/strategies/jwt.strategy.ts"
      provides: "JWT 토큰 검증 전략"
      contains: "PassportStrategy(Strategy)"
    - path: "backend/src/users/users.service.ts"
      provides: "사용자 CRUD 서비스"
      exports: ["findByEmail", "create"]
  key_links:
    - from: "backend/src/auth/auth.controller.ts"
      to: "backend/src/auth/auth.service.ts"
      via: "의존성 주입"
      pattern: "constructor\\(private authService: AuthService\\)"
    - from: "backend/src/auth/auth.service.ts"
      to: "backend/src/users/users.service.ts"
      via: "UsersModule 임포트"
      pattern: "imports:.*UsersModule"
    - from: "backend/src/auth/auth.service.ts"
      to: "backend/prisma/schema.prisma"
      via: "PrismaService"
      pattern: "prisma\\.user\\.(create|findUnique)"
    - from: "backend/src/auth/auth.controller.ts"
      to: "backend/src/auth/strategies/jwt.strategy.ts"
      via: "JwtAuthGuard"
      pattern: "@UseGuards(JwtAuthGuard)"
---

<objective>
NestJS Passport/JWT 기반 인증 시스템의 백엔드 인프라와 API를 구축합니다. 이메일/비밀번호 회원가입, JWT 듀얼 토큰 전략(액세스 15분/리프레시 7일), 리프레시 토큰 DB 저장, httpOnly 쿠키 기반 리프레시 토큰 전달을 구현합니다.

Purpose: 사용자 인증의 기반이 되는 백엔드 API와 보안 레이어 구축
Output: 작동하는 인증 API (register, login, refresh, logout, me)와 JWT 토큰 발급/검증 시스템
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app.worktrees/gsd-glm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app.worktrees/gsd-glm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-auth-system/02-CONTEXT.md
@.planning/phases/02-auth-system/02-RESEARCH.md

# 기존 코드 패턴
@backend/prisma/schema.prisma
@backend/src/main.ts
@backend/src/app.module.ts
</context>

<interfaces>
<!-- 기존 코드에서 추출한 인터페이스 -->

From backend/prisma/schema.prisma:
```typescript
model User {
  id        String   @id @default(uuid())
  email     String   @unique
  password  String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  chats     Chat[]
}
```

From backend/src/main.ts:
```typescript
// ValidationPipe 설정: whitelist + forbidNonWhitelisted + transform
app.useGlobalPipes(new ValidationPipe({ whitelist: true, forbidNonWhitelisted: true, transform: true }));

// CORS 설정: credentials: true
app.enableCors({ origin: process.env.FRONTEND_URL || "http://localhost:5173", credentials: true });
```

From backend/src/app.module.ts:
```typescript
ConfigModule.forRoot({ isGlobal: true, envFilePath: ".env" })
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Prisma 스키마 업데이트 및 인증 의존성 설치</name>
  <files>backend/prisma/schema.prisma, backend/package.json, package.json</files>
  <read_first>
    backend/prisma/schema.prisma
    backend/package.json
  </read_first>
  <action>
    **Prisma 스키마 업데이트 (D-02)**:
    1. backend/prisma/schema.prisma의 User 모델에 refreshToken 필드 추가:
       ```prisma
       model User {
         id           String    @id @default(uuid())
         email        String    @unique
         password     String
         refreshToken String?   @unique
         createdAt    DateTime  @default(now())
         updatedAt    DateTime  @updatedAt
         chats        Chat[]
       }
       ```
    2. 마이그레이션 생성 및 적용: `cd backend && npx prisma migrate dev --name add_refresh_token`
    3. Prisma 클라이언트 재생성: `cd backend && npx prisma generate`

    **인증 의존성 설치**:
    1. 백엔드 의존성 설치:
       ```bash
       cd backend
       pnpm install @nestjs/passport@^11.0.0 @nestjs/jwt@^11.0.0 passport@^0.7.0 passport-jwt@^4.0.0 passport-local@^1.0.0
       pnpm install bcrypt@^5.1.0
       pnpm install cookie-parser@^1.4.7
       ```
    2. 타입 정의 설치:
       ```bash
       pnpm install -D @types/passport-jwt@^4.0.0 @types/bcrypt@^5.0.0 @types/passport-local@^1.0.0 @types/cookie-parser@^1.4.7
       ```

    **주의사항**:
    - refreshToken 필드는 nullable (@unique)로 설정하여 로그아웃 시 null로 초기화 가능
    - bcrypt는 비밀번호 해싱용 (D-15)
    - cookie-parser는 httpOnly 쿠키 파싱용 (D-05)
  </action>
  <verify>
    <automated>grep -q "refreshToken String?\s*@unique" backend/prisma/schema.prisma && pnpm --filter backend list | grep -E "@nestjs/passport|@nestjs/jwt|passport|bcrypt"</automated>
  </verify>
  <done>
    - Prisma 스키마에 refreshToken 필드 추가됨
    - 마이그레이션 적용되어 DB 스키마 업데이트됨
    - 인증 관련 의존성 설치 완료 (@nestjs/passport, @nestjs/jwt, bcrypt 등)
  </done>
</task>

<task type="auto">
  <name>Task 2: PrismaModule, UsersModule, AuthModule 생성</name>
  <files>backend/src/prisma/prisma.module.ts, backend/src/prisma/prisma.service.ts, backend/src/users/users.module.ts, backend/src/users/users.service.ts, backend/src/users/dto/create-user.dto.ts, backend/src/auth/auth.module.ts</files>
  <read_first>
    backend/src/app.module.ts
  </read_first>
  <action>
    **PrismaModule 생성** (D-24):
    1. backend/src/prisma/prisma.service.ts 생성:
       ```typescript
       import { Injectable, OnModuleInit } from '@nestjs/common';
       import { PrismaClient } from '@prisma/client';

       @Injectable()
       export class PrismaService extends PrismaClient implements OnModuleInit {
         async onModuleInit() {
           await this.$connect();
         }
       }
       ```
    2. backend/src/prisma/prisma.module.ts 생성:
       ```typescript
       import { Module, Global } from '@nestjs/common';
       import { PrismaService } from './prisma.service';

       @Global()
       @Module({
         providers: [PrismaService],
         exports: [PrismaService],
       })
       export class PrismaModule {}
       ```

    **UsersModule 생성** (D-22):
    1. backend/src/users/dto/create-user.dto.ts 생성:
       ```typescript
       import { IsEmail, IsString, MinLength, MaxLength } from 'class-validator';

       export class CreateUserDto {
         @IsEmail()
         email: string;

         @IsString()
         @MinLength(8)
         @MaxLength(128)
         password: string;
       }
       ```
    2. backend/src/users/users.service.ts 생성:
       ```typescript
       import { Injectable, ConflictException } from '@nestjs/common';
       import { PrismaService } from '../prisma/prisma.service';
       import * as bcrypt from 'bcrypt';
       import { CreateUserDto } from './dto/create-user.dto';

       @Injectable()
       export class UsersService {
         constructor(private prisma: PrismaService) {}

         async findByEmail(email: string) {
           return this.prisma.user.findUnique({ where: { email } });
         }

         async findById(id: string) {
           return this.prisma.user.findUnique({ where: { id } });
         }

         async create(dto: CreateUserDto) {
           const existingUser = await this.findByEmail(dto.email);
           if (existingUser) {
             throw new ConflictException('이미 존재하는 이메일입니다');
           }

           const hashedPassword = await bcrypt.hash(dto.password, 10);
           return this.prisma.user.create({
             data: {
               email: dto.email,
               password: hashedPassword,
             },
           });
         }
       }
       ```
    3. backend/src/users/users.module.ts 생성:
       ```typescript
       import { Module } from '@nestjs/common';
       import { UsersService } from './users.service';

       @Module({
         providers: [UsersService],
         exports: [UsersService],
       })
       export class UsersModule {}
       ```

    **AuthModule 생성** (D-21):
    1. backend/src/auth/auth.module.ts 생성:
       ```typescript
       import { Module } from '@nestjs/common';
       import { JwtModule } from '@nestjs/jwt';
       import { PassportModule } from '@nestjs/passport';
       import { ConfigModule, ConfigService } from '@nestjs/config';
       import { AuthController } from './auth.controller';
       import { AuthService } from './auth.service';
       import { UsersModule } from '../users/users.module';
       import { JwtStrategy } from './strategies/jwt.strategy';
       import { LocalStrategy } from './strategies/local.strategy';

       @Module({
         imports: [
           UsersModule,
           PassportModule,
           JwtModule.registerAsync({
             imports: [ConfigModule],
             useFactory: async (configService: ConfigService) => ({
               secret: configService.get<string>('JWT_SECRET'),
               signOptions: { expiresIn: '15m' }, // D-01: 액세스 토큰 15분
             }),
             inject: [ConfigService],
           }),
         ],
         providers: [AuthService, LocalStrategy, JwtStrategy],
         controllers: [AuthController],
         exports: [AuthService],
       })
       export class AuthModule {}
       ```

    **AppModule 업데이트**:
    1. backend/src/app.module.ts에 모듈 임포트 추가:
       ```typescript
       import { Module } from "@nestjs/common";
       import { ConfigModule } from "@nestjs/config";
       import { PrismaModule } from "./prisma/prisma.module";
       import { AuthModule } from "./auth/auth.module";

       @Module({
         imports: [
           ConfigModule.forRoot({
             isGlobal: true,
             envFilePath: ".env",
           }),
           PrismaModule,
           AuthModule,
         ],
         controllers: [],
         providers: [],
       })
       export class AppModule {}
       ```

    **주의사항**:
    - PrismaModule은 @Global()로 선언하여 모든 모듈에서 PrismaService 접근 가능
    - 비밀번호는 bcrypt cost 10으로 해싱 (D-15)
    - 이메일 중복 체크는 DB @unique 제약조건과 함께 이중 확인
    - JWT 만료는 15분 (D-01)
  </action>
  <verify>
    <automated>ls backend/src/prisma/*.ts backend/src/users/*.ts backend/src/auth/auth.module.ts && grep -q "PrismaModule" backend/src/app.module.ts && grep -q "AuthModule" backend/src/app.module.ts</automated>
  </verify>
  <done>
    - PrismaModule: PrismaService 제공자 등록
    - UsersModule: UsersService (findByEmail, create) 구현
    - AuthModule: JwtModule, PassportModule 설정 완료
    - AppModule에 PrismaModule, AuthModule 임포트됨
  </done>
</task>

<task type="auto">
  <name>Task 3: Passport 전략 및 AuthService 구현</name>
  <files>backend/src/auth/strategies/local.strategy.ts, backend/src/auth/strategies/jwt.strategy.ts, backend/src/auth/guards/jwt-auth.guard.ts, backend/src/auth/dto/register.dto.ts, backend/src/auth/dto/login.dto.ts, backend/src/auth/dto/auth-response.dto.ts, backend/src/auth/auth.service.ts</files>
  <read_first>
    backend/src/auth/auth.module.ts
  </read_first>
  <action>
    **DTO 생성**:
    1. backend/src/auth/dto/register.dto.ts 생성 (D-13, D-14):
       ```typescript
       import { IsEmail, IsString, MinLength, MaxLength } from 'class-validator';

       export class RegisterDto {
         @IsEmail()
         email: string;

         @IsString()
         @MinLength(8)
         @MaxLength(128)
         password: string;
       }
       ```
    2. backend/src/auth/dto/login.dto.ts 생성:
       ```typescript
       import { IsEmail, IsString } from 'class-validator';

       export class LoginDto {
         @IsEmail()
         email: string;

         @IsString()
         password: string;
       }
       ```
    3. backend/src/auth/dto/auth-response.dto.ts 생성:
       ```typescript
       import { UserResponseDto } from './user-response.dto';

       export class AuthResponseDto {
         accessToken: string;
         user: UserResponseDto;
       }

       export class UserResponseDto {
         id: string;
         email: string;
         createdAt: Date;
         updatedAt: Date;
       }
       ```

    **Passport 전략 생성** (D-23):
    1. backend/src/auth/strategies/local.strategy.ts 생성:
       ```typescript
       import { Strategy } from 'passport-local';
       import { PassportStrategy } from '@nestjs/passport';
       import { Injectable, UnauthorizedException } from '@nestjs/common';
       import { AuthService } from '../auth.service';

      @Injectable()
       export class LocalStrategy extends PassportStrategy(Strategy) {
         constructor(private authService: AuthService) {
           super({ usernameField: 'email' });
         }

         async validate(email: string, password: string): Promise<any> {
           const user = await this.authService.validateUser(email, password);
           if (!user) {
             throw new UnauthorizedException('이메일 또는 비밀번호가 올바르지 않습니다');
           }
           return user;
         }
       }
       ```
    2. backend/src/auth/strategies/jwt.strategy.ts 생성:
       ```typescript
       import { ExtractJwt, Strategy } from 'passport-jwt';
       import { PassportStrategy } from '@nestjs/passport';
       import { Injectable } from '@nestjs/common';
       import { ConfigService } from '@nestjs/config';

       @Injectable()
       export class JwtStrategy extends PassportStrategy(Strategy) {
         constructor(private configService: ConfigService) {
           super({
             jwtFromRequest: ExtractJwt.fromAuthHeaderAsBearerToken(),
             ignoreExpiration: false,
             secretOrKey: configService.get<string>('JWT_SECRET'),
           });
         }

         async validate(payload: any) {
           return { userId: payload.sub, email: payload.email };
         }
       }
       ```
    3. backend/src/auth/guards/jwt-auth.guard.ts 생성:
       ```typescript
       import { Injectable } from '@nestjs/common';
       import { AuthGuard } from '@nestjs/passport';

       @Injectable()
       export class JwtAuthGuard extends AuthGuard('jwt') {}
       ```

    **AuthService 구현** (D-01, D-02, D-15):
    backend/src/auth/auth.service.ts 생성:
       ```typescript
       import { Injectable, UnauthorizedException } from '@nestjs/common';
       import { JwtService } from '@nestjs/jwt';
       import { ConfigService } from '@nestjs/config';
       import * as bcrypt from 'bcrypt';
       import { UsersService } from '../users/users.service';
       import { RegisterDto } from './dto/register.dto';
       import { randomBytes } from 'crypto';

       @Injectable()
       export class AuthService {
         constructor(
           private usersService: UsersService,
           private jwtService: JwtService,
           private configService: ConfigService,
         ) {}

         async validateUser(email: string, password: string): Promise<any> {
           const user = await this.usersService.findByEmail(email);
           if (!user) {
             return null;
           }
           const isPasswordValid = await bcrypt.compare(password, user.password);
           if (!isPasswordValid) {
             return null;
           }
           const { password: _, ...result } = user;
           return result;
         }

         async register(dto: RegisterDto) {
           const user = await this.usersService.create(dto);
           const tokens = await this.generateTokens(user.id, user.email);
           await this.saveRefreshToken(user.id, tokens.refreshToken);
           return tokens;
         }

         async login(user: any) {
           const tokens = await this.generateTokens(user.userId, user.email);
           await this.saveRefreshToken(user.userId, tokens.refreshToken);
           return tokens;
         }

         async refresh(userId: string, refreshToken: string) {
           const user = await this.usersService.findById(userId);
           if (!user || user.refreshToken !== refreshToken) {
             throw new UnauthorizedException('Invalid refresh token');
           }
           const tokens = await this.generateTokens(user.id, user.email);
           await this.saveRefreshToken(user.id, tokens.refreshToken);
           return tokens;
         }

         async logout(userId: string) {
           await this.usersService.updateRefreshToken(userId, null);
         }

         private async generateTokens(userId: string, email: string) {
           const payload = { sub: userId, email };
           const accessToken = this.jwtService.sign(payload);
           const refreshToken = this.generateRefreshToken();
           return {
             accessToken,
             refreshToken,
             user: { id: userId, email },
           };
         }

         private generateRefreshToken(): string {
           return randomBytes(32).toString('hex');
         }

         private async saveRefreshToken(userId: string, refreshToken: string | null) {
           await this.usersService.updateRefreshToken(userId, refreshToken);
         }
       }
       ```

    **UsersService 업데이트**:
    backend/src/users/users.service.ts에 메서드 추가:
       ```typescript
       async updateRefreshToken(userId: string, refreshToken: string | null) {
         return this.prisma.user.update({
           where: { id: userId },
           data: { refreshToken },
         });
       }
       ```

    **주의사항**:
    - LocalStrategy는 usernameField를 'email'로 설정 (D-08, D-09)
    - JwtStrategy는 Bearer 토큰에서 추출 (D-12)
    - 리프레시 토큰은 DB에 저장하여 서버 사이드 무효화 가능 (D-02)
    - 비밀번호 비교는 bcrypt.compare 사용
  </action>
  <verify>
    <automated>ls backend/src/auth/strategies/*.ts backend/src/auth/guards/*.ts backend/src/auth/dto/*.ts && grep -q "export class AuthService" backend/src/auth/auth.service.ts</automated>
  </verify>
  <done>
    - LocalStrategy: 이메일/비밀번호 검증
    - JwtStrategy: JWT 토큰 검증
    - JwtAuthGuard: 보호된 라우트용 가드
    - AuthService: 회원가입, 로그인, 리프레시, 로그아웃 로직 구현
    - 모든 DTO: class-validator 데코레이터로 검증 규칙 적용
  </done>
</task>

<task type="auto">
  <name>Task 4: AuthController 엔드포인트 구현</name>
  <files>backend/src/auth/auth.controller.ts</files>
  <read_first>
    backend/src/auth/auth.service.ts
    backend/src/auth/auth.module.ts
  </read_first>
  <action>
    backend/src/auth/auth.controller.ts 생성 (D-08 ~ D-12):

    ```typescript
    import { Controller, Post, Body, Get, UseGuards, Req, Res, Put, HttpCode, HttpStatus } from '@nestjs/common';
    import { Request, Response } from 'express';
    import { AuthService } from './auth.service';
    import { RegisterDto } from './dto/register.dto';
    import { LoginDto } from './dto/login.dto';
    import { JwtAuthGuard } from './guards/jwt-auth.guard';
    import { AuthenticatedRequest } from './types/authenticated-request.interface';

    @Controller('auth')
    export class AuthController {
      constructor(private authService: AuthService) {}

      @Post('register')
      async register(@Body() dto: RegisterDto, @Res({ passthrough: true }) res: Response) {
        const result = await this.authService.register(dto);
        this.setRefreshTokenCookie(res, result.refreshToken);
        return { accessToken: result.accessToken, user: result.user };
      }

      @UseGuards(LocalStrategy)
      @Post('login')
      async login(@Req() req: Request, @Res({ passthrough: true }) res: Response) {
        const result = await this.authService.login(req.user);
        this.setRefreshTokenCookie(res, result.refreshToken);
        return { accessToken: result.accessToken, user: result.user };
      }

      @Post('refresh')
      async refresh(@Req() req: Request, @Res({ passthrough: true }) res: Response) {
        const refreshToken = req.cookies?.refreshToken;
        if (!refreshToken) {
          throw new Error('Refresh token not found');
        }
        const decoded = this.authService.verifyRefreshToken(refreshToken);
        const result = await this.authService.refresh(decoded.userId, refreshToken);
        this.setRefreshTokenCookie(res, result.refreshToken);
        return { accessToken: result.accessToken, user: result.user };
      }

      @UseGuards(JwtAuthGuard)
      @Post('logout')
      @HttpCode(HttpStatus.NO_CONTENT)
      async logout(@Req() req: AuthenticatedRequest, @Res({ passthrough: true }) res: Response) {
        await this.authService.logout(req.user.userId);
        res.clearCookie('refreshToken', {
          httpOnly: true,
          sameSite: 'lax',
          path: '/api/auth/refresh',
        });
      }

      @UseGuards(JwtAuthGuard)
      @Get('me')
      async me(@Req() req: AuthenticatedRequest) {
        return this.usersService.findById(req.user.userId);
      }

      private setRefreshTokenCookie(res: Response, refreshToken: string) {
        res.cookie('refreshToken', refreshToken, {
          httpOnly: true, // D-05: XSS 방지
          secure: process.env.NODE_ENV === 'production', // D-07
          sameSite: 'lax', // D-07
          path: '/api/auth/refresh', // D-07
          maxAge: 7 * 24 * 60 * 60 * 1000, // 7일 (D-01)
        });
      }
    }
    ```

    **타입 정의 파일 생성**:
    backend/src/auth/types/authenticated-request.interface.ts:
       ```typescript
       import { Request } from 'express';

       export interface AuthenticatedRequest extends Request {
         user: {
           userId: string;
           email: string;
         };
       }
       ```

    **main.ts 업데이트** (cookie-parser 미들웨어 추가):
    1. backend/src/main.ts에 cookie-parser 추가:
       ```typescript
       import * as cookieParser from 'cookie-parser';

       async function bootstrap() {
         const app = await NestFactory.create(AppModule);

         // Cookie parser 미들웨어 추가 (리프레시 토큰 쿠키 파싱용)
         app.use(cookieParser());

         // 기존 설정 유지...
       }
       ```

    **주의사항**:
    - register: 회원가입 후 자동 로그인 처리 (D-19)
    - login: LocalStrategy로 인증 후 토큰 발급 (D-09)
    - refresh: 쿠키에서 리프레시 토큰 읽어서 액세스 토큰 재발급 (D-10)
    - logout: 쿠키 삭제 + DB 리프레시 토큰 무효화 (D-11)
    - me: 현재 사용자 정보 반환 (D-12)
    - httpOnly 쿠키는 /api/auth/refresh 경로로 제한 (D-07)
    - secure는 production에서만 true (개발 환경에서는 false)
  </action>
  <verify>
    <automated>grep -q "export class AuthController" backend/src/auth/auth.controller.ts && grep -q "cookieParser" backend/src/main.ts</automated>
  </verify>
  <done>
    - POST /api/auth/register: 회원가입 + 리프레시 토큰 쿠키 설정
    - POST /api/auth/login: 로그인 + 리프레시 토큰 쿠키 설정
    - POST /api/auth/refresh: 액세스 토큰 재발급
    - POST /api/auth/logout: 로그아웃 (쿠키 삭제, DB 토큰 무효화)
    - GET /api/auth/me: 현재 사용자 정보 조회
    - cookie-parser 미들웨어가 main.ts에 추가됨
  </done>
</task>

</tasks>

<verification>
1. **백엔드 서버 시작**: `pnpm --filter backend dev`로 서버 실행, "Nest application successfully started" 로그 확인
2. **Prisma 마이그레이션 확인**: `ls backend/prisma/migrations/*_add_refresh_token/`로 마이그레이션 파일 존재 확인
3. **인증 API 테스트**:
   ```bash
   # 회원가입
   curl -X POST http://localhost:3000/api/auth/register \
     -H "Content-Type: application/json" \
     -d '{"email":"test@example.com","password":"test1234"}' \
     -v cookies.txt

   # 로그인
   curl -X POST http://localhost:3000/api/auth/login \
     -H "Content-Type: application/json" \
     -d '{"email":"test@example.com","password":"test1234"}' \
     -v cookies.txt

   # /me 엔드포인트 (액세스 토큰 필요)
   curl -X GET http://localhost:3000/api/auth/me \
     -H "Authorization: Bearer {accessToken}"

   # 로그아웃
   curl -X POST http://localhost:3000/api/auth/logout \
     -H "Authorization: Bearer {accessToken}" \
     -b cookies.txt
   ```
4. **모듈 의존성 확인**: `nest info`로 AuthModule, UsersModule, PrismaModule 로딩 확인
</verification>

<success_criteria>
1. Prisma 스키마에 refreshToken 필드가 추가되고 마이그레이션 완료
2. 인증 API (register, login, refresh, logout, me)가 정상 응답
3. JWT 액세스 토큰이 15분 만료로 발급되고 Bearer 헤더로 전달
4. 리프레시 토큰이 httpOnly 쿠키로 설정되고 DB에 저장
5. 비밀번호가 bcrypt로 해싱되어 DB에 저장
6. 로그아웃 시 리프레시 토큰 쿠키가 삭제되고 DB에서 null로 초기화
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-system/02-01-SUMMARY.md`
</output>
