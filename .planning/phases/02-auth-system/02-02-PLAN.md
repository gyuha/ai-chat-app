---
phase: 02-auth-system
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - frontend/package.json
  - frontend/src/stores/auth.ts
  - frontend/src/lib/api/client.ts
  - frontend/src/lib/api/auth.ts
  - frontend/src/routes/__root.tsx
  - frontend/src/routes/index.tsx
  - frontend/src/routes/login.tsx
  - frontend/src/routes/register.tsx
  - frontend/src/components/auth/LoginForm.tsx
  - frontend/src/components/auth/RegisterForm.tsx
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - SEC-02
user_setup: []

must_haves:
  truths:
    - "사용자가 로그인 페이지에서 이메일과 비밀번호로 로그인할 수 있다"
    - "사용자가 회원가입 페이지에서 계정을 생성할 수 있다"
    - "사용자가 로그인 후 메인 페이지로 리다이렉트된다"
    - "사용자가 브라우저를 새로고침해도 로그인 상태가 유지된다"
    - "사용자가 로그아웃하면 로그인 페이지로 리다이렉트된다"
  artifacts:
    - path: "frontend/src/stores/auth.ts"
      provides: "Zustand 인증 상태 관리"
      exports: ["useAuthStore", "AuthState"]
    - path: "frontend/src/lib/api/client.ts"
      provides: "Axios 클라이언트 및 인터셉터"
      contains: "axios.interceptors.request.use"
    - path: "frontend/src/lib/api/auth.ts"
      provides: "인증 API 함수"
      exports: ["login", "register", "logout", "me"]
    - path: "frontend/src/routes/login.tsx"
      provides: "로그인 페이지"
      contains: "LoginForm 컴포넌트"
    - path: "frontend/src/routes/register.tsx"
      provides: "회원가입 페이지"
      contains: "RegisterForm 컴포넌트"
    - path: "frontend/src/components/auth/LoginForm.tsx"
      provides: "로그인 폼 컴포넌트"
      exports: ["LoginForm"]
    - path: "frontend/src/components/auth/RegisterForm.tsx"
      provides: "회원가입 폼 컴포넌트"
      exports: ["RegisterForm"]
  key_links:
    - from: "frontend/src/lib/api/client.ts"
      to: "http://localhost:3000/api/auth"
      via: "axios baseURL"
      pattern: "baseURL.*localhost:3000"
    - from: "frontend/src/lib/api/client.ts"
      to: "frontend/src/stores/auth.ts"
      via: "import useAuthStore"
      pattern: "import.*useAuthStore.*from.*stores/auth"
    - from: "frontend/src/routes/login.tsx"
      to: "frontend/src/lib/api/auth.ts"
      via: "import login mutation"
      pattern: "import.*login.*from.*lib/api/auth"
    - from: "axios interceptor"
      to: "http://localhost:3000/api/auth/refresh"
      via: "401 에러 처리"
      pattern: "axios.interceptors.response.use.*401"
    - from: "frontend/src/routes/index.tsx"
      to: "/login"
      via: "리다이렉트 (미인증 시)"
      pattern: "navigate.*login"
---

<objective>
Zustand + TanStack Query 기반 프론트엔드 인증 시스템과 로그인/회원가입 UI를 구축합니다. 액세스 토큰은 메모리(zustand)에 저장하고, 리프레시 토큰은 httpOnly 쿠키에서 자동으로 읽어 401 에러 시 자동 갱신합니다. TanStack Router 기반 라우트 가드로 미인증 사용자를 로그인 페이지로 리다이렉트합니다.

Purpose: 사용자 인증 UI와 상태 관리, 자동 토큰 갱신으로 원활한 사용자 경험 제공
Output: 작동하는 로그인/회원가입 페이지와 인증 상태 관리, 자동 토큰 갱신 시스템
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app.worktrees/gsd-glm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app.worktrees/gsd-glm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-auth-system/02-CONTEXT.md
@.planning/phases/02-auth-system/02-RESEARCH.md
@.planning/phases/02-auth-system/02-01-PLAN.md

# 기존 프론트엔드 코드
@frontend/src/main.tsx
@frontend/package.json
</context>

<interfaces>
<!-- 백엔드 API 인터페이스 (02-01에서 구현) -->

From backend/src/auth/auth.controller.ts:
```typescript
// POST /api/auth/register
// Request: { email: string, password: string }
// Response: { accessToken: string, user: { id: string, email: string } }
// Sets: refreshToken cookie (httpOnly)

// POST /api/auth/login
// Request: { email: string, password: string }
// Response: { accessToken: string, user: { id: string, email: string } }
// Sets: refreshToken cookie (httpOnly)

// POST /api/auth/refresh
// Request: (none - reads from cookie)
// Response: { accessToken: string, user: { id: string, email: string } }

// POST /api/auth/logout
// Headers: Authorization: Bearer {accessToken}
// Response: 204 No Content
// Clears: refreshToken cookie

// GET /api/auth/me
// Headers: Authorization: Bearer {accessToken}
// Response: { id: string, email: string, createdAt: Date, updatedAt: Date }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 프론트엔드 의존성 설치 및 API 클라이언트 설정</name>
  <files>frontend/package.json, package.json, frontend/src/lib/api/client.ts, frontend/src/lib/api/auth.ts</files>
  <read_first>
    frontend/package.json
  </read_first>
  <action>
    **의존성 설치**:
    1. 프론트엔드 의존성 설치:
       ```bash
       cd frontend
       pnpm install zustand@^5.0.0 @tanstack/react-query@^5.62.0 @tanstack/react-router@^1.90.0 axios@^1.7.0
       ```
    2. shadcn/ui 초기화 (필요한 경우):
       ```bash
       cd frontend
       pnpm install -D @types/node@^22.0.0
       pnpm install tailwindcss@^4.0.0 clsx@^2.1.0 tailwind-merge@^3.0.0
       ```

    **Axios 클라이언트 생성** (D-26, 자동 토큰 갱신):
    1. frontend/src/lib/api/client.ts 생성:
       ```typescript
       import axios from 'axios';
       import { useAuthStore } from '../../stores/auth';

       const api = axios.create({
         baseURL: import.meta.env.VITE_API_URL || 'http://localhost:3000',
         withCredentials: true, // 쿠키 전송을 위해 (리프레시 토큰)
       });

       // 요청 인터셉터: 액세스 토큰 자동 주입
       api.interceptors.request.use((config) => {
         const token = useAuthStore.getState().accessToken;
         if (token) {
           config.headers.Authorization = \`Bearer \${token}\`;
         }
         return config;
       });

       // 응답 인터셉터: 401 에러 시 자동 토큰 갱신 (D-04, SEC-02)
       let isRefreshing = false;
       let refreshSubscribers: ((token: string) => void)[] = [];

       const subscribeTokenRefresh = (callback: (token: string) => void) => {
         refreshSubscribers.push(callback);
       };

       const onTokenRefreshed = (token: string) => {
         refreshSubscribers.forEach((callback) => callback(token));
         refreshSubscribers = [];
       };

       api.interceptors.response.use(
         (response) => response,
         async (error) => {
           const originalRequest = error.config;

           // 401 에러且 재시도하지 않은 요청
           if (error.response?.status === 401 && !originalRequest._retry) {
             if (isRefreshing) {
               // 이미 갱신 중이면 대기열에 추가
               return new Promise((resolve) => {
                 subscribeTokenRefresh((token) => {
                   originalRequest.headers.Authorization = \`Bearer \${token}\`;
                   resolve(api(originalRequest));
                 });
               });
             }

             originalRequest._retry = true;
             isRefreshing = true;

             try {
               // 리프레시 토큰으로 액세스 토큰 갱신
               const { data } = await api.post('/api/auth/refresh');
               const newToken = data.accessToken;

               // Zustand 스토어 업데이트 (D-06)
               useAuthStore.getState().setAuth(data.user, newToken);

               // 대기열 처리
               onTokenRefreshed(newToken);

               // 원래 요청 재시도
               originalRequest.headers.Authorization = \`Bearer \${newToken}\`;
               return api(originalRequest);
             } catch (refreshError) {
               // 리프레시 실패: 로그아웃 처리
               useAuthStore.getState().clearAuth();
               window.location.href = '/login';
               return Promise.reject(refreshError);
             } finally {
               isRefreshing = false;
             }
           }

           return Promise.reject(error);
         }
       );

       export default api;
       ```

    **인증 API 함수 생성**:
    1. frontend/src/lib/api/auth.ts 생성:
       ```typescript
       import api from './client';
       import { LoginDto, RegisterDto, AuthResponseDto, UserDto } from './types';

       export const authApi = {
         // 회원가입
         register: async (dto: RegisterDto): Promise<AuthResponseDto> => {
           const { data } = await api.post<AuthResponseDto>('/api/auth/register', dto);
           return data;
         },

         // 로그인
         login: async (dto: LoginDto): Promise<AuthResponseDto> => {
           const { data } = await api.post<AuthResponseDto>('/api/auth/login', dto);
           return data;
         },

         // 로그아웃
         logout: async (): Promise<void> => {
           await api.post('/api/auth/logout');
         },

         // 현재 사용자 정보
         me: async (): Promise<UserDto> => {
           const { data } = await api.get<UserDto>('/api/auth/me');
           return data;
         },
       };
       ```

    **타입 정의**:
    1. frontend/src/lib/api/types.ts 생성:
       ```typescript
       export interface LoginDto {
         email: string;
         password: string;
       }

       export interface RegisterDto {
         email: string;
         password: string;
       }

       export interface AuthResponseDto {
         accessToken: string;
         user: UserDto;
       }

       export interface UserDto {
         id: string;
         email: string;
         createdAt: string;
         updatedAt: string;
       }
       ```

    **주의사항**:
    - withCredentials: true로 쿠키 전송 활성화 (리프레시 토큰)
    - 401 에러 시 자동 토큰 갱신 로직 구현 (SEC-02)
    - 동시 요청 시 토큰 갱신 중복 방지 (isRefreshing 플래그)
    - 갱신 실패 시 로그아웃하고 /login으로 리다이렉트
  </action>
  <verify>
    <automated>grep -q "zustand" frontend/package.json && grep -q "@tanstack/react-query" frontend/package.json && grep -q "@tanstack/react-router" frontend/package.json && ls frontend/src/lib/api/*.ts</automated>
  </verify>
  <done>
    - 프론트엔드 인증 의존성 설치 완료 (zustand, TanStack Query, TanStack Router, axios)
    - Axios 클라이언트: 요청/응답 인터셉터로 토큰 자동 주입 및 갱신
    - 인증 API 함수: register, login, logout, me
    - 타입 정의: LoginDto, RegisterDto, AuthResponseDto, UserDto
  </done>
</task>

<task type="auto">
  <name>Task 2: Zustand authStore 및 TanStack Query Provider 설정</name>
  <files>frontend/src/stores/auth.ts, frontend/src/main.tsx</files>
  <read_first>
    frontend/src/main.tsx
  </read_first>
  <action>
    **Zustand authStore 생성** (D-25, D-06):
    1. frontend/src/stores/auth.ts 생성:
       ```typescript
       import { create } from 'zustand';
       import { persist } from 'zustand/middleware';
       import { UserDto } from '../lib/api/types';

       interface AuthState {
         // 상태
         user: UserDto | null;
         accessToken: string | null;
         isAuthenticated: boolean;
         isLoading: boolean;

         // 액션
         setAuth: (user: UserDto, accessToken: string) => void;
         clearAuth: () => void;
         setLoading: (loading: boolean) => void;
       }

       export const useAuthStore = create<AuthState>()(
       persist(
         (set) => ({
           // 초기 상태
           user: null,
           accessToken: null,
           isAuthenticated: false,
           isLoading: true,

           // 액션
           setAuth: (user, accessToken) =>
             set({
               user,
               accessToken,
               isAuthenticated: true,
               isLoading: false,
             }),

           clearAuth: () =>
             set({
               user: null,
               accessToken: null,
               isAuthenticated: false,
               isLoading: false,
             }),

           setLoading: (isLoading) => set({ isLoading }),
         }),
         {
           name: 'auth-storage',
           // 액세스 토큰은 persist에서 제외 (보안: D-06)
           partialize: (state) => ({
             user: state.user,
             isAuthenticated: state.isAuthenticated,
             // accessToken은 저장하지 않음
           }),
         }
       )
       );
       ```

    **main.tsx 업데이트** (TanStack Query Provider):
    1. frontend/src/main.tsx 수정:
       ```typescript
       import { StrictMode } from 'react';
       import { createRoot } from 'react-dom/client';
       import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
       import { RouterProvider, createRouter } from '@tanstack/react-router';
       import { routeTree } from './routes';

       // TanStack Query 클라이언트 생성
       const queryClient = new QueryClient({
         defaultOptions: {
           queries: {
             retry: false,
             refetchOnWindowFocus: false,
           },
         },
       });

       // 라우터 생성
       const router = createRouter({ routeTree });

       createRoot(document.getElementById('root')!).render(
         <StrictMode>
           <QueryClientProvider client={queryClient}>
             <RouterProvider router={router} />
           </QueryClientProvider>
         </StrictMode>
       );
       ```

    **주의사항**:
    - 액세스 토큰은 persist에서 제외하여 localStorage에 저장하지 않음 (보안: D-06)
    - 사용자 정보와 인증 상태만 localStorage에 저장 (새로고침 후 상태 유지: D-02, AUTH-02)
    - 액세스 토큰은 메모리에만 저장되어 새로고침 시 사라짐 → /api/auth/refresh로 재발급
    - TanStack Query는 재시도 비활성화 (인증 에러 시 자동 처리)
  </action>
  <verify>
    <automated>grep -q "export const useAuthStore" frontend/src/stores/auth.ts && grep -q "QueryClientProvider" frontend/src/main.tsx</automated>
  </verify>
  <done>
    - Zustand authStore: user, accessToken, isAuthenticated, isLoading 상태
    - persist 미들웨어로 사용자 정보와 인증 상태만 localStorage에 저장
    - 액세스 토큰은 메모리에만 저장 (보안 강화)
    - TanStack Query Provider가 main.tsx에 설정됨
  </done>
</task>

<task type="auto">
  <name>Task 3: TanStack Router 라우트 및 인증 가드 구현</name>
  <files>frontend/src/routes/__root.tsx, frontend/src/routes/index.tsx, frontend/src/routes/login.tsx, frontend/src/routes/register.tsx</files>
  <read_first>
    frontend/src/main.tsx
  </read_first>
  <action>
    **루트 라우트 생성** (D-27):
    1. frontend/src/routes/__root.tsx 생성:
       ```typescript
       import { createRootRouteWithContext, Outlet } from '@tanstack/react-router';
       import { TanStackRouterDevtools } from '@tanstack/router-devtools';
       import { useAuthStore } from '../stores/auth';
       import { useEffect } from 'react';

       interface RouterContext {
         auth: ReturnType<typeof useAuthStore>;
       }

       export const Route = createRootRouteWithContext<RouterContext>()({
         component: RootComponent,
       });

       function RootComponent() {
         const { setLoading } = useAuthStore();

         useEffect(() => {
           // 앱 마운트 시 로딩 완료
           setLoading(false);
         }, [setLoading]);

         return (
           <>
             <Outlet />
             <TanStackRouterDevtools />
           </>
         );
       }
       ```

    **인증된 라우트 (메인 페이지)** (D-17):
    1. frontend/src/routes/index.tsx 생성:
       ```typescript
       import { createFileRoute, redirect } from '@tanstack/react-router';
       import { useAuthStore } from '../stores/auth';

       export const Route = createFileRoute('/')({
         beforeLoad: ({ location }) => {
           const isAuthenticated = useAuthStore.getState().isAuthenticated;

           if (!isAuthenticated) {
             // 미인증 시 로그인 페이지로 리다이렉트 (D-17)
             throw redirect({
               to: '/login',
               search: { redirect: location.href },
             });
           }
         },
         component: IndexComponent,
       });

       function IndexComponent() {
         const user = useAuthStore((state) => state.user);

         return (
           <div className="min-h-screen bg-gray-50">
             <div className="max-w-4xl mx-auto py-12 px-4">
               <h1 className="text-3xl font-bold text-gray-900 mb-4">
                 환영합니다, {user?.email}!
               </h1>
               <p className="text-gray-600">
                 인증된 사용자만 접근할 수 있는 페이지입니다.
               </p>
             </div>
           </div>
         );
       }
       ```

    **로그인 페이지** (D-16):
    1. frontend/src/routes/login.tsx 생성:
       ```typescript
       import { createFileRoute, useNavigate, useSearch } from '@tanstack/react-router';
       import { LoginForm } from '../components/auth/LoginForm';

       export const Route = createFileRoute('/login')({
         component: LoginComponent,
       });

       function LoginComponent() {
         const navigate = useNavigate();
         const search = useSearch({ from: '/login' });

         return (
           <div className="min-h-screen bg-gray-50 flex items-center justify-center px-4">
             <div className="max-w-md w-full">
               <LoginForm
                 onSuccess={() => {
                   // 로그인 성공 후 이전 페이지 또는 메인으로 리다이렉트 (D-18)
                   const redirect = (search as any).redirect;
                   navigate({ to: redirect || '/' });
                 }}
               />
             </div>
           </div>
         );
       }
       ```

    **회원가입 페이지** (D-16):
    1. frontend/src/routes/register.tsx 생성:
       ```typescript
       import { createFileRoute, useNavigate } from '@tanstack/react-router';
       import { RegisterForm } from '../components/auth/RegisterForm';

       export const Route = createFileRoute('/register')({
         component: RegisterComponent,
       });

       function RegisterComponent() {
         const navigate = useNavigate();

         return (
           <div className="min-h-screen bg-gray-50 flex items-center justify-center px-4">
             <div className="max-w-md w-full">
               <RegisterForm
                 onSuccess={() => {
                   // 회원가입 성공 후 메인으로 리다이렉트 (자동 로그인됨)
                   navigate({ to: '/' });
                 }}
               />
             </div>
           </div>
         );
       }
       ```

    **로그아웃 라우트** (D-20):
    1. frontend/src/routes/logout.tsx 생성:
       ```typescript
       import { createFileRoute, redirect } from '@tanstack/react-router';
       import { authApi } from '../lib/api/auth';
       import { useAuthStore } from '../stores/auth';

       export const Route = createFileRoute('/logout')({
         beforeLoad: async () => {
           // 로그아웃 API 호출
           await authApi.logout();

           // 상태 초기화
           useAuthStore.getState().clearAuth();

           // 로그인 페이지로 리다이렉트 (D-20)
           throw redirect({ to: '/login' });
         },
       });
       ```

    **routeTree.gen.ts 생성** (라우트 트리):
    1. frontend/src/routes/__root.tsx 하단에 추가:
       ```typescript
       import { createFileRoute } from '@tanstack/react-router';

       // 라우트 트리 (TanStack Router가 자동 생성)
       export const routeTree = Route.addChildren([
         createFileRoute('/')(Route),
         createFileRoute('/login')(Route),
         createFileRoute('/register')(Route),
         createFileRoute('/logout')(Route),
       ]);
       ```

    **주의사항**:
    - 인증 가드: beforeLoad에서 isAuthenticated 확인 (D-27)
    - 미인증 시 /login으로 리다이렉트 (D-17)
    - 로그인 성공 후 이전 페이지로 리다이렉트 (D-18)
    - 회원가입 성공 후 자동 로그인 처리 (D-19)
    - 로그아웃 시 모든 상태 초기화 후 /login으로 리다이렉트 (D-20)
  </action>
  <verify>
    <automated>ls frontend/src/routes/*.tsx && grep -q "beforeLoad" frontend/src/routes/index.tsx</automated>
  </verify>
  <done>
    - 루트 라우트: Outlet으로 자식 라우트 렌더링
    - 인덱스 라우트: 인증 가드로 미인증 시 /login으로 리다이렉트
    - 로그인 라우트: LoginForm 컴포넌트 렌더링
    - 회원가입 라우트: RegisterForm 컴포넌트 렌더링
    - 로그아웃 라우트: 로그아웃 API 호출 후 상태 초기화 및 리다이렉트
  </done>
</task>

<task type="auto">
  <name>Task 4: 로그인/회원가입 폼 컴포넌트 구현</name>
  <files>frontend/src/components/auth/LoginForm.tsx, frontend/src/components/auth/RegisterForm.tsx</files>
  <read_first>
    frontend/src/lib/api/auth.ts
    frontend/src/stores/auth.ts
  </read_first>
  <action>
    **로그인 폼 컴포넌트** (D-16, shadcn/ui 스타일):
    1. frontend/src/components/auth/LoginForm.tsx 생성:
       ```typescript
       import { useState } from 'react';
       import { useMutation } from '@tanstack/react-query';
       import { authApi } from '../../lib/api/auth';
       import { useAuthStore } from '../../stores/auth';
       import { LoginDto } from '../../lib/api/types';

       interface LoginFormProps {
         onSuccess: () => void;
       }

       export function LoginForm({ onSuccess }: LoginFormProps) {
         const navigate = useNavigate();
         const setAuth = useAuthStore((state) => state.setAuth);
         const [formData, setFormData] = useState<LoginDto>({
           email: '',
           password: '',
         });
         const [error, setError] = useState<string>('');

         const loginMutation = useMutation({
           mutationFn: authApi.login,
           onSuccess: (data) => {
             setAuth(data.user, data.accessToken);
             onSuccess();
           },
           onError: (err: any) => {
             setError(err.response?.data?.message || '로그인에 실패했습니다');
           },
         });

         const handleSubmit = (e: React.FormEvent) => {
           e.preventDefault();
           setError('');
           loginMutation.mutate(formData);
         };

         return (
           <div className="bg-white rounded-lg shadow-md p-8">
             <h2 className="text-2xl font-bold text-gray-900 mb-6">로그인</h2>

             <form onSubmit={handleSubmit} className="space-y-4">
               <div>
                 <label htmlFor="email" className="block text-sm font-medium text-gray-700 mb-1">
                   이메일
                 </label>
                 <input
                   id="email"
                   type="email"
                   value={formData.email}
                   onChange={(e) => setFormData({ ...formData, email: e.target.value })}
                   className="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
                   required
                 />
               </div>

               <div>
                 <label htmlFor="password" className="block text-sm font-medium text-gray-700 mb-1">
                   비밀번호
                 </label>
                 <input
                   id="password"
                   type="password"
                   value={formData.password}
                   onChange={(e) => setFormData({ ...formData, password: e.target.value })}
                   className="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
                   required
                 />
               </div>

               {error && (
                 <div className="bg-red-50 text-red-700 px-3 py-2 rounded-md text-sm">
                   {error}
                 </div>
               )}

               <button
                 type="submit"
                 disabled={loginMutation.isPending}
                 className="w-full bg-blue-600 text-white py-2 px-4 rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 disabled:opacity-50"
               >
                 {loginMutation.isPending ? '로그인 중...' : '로그인'}
               </button>
             </form>

             <div className="mt-4 text-center text-sm text-gray-600">
               계정이 없으신가요?{' '}
               <a href="/register" className="text-blue-600 hover:underline">
                 회원가입
               </a>
             </div>
           </div>
         );
       }
       ```

    **회원가입 폼 컴포넌트** (D-16, shadcn/ui 스타일):
    1. frontend/src/components/auth/RegisterForm.tsx 생성:
       ```typescript
       import { useState } from 'react';
       import { useMutation } from '@tanstack/react-query';
       import { authApi } from '../../lib/api/auth';
       import { useAuthStore } from '../../stores/auth';
       import { RegisterDto } from '../../lib/api/types';

       interface RegisterFormProps {
         onSuccess: () => void;
       }

       export function RegisterForm({ onSuccess }: RegisterFormProps) {
         const setAuth = useAuthStore((state) => state.setAuth);
         const [formData, setFormData] = useState<RegisterDto>({
           email: '',
           password: '',
         });
         const [confirmPassword, setConfirmPassword] = useState('');
         const [error, setError] = useState<string>('');

         const registerMutation = useMutation({
           mutationFn: authApi.register,
           onSuccess: (data) => {
             setAuth(data.user, data.accessToken);
             onSuccess();
           },
           onError: (err: any) => {
             setError(err.response?.data?.message || '회원가입에 실패했습니다');
           },
         });

         const handleSubmit = (e: React.FormEvent) => {
           e.preventDefault();
           setError('');

           if (formData.password !== confirmPassword) {
             setError('비밀번호가 일치하지 않습니다');
             return;
           }

           if (formData.password.length < 8) {
             setError('비밀번호는 8자 이상이어야 합니다');
             return;
           }

           registerMutation.mutate(formData);
         };

         return (
           <div className="bg-white rounded-lg shadow-md p-8">
             <h2 className="text-2xl font-bold text-gray-900 mb-6">회원가입</h2>

             <form onSubmit={handleSubmit} className="space-y-4">
               <div>
                 <label htmlFor="email" className="block text-sm font-medium text-gray-700 mb-1">
                   이메일
                 </label>
                 <input
                   id="email"
                   type="email"
                   value={formData.email}
                   onChange={(e) => setFormData({ ...formData, email: e.target.value })}
                   className="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
                   required
                 />
               </div>

               <div>
                 <label htmlFor="password" className="block text-sm font-medium text-gray-700 mb-1">
                   비밀번호 (8자 이상)
                 </label>
                 <input
                   id="password"
                   type="password"
                   value={formData.password}
                   onChange={(e) => setFormData({ ...formData, password: e.target.value })}
                   className="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
                   required
                 />
               </div>

               <div>
                 <label htmlFor="confirmPassword" className="block text-sm font-medium text-gray-700 mb-1">
                   비밀번호 확인
                 </label>
                 <input
                   id="confirmPassword"
                   type="password"
                   value={confirmPassword}
                   onChange={(e) => setConfirmPassword(e.target.value)}
                   className="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
                   required
                 />
               </div>

               {error && (
                 <div className="bg-red-50 text-red-700 px-3 py-2 rounded-md text-sm">
                   {error}
                 </div>
               )}

               <button
                 type="submit"
                 disabled={registerMutation.isPending}
                 className="w-full bg-blue-600 text-white py-2 px-4 rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 disabled:opacity-50"
               >
                 {registerMutation.isPending ? '가입 중...' : '회원가입'}
               </button>
             </form>

             <div className="mt-4 text-center text-sm text-gray-600">
               이미 계정이 있으신가요?{' '}
               <a href="/login" className="text-blue-600 hover:underline">
                 로그인
               </a>
             </div>
           </div>
         );
       }
       ```

    **로그아웃 버튼 컴포넌트** (D-20, AUTH-03):
    1. frontend/src/components/auth/LogoutButton.tsx 생성:
       ```typescript
       import { useNavigate } from '@tanstack/react-router';
       import { useAuthStore } from '../../stores/auth';

       export function LogoutButton() {
         const navigate = useNavigate();
         const clearAuth = useAuthStore((state) => state.clearAuth);

         const handleLogout = async () => {
           // /logout 라우트로 이동 (beforeLoad에서 로그아웃 처리)
           navigate({ to: '/logout' });
         };

         return (
           <button
             onClick={handleLogout}
             className="px-4 py-2 bg-gray-200 text-gray-700 rounded-md hover:bg-gray-300 focus:outline-none focus:ring-2 focus:ring-gray-500"
           >
             로그아웃
           </button>
         );
       }
       ```

    **주의사항**:
    - 폼 유효성 검증: 이메일 형식, 비밀번호 최소 8자 (D-13)
    - 비밀번호 확인 필드로 실수 방지
    - 에러 메시지는 인라인으로 표시 (사용자 경험)
    - 로딩 상태는 버튼 텍스트로 표시
    - TanStack Query mutation으로 API 호출 및 상태 관리
  </action>
  <verify>
    <automated>ls frontend/src/components/auth/*.tsx && grep -q "export function LoginForm" frontend/src/components/auth/LoginForm.tsx && grep -q "export function RegisterForm" frontend/src/components/auth/RegisterForm.tsx</automated>
  </verify>
  <done>
    - LoginForm: 이메일/비밀번호 입력, 유효성 검증, 로그인 mutation
    - RegisterForm: 이메일/비밀번호/확인 입력, 유효성 검증, 회원가입 mutation
    - LogoutButton: 로그아웃 라우트로 이동
    - 모든 폼: 에러 메시지 인라인 표시, 로딩 상태 표시
    - 회원가입 성공 후 자동 로그인 처리 (Zustand 스토어 업데이트)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>완전한 인증 시스템 (백엔드 API + 프론트엔드 UI)</what-built>
  <how-to-verify>
    1. **백엔드 실행**: `pnpm --filter backend dev`로 백엔드 서버 시작 (port 3000)
    2. **프론트엔드 실행**: `pnpm --filter frontend dev`로 프론트엔드 서버 시작 (port 5173)
    3. **회원가입 테스트**:
       - http://localhost:5173/register 방문
       - 이메일: test@example.com, 비밀번호: test1234로 회원가입
       - 성공 시 자동 로그인되어 메인 페이지(/)로 리다이렉트되는지 확인
    4. **로그인 상태 유지 테스트**:
       - 브라우저 새로고침 (F5)
       - 로그인 상태가 유지되고 메인 페이지에 머무는지 확인
    5. **로그아웃 테스트**:
       - 로그아웃 버튼 클릭
       - /login으로 리다이렉트되는지 확인
       - http://localhost:5173에 직접 접근 시 /login으로 리다이렉트되는지 확인
    6. **자동 토큰 갱신 테스트**:
       - 개발자 도구 → Application → Cookies에서 refreshToken 쿠키 확인 (httpOnly 체크)
       - 개발자 도구 → Console에서 localStorage 확인 (accessToken 없어야 함)
       - 로그인 후 15분 대기 후 API 요청 시 자동 갱신되는지 확인 (실제 테스트는 만료 시간 단축 후)
    7. **비밀번호 해싱 확인**:
       - `sqlite3 backend/dev.db "SELECT email, password FROM users;"`
       - password가 bcrypt 해시로 저장되어 있는지 확인 (평문 아님)
  </how-to-verify>
  <resume-signal>인증 테스트 완료 후 "approved" 또는 발견한 문제 설명</resume-signal>
</task>

</tasks>

<verification>
1. **프론트엔드 서버 시작**: `pnpm --filter frontend dev`로 Vite 개발 서버 실행
2. **라우팅 확인**: /, /login, /register, /logout 경로 접근 시 올바른 페이지 렌더링
3. **인증 흐름 테스트**:
   - 회원가입 → 자동 로그인 → 메인 페이지 리다이렉트
   - 로그아웃 → 로그인 페이지 리다이렉트
   - 미인증 상태에서 / 접근 → /login으로 리다이렉트
4. **상태 관리 확인**:
   - 로그인 후 Zustand 스토어에 user, accessToken 저장
   - 새로고침 후 localStorage에서 user 복원
   - 액세스 토큰은 localStorage에 저장되지 않음
5. **토큰 갱신 확인**:
   - 401 에러 발생 시 /api/auth/refresh 자동 호출
   - 갱신 실패 시 로그아웃 처리
</verification>

<success_criteria>
1. 사용자가 회원가입 폼에서 계정을 생성할 수 있다
2. 사용자가 로그인 폼에서 로그인할 수 있다
3. 로그인 성공 후 메인 페이지로 리다이렉트된다
4. 브라우저 새로고침 후 로그인 상태가 유지된다
5. 로그아웃 후 로그인 페이지로 리다이렉트된다
6. 미인증 사용자가 보호된 라우트 접근 시 로그인 페이지로 리다이렉트된다
7. 액세스 토큰이 만료되면 자동으로 갱신된다
8. 액세스 토큰이 메모리에만 저장되고 localStorage에 저장되지 않는다
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-system/02-02-SUMMARY.md`
</output>
