---
phase: 01-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tsconfig.json
  - tsconfig.app.json
  - index.html
  - src/index.css
  - src/main.tsx
  - src/App.tsx
autonomous: true
requirements:
  - API-01
  - API-03
  - CHAT-01
  - CHAT-02
  - CHAT-03
  - CHAT-05
  - MODEL-01
  - MODEL-02

must_haves:
  truths:
    - "Vite dev server runs on localhost:5173"
    - "Tailwind CSS v4 classes are applied correctly"
    - "npm run build completes without errors"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "ai-chat-app"
    - path: "vite.config.ts"
      provides: "Vite configuration with Tailwind plugin"
      contains: "@tailwindcss/vite"
    - path: "src/index.css"
      provides: "Tailwind v4 CSS entry point"
      contains: '@import "tailwindcss"'
    - path: "src/App.tsx"
      provides: "Root React component"
      contains: "export default"
  key_links:
    - from: "vite.config.ts"
      to: "@tailwindcss/vite"
      via: "plugins array"
      pattern: "plugins.*tailwindcss"
    - from: "src/index.css"
      to: "tailwindcss"
      via: "@import directive"
      pattern: '@import "tailwindcss"'
    - from: "src/main.tsx"
      to: "src/App.tsx"
      via: "import and render"
      pattern: "render.*App"
---

<objective>
프로젝트 스캐폴딩: Vite + React + TypeScript + Tailwind CSS v4 개발 환경 구축

Purpose: Phase 1의 모든后续 플랜이依赖하는 기초 환경 구축
Output: 실행 가능한 개발 서버, Tailwind CSS v4 설정 완료
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app.worktrees/simple/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-CONTEXT.md

# Tailwind v4 critical setup (from research)
- v4 uses `@import "tailwindcss"` NOT `@tailwind base/components/utilities`
- Must use `@tailwindcss/vite` plugin, NOT postcss
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vite 프로젝트 초기화</name>
  <files>package.json, vite.config.ts, tsconfig.json, tsconfig.app.json, index.html, src/main.tsx</files>
  <read_first></read_first>
  <action>
    # 1. Vite React TypeScript 프로젝트 생성
    # 현재 디렉토리에 --template react-ts 사용
    npm create vite@latest . -- --template react-ts

    # 2. vite.config.ts에 Tailwind CSS v4 plugin 추가
    # 파일이 이미 존재하면 Edit로 수정, 없으면 Write로 생성

    ```typescript
    // vite.config.ts
    import { defineConfig } from 'vite'
    import tailwindcss from '@tailwindcss/vite'

    export default defineConfig({
      plugins: [tailwindcss()],
    })
    ```

    # 3. package.json의 name을 "ai-chat-app"으로 설정 (이미 있으면 생략)
  </action>
  <acceptance_criteria>
    - package.json 존재하고 name이 "ai-chat-app"
    - vite.config.ts에 @tailwindcss/vite plugin이 import되고 plugins 배열에 포함
    - tsconfig.json과 tsconfig.app.json이 존재
    - index.html이 존재하고 id="root"인 div 포함
    - src/main.tsx가 index.html의 type="module" 스크립트로 참조
  </acceptance_criteria>
  <verify>
    <automated>ls package.json vite.config.ts tsconfig.json tsconfig.app.json index.html src/main.tsx</automated>
  </verify>
  <done>npm run dev 실행 시 Vite dev server가 5173端口で起動</done>
</task>

<task type="auto">
  <name>Task 2: Tailwind CSS v4 설정</name>
  <files>src/index.css</files>
  <read_first>src/index.css</read_first>
  <action>
    # src/index.css 파일의 내용을 다음과 같이 설정:

    ```css
    @import "tailwindcss";

    /* Phase 1 foundation styles */
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif;
    }
    ```

    # 중요: @tailwind directives (base, components, utilities) 사용禁止 - v4에서 동작하지 않음
  </action>
  <acceptance_criteria>
    - src/index.css 첫 줄이 `@import "tailwindcss";`
    - @tailwind base/components/utilities directives가 없음
  </acceptance_criteria>
  <verify>
    <automated>head -1 src/index.css | grep -q '@import "tailwindcss"' && echo "OK"</automated>
  </verify>
  <done>Tailwind CSS v4 설정 완료, dev server에서 Tailwind 클래스 적용 확인</done>
</task>

<task type="auto">
  <name>Task 3: 의존성 설치 및 검증</name>
  <files>package.json, node_modules/</files>
  <read_first>package.json</read_first>
  <action>
    # 1. 의존성 설치
    npm install

    # 2. Tailwind CSS v4 및 Vite 플러그인 설치
    npm install -D tailwindcss @tailwindcss/vite

    # 3. 기본 App.tsx 작성 (Phase 1 레이아웃 테스트용)
    ```tsx
    // src/App.tsx
    function App() {
      return (
        <div className="min-h-screen bg-white">
          <h1 className="text-2xl font-bold">AI Chat App</h1>
        </div>
      );
    }

    export default App;
    ```

    # 4. npm run dev 실행하여 dev server 동작 확인
  </action>
  <acceptance_criteria>
    - node_modules/.package-lock.json 존재 (의존성 설치 완료)
    - npm run dev 실행 시 "Local: http://localhost:5173" 출력
    - http://localhost:5173 접속 시 "AI Chat App" 텍스트 표시
  </acceptance_criteria>
  <verify>
    <automated>npm run build --silent 2>&1 | tail -5</automated>
  </verify>
  <done>개발 환경 구축 완료, http://localhost:5173 에서 앱 확인 가능</done>
</task>

</tasks>

<verification>
- npm run dev 실행 시 Vite dev server 정상 동작
- npm run build 실행 시 오류 없이 빌드 완료
- Tailwind CSS 클래스가 HTML 요소에 정상 적용 (background-white, text-2xl 등)
</verification>

<success_criteria>
Phase 1的基础开发环境 구축 완료 - Plan 01-02와 01-03의 선행 조건 충족
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
