---
phase: 01-foundation
plan: "03"
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - src/components/modals/ApiKeyModal.tsx
  - src/lib/api.ts
  - src/context/ChatContext.tsx
  - src/App.tsx
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
  - CHAT-04

must_haves:
  truths:
    - "API key input modal blocks UI until valid key is entered"
    - "Connect button validates API key against OpenRouter"
    - "Invalid API key shows error message in modal"
    - "Valid API key persists to localStorage and closes modal"
    - "First user message auto-sets conversation name"
    - "Conversation name truncates at 30 characters"
  artifacts:
    - path: "src/components/modals/ApiKeyModal.tsx"
      provides: "Modal overlay for API key input with validation"
      contains: "fixed inset-0"
    - path: "src/lib/api.ts"
      provides: "OpenRouter API validation function"
      exports: "validateApiKey"
    - path: "src/context/ChatContext.tsx"
      provides: "Chat state with API key and conversation management"
      contains: "ADD_MESSAGE"
    - path: "src/App.tsx"
      provides: "App with conditional ApiKeyModal rendering"
      contains: "showApiKeyModal"
  key_links:
    - from: "ApiKeyModal.tsx"
      to: "validateApiKey"
      via: "form submit handler"
      pattern: "validateApiKey"
    - from: "ApiKeyModal.tsx"
      to: "ChatContext"
      via: "setApiKey callback"
      pattern: "setApiKey"
    - from: "App.tsx"
      to: "ApiKeyModal"
      via: "conditional render based on state.apiKey"
      pattern: "showApiKeyModal"
    - from: "ChatContext.tsx"
      to: "ADD_MESSAGE"
      via: "auto-name on first user message"
      pattern: "isFirstMessage.*shouldUpdateName"
---

<objective>
API 키 입력/검증 모달, OpenRouter API 연동, 대화 CRUD 최종 연결

Purpose: 사용자가 API 키를 입력하고 검증할 수 있는 완전한 기능 구축
Output: ApiKeyModal (D-03: Modal overlay, D-04: 에러 메시지), API 검증 로직, 대화방 이름 자동 설정
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app.worktrees/simple/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-RESEARCH.md (sections: OpenRouter API Key Validation, Common Pitfalls)
@.planning/phases/01-foundation/01-CONTEXT.md (D-03, D-04)

# Locked Decisions to implement exactly:
- D-03: API 키 입력 UI — Modal overlay 방식 (중앙 팝업, 입력 완료 전 메인 UI 비활성화)
- D-04: API 키 유효하지 않을 때 — Modal 내 에러 메시지 표시
- D-06: 새 대화 버튼 — 사이드바 상단 고정
- D-07: 대화 삭제 — 대화 우측에 삭제 아이콘 (hover 시 표시)
</context>

<interfaces>
<!-- Key types and functions needed -->

From src/context/ChatContext.tsx:
```typescript
// ChatContextValue
interface ChatContextValue {
  state: ChatState;
  dispatch: React.Dispatch<ChatAction>;
  createChat: () => void;
  selectChat: (id: string) => void;
  deleteChat: (id: string) => void;
  addMessage: (conversationId: string, message: Message) => void;
  updateChatName: (conversationId: string, name: string) => void;
  setModel: (model: string) => void;
  setApiKey: (key: string) => void;
}
```

From src/types/chat.ts:
```typescript
interface ChatState {
  apiKey: string | null;
  conversations: Conversation[];
  selectedConversationId: string | null;
  selectedModel: string;
  isValidating: boolean;
  error: string | null;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: OpenRouter API 검증 함수</name>
  <files>src/lib/api.ts</files>
  <read_first></read_first>
  <action>
    # src/lib/api.ts 생성
    # mkdir -p src/lib

    ```typescript
    const OPENROUTER_API_BASE = 'https://openrouter.ai/api/v1';

    export interface KeyValidationResult {
      valid: boolean;
      error?: string;
    }

    export async function validateApiKey(apiKey: string): Promise<KeyValidationResult> {
      try {
        const response = await fetch(`${OPENROUTER_API_BASE}/auth/key`, {
          method: 'GET',
          headers: {
            'Authorization': `Bearer ${apiKey}`,
            'Content-Type': 'application/json',
          },
        });

        if (response.ok) {
          return { valid: true };
        }

        // Try to parse error response
        let errorMessage = 'Invalid API key';
        try {
          const errorData = await response.json();
          errorMessage = errorData.error?.message || errorData.error || errorMessage;
        } catch {
          // Response wasn't JSON, use default message
        }

        return { valid: false, error: errorMessage };
      } catch (err) {
        return { valid: false, error: 'Network error - please check your connection and try again' };
      }
    }
    ```

    # Note: OpenRouter API endpoint는 training data 기반 - 구현 시 실제 테스트 필요
    # 실제 API 응답 구조와 다를 수 있으므로, 실패 시 일반적인 에러 메시지 반환
  </action>
  <acceptance_criteria>
    - src/lib/api.ts 파일이 존재
    - validateApiKey 함수가 export됨
    - 성공 시 { valid: true } 반환
    - 네트워크 오류 시 { valid: false, error: 'Network error...' } 반환
    - API 오류 시 { valid: false, error: 'Invalid API key' } 반환
  </acceptance_criteria>
  <verify>
    <automated>grep -E "^export (async )?function validateApiKey" src/lib/api.ts</automated>
  </verify>
  <done>OpenRouter API 검증 함수 완료</done>
</task>

<task type="auto">
  <name>Task 2: ApiKeyModal 컴포넌트</name>
  <files>src/components/modals/ApiKeyModal.tsx</files>
  <read_first>src/lib/api.ts, src/context/ChatContext.tsx</read_first>
  <action>
    # src/components/modals/ApiKeyModal.tsx 생성
    # mkdir -p src/components/modals

    ```tsx
    // src/components/modals/ApiKeyModal.tsx
    import { useState, FormEvent } from 'react';
    import { useChat } from '../../context/ChatContext';
    import { validateApiKey } from '../../lib/api';

    export function ApiKeyModal() {
      const { state, dispatch, setApiKey } = useChat();
      const { isValidating, error } = state;
      const [inputValue, setInputValue] = useState('');

      const handleSubmit = async (e: FormEvent) => {
        e.preventDefault();
        const trimmedKey = inputValue.trim();

        if (!trimmedKey) {
          dispatch({ type: 'SET_API_KEY_ERROR', payload: 'Please enter an API key' });
          return;
        }

        dispatch({ type: 'SET_API_KEY_VALIDATING', payload: true });
        dispatch({ type: 'SET_API_KEY_ERROR', payload: '' });

        const result = await validateApiKey(trimmedKey);

        if (result.valid) {
          setApiKey(trimmedKey);
        } else {
          dispatch({ type: 'SET_API_KEY_ERROR', payload: result.error || 'Invalid API key' });
        }
      };

      return (
        // D-03: Modal overlay - 메인 UI 비활성화 (backdrop 클릭으로 닫기禁止)
        <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/50">
          <div className="bg-white rounded-lg shadow-xl p-6 w-full max-w-md mx-4">
            <h2 className="text-xl font-semibold text-gray-900 mb-2">Enter Your API Key</h2>
            <p className="text-sm text-gray-600 mb-4">
              To use this chat app, you need an OpenRouter API key. Get one for free at{' '}
              <a
                href="https://openrouter.ai/keys"
                target="_blank"
                rel="noopener noreferrer"
                className="text-blue-500 hover:underline"
              >
                openrouter.ai/keys
              </a>
            </p>

            <form onSubmit={handleSubmit}>
              <div className="mb-4">
                <input
                  type="password"
                  value={inputValue}
                  onChange={e => setInputValue(e.target.value)}
                  placeholder="sk-or-..."
                  className="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                  autoFocus
                  disabled={isValidating}
                />
                {/* D-04: API 키 유효하지 않을 때 Modal 내 에러 메시지 */}
                {error && (
                  <p className="mt-2 text-sm text-red-500">{error}</p>
                )}
              </div>

              <button
                type="submit"
                disabled={isValidating}
                className={`w-full py-2 px-4 rounded-lg font-medium transition-colors ${
                  isValidating
                    ? 'bg-gray-300 text-gray-500 cursor-not-allowed'
                    : 'bg-blue-500 text-white hover:bg-blue-600'
                }`}
              >
                {isValidating ? 'Validating...' : 'Connect'}
              </button>
            </form>
          </div>
        </div>
      );
    }
    ```
  </action>
  <acceptance_criteria>
    - src/components/modals/ApiKeyModal.tsx 파일이 존재
    - Modal이 fixed inset-0 z-50 bg-black/50 overlay 표시
    - input type="password"로 API 키 마스킹
    - Connect 버튼 클릭 시 validateApiKey 호출
    - isValidating 상태에서 버튼 비활성화 및 "Validating..." 텍스트
    - error가 있으면 빨간색 에러 메시지 표시
    - backdrop 클릭으로 Modal이 닫히지 않음 (form 제출로만 닫힘)
  </acceptance_criteria>
  <verify>
    <automated>grep -E "(fixed inset-0|type=\"password\"|isValidating)" src/components/modals/ApiKeyModal.tsx</automated>
  </verify>
  <done>API 키 입력 모달 완료</done>
</task>

<task type="auto">
  <name>Task 3: App.tsx에 Modal 연동 및 대화 이름 자동 설정</name>
  <files>src/App.tsx, src/context/ChatContext.tsx</files>
  <read_first>src/components/modals/ApiKeyModal.tsx, src/context/ChatContext.tsx</read_first>
  <action>
    # Step 1: App.tsx 수정 - ApiKeyModal 조건부 렌더링

    ```tsx
    // src/App.tsx
    import { ChatProvider, useChat } from './context/ChatContext';
    import { Sidebar } from './components/layout/Sidebar';
    import { ChatArea } from './components/layout/ChatArea';
    import { ApiKeyModal } from './components/modals/ApiKeyModal';

    function AppContent() {
      const { state } = useChat();

      // D-03: API 키가 없으면 Modal overlay 표시
      const showApiKeyModal = !state.apiKey;

      return (
        <div className="flex h-screen bg-white text-gray-900">
          <Sidebar />
          <ChatArea />
          {showApiKeyModal && <ApiKeyModal />}
        </div>
      );
    }

    function App() {
      return (
        <ChatProvider>
          <AppContent />
        </ChatProvider>
      );
    }

    export default App;
    ```

    # Step 2: ChatContext에 updateChatName 로직 확인
    # 이미 01-02에서 구현됨:
    # - ADD_MESSAGE action 추가 시 conversationId로 해당 대화찾기
    # - 첫 번째 user 메시지일 때 (messages.length === 0) 자동으로 conversation name update
    # - CHAT-04: 대화방 이름은 첫 번째 사용자 메시지 내용으로 자동 설정

    # Reducer의 ADD_MESSAGE case를 수정하여 첫 번째 user 메시지 시 자동으로 이름 설정:

    ```typescript
    // ADD_MESSAGE case 수정 - src/context/ChatContext.tsx
    case 'ADD_MESSAGE': {
      const { conversationId, message } = action.payload;
      return {
        ...state,
        conversations: state.conversations.map(c => {
          if (c.id !== conversationId) return c;
          const isFirstMessage = c.messages.length === 0;
          const shouldUpdateName = isFirstMessage && message.role === 'user';
          return {
            ...c,
            messages: [...c.messages, message],
            updatedAt: Date.now(),
            name: shouldUpdateName
              ? message.content.slice(0, 30) + (message.content.length > 30 ? '...' : '')
              : c.name,
          };
        }),
      };
    }
    ```
  </action>
  <acceptance_criteria>
    - state.apiKey가 null이면 ApiKeyModal이 화면에 표시
    - state.apiKey가 존재하면 ApiKeyModal이 표시되지 않음
    - ADD_MESSAGE 시 첫 번째 user 메시지면 대화 이름이 메시지 내용으로 자동 설정
    - 대화 이름이 30자 이상이면 "..."으로 잘림
  </acceptance_criteria>
  <verify>
    <automated>grep -E "(showApiKeyModal|state.apiKey)" src/App.tsx && grep -E "(isFirstMessage|shouldUpdateName)" src/context/ChatContext.tsx</automated>
  </verify>
  <done>API 키 모달 연동 및 대화 이름 자동 설정 완료 - Phase 1 모든 요구사항 구현</done>
</task>

</tasks>

<verification>
- API 키 입력 없이 앱 실행 시 Modal overlay 표시
- Modal에서 API 키 입력 후 Connect 클릭 시 검증 진행
- 유효한 API 키 입력 시 Modal이 닫히고 메인 UI 표시
- 유효하지 않은 API 키 입력 시 에러 메시지 표시
- 새 대화 생성 후 메시지 입력 시 대화 이름이 자동으로 설정
</verification>

<success_criteria>
Phase 1的所有要求事项 (API-01~04, CHAT-01~05, MODEL-01~02) 实现完成
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
