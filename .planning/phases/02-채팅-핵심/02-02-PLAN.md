---
phase: 02-채팅-핵심
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/chat/MessageItem.tsx
  - src/components/chat/MessageList.tsx
  - src/components/chat/StreamingIndicator.tsx
  - src/components/chat/MessageInput.tsx
  - src/components/chat/ModelSelectorPopover.tsx
  - src/components/chat/ChatHeader.tsx
  - src/components/chat/ChatPage.tsx
  - src/routes/chat/$conversationId.tsx
  - src/routes/index.tsx
autonomous: true
requirements:
  - CHAT-03
  - CHAT-06
  - CHAT-07
  - CHAT-08
  - MODL-04

must_haves:
  truths:
    - "어시스턴트 응답이 마크다운으로 렌더링된다 (코드블록 구문 강조 포함)"
    - "스트리밍 중 오토스크롤이 동작한다 (사용자 수동 스크롤 시 자동 스크롤 중지)"
    - "응답 대기 중 로딩 인디케이터가 표시된다"
    - "Enter로 전송, Shift+Enter로 줄바꿈이 동작한다"
    - "API 오류 발생 시 재시도 버튼이 표시된다"
    - "채팅 페이지에서 메시지를 전송하고 실시간 응답을 볼 수 있다"
  artifacts:
    - path: "src/components/chat/MessageItem.tsx"
      provides: "개별 메시지 렌더링 (마크다운 + 코드 하이라이팅)"
      contains: "ReactMarkdown"
    - path: "src/components/chat/MessageList.tsx"
      provides: "메시지 목록 (오토스크롤)"
      contains: "useAutoScroll"
    - path: "src/components/chat/StreamingIndicator.tsx"
      provides: "로딩/타이핑 인디케이터"
    - path: "src/components/chat/MessageInput.tsx"
      provides: "입력 영역 (Enter/Shift+Enter)"
      contains: 'key === "Enter" && !shiftKey'
    - path: "src/components/chat/ModelSelectorPopover.tsx"
      provides: "대화별 모델 선택 팝오버"
    - path: "src/components/chat/ChatHeader.tsx"
      provides: "채팅 헤더 (제목 + 모델 선택)"
    - path: "src/components/chat/ChatPage.tsx"
      provides: "채팅 페이지 메인 컴포넌트"
  key_links:
    - from: "src/components/chat/ChatPage.tsx"
      to: "src/hooks/use-chat-stream.ts"
      via: "useChatStream 훅 사용"
      pattern: "useChatStream"
    - from: "src/components/chat/ChatPage.tsx"
      to: "src/hooks/use-messages.ts"
      via: "useMessages, useAddMessage 훅 사용"
      pattern: "useMessages|useAddMessage"
    - from: "src/components/chat/MessageItem.tsx"
      to: "react-markdown"
      via: "ReactMarkdown 컴포넌트"
      pattern: "ReactMarkdown"
    - from: "src/routes/chat/$conversationId.tsx"
      to: "src/components/chat/ChatPage.tsx"
      via: "ChatPage 컴포넌트 렌더링"
      pattern: "ChatPage"
    - from: "src/components/chat/MessageInput.tsx"
      to: "ChatPage sendMessage"
      via: "onSend 콜백 prop"
      pattern: "onSend"
---

<objective>
채팅 UI 컴포넌트를 구현하고 라우트에 통합한다.

Purpose: Plan 01에서 구축한 API 서비스, 스트리밍 훅, 메시지 훅을 소비하는 전체 채팅 UI를 완성한다. 사용자가 메시지를 입력하고 실시간 스트리밍 응답을 마크다운으로 확인하며, 모델을 선택하고, 오류 시 재시도할 수 있는 완전한 채팅 경험을 제공한다.

Output: 7개 채팅 UI 컴포넌트, 2개 라우트 업데이트
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app.worktrees/frontend-glm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app.worktrees/frontend-glm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-채팅-핵심/02-RESEARCH.md
@.planning/phases/02-채팅-핵심/02-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 생성될 인터페이스. 이 플랜의 UI 컴포넌트가 소비한다. -->

From src/services/openrouter-api.ts (Plan 01):
```typescript
interface StreamCallbacks {
  onToken: (token: string) => void;
  onComplete: (fullContent: string) => void;
  onError: (error: Error) => void;
}

export async function streamChatCompletion(
  apiKey: string,
  modelId: string,
  messages: Pick<Message, "role" | "content">[],
  systemPrompt: string | null,
  signal: AbortSignal,
  callbacks: StreamCallbacks,
): Promise<void>;

interface ModelInfo {
  id: string;
  name: string;
  contextLength?: number;
  description?: string;
}

export async function fetchFreeModels(): Promise<ModelInfo[]>;
```

From src/hooks/use-chat-stream.ts (Plan 01):
```typescript
interface UseChatStreamReturn {
  streamingContent: string;
  isStreaming: boolean;
  error: Error & { status?: number } | null;
  sendMessage: (messages: Pick<Message, "role" | "content">[]) => Promise<string>;
  stopStreaming: () => void;
}

export function useChatStream(
  modelId: string,
  systemPrompt: string | null,
): UseChatStreamReturn;
```

From src/hooks/use-messages.ts (Plan 01):
```typescript
export function useMessages(conversationId: string): UseQueryResult<Message[]>;
export function useAddMessage(): UseMutationResult<void, Error, Omit<Message, "createdAt">>;
```

From src/hooks/use-free-models.ts (Plan 01):
```typescript
interface ModelInfo { id: string; name: string; contextLength?: number; description?: string; }
export function useFreeModels(): UseQueryResult<ModelInfo[]>;
```

From src/lib/error-handler.ts (Plan 01):
```typescript
export function handleApiError(error: unknown): void;
```

From src/hooks/use-settings.ts (Phase 1):
```typescript
export const SETTINGS_KEYS = { API_KEY: "apiKey", DEFAULT_MODEL_ID: "defaultModelId", SYSTEM_PROMPT: "systemPrompt" };
export function useSetting(key: string): UseQueryResult<string | null>;
```

From src/db/index.ts (Phase 1):
```typescript
export interface Message {
  id: string;
  conversationId: string;
  role: "user" | "assistant" | "system";
  content: string;
  createdAt: Date;
}
```

From src/routes/__root.tsx (Plan 01 updates):
```typescript
export const Route = createRootRoute({
  component: () => (
    <SidebarLayout>
      <Outlet />
      <Toaster />
    </SidebarLayout>
  ),
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 메시지 렌더링 컴포넌트 (MessageItem, MessageList, StreamingIndicator)</name>
  <files>
    src/components/chat/MessageItem.tsx,
    src/components/chat/MessageList.tsx,
    src/components/chat/StreamingIndicator.tsx
  </files>
  <read_first>
    src/db/index.ts,
    src/lib/utils.ts,
    src/components/ui/button.tsx,
    src/components/ui/scroll-area.tsx
  </read_first>
  <behavior>
    - Test 1: MessageItem이 role "user"인 메시지를 우측 정렬로 렌더링한다
    - Test 2: MessageItem이 role "assistant"인 마크다운 텍스트를 ReactMarkdown으로 렌더링한다
    - Test 3: MessageItem이 코드블록을 포함한 마크다운에서 pre > code 요소를 렌더링한다
    - Test 4: StreamingIndicator가 렌더링 시 "thinking" 또는 로딩 관련 텍스트/애니메이션을 포함한다
    - Test 5: MessageList의 useAutoScroll 훅이 isStreaming=true일 때 shouldAutoScroll 플래그를 유지한다
    - Test 6: MessageList가 사용자 스크롤 시 shouldAutoScroll을 false로 설정한다
  </behavior>
  <action>
    **1. src/components/chat/MessageItem.tsx 생성:**
    Props: `{ message: Message, isStreaming?: boolean }`
    - message.role이 "user"면:
      - 우측 정렬 (`justify-end`)
      - `bg-primary text-primary-foreground` 말풍선 스타일
      - `max-w-[80%]` 너비 제한
      - 내용을 `<p>` 태그로 렌더링 (일반 텍스트)
    - message.role이 "assistant"면:
      - 좌측 정렬 (`justify-start`)
      - `bg-muted text-foreground` 말풍선 스타일
      - `max-w-[80%]` 너비 제한
      - `ReactMarkdown` 컴포넌트로 렌더링:
        - `remarkPlugins={[remarkGfm]}`
        - `rehypePlugins={[rehypeHighlight]}`
        - 코드블록 CSS: `import "highlight.js/styles/github-dark.css"` (다크 테마 기본)
      - 마크다운 내부 스타일: `prose prose-invert max-w-none` 클래스 사용 (또는 직접 Tailwind 스타일링)
    - `crypto.randomUUID()`로 고유 key 사용 (message.id)

    **2. src/components/chat/MessageList.tsx 생성:**
    Props: `{ messages: Message[], streamingContent: string, isStreaming: boolean }`
    - `useAutoScroll(isStreaming)` 커스텀 훅을 컴포넌트 내부에 정의:
      - `containerRef = useRef<HTMLDivElement>(null)`
      - `shouldAutoScrollRef = useRef(true)`
      - `handleScroll`: scrollHeight - scrollTop - clientHeight < 100 이면 true, 아니면 false
      - `useEffect`: isStreaming && shouldAutoScrollRef.current일 때 `containerRef.current.scrollTo({ top: scrollHeight, behavior: "smooth" })`
    - 메시지 목록 렌더링:
      - `<div ref={containerRef} onScroll={handleScroll}>` 로 스크롤 컨테이너
      - `className="flex-1 overflow-y-auto p-4"` 스타일
      - messages.map(m => `<MessageItem key={m.id} message={m} />`)
      - isStreaming && streamingContent이 있으면: `<MessageItem message={{ role: "assistant", content: streamingContent, ... }} isStreaming />`
      - isStreaming && !streamingContent이면: `<StreamingIndicator />`
    - 빈 메시지 목록: "대화를 시작해보세요." 안내 텍스트

    **3. src/components/chat/StreamingIndicator.tsx 생성:**
    - `Loader2` 아이콘 (lucide-react) 사용, `animate-spin` 클래스
    - "생각 중..." 한국어 텍스트
    - `flex items-center gap-2 text-muted-foreground` 스타일
    - 좌측 정렬 (assistant 메시지 위치와 동일)
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/ai-chat-app.worktrees/frontend-glm && pnpm test --run && pnpm build</automated>
  </verify>
  <done>
    - pnpm test 통과 (user/assistant 렌더링, 마크다운, 코드블록, 로딩 인디케이터, 오토스크롤)
    - pnpm build 성공
    - MessageItem이 ReactMarkdown + remarkGfm + rehypeHighlight로 마크다운 렌더링
    - MessageItem이 user/assistant 역할에 따라 다른 스타일 적용
    - MessageList에 useAutoScroll 훅 포함 (100px 임계값, smooth 스크롤)
    - StreamingIndicator가 Loader2 스피너 + "생각 중..." 텍스트 표시
  </done>
</task>

<task type="auto">
  <name>Task 2: 입력/모델 선택/채팅 페이지 + 라우트 통합</name>
  <files>
    src/components/chat/MessageInput.tsx,
    src/components/chat/ModelSelectorPopover.tsx,
    src/components/chat/ChatHeader.tsx,
    src/components/chat/ChatPage.tsx,
    src/routes/chat/$conversationId.tsx,
    src/routes/index.tsx
  </files>
  <read_first>
    src/hooks/use-chat-stream.ts,
    src/hooks/use-messages.ts,
    src/hooks/use-free-models.ts,
    src/hooks/use-settings.ts,
    src/hooks/use-conversations.ts,
    src/lib/error-handler.ts,
    src/services/openrouter-api.ts,
    src/components/chat/MessageItem.tsx,
    src/components/chat/MessageList.tsx,
    src/components/chat/StreamingIndicator.tsx,
    src/routes/chat/$conversationId.tsx,
    src/routes/index.tsx,
    src/components/layout/SidebarLayout.tsx
  </read_first>
  <acceptance_criteria>
    - src/components/chat/MessageInput.tsx contains 'key === "Enter" && !shiftKey'
    - src/components/chat/MessageInput.tsx contains 'e.preventDefault()' in onKeyDown handler
    - src/components/chat/ModelSelectorPopover.tsx contains 'useFreeModels'
    - src/components/chat/ChatHeader.tsx contains 'ModelSelectorPopover'
    - src/components/chat/ChatPage.tsx contains 'useChatStream'
    - src/components/chat/ChatPage.tsx contains 'useMessages'
    - src/components/chat/ChatPage.tsx contains 'useAddMessage'
    - src/components/chat/ChatPage.tsx contains 'handleApiError' or 'MODL-04 retry'
    - src/routes/chat/$conversationId.tsx contains 'ChatPage'
    - src/routes/index.tsx contains API key check or empty state guidance
    - pnpm build exits with code 0
    - pnpm test exits with code 0
  </acceptance_criteria>
  <action>
    **1. src/components/chat/MessageInput.tsx 생성:**
    Props: `{ onSend: (content: string) => void, disabled?: boolean, isStreaming?: boolean, onStop: () => void }`
    - `<textarea>` 기반 입력:
      - `value` 상태로 관리 (useState<string>)
      - `placeholder="메시지를 입력하세요..."` (한국어)
      - `rows={1}` 기본, 내용에 따라 자동 높이 조절 (scrollHeight 기반, max 200px)
      - `onKeyDown`: `key === "Enter" && !shiftKey`이면 `e.preventDefault()` 후 handleSend()
      - `disabled={disabled}` (스트리밍 중 비활성화)
    - 전송 버튼:
      - `isStreaming`이면 Square(Stop) 아이콘 버튼 (빨간색), `onClick={onStop}`
      - `!isStreaming`이면 Send 아이콘 버튼, `disabled={disabled || !value.trim()}`
      - 둘 다 `cursor-pointer` 명시
    - 하단 고정 레이아웃: `border-t bg-background p-4`
    - `flex gap-2 items-end`로 textarea + 버튼 배치

    **2. src/components/chat/ModelSelectorPopover.tsx 생성:**
    Props: `{ modelId: string, onModelChange: (modelId: string) => void }`
    - `useFreeModels()`로 모델 목록 조회
    - `useSetting(SETTINGS_KEYS.DEFAULT_MODEL_ID)`로 기본 모델 ID 조회
    - shadcn/ui Popover 컴포넌트 사용 (또는 Select):
      - 트리거: 현재 모델명 표시 + ChevronDown 아이콘
      - 목록: 모델명 + contextLength 표시 (예: "GPT-4o Mini (128K)")
      - 선택 시 `onModelChange(model.id)` 콜백 호출
    - 로딩 상태: "모델 불러오는 중..." 스켈레톤
    - 에러 상태: "모델을 불러올 수 없습니다" 안내
    - 목록이 비어있으면: "사용 가능한 무료 모델이 없습니다"

    **3. src/components/chat/ChatHeader.tsx 생성:**
    Props: `{ conversationTitle: string, modelId: string, onModelChange: (modelId: string) => void }`
    - 상단 고정 헤더: `border-b bg-background px-4 py-3 flex items-center justify-between`
    - 좌측: 대화 제목 (`text-lg font-semibold truncate`)
    - 우측: `<ModelSelectorPopover modelId={modelId} onModelChange={onModelChange} />`

    **4. src/components/chat/ChatPage.tsx 생성:**
    Props: `{ conversationId: string }`
    - 훅 호출:
      - `useSetting(SETTINGS_KEYS.API_KEY)` → apiKey
      - `useSetting(SETTINGS_KEYS.DEFAULT_MODEL_ID)` → defaultModelId
      - `useSetting(SETTINGS_KEYS.SYSTEM_PROMPT)` → globalSystemPrompt
      - `useMessages(conversationId)` → { data: messages }
      - `useAddMessage()` → addMessage
      - `useQuery(["conversations", conversationId])` 또는 db.conversations.get(conversationId) → conversation
    - 로컬 상태:
      - `const [modelId, setModelId] = useState(conversation?.modelId ?? defaultModelId ?? "default")`
    - `useChatStream(modelId, conversation?.systemPrompt ?? globalSystemPrompt)` → { streamingContent, isStreaming, error, sendMessage, stopStreaming }
    - `handleSend` 함수:
      - content가 빈 문자열이면 무시
      - 사용자 메시지 IndexedDB에 저장: `addMessage.mutate({ id: crypto.randomUUID(), conversationId, role: "user", content })`
      - 기존 메시지 + 새 사용자 메시지로 sendMessage 호출
      - sendMessage 완료 후 어시스턴트 메시지 저장: `addMessage.mutate({ id: crypto.randomUUID(), conversationId, role: "assistant", content: fullContent })`
      - 에러 발생 시: `handleApiError(error)` (error-handler.ts)
      - stopStreaming 후에도 현재까지의 streamingContent를 저장
    - `handleStop` 함수: `stopStreaming()` + 현재 streamingContent가 있으면 저장
    - 렌더링 구조:
      ```
      <div className="flex flex-col h-full">
        <ChatHeader conversationTitle={conversation?.title ?? "새 대화"} modelId={modelId} onModelChange={setModelId} />
        <MessageList messages={messages} streamingContent={streamingContent} isStreaming={isStreaming} />
        {error && (
          <div className="px-4 py-2 bg-destructive/10 flex items-center gap-2">
            <span className="text-sm text-destructive">{error.message}</span>
            <Button size="sm" variant="outline" onClick={retryLastMessage}>
              <RefreshCw className="h-3 w-3 mr-1" /> 재시도
            </Button>
          </div>
        )}
        <MessageInput onSend={handleSend} disabled={!apiKey} isStreaming={isStreaming} onStop={handleStop} />
      </div>
      ```
    - API 키 없을 때: `<EmptyApiKeyListener />` 표시 (Phase 1의 설정 컴포넌트 재사용)
    - 재시도 버튼 (MODL-04): error가 있을 때 표시, 클릭 시 마지막 사용자 메시지로 재전송

    **5. src/routes/chat/$conversationId.tsx 수정:**
    - `import { ChatPage } from "@/components/chat/ChatPage"`
    - `useParams`에서 conversationId 추출: `const { conversationId } = Route.useParams()`
    - `<ChatPage conversationId={conversationId} />` 렌더링

    **6. src/routes/index.tsx 수정:**
    - API 키가 설정되어 있으면: "새 대화를 시작하려면 사이드바에서 새 대화 버튼을 클릭하세요." 안내
    - API 키가 없으면: `<EmptyApiKeyListener />` 표시 + API 키 설정 안내
    - `useSetting(SETTINGS_KEYS.API_KEY)`로 API 키 존재 여부 확인

    **공통 주의사항:**
    - 모든 한국어 텍스트: "메시지를 입력하세요...", "생각 중...", "재시도", "새 대화", "모델 불러오는 중..." 등
    - 다크모드 기본 스타일: bg-background, text-foreground, bg-muted 등 CSS 변수 사용
    - cursor-pointer: 모든 클릭 가능한 요소에 추가
    - transition-colors: hover 효과가 있는 요소에 추가
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/ai-chat-app.worktrees/frontend-glm && pnpm test --run && pnpm build</automated>
  </verify>
  <done>
    - pnpm test 통과
    - pnpm build 성공
    - MessageInput이 Enter로 전송, Shift+Enter로 줄바꿈 처리
    - MessageInput이 스트리밍 중 Stop 버튼(Square 아이콘)으로 전환
    - ModelSelectorPopover가 무료 모델 목록을 표시하고 선택 가능
    - ChatHeader가 대화 제목과 모델 선택기를 표시
    - ChatPage가 전체 채팅 흐름을 연결 (메시지 저장 → 스트리밍 → 마크다운 렌더링 → 저장)
    - ChatPage에 API 오류 시 재시도 버튼 표시
    - /chat/$conversationId 라우트가 ChatPage를 렌더링
    - / 인덱스 라우트가 API 키 설정 안내를 표시
    - API 키 없을 때 EmptyApiKeyListener 표시
  </done>
</task>

</tasks>

<verification>
1. pnpm test --run 통과 (모든 단위 테스트)
2. pnpm build 성공 (TypeScript 타입 체크 + Vite 번들링)
3. 브라우저에서 /chat/{id} 접속 시 전체 채팅 UI 렌더링 (수동 검증은 execute-phase에서)
</verification>

<success_criteria>
- 어시스턴트 응답이 react-markdown + remark-gfm + rehype-highlight로 마크다운 렌더링된다
- 스트리밍 중 오토스크롤이 동작하고 사용자 수동 스크롤 시 중지된다
- 응답 대기 중 StreamingIndicator (Loader2 + "생각 중...")가 표시된다
- MessageInput에서 Enter로 전송, Shift+Enter로 줄바꿈이 동작한다
- API 오류 발생 시 오류 메시지와 재시도 버튼이 표시된다
- /chat/$conversationId 라우트에서 전체 채팅 흐름이 동작한다
- 모델 선택 팝오버에서 무료 모델 목록을 조회하고 선택할 수 있다
</success_criteria>

<output>
After completion, create `.planning/phases/02-채팅-핵심/02-02-SUMMARY.md`
</output>
