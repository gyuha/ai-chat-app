---
phase: 03-conversation-management
plan: "03-03"
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - src/routes/home.tsx
autonomous: true
requirements:
  - CONV-03
  - UI-04

must_haves:
  truths:
    - "When API key is not set, show API key setup prompt"
    - "When API key set but no conversation, show '대화를 시작하세요' card"
    - "First user message generates conversation title automatically"
  artifacts:
    - path: "src/routes/home.tsx"
      provides: "Empty states + auto-title generation"
      contains: "ApiKeySetup.*empty.*대화를 시작하세요|updateConversationTitle"
  key_links:
    - from: "src/routes/home.tsx"
      to: "src/hooks/use-db.ts"
      via: "updateConversationTitle, db.messages.count"
      pattern: "updateConversationTitle"
---

<objective>
홈 페이지 빈 상태 구현 및 자동 제목 생성 기능 추가. 두 가지 빈 상태 (API 키 미설정, 대화 없음) 처리와 첫 메시지 기반 제목 자동 생성을 구현한다.
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app.worktrees/front-chat-minimax/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app.worktrees/front-chat-minimax/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/routes/home.tsx
@src/hooks/use-db.ts
@src/db/index.ts
@src/components/api-key-setup.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add auto-title generation after first user message</name>
  <files>src/routes/home.tsx</files>
  <read_first>
- src/routes/home.tsx
- src/hooks/use-db.ts
- src/db/index.ts
  </read_first>
  <action>
handleSend 함수에서 첫 번째 사용자 메시지가 전송될 때 대화가 자동으로 생성된 직후, 대화 제목을 메시지 내용을 기반으로 자동 생성한다.

**현재 handleSend (개선 전):**
```tsx
const handleSend = useCallback(
  async (content: string) => {
    if (!currentConversationId) {
      const newId = await createConversation()
      setCurrentConversation(newId)
      await addMessage(newId, "user", content)
    } else {
      await addMessage(currentConversationId, "user", content)
    }
    await sendMessage(content)
  },
  [currentConversationId, setCurrentConversation, sendMessage]
)
```

**개선된 handleSend:**
```tsx
const handleSend = useCallback(
  async (content: string) => {
    let targetConvId = currentConversationId

    if (!targetConvId) {
      targetConvId = await createConversation()
      setCurrentConversation(targetConvId)
    }

    // Add message first
    await addMessage(targetConvId, "user", content)

    // CONV-03: Auto-generate title from first message
    const messageCount = await db.messages
      .where("conversationId")
      .equals(targetConvId)
      .count()
    if (messageCount === 1) {
      // First message in conversation - generate title
      const raw = content.slice(0, 40).replace(/[#*`_\[\]]/g, "").trim()
      const title = raw + (content.length > 40 ? "..." : "") || "새 대화"
      await updateConversationTitle(targetConvId, title)
    }

    await sendMessage(content)
  },
  [currentConversationId, setCurrentConversation, sendMessage]
)
```

**import 추가:**
```tsx
import { db } from "@/db"
import { updateConversationTitle } from "@/hooks/use-db"
```

**Title generation 규칙:**
- 첫 40자만 사용
- Markdown 특수문자 (#*`_\[\]) 제거
- 40자 초과 시 "..." 추가
- 빈 문자열이면 "새 대화" 사용
  </action>
  <verify>
    <automated>grep -c "updateConversationTitle" src/routes/home.tsx && grep -c "content.slice.0.*40" src/routes/home.tsx</automated>
  </verify>
  <done>First user message generates conversation title from message content</done>
</task>

<task type="auto">
  <name>Task 2: Implement UI-04 empty state when no conversation selected</name>
  <files>src/routes/home.tsx</files>
  <read_first>
- src/routes/home.tsx
- src/components/api-key-setup.tsx
  </read_first>
  <action>
API 키는 설정되어 있지만 대화가 선택되지 않은 상태에서 빈 상태 카드를 표시한다. 현재 messages.length === 0 일 때 표시되는 placeholder를 교체한다.

**현재 placeholder:**
```tsx
{messages.length === 0 ? (
  <div className="flex h-full items-center justify-center">
    <div className="text-center space-y-2">
      <p className="text-muted-foreground">
        {defaultModel
          ? "메시지를 입력하여 대화를 시작하세요."
          : "먼저 모델을 선택해 주세요."}
      </p>
      {!defaultModel && (
        <Button
          variant="link"
          onClick={() => navigate({ to: "/settings" })}
        >
          설정에서 모델 선택하기
        </Button>
      )}
    </div>
  </div>
) : (...)}
```

**새로운 빈 상태:**
```tsx
{messages.length === 0 && !currentConversationId ? (
  <div className="flex h-full items-center justify-center">
    <div className="text-center space-y-3 max-w-md px-6">
      <div className="mx-auto w-12 h-12 rounded-full bg-accent/10 flex items-center justify-center">
        <MessageSquare className="h-6 w-6 text-accent" />
      </div>
      <h2 className="text-xl font-semibold text-foreground">대화를 시작하세요</h2>
      <p className="text-muted-foreground text-sm">
        질문을 입력해 AI와 대화를 시작하세요.
      </p>
      <Button
        onClick={async () => {
          const id = await createConversation()
          setCurrentConversation(id)
        }}
        className="mt-2"
      >
        새 대화
      </Button>
    </div>
  </div>
) : messages.length === 0 && !defaultModel ? (
  <div className="flex h-full items-center justify-center">
    <div className="text-center space-y-2">
      <p className="text-muted-foreground">먼저 모델을 선택해 주세요.</p>
      <Button
        variant="link"
        onClick={() => navigate({ to: "/settings" })}
      >
        설정에서 모델 선택하기
      </Button>
    </div>
  </div>
) : (...)}
```

**MessageSquare import 추가:**
```tsx
import { MessageSquare } from "lucide-react"
```

**조건부 렌더링 로직:**
1. messages.length === 0 && !currentConversationId: "대화를 시작하세요" 카드 + 새 대화 CTA
2. messages.length === 0 && !defaultModel: 모델 선택 안내
3. messages.length > 0: 기존 메시지 렌더링
  </action>
  <verify>
    <automated>grep -c "대화를 시작하세요" src/routes/home.tsx && grep -c "MessageSquare" src/routes/home.tsx</automated>
  </verify>
  <done>Empty state shows "대화를 시작하세요" card with new conversation CTA</done>
</task>

</tasks>

<verification>
Home page shows appropriate empty states. Auto-title generates from first message.
</verification>

<success_criteria>
- API key not set: shows ApiKeySetup (unchanged)
- API key set, no conversation selected: shows "대화를 시작하세요" card with MessageSquare icon
- First user message generates title (40 chars max, markdown stripped)
- New conversation button in empty state works correctly
</success_criteria>

<output>
After completion, create `.planning/phases/03-conversation-management/03-03-SUMMARY.md`
</output>
