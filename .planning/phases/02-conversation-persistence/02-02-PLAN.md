---
phase: 02-conversation-persistence
plan: "02"
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - frontend/src/features/conversations/api.ts
  - frontend/src/features/conversations/query.ts
  - frontend/src/features/conversations/types.ts
  - frontend/src/routes/index.tsx
  - frontend/tests/conversation-routing.test.tsx
autonomous: true
requirements:
  - CONV-01
  - CONV-02
must_haves:
  truths:
    - "An authenticated user landing on `/` with no conversations gets one conversation auto-created without pressing a button."
    - "The protected home route renders only conversation titles and selects the created or chosen conversation immediately."
    - "The frontend uses the existing credentialed API client and never adds OpenRouter or long-lived conversation state outside the server/query layer."
  artifacts:
    - path: frontend/src/features/conversations/api.ts
      provides: "Conversation list/create/detail request helpers."
    - path: frontend/src/routes/index.tsx
      provides: "Protected conversation bootstrap and title-only list UI."
    - path: frontend/tests/conversation-routing.test.tsx
      provides: "Frontend coverage for empty-list bootstrap and title-only rendering."
  key_links:
    - from: frontend/src/features/conversations/api.ts
      to: frontend/src/lib/api/client.ts
      via: "shared credentialed fetch wrapper"
      pattern: "apiRequest"
    - from: frontend/src/routes/index.tsx
      to: frontend/src/features/conversations/query.ts
      via: "query-driven bootstrap"
      pattern: "useQuery"
    - from: frontend/tests/conversation-routing.test.tsx
      to: frontend/src/routes/index.tsx
      via: "protected route bootstrap verification"
      pattern: "새 대화"
---

<objective>
Connect the conversation API to the protected frontend entry and render the minimal conversation list UX.

Purpose: Turn the Phase 2 backend contract into a usable authenticated shell that auto-creates the first conversation and displays title-only ownership-safe results.
Output: Conversation frontend feature helpers, protected route bootstrap logic, and frontend tests for empty-list auto-create plus title-only rendering.
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-conversation-persistence/02-CONTEXT.md
@.planning/phases/02-conversation-persistence/02-RESEARCH.md
@.planning/phases/02-conversation-persistence/02-VALIDATION.md
@.planning/phases/02-conversation-persistence/02-01-PLAN.md
@frontend/src/lib/api/client.ts
@frontend/src/features/auth/api.ts
@frontend/src/routes/index.tsx
@frontend/tests/auth-routing.test.tsx

<interfaces>
Frontend conversation contracts to create and use:

```ts
export type ConversationSummary = {
  id: string;
  title: string;
}

export type CreateConversationPayload = {
  mode?: "default" | "bootstrap";
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add conversation feature contracts and query helpers on top of the shared API client</name>
  <files>frontend/src/features/conversations/api.ts, frontend/src/features/conversations/query.ts, frontend/src/features/conversations/types.ts</files>
  <read_first>
    - frontend/src/lib/api/client.ts
    - frontend/src/features/auth/api.ts
    - frontend/src/features/auth/session.ts
    - .planning/phases/02-conversation-persistence/02-01-PLAN.md
    - .planning/phases/02-conversation-persistence/02-CONTEXT.md
    - .planning/phases/02-conversation-persistence/02-RESEARCH.md
  </read_first>
  <action>Create `frontend/src/features/conversations/types.ts` exporting `ConversationSummary` and `CreateConversationPayload`. Create `frontend/src/features/conversations/api.ts` with `listConversations()`, `createConversation(payload?: CreateConversationPayload)`, and `getConversation(id: string)` that call `/conversations`, `/conversations`, and `/conversations/:id` through `apiRequest`. Create `frontend/src/features/conversations/query.ts` with stable TanStack Query helpers rooted at a `["conversations"]` key and a detail key `["conversations", id]`. Keep all requests credentialed through the existing shared client. Do not add message-history response types, preview fields, timestamps, or a separate client-side persistence layer in this phase per D-03 and deferred scope.</action>
  <acceptance_criteria>
    - `frontend/src/features/conversations/api.ts` contains `"/conversations"` and `"/conversations/${id}"`.
    - `frontend/src/features/conversations/query.ts` contains `["conversations"]`.
    - `frontend/src/features/conversations/types.ts` contains `title: string`.
    - `rg -n "preview|updatedAt|createdAt" frontend/src/features/conversations` returns no matches in the exported UI types.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "/conversations|\\[\"conversations\"\\]|title: string" frontend/src/features/conversations && ! rg -n "preview|updatedAt|createdAt" frontend/src/features/conversations</automated>
  </verify>
  <done>The frontend has a small conversation feature slice that mirrors the backend contract and stays within the title-only Phase 2 boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the protected home placeholder with auto-bootstrap conversation list behavior</name>
  <files>frontend/src/routes/index.tsx, frontend/tests/conversation-routing.test.tsx</files>
  <read_first>
    - frontend/src/routes/index.tsx
    - frontend/tests/auth-routing.test.tsx
    - frontend/src/features/auth/auth.store.ts
    - frontend/src/features/conversations/api.ts
    - frontend/src/features/conversations/query.ts
    - .planning/phases/02-conversation-persistence/02-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: Authenticated load with one existing conversation shows its title and keeps `/` selected.
    - Test 2: Authenticated load with an empty list calls bootstrap creation once and then renders `새 대화`.
    - Test 3: The route does not render any manual "create conversation" button before auto-bootstrap completes.
  </behavior>
  <action>Create `frontend/tests/conversation-routing.test.tsx` by extending the existing auth routing pattern with mocked conversation API helpers. Assert one empty-list bootstrap path, exact rendered title `새 대화`, and absence of manual creation CTA text per D-02 and D-08. Then replace the placeholder card in `frontend/src/routes/index.tsx` with a protected conversation shell that uses the conversation query helpers, triggers `createConversation({ mode: "bootstrap" })` only after a successful empty list response, stores the selected conversation id in component state or a small route-local UI state, renders a title-only list, and marks the selected row immediately per D-07. Keep the UI minimal: title-only list items, no timestamps, no last-message preview, no welcome copy, and no message history panel yet.</action>
  <acceptance_criteria>
    - `frontend/src/routes/index.tsx` contains `createConversation({ mode: "bootstrap" })`.
    - `frontend/src/routes/index.tsx` contains `새 대화` only via data rendering or fallback selection logic, not as static hero copy.
    - `frontend/tests/conversation-routing.test.tsx` contains assertions for `새 대화` and for bootstrap create behavior.
    - `rg -n "최근|updatedAt|createdAt|message preview|환영" frontend/src/routes/index.tsx` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir frontend test -- conversation-routing.test.tsx && pnpm --dir frontend typecheck && rg -n "createConversation\\(\\{ mode: \"bootstrap\" \\}\\)|새 대화" frontend/src/routes/index.tsx frontend/tests/conversation-routing.test.tsx</automated>
  </verify>
  <done>The protected home route now boots directly into a minimal conversation list, auto-creates the first conversation when needed, and proves the behavior with targeted frontend tests.</done>
</task>

</tasks>

<verification>
Run the targeted conversation routing test and frontend typecheck. Confirm the protected route auto-creates a single `새 대화` when the list is empty and otherwise renders only title rows for the authenticated user's conversations.
</verification>

<success_criteria>
Phase 2 frontend work is complete when `/` stops being an auth placeholder and instead becomes a protected conversation entry that auto-creates the first conversation, renders title-only list items, and stays inside the ownership-safe server contract built in Plan 02-01.
</success_criteria>

<output>
After completion, create `.planning/phases/02-conversation-persistence/02-02-SUMMARY.md`
</output>
