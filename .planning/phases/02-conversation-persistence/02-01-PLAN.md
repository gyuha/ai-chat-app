---
phase: 02-conversation-persistence
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/prisma/schema.prisma
  - backend/prisma/migrations/*
  - backend/src/app.module.ts
  - backend/src/conversations/conversations.module.ts
  - backend/src/conversations/conversations.controller.ts
  - backend/src/conversations/conversations.service.ts
  - backend/src/conversations/dto/create-conversation.dto.ts
  - backend/src/conversations/dto/conversation-summary.dto.ts
  - backend/test/conversations.e2e-spec.ts
autonomous: true
requirements:
  - CONV-01
  - CONV-04
must_haves:
  truths:
    - "An authenticated user can create a conversation titled `새 대화` through the backend API."
    - "Conversation reads and writes are always scoped by the authenticated user's `userId` so another user's conversation ID is not returned."
    - "The database schema already contains `Conversation` and `Message` so later chat/history phases do not need to redesign persistence."
  artifacts:
    - path: backend/prisma/schema.prisma
      provides: "Prisma `Conversation`, `Message`, and `MessageRole` definitions linked to `User`."
    - path: backend/src/conversations/conversations.service.ts
      provides: "Ownership-scoped create/list/get service methods."
    - path: backend/test/conversations.e2e-spec.ts
      provides: "Backend e2e coverage for create/list/ownership behavior."
  key_links:
    - from: backend/src/conversations/conversations.controller.ts
      to: backend/src/auth/decorators/current-user.decorator.ts
      via: "authenticated user extraction"
      pattern: "@CurrentUser()"
    - from: backend/src/conversations/conversations.service.ts
      to: backend/prisma/schema.prisma
      via: "ownership-scoped Prisma access"
      pattern: "where: { userId"
    - from: backend/src/app.module.ts
      to: backend/src/conversations/conversations.module.ts
      via: "Nest module registration"
      pattern: "ConversationsModule"
---

<objective>
Define the backend persistence and ownership-safe API contract for conversations.

Purpose: Land the schema, module boundary, and security tests first so frontend work can build against a stable conversation contract.
Output: Prisma conversation/message models, authenticated conversations module, and backend e2e coverage for create/list/ownership.
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-conversation-persistence/02-CONTEXT.md
@.planning/phases/02-conversation-persistence/02-RESEARCH.md
@.planning/phases/02-conversation-persistence/02-VALIDATION.md
@backend/prisma/schema.prisma
@backend/src/app.module.ts
@backend/src/auth/auth.controller.ts
@backend/test/auth.e2e-spec.ts

<interfaces>
Backend auth/session contracts already available:

```ts
type SessionUser = { id: string; email: string }
```

Conversation API contracts to create in this plan:

```ts
type CreateConversationDto = {
  mode?: "default" | "bootstrap";
}

type ConversationSummaryDto = {
  id: string;
  title: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add conversation/message persistence contracts and backend e2e scaffolding</name>
  <files>backend/prisma/schema.prisma, backend/prisma/migrations/*, backend/test/conversations.e2e-spec.ts</files>
  <read_first>
    - backend/prisma/schema.prisma
    - backend/test/auth.e2e-spec.ts
    - .planning/phases/02-conversation-persistence/02-CONTEXT.md
    - .planning/phases/02-conversation-persistence/02-RESEARCH.md
    - .planning/phases/02-conversation-persistence/02-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: `POST /conversations` by an authenticated user returns `201` with `{ id, title: "새 대화" }`.
    - Test 2: `GET /conversations` returns only the authenticated user's conversation titles.
    - Test 3: `GET /conversations/:id` for another user's conversation returns `404`.
  </behavior>
  <action>Add `Conversation`, `Message`, and `MessageRole` to `backend/prisma/schema.prisma` with required relations `User -> Conversation -> Message`, `Conversation.title String`, `Conversation.userId String`, `Message.role MessageRole`, and indexes `@@index([userId, updatedAt])` plus `@@index([conversationId, createdAt])`. Generate a Prisma migration under `backend/prisma/migrations/` for the new tables. Replace the Wave 0 placeholder with `backend/test/conversations.e2e-spec.ts` that follows the auth e2e pattern: create the SQLite tables explicitly for test boot, seed two users, log in with cookie auth, and assert D-05 exact title `새 대화`, CONV-01 create behavior, and CONV-04 cross-user `404` behavior.</action>
  <acceptance_criteria>
    - `backend/prisma/schema.prisma` contains `model Conversation {`, `model Message {`, and `enum MessageRole {`.
    - `backend/prisma/schema.prisma` contains `title     String` and `@@index([userId, updatedAt])`.
    - `backend/test/conversations.e2e-spec.ts` contains `POST /conversations`, `GET /conversations`, and `.expect(404)`.
    - `pnpm --dir backend prisma generate` exits 0 after the schema change.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir backend prisma generate && pnpm --dir backend test:e2e -- --runInBand conversations.e2e-spec.ts</automated>
  </verify>
  <done>The repository has a forward-compatible conversation/message schema and failing-or-passing backend tests that pin the API contract and ownership boundary before module implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the authenticated conversations module and ownership-scoped API</name>
  <files>backend/src/app.module.ts, backend/src/conversations/conversations.module.ts, backend/src/conversations/conversations.controller.ts, backend/src/conversations/conversations.service.ts, backend/src/conversations/dto/create-conversation.dto.ts, backend/src/conversations/dto/conversation-summary.dto.ts</files>
  <read_first>
    - backend/src/app.module.ts
    - backend/src/auth/auth.controller.ts
    - backend/src/auth/decorators/current-user.decorator.ts
    - backend/src/auth/guards/jwt-auth.guard.ts
    - backend/src/prisma/prisma.service.ts
    - backend/test/conversations.e2e-spec.ts
    - .planning/phases/02-conversation-persistence/02-CONTEXT.md
  </read_first>
  <action>Create `backend/src/conversations/` with a Nest module, controller, service, and DTOs. Register `ConversationsModule` in `backend/src/app.module.ts`. Expose authenticated endpoints `POST /conversations`, `GET /conversations`, and `GET /conversations/:id` guarded by `JwtAuthGuard` and driven by `@CurrentUser() user.id`. Implement `createConversation(userId, mode)` with exact default title `새 대화` per D-05 and support `mode` values `"default"` and `"bootstrap"`, where bootstrap uses a short Prisma transaction to return the newest existing conversation for that user before creating one. Make `listForUser(userId)` return title-only DTOs sorted by `updatedAt desc`. Make `getForUser(userId, id)` scope the Prisma lookup with both `id` and `userId` and throw `NotFoundException` when missing so CONV-04 is enforced without resource enumeration.</action>
  <acceptance_criteria>
    - `backend/src/app.module.ts` contains `ConversationsModule`.
    - `backend/src/conversations/conversations.controller.ts` contains `@Controller('conversations')`, `@UseGuards(JwtAuthGuard)`, `@Get()`, `@Get(':id')`, and `@Post()`.
    - `backend/src/conversations/conversations.service.ts` contains `createConversation`, `listForUser`, and `getForUser`.
    - `backend/src/conversations/conversations.service.ts` contains both `where: { userId }` and `where: { id, userId }`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir backend test:e2e -- --runInBand conversations.e2e-spec.ts && rg -n "ConversationsModule|@Controller\\('conversations'\\)|where: \\{ id, userId \\}|title: '새 대화'" backend/src/app.module.ts backend/src/conversations/*.ts</automated>
  </verify>
  <done>The backend exposes an authenticated conversation contract with ownership-safe Prisma queries and a bootstrap-safe create path that later frontend work can call directly.</done>
</task>

</tasks>

<verification>
Run the conversation e2e suite and Prisma client generation. Confirm the backend creates `새 대화`, lists only title summaries for the current user, and returns `404` for foreign conversation IDs.
</verification>

<success_criteria>
Phase 2 backend groundwork is complete when the schema supports Conversation and Message entities, the Nest conversations module is registered, and automated e2e tests prove authenticated create/list/detail behavior without cross-user leakage.
</success_criteria>

<output>
After completion, create `.planning/phases/02-conversation-persistence/02-01-SUMMARY.md`
</output>
