---
phase: 01-foundation-secure-proxy
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - apps/server/src/main.ts
  - apps/server/src/app.module.ts
  - apps/server/src/config/env.schema.ts
  - apps/server/src/config/app.config.ts
  - apps/server/src/modules/health/health.controller.ts
  - apps/server/src/modules/health/health.module.ts
  - apps/server/src/modules/models/models.controller.ts
  - apps/server/src/modules/models/models.service.ts
  - apps/server/src/modules/models/models.module.ts
  - apps/server/src/common/dto
autonomous: true
requirements:
  - PLAT-02
  - PLAT-03
user_setup: []
must_haves:
  truths:
    - Server fails fast when required OpenRouter env configuration is invalid.
    - `/api/v1/health` responds successfully from the NestJS app.
    - `/api/v1/models` returns model options derived from the server allowlist.
  artifacts:
    - apps/server/src/config/env.schema.ts
    - apps/server/src/modules/health/health.controller.ts
    - apps/server/src/modules/models/models.controller.ts
  key_links:
    - App module wires the config layer and health/models modules together.
    - Models service reads the allowlist from validated config rather than raw process env in controllers.
---

<objective>
Build the NestJS skeleton, env validation, and models/health endpoints.

Purpose: Establish the secure server-side boundary before proxy and repository behavior are added.
Output: Working NestJS application skeleton with validated config and Phase 1 smoke APIs.
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app.worktrees/codex/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app.worktrees/codex/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-secure-proxy/01-CONTEXT.md
@.planning/phases/01-foundation-secure-proxy/01-RESEARCH.md
@.planning/phases/01-foundation-secure-proxy/01-VALIDATION.md
@apps/server/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NestJS bootstrap and validated configuration layer</name>
  <files>apps/server/src/main.ts, apps/server/src/app.module.ts, apps/server/src/config/env.schema.ts, apps/server/src/config/app.config.ts</files>
  <read_first>apps/server/package.json, .planning/phases/01-foundation-secure-proxy/01-CONTEXT.md, .planning/phases/01-foundation-secure-proxy/01-RESEARCH.md, .env.example</read_first>
  <action>Implement the NestJS bootstrap with a single `/api/v1` global prefix, CORS configuration appropriate for local web development, and config bootstrap through `@nestjs/config`. Create `env.schema.ts` validating `OPENROUTER_API_KEY`, `OPENROUTER_MODEL_ALLOWLIST`, `CHAT_STORAGE_MODE`, `CHAT_STORAGE_DIR`, and `PORT`. Create `app.config.ts` that parses `OPENROUTER_MODEL_ALLOWLIST` into a trimmed string array and exposes a typed app config object for downstream modules.</action>
  <verify>test -f apps/server/src/config/env.schema.ts && test -f apps/server/src/app.module.ts</verify>
  <acceptance_criteria>
    - `apps/server/src/main.ts` contains `setGlobalPrefix('api/v1')`
    - `apps/server/src/config/env.schema.ts` contains `OPENROUTER_API_KEY` and `OPENROUTER_MODEL_ALLOWLIST`
    - `apps/server/src/config/app.config.ts` parses the allowlist with `.split(',')`
  </acceptance_criteria>
  <done>Nest bootstrap and config validation exist with the required env keys and API prefix.</done>
</task>

<task type="auto">
  <name>Task 2: Implement health and models modules with typed responses</name>
  <files>apps/server/src/modules/health/health.controller.ts, apps/server/src/modules/health/health.module.ts, apps/server/src/modules/models/models.controller.ts, apps/server/src/modules/models/models.service.ts, apps/server/src/modules/models/models.module.ts, packages/contracts/src/index.ts</files>
  <read_first>packages/contracts/src/index.ts, apps/server/src/app.module.ts, apps/server/src/config/app.config.ts, .planning/REQUIREMENTS.md, .planning/TECH-DESIGN.md</read_first>
  <action>Create a health controller returning a simple JSON status payload and a models module that exposes `GET /api/v1/models`. Update `packages/contracts/src/index.ts` so `ModelOption` includes at least `id`, `label`, and `provider`. Implement `models.service.ts` to map the validated allowlist into `ModelOption[]` without calling OpenRouter. Wire both modules into `app.module.ts`.</action>
  <verify>test -f apps/server/src/modules/health/health.controller.ts && test -f apps/server/src/modules/models/models.service.ts</verify>
  <acceptance_criteria>
    - `apps/server/src/modules/health/health.controller.ts` contains `@Get()` and returns a status object
    - `apps/server/src/modules/models/models.controller.ts` contains `@Get()` and returns `ModelOption[]`
    - `packages/contracts/src/index.ts` exports a `ModelOption` type with `id`, `label`, and `provider`
  </acceptance_criteria>
  <done>Server exposes health and models endpoints using the shared `ModelOption` contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter server build` exits 0
- [ ] `pnpm typecheck` exits 0
- [ ] server smoke test or direct endpoint check confirms `/api/v1/health` and `/api/v1/models`
</verification>

<success_criteria>

- All tasks completed
- Config validation prevents missing/invalid env setup from silently booting
- Health and models endpoints exist and match contracts
- No controller reads raw env directly
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-secure-proxy/01-02-SUMMARY.md`
</output>
