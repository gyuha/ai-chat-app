---
phase: 01-foundation-secure-proxy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - biome.json
  - tsconfig.base.json
  - apps/web/package.json
  - apps/server/package.json
  - packages/contracts/package.json
  - packages/contracts/src/index.ts
  - packages/config/package.json
autonomous: true
requirements:
  - PLAT-03
user_setup: []
must_haves:
  truths:
    - Root workspace scripts exist and can address web and server consistently.
    - Shared contracts package exists and can be imported by both apps.
    - Biome and TypeScript base configuration are centralized at the repo root.
  artifacts:
    - package.json
    - pnpm-workspace.yaml
    - biome.json
    - tsconfig.base.json
    - packages/contracts/src/index.ts
  key_links:
    - Root package.json scripts reference workspace package names used by apps/web and apps/server.
    - apps/web and apps/server package manifests extend shared workspace conventions rather than standalone configs.
---

<objective>
Create the monorepo foundation and shared contracts package for the Phase 1 stack.

Purpose: Lock the workspace topology before backend and frontend implementation starts.
Output: Root workspace config, app package manifests, shared contracts package, and centralized tooling config.
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app.worktrees/codex/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app.worktrees/codex/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-secure-proxy/01-CONTEXT.md
@.planning/phases/01-foundation-secure-proxy/01-RESEARCH.md
@.planning/TECH-DESIGN.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create root workspace and toolchain manifests</name>
  <files>package.json, pnpm-workspace.yaml, biome.json, tsconfig.base.json, .gitignore, .env.example</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/TECH-DESIGN.md, .planning/phases/01-foundation-secure-proxy/01-CONTEXT.md, AGENTS.md</read_first>
  <action>Create the root workspace files with concrete values. Add `pnpm-workspace.yaml` including `apps/*` and `packages/*`. Create root `package.json` with scripts `dev`, `dev:web`, `dev:server`, `build`, `lint`, `format`, `typecheck`, `test`; ensure `dev` runs both `web` and `server` in parallel through pnpm filters. Add `biome.json` for repository-wide lint/format settings and `tsconfig.base.json` containing shared TypeScript compiler options for Node + React packages. Update `.gitignore` to cover `node_modules`, build outputs, `.env`, and data directories without ignoring `.planning`. Create `.env.example` with `OPENROUTER_API_KEY=`, `OPENROUTER_BASE_URL=`, `OPENROUTER_MODEL_ALLOWLIST=`, `CHAT_STORAGE_MODE=`, `CHAT_STORAGE_DIR=`, and `PORT=`.</action>
  <verify>test -f package.json && test -f pnpm-workspace.yaml && test -f biome.json && test -f tsconfig.base.json && test -f .env.example</verify>
  <acceptance_criteria>
    - `pnpm-workspace.yaml` contains `apps/*` and `packages/*`
    - `package.json` contains scripts named `"dev"`, `"dev:web"`, `"dev:server"`, `"build"`, `"lint"`, `"typecheck"`, and `"test"`
    - `.env.example` contains `OPENROUTER_API_KEY=` and `OPENROUTER_MODEL_ALLOWLIST=`
  </acceptance_criteria>
  <done>Root workspace files exist with the required scripts and env samples.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold web, server, and shared package manifests</name>
  <files>apps/web/package.json, apps/server/package.json, packages/contracts/package.json, packages/config/package.json, packages/contracts/src/index.ts</files>
  <read_first>package.json, pnpm-workspace.yaml, .planning/TECH-DESIGN.md, .planning/phases/01-foundation-secure-proxy/01-RESEARCH.md</read_first>
  <action>Create `apps/web/package.json` for a Vite React TypeScript app and `apps/server/package.json` for a NestJS TypeScript app, each exposing `dev`, `build`, `typecheck`, and `test` scripts. Create `packages/contracts/package.json` and `packages/config/package.json`. Add `packages/contracts/src/index.ts` exporting placeholder domain types named `ModelOption`, `ChatSummary`, `ChatDetail`, `ChatMessage`, and `StreamEvent` so later plans can import stable symbols immediately.</action>
  <verify>test -f apps/web/package.json && test -f apps/server/package.json && test -f packages/contracts/src/index.ts</verify>
  <acceptance_criteria>
    - `apps/web/package.json` contains a `"dev"` script for Vite
    - `apps/server/package.json` contains a `"dev"` script for Nest or tsx watch flow
    - `packages/contracts/src/index.ts` exports `ModelOption`, `ChatSummary`, `ChatDetail`, `ChatMessage`, and `StreamEvent`
  </acceptance_criteria>
  <done>All workspace packages have manifests and the contracts package exports the agreed type names.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm install` succeeds
- [ ] `pnpm lint` exits 0
- [ ] `pnpm typecheck` exits 0 or fails only on files planned for later plans
</verification>

<success_criteria>

- All tasks completed
- Root workspace can address both apps through pnpm filters
- Shared contracts package exists and exports initial domain types
- No errors or warnings introduced in repository-level config
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-secure-proxy/01-01-SUMMARY.md`
</output>
