---
phase: 01-foundation-secure-proxy
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - apps/server/src/infrastructure/openrouter/openrouter.client.ts
  - apps/server/src/modules/chats/chats.repository.ts
  - apps/server/src/infrastructure/storage/memory-chat.repository.ts
  - apps/server/src/infrastructure/storage/file-chat.repository.ts
  - apps/server/src/modules/streaming/streaming.controller.ts
  - apps/server/src/modules/streaming/streaming.module.ts
  - apps/web/src/app/router.tsx
  - apps/web/src/app/providers
  - apps/web/src/routes/__root.tsx
  - apps/web/src/routes/index.tsx
  - apps/web/src/lib/api
autonomous: true
requirements:
  - SET-04
  - PLAT-01
user_setup: []
must_haves:
  truths:
    - Server owns the OpenRouter integration and browser code never embeds the upstream key.
    - Repository seam exists with memory and file adapters ready for later chat persistence work.
    - Web app can load and display internal health/models smoke data through its own API layer.
  artifacts:
    - apps/server/src/infrastructure/openrouter/openrouter.client.ts
    - apps/server/src/infrastructure/storage/memory-chat.repository.ts
    - apps/server/src/infrastructure/storage/file-chat.repository.ts
    - apps/web/src/lib/api
  key_links:
    - Streaming controller depends on the OpenRouter client abstraction rather than inline fetch logic.
    - Web API helpers target local server endpoints, not `openrouter.ai` URLs.
---

<objective>
Add the OpenRouter proxy seam, repository adapters, streaming skeleton, and minimal web smoke path.

Purpose: Finish Phase 1 with an end-to-end-safe foundation that later chat UI work can consume.
Output: OpenRouter client abstraction, repository adapters, streaming module skeleton, and web shell that talks to internal APIs only.
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app.worktrees/codex/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app.worktrees/codex/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-secure-proxy/01-CONTEXT.md
@.planning/phases/01-foundation-secure-proxy/01-RESEARCH.md
@apps/server/src/modules/models/models.service.ts
@packages/contracts/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OpenRouter client abstraction and repository seam</name>
  <files>apps/server/src/infrastructure/openrouter/openrouter.client.ts, apps/server/src/modules/chats/chats.repository.ts, apps/server/src/infrastructure/storage/memory-chat.repository.ts, apps/server/src/infrastructure/storage/file-chat.repository.ts</files>
  <read_first>.planning/phases/01-foundation-secure-proxy/01-CONTEXT.md, .planning/phases/01-foundation-secure-proxy/01-RESEARCH.md, apps/server/src/config/app.config.ts, packages/contracts/src/index.ts</read_first>
  <action>Define a server-only `OpenRouterClient` abstraction that accepts validated config and exposes methods for model metadata access and streaming request execution. Create `ChatRepository` interface with the concrete methods named in research/context. Implement `memory-chat.repository.ts` and `file-chat.repository.ts` as compile-ready adapters with placeholder-safe logic for list/create/append/updateTitle paths, but do not overbuild persistence features beyond what Phase 1 needs.</action>
  <verify>test -f apps/server/src/infrastructure/openrouter/openrouter.client.ts && test -f apps/server/src/infrastructure/storage/file-chat.repository.ts</verify>
  <acceptance_criteria>
    - `apps/server/src/infrastructure/openrouter/openrouter.client.ts` does not hardcode secrets and reads config through injection
    - `apps/server/src/modules/chats/chats.repository.ts` declares a `ChatRepository` interface
    - both `memory-chat.repository.ts` and `file-chat.repository.ts` exist
  </acceptance_criteria>
  <done>Server has a proxy boundary and pluggable repository seam ready for chat features.</done>
</task>

<task type="auto">
  <name>Task 2: Add streaming module skeleton and web smoke path against internal APIs</name>
  <files>apps/server/src/modules/streaming/streaming.controller.ts, apps/server/src/modules/streaming/streaming.module.ts, apps/web/src/app/router.tsx, apps/web/src/routes/__root.tsx, apps/web/src/routes/index.tsx, apps/web/src/lib/api/index.ts, apps/web/src/app/providers/index.tsx</files>
  <read_first>.planning/PRD.md, .planning/TECH-DESIGN.md, .planning/phases/01-foundation-secure-proxy/01-CONTEXT.md, apps/server/src/modules/models/models.controller.ts, apps/web/package.json</read_first>
  <action>Create a streaming module skeleton that declares the Phase 1 endpoints and event names without requiring the full chat UI yet. In the web app, scaffold the root router/providers and an index route that fetches `/api/v1/health` and `/api/v1/models` through a local API helper. Add a minimal dark-mode shell using `SidebarProvider` and visible focus styles, but keep it intentionally thin: one smoke page that proves internal API integration works and does not call OpenRouter directly from the browser.</action>
  <verify>test -f apps/web/src/routes/index.tsx && test -f apps/server/src/modules/streaming/streaming.controller.ts</verify>
  <acceptance_criteria>
    - `apps/server/src/modules/streaming/streaming.controller.ts` contains route handlers for `messages/stream` or `regenerate/stream`
    - `apps/web/src/lib/api` contains functions calling `/api/v1/health` and `/api/v1/models`
    - `apps/web/src/routes/index.tsx` renders internal API smoke data and does not contain `openrouter.ai`
    - web shell includes `SidebarProvider` or equivalent provider wiring from shadcn sidebar foundation
  </acceptance_criteria>
  <done>Minimal web app proves browser-to-server integration while preserving the server-only OpenRouter boundary.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter server build` exits 0
- [ ] `pnpm --filter web build` exits 0
- [ ] `pnpm lint && pnpm typecheck && pnpm test` exits 0
- [ ] Browser smoke path confirms health/models load from internal API only
</verification>

<success_criteria>

- All tasks completed
- OpenRouter integration exists only on the server side
- Repository seam exists with memory and file adapters
- Web smoke path reaches internal APIs and respects dark/focus foundation rules
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-secure-proxy/01-03-SUMMARY.md`
</output>
