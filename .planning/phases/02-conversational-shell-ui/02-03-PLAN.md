---
phase: 02-conversational-shell-ui
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - apps/server/src/modules/chats
  - apps/server/src/app.module.ts
  - apps/web/src/lib/api
  - apps/web/src/features/chats
  - apps/web/src/routes/index.tsx
  - apps/web/src/routes/chat/$chatId.tsx
  - apps/web/src/components/ui/sidebar.tsx
autonomous: true
requirements:
  - SHELL-02
  - CHAT-01
user_setup: []
must_haves:
  truths:
    - Sidebar shows real chats ordered by recent activity.
    - New chat creation navigates immediately to an empty conversation route.
    - Active chat highlighting is derived from the current route and server-backed chat list.
  artifacts:
    - apps/server/src/modules/chats
    - apps/web/src/features/chats
    - apps/web/src/lib/api
  key_links:
    - Query cache and route navigation cooperate for list/detail/new chat flows.
    - Minimal REST endpoints are added without leaking streaming complexity into this phase.
---

<objective>
Connect sidebar and routes to real chat list/detail/create data and make active-chat navigation route-driven.

Purpose: Finish Phase 2 with a shell that is not just visually correct, but actually backed by chat records and navigation state.
Output: Minimal chats API, web query hooks/helpers, new chat mutation, route-driven active chat selection, and recent-first sidebar list behavior.
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app.worktrees/codex/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app.worktrees/codex/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/TECH-DESIGN.md
@.planning/phases/02-conversational-shell-ui/02-CONTEXT.md
@.planning/phases/02-conversational-shell-ui/02-RESEARCH.md
@apps/server/src/modules/chats/chats.repository.ts
@apps/web/src/lib/api/index.ts
@packages/contracts/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add minimal chats REST API on the server</name>
  <files>apps/server/src/modules/chats, apps/server/src/app.module.ts, packages/contracts/src/index.ts</files>
  <read_first>.planning/phases/02-conversational-shell-ui/02-CONTEXT.md, .planning/TECH-DESIGN.md, apps/server/src/modules/chats/chats.repository.ts</read_first>
  <action>Implement the smallest backend slice needed for the shell: list chats, create chat, and fetch chat detail. Reuse the repository seam from Phase 1. Add any missing service/controller/module files and keep the API focused on shell needs only — no streaming, no settings mutation, no title generation logic beyond current repository fields.</action>
  <verify>test -f apps/server/src/modules/chats/chats.controller.ts && test -f apps/server/src/modules/chats/chats.service.ts</verify>
  <acceptance_criteria>
    - server exposes `GET /api/v1/chats`, `POST /api/v1/chats`, and `GET /api/v1/chats/:chatId`
    - list response is recent-first
    - create response contains enough data for immediate route navigation
  </acceptance_criteria>
  <done>Web shell has the minimal server API it needs for realistic chat navigation.</done>
</task>

<task type="auto">
  <name>Task 2: Bind sidebar and chat route to query-backed chat data</name>
  <files>apps/web/src/lib/api, apps/web/src/features/chats, apps/web/src/routes/index.tsx, apps/web/src/routes/chat/$chatId.tsx, apps/web/src/components/ui/sidebar.tsx</files>
  <read_first>.planning/phases/02-conversational-shell-ui/02-RESEARCH.md, apps/web/src/lib/api/index.ts, apps/web/src/routes/chat/$chatId.tsx</read_first>
  <action>Add chat API helpers and query/mutation hooks, then wire the sidebar list, new chat action, and chat detail route to them. Ensure `/` redirects to the latest chat only when chats exist; otherwise it renders onboarding empty state. Derive active highlighting from the route param and keep query invalidation/update logic minimal and readable.</action>
  <verify>test -d apps/web/src/features/chats</verify>
  <acceptance_criteria>
    - sidebar renders real chat items from query data
    - new chat action creates and navigates to `/chat/$chatId`
    - active chat is visually distinguished based on current route
    - `/` does not create duplicate chats on every visit
  </acceptance_criteria>
  <done>Phase 2 shell works against real chat records and route semantics instead of mock-only UI state.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm lint && pnpm typecheck && pnpm test && pnpm build` exits 0
- [ ] Manual smoke confirms `/` redirect/onboarding behavior and new chat navigation
- [ ] Sidebar shows recent-first list and active chat state correctly
</verification>

<success_criteria>

- All tasks completed
- Sidebar, new chat, and active route behaviors work with real data
- Phase 3 can start from an already-functional shell and focus purely on streaming/message lifecycle
</success_criteria>

<output>
After completion, create `.planning/phases/02-conversational-shell-ui/02-03-SUMMARY.md`
</output>
