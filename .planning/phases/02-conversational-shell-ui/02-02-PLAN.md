---
phase: 02-conversational-shell-ui
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - apps/web/src/components/chat
  - apps/web/src/components/feedback
  - apps/web/src/features/composer
  - apps/web/src/store/ui
  - apps/web/src/routes/chat/$chatId.tsx
  - apps/web/src/routes/index.tsx
autonomous: true
requirements:
  - CHAT-01
  - MSG-01
  - REND-04
  - FB-01
user_setup: []
must_haves:
  truths:
    - A newly created chat can show an intentional empty conversation state.
    - Composer is multiline, keyboard-friendly, and ready for future streaming controls.
    - User, assistant, and system presentation rules are visible in the UI structure even before markdown/streaming lands.
  artifacts:
    - apps/web/src/features/composer
    - apps/web/src/components/chat
    - apps/web/src/store/ui
  key_links:
    - Composer draft is UI state, not query cache state.
    - Empty state components share the same visual language as the new shell.
---

<objective>
Implement the empty state experience, chat header, composer behavior, and draft state for the shell.

Purpose: Turn the shell scaffold into an actually usable conversation surface for starting chats and composing prompts.
Output: Empty conversation screen, onboarding prompts, composer interactions, role-aware message layout primitives, and UI-state-backed drafts.
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app.worktrees/codex/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app.worktrees/codex/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PRD.md
@.planning/TECH-DESIGN.md
@.planning/phases/02-conversational-shell-ui/02-CONTEXT.md
@.planning/phases/02-conversational-shell-ui/02-RESEARCH.md
@apps/web/src/routes/chat/$chatId.tsx
@apps/web/src/app/providers/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build empty conversation and onboarding surfaces</name>
  <files>apps/web/src/components/chat, apps/web/src/components/feedback, apps/web/src/routes/index.tsx, apps/web/src/routes/chat/$chatId.tsx</files>
  <read_first>.planning/PRD.md, .planning/phases/02-conversational-shell-ui/02-CONTEXT.md, apps/web/src/routes/index.tsx</read_first>
  <action>Create two related but distinct empty surfaces: onboarding root state and empty conversation state. Both should include product guidance and action affordances, but the chat route version must feel like the inside of an active conversation. Add quick prompt cards and a read-only model/status hint area consistent with the shell rules.</action>
  <verify>test -d apps/web/src/components/chat</verify>
  <acceptance_criteria>
    - root empty state shows examples and a start action
    - empty chat screen looks like a conversation surface, not a marketing hero
    - empty state copy and cards remain keyboard-focusable and visually consistent
  </acceptance_criteria>
  <done>Users have an intentional first-use and empty-chat experience rather than a blank pane.</done>
</task>

<task type="auto">
  <name>Task 2: Implement composer and route-safe draft state</name>
  <files>apps/web/src/features/composer, apps/web/src/store/ui, apps/web/src/routes/chat/$chatId.tsx</files>
  <read_first>.planning/phases/02-conversational-shell-ui/02-RESEARCH.md, .planning/TECH-DESIGN.md, apps/web/src/app/providers/index.tsx</read_first>
  <action>Build the prompt composer with multiline textarea, `Enter` send, `Shift+Enter` newline, capped auto-resize, and a visible submit button. Add a lightweight Zustand UI store for shell-only state such as draft-by-chat-id, sidebar open, and any local composer affordances. Add role-aware static message layout primitives so later streaming work has a stable rendering container to extend.</action>
  <verify>test -d apps/web/src/features/composer && test -d apps/web/src/store/ui</verify>
  <acceptance_criteria>
    - textarea supports multiline input and keyboard submission rules
    - draft state survives route changes for the same session as designed
    - message layout primitives visually distinguish `user`, `assistant`, and `system`
  </acceptance_criteria>
  <done>Conversation screen supports realistic prompt entry and empty-message presentation rules.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm lint && pnpm typecheck` exits 0
- [ ] composer keyboard rules work in manual smoke
- [ ] empty state and empty conversation state both render as designed
</verification>

<success_criteria>

- All tasks completed
- Users can start composing from a believable empty chat screen
- UI state is separated from server state in a way that won't fight later streaming work
</success_criteria>

<output>
After completion, create `.planning/phases/02-conversational-shell-ui/02-02-SUMMARY.md`
</output>
