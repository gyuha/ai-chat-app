---
phase: 02-conversational-shell-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/routes/__root.tsx
  - apps/web/src/routes/index.tsx
  - apps/web/src/routes/chat/$chatId.tsx
  - apps/web/src/app/router.tsx
  - apps/web/src/app/providers
  - apps/web/src/components/ui/sidebar.tsx
  - apps/web/src/components/shell
  - apps/web/src/styles/index.css
autonomous: true
requirements:
  - SHELL-01
  - SHELL-03
user_setup: []
must_haves:
  truths:
    - Desktop shell has a persistent sidebar and a main chat pane with stable spacing.
    - Mobile shell uses a sheet/trigger pattern instead of an always-visible sidebar.
    - The route tree includes a real `/chat/$chatId` screen scaffold, not a single smoke page.
  artifacts:
    - apps/web/src/routes/chat/$chatId.tsx
    - apps/web/src/components/shell
    - apps/web/src/styles/index.css
  key_links:
    - Existing SidebarProvider foundation is reused rather than replaced with ad-hoc state.
    - Root route and chat route share one shell language and theme token layer.
---

<objective>
Build the actual app shell, sidebar navigation scaffold, chat route scaffold, and theme tokens for the conversational UI.

Purpose: Replace the Phase 1 smoke layout with a reusable shell that looks and behaves like the product foundation.
Output: Desktop/mobile shell structure, chat route scaffold, sidebar navigation UI, and durable dark theme/layout rules.
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app.worktrees/codex/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app.worktrees/codex/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/PRD.md
@.planning/TECH-DESIGN.md
@.planning/STATE.md
@.planning/phases/02-conversational-shell-ui/02-CONTEXT.md
@.planning/phases/02-conversational-shell-ui/02-RESEARCH.md
@apps/web/src/components/ui/sidebar.tsx
@apps/web/src/routes/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace smoke layout with route-driven shell scaffold</name>
  <files>apps/web/src/routes/__root.tsx, apps/web/src/routes/index.tsx, apps/web/src/routes/chat/$chatId.tsx, apps/web/src/app/router.tsx, apps/web/src/components/shell</files>
  <read_first>.planning/phases/02-conversational-shell-ui/02-CONTEXT.md, .planning/PRD.md, .planning/TECH-DESIGN.md, apps/web/src/routes/index.tsx</read_first>
  <action>Create the real route scaffold for `/` and `/chat/$chatId`. Replace the Phase 1 smoke page with a reusable shell structure containing sidebar region, header region, main content container, and mobile sidebar trigger. Keep `/` as onboarding/redirect entry and add a dedicated chat route screen scaffold that later plans can hydrate with data and composer behavior.</action>
  <verify>test -f apps/web/src/routes/chat/\$chatId.tsx</verify>
  <acceptance_criteria>
    - `apps/web/src/routes/chat/$chatId.tsx` exists
    - root and chat routes both render within a shared shell layout
    - mobile entry includes a sidebar trigger rather than a permanently visible sidebar
  </acceptance_criteria>
  <done>Web app has a reusable conversational shell scaffold instead of a one-off smoke screen.</done>
</task>

<task type="auto">
  <name>Task 2: Introduce dark theme tokens and sidebar visual language</name>
  <files>apps/web/src/components/ui/sidebar.tsx, apps/web/src/components/shell, apps/web/src/styles/index.css</files>
  <read_first>.planning/PRD.md, .planning/phases/02-conversational-shell-ui/02-RESEARCH.md, apps/web/src/styles/index.css</read_first>
  <action>Define theme/layout tokens for sidebar surfaces, chat content width, header spacing, focus rings, and mobile-safe paddings. Rework the existing sidebar foundation so it supports the intended app navigation pattern: brand area, new chat CTA area, chat list container, and settings entry affordance. Preserve the current provider pattern and visible focus states.</action>
  <verify>test -f apps/web/src/components/ui/sidebar.tsx && test -f apps/web/src/styles/index.css</verify>
  <acceptance_criteria>
    - sidebar width and content max-width follow the phase context rules
    - visible focus styles remain present on new interactive elements
    - theme token changes do not reintroduce OpenRouter-specific browser calls or smoke-only copy
  </acceptance_criteria>
  <done>Shell visuals match the product direction closely enough that later phases can focus on behavior rather than layout replacement.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm lint && pnpm typecheck` exits 0
- [ ] route tree includes `/chat/$chatId`
- [ ] desktop/mobile shell renders without layout breakage
</verification>

<success_criteria>

- All tasks completed
- Web shell now resembles the intended conversational workspace
- Sidebar and route scaffold are stable enough for empty state and data hookup work
</success_criteria>

<output>
After completion, create `.planning/phases/02-conversational-shell-ui/02-01-SUMMARY.md`
</output>
