---
phase: 03-openrouter-streaming-chat
plan: "02"
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - frontend/src/features/chat/api.ts
  - frontend/src/features/chat/stream.ts
  - frontend/src/features/chat/types.ts
  - frontend/src/routes/index.tsx
  - frontend/tests/chat-streaming.test.tsx
autonomous: true
requirements:
  - CHAT-01
  - CHAT-02
  - CHAT-03
must_haves:
  truths:
    - "The selected conversation shell can send a user message and show assistant text incrementally while the response streams."
    - "The composer uses `Enter` to send and `Shift+Enter` to insert a newline."
    - "While streaming, the input remains editable but additional send actions are blocked until completion."
  artifacts:
    - path: frontend/src/features/chat/api.ts
      provides: "Chat send request helper for the authenticated backend proxy."
    - path: frontend/src/routes/index.tsx
      provides: "Conversation shell expanded into a streaming chat panel and composer."
    - path: frontend/tests/chat-streaming.test.tsx
      provides: "Frontend verification for streaming rendering, send-key behavior, and disabled-send during stream."
  key_links:
    - from: frontend/src/features/chat/api.ts
      to: frontend/src/lib/api/client.ts
      via: "shared credentialed request boundary"
      pattern: "credentials: 'include'"
    - from: frontend/src/routes/index.tsx
      to: backend chat contract
      via: "conversation chat send"
      pattern: "/chat"
    - from: frontend/src/routes/index.tsx
      to: frontend/src/features/conversations/query.ts
      via: "post-stream refresh into canonical conversation state"
      pattern: "conversationsQueryKey"
---

<objective>
Connect the backend chat proxy to the protected conversation shell and render a streaming chat experience.

Purpose: turn the Phase 2 conversation list into a usable MVP chat surface that streams assistant output live and refreshes persisted history after completion.
Output: chat request helpers, streaming reader logic, composer behavior, and frontend tests for send/stream/update flow.
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-openrouter-streaming-chat/03-CONTEXT.md
@.planning/phases/03-openrouter-streaming-chat/03-RESEARCH.md
@.planning/phases/03-openrouter-streaming-chat/03-VALIDATION.md
@.planning/phases/03-openrouter-streaming-chat/03-01-PLAN.md
@frontend/src/routes/index.tsx
@frontend/src/lib/api/client.ts
@frontend/src/features/conversations/api.ts
@frontend/src/features/conversations/query.ts
@frontend/tests/conversation-routing.test.tsx

<interfaces>
Frontend contracts to create and use:

```ts
type SendChatMessagePayload = {
  content: string;
}

type StreamDelta = {
  type: "delta" | "done" | "error";
  content?: string;
  message?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add frontend chat request and stream reader helpers</name>
  <files>frontend/src/features/chat/api.ts, frontend/src/features/chat/stream.ts, frontend/src/features/chat/types.ts</files>
  <read_first>
    - frontend/src/lib/api/client.ts
    - frontend/src/features/conversations/api.ts
    - frontend/src/features/conversations/query.ts
    - .planning/phases/03-openrouter-streaming-chat/03-RESEARCH.md
    - .planning/phases/03-openrouter-streaming-chat/03-CONTEXT.md
  </read_first>
  <action>Create `frontend/src/features/chat/types.ts` with the chat send payload and stream event types. Create `frontend/src/features/chat/api.ts` with a helper that starts `POST /conversations/:id/chat` using `fetch` or a thin wrapper that preserves `credentials: 'include'`. Create `frontend/src/features/chat/stream.ts` with a browser stream reader that consumes `response.body`, decodes chunks incrementally, and surfaces normalized `{ type: 'delta' | 'done' | 'error' }` events. Do not add local persistence or alternate model selection. Keep the helper focused on the authenticated backend proxy contract only.</action>
  <acceptance_criteria>
    - `frontend/src/features/chat/api.ts` contains `/conversations/${conversationId}/chat` or equivalent conversation chat path.
    - `frontend/src/features/chat/stream.ts` contains `TextDecoder` and `getReader`.
    - `frontend/src/features/chat/api.ts` contains `credentials: 'include'` directly or calls a helper that guarantees it.
    - `frontend/src/features/chat/types.ts` contains `type: "delta" | "done" | "error"`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "/chat|TextDecoder|getReader|delta|done|error|credentials: 'include'" frontend/src/features/chat frontend/src/lib/api/client.ts</automated>
  </verify>
  <done>The frontend has a reusable transport layer for chat streaming that matches the backend contract and current cookie-auth boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the protected conversation shell into a streaming chat panel</name>
  <files>frontend/src/routes/index.tsx, frontend/tests/chat-streaming.test.tsx</files>
  <read_first>
    - frontend/src/routes/index.tsx
    - frontend/tests/conversation-routing.test.tsx
    - frontend/src/features/conversations/query.ts
    - frontend/src/features/chat/api.ts
    - frontend/src/features/chat/stream.ts
    - .planning/phases/03-openrouter-streaming-chat/03-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: pressing `Enter` sends the message, but `Shift+Enter` inserts a newline.
    - Test 2: while streaming, the input remains editable and the send action is disabled.
    - Test 3: assistant text appears incrementally before final refresh.
    - Test 4: when the stream completes, the selected conversation refreshes into canonical persisted state.
  </behavior>
  <action>Create `frontend/tests/chat-streaming.test.tsx` with mocked chat stream chunks and conversation-detail refresh behavior. Then extend `frontend/src/routes/index.tsx` to render a minimal message panel for the selected conversation, a textarea composer, and a send button. Use `Enter` to submit and `Shift+Enter` to newline exactly as decided in CONTEXT.md. Keep the input editable during streaming, but block additional submit actions while `isStreaming` is true. Append streaming assistant deltas to temporary local UI state, and after `done`, invalidate or refetch the selected conversation query so persisted `Message` rows become the canonical display. Do not add timestamps, markdown extras, attachments, or history-restoration UI beyond the active selected conversation.</action>
  <acceptance_criteria>
    - `frontend/src/routes/index.tsx` contains key handling for `Enter` and `Shift+Enter`.
    - `frontend/src/routes/index.tsx` contains an `isStreaming` gate that blocks submit while leaving the input enabled.
    - `frontend/tests/chat-streaming.test.tsx` contains assertions for incremental assistant rendering and blocked re-submit during stream.
    - `rg -n "attachment|model selector|timestamp|preview" frontend/src/routes/index.tsx frontend/tests/chat-streaming.test.tsx` returns no matches for new scope creep.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir frontend test -- chat-streaming.test.tsx && pnpm --dir frontend typecheck && rg -n "Enter|Shift\\+Enter|isStreaming" frontend/src/routes/index.tsx frontend/tests/chat-streaming.test.tsx</automated>
  </verify>
  <done>The protected conversation route becomes a real streaming chat UI with the exact composer behavior decided in discuss-phase and a clean refresh back into server-persisted state after completion.</done>
</task>

</tasks>

<verification>
Run the targeted frontend chat-streaming test and frontend typecheck. Confirm the selected conversation can send a message, show assistant output incrementally, keep the input editable while streaming, and refresh to persisted history after completion.
</verification>

<success_criteria>
Phase 3 frontend work is complete when the current protected conversation shell supports streaming message send/receive behavior, honors the locked composer interaction rules, and syncs back to persisted server history after the stream finishes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-openrouter-streaming-chat/03-02-SUMMARY.md`
</output>
