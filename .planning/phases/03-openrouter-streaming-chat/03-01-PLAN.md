---
phase: 03-openrouter-streaming-chat
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/conversations/conversations.controller.ts
  - backend/src/conversations/conversations.service.ts
  - backend/src/conversations/openrouter-chat.service.ts
  - backend/src/conversations/dto/send-message.dto.ts
  - backend/src/conversations/dto/conversation-message.dto.ts
  - backend/src/conversations/dto/conversation-detail.dto.ts
  - backend/src/conversations/conversations.module.ts
  - backend/test/conversations-chat.e2e-spec.ts
autonomous: true
requirements:
  - CHAT-01
  - CHAT-03
must_haves:
  truths:
    - "An authenticated send request proxies to the fixed backend `OPENROUTER_MODEL` and never exposes the API key to the browser."
    - "A user message is persisted before the upstream call, and the assistant message is persisted exactly once after successful stream completion."
    - "Conversation reads and chat writes remain ownership-scoped by `userId`."
  artifacts:
    - path: backend/src/conversations/openrouter-chat.service.ts
      provides: "OpenRouter proxy, SSE parsing, and final assistant persistence."
    - path: backend/src/conversations/conversations.controller.ts
      provides: "Authenticated chat send endpoint and active-conversation detail contract."
    - path: backend/test/conversations-chat.e2e-spec.ts
      provides: "Backend verification for proxy request shaping, stream relay, and final persistence."
  key_links:
    - from: backend/src/conversations/conversations.controller.ts
      to: backend/src/auth/decorators/current-user.decorator.ts
      via: "authenticated chat user extraction"
      pattern: "@CurrentUser()"
    - from: backend/src/conversations/openrouter-chat.service.ts
      to: backend/src/config/env.schema.ts
      via: "backend-only OpenRouter env dependency"
      pattern: "OPENROUTER_API_KEY"
    - from: backend/src/conversations/conversations.service.ts
      to: backend/prisma/schema.prisma
      via: "message persistence"
      pattern: "role           MessageRole"
---

<objective>
Implement the backend OpenRouter proxy, streaming relay, and final assistant-message persistence.

Purpose: establish a secure server-side chat contract that Phase 3 frontend work can stream from without exposing provider credentials or breaking conversation ownership guarantees.
Output: authenticated chat endpoint, OpenRouter streaming service, active-conversation message read contract, and backend e2e coverage.
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-openrouter-streaming-chat/03-CONTEXT.md
@.planning/phases/03-openrouter-streaming-chat/03-RESEARCH.md
@.planning/phases/03-openrouter-streaming-chat/03-VALIDATION.md
@backend/src/config/env.schema.ts
@backend/src/conversations/conversations.controller.ts
@backend/src/conversations/conversations.service.ts
@backend/test/conversations.e2e-spec.ts

<interfaces>
Backend contracts to create or extend:

```ts
type SendMessageDto = {
  content: string;
}

type ConversationMessageDto = {
  id: string;
  role: "user" | "assistant";
  content: string;
}

type ConversationDetailDto = {
  id: string;
  title: string;
  messages: ConversationMessageDto[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add backend chat contract tests and active-conversation detail shape</name>
  <files>backend/test/conversations-chat.e2e-spec.ts, backend/src/conversations/dto/send-message.dto.ts, backend/src/conversations/dto/conversation-message.dto.ts, backend/src/conversations/dto/conversation-detail.dto.ts, backend/src/conversations/conversations.service.ts, backend/src/conversations/conversations.controller.ts</files>
  <read_first>
    - backend/test/conversations.e2e-spec.ts
    - backend/src/conversations/conversations.controller.ts
    - backend/src/conversations/conversations.service.ts
    - backend/src/config/env.schema.ts
    - .planning/phases/03-openrouter-streaming-chat/03-CONTEXT.md
    - .planning/phases/03-openrouter-streaming-chat/03-RESEARCH.md
    - .planning/phases/03-openrouter-streaming-chat/03-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: `POST /conversations/:id/chat` by an authenticated owner sends the user content to the backend chat service and returns a streaming response.
    - Test 2: successful stream completion creates one persisted assistant message in the same conversation.
    - Test 3: `GET /conversations/:id` for the owner returns title plus chronological `messages`.
    - Test 4: foreign-user chat send or detail access returns `404`.
  </behavior>
  <action>Create `backend/test/conversations-chat.e2e-spec.ts` by extending the existing conversation e2e setup with a mocked upstream OpenRouter stream. Add DTOs `SendMessageDto`, `ConversationMessageDto`, and `ConversationDetailDto`. Extend `ConversationsService` with an ownership-safe detail reader that returns `id`, `title`, and `messages` ordered by `createdAt asc`, each message including exact `role` and `content`. Extend `ConversationsController` with a detail route contract that returns this DTO and with a `POST /conversations/:id/chat` shape expected by the tests. Keep ownership enforcement on both routes via `userId` + `conversationId` scoping and `404` on mismatch.</action>
  <acceptance_criteria>
    - `backend/test/conversations-chat.e2e-spec.ts` contains `POST /conversations/:id/chat`, `GET /conversations/:id`, and `.expect(404)`.
    - `backend/src/conversations/dto/send-message.dto.ts` contains `content!: string` or validated equivalent.
    - `backend/src/conversations/conversations.service.ts` contains a detail reader with `orderBy: { createdAt: 'asc' }`.
    - `pnpm --dir backend test:e2e -- --runInBand conversations-chat.e2e-spec.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir backend test:e2e -- --runInBand conversations-chat.e2e-spec.ts</automated>
  </verify>
  <done>The backend contract for active-conversation read and chat send is pinned by tests before the OpenRouter proxy implementation is finalized.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the OpenRouter streaming proxy and final assistant persistence</name>
  <files>backend/src/conversations/openrouter-chat.service.ts, backend/src/conversations/conversations.module.ts, backend/src/conversations/conversations.controller.ts, backend/src/conversations/conversations.service.ts</files>
  <read_first>
    - backend/src/conversations/conversations.module.ts
    - backend/src/conversations/conversations.controller.ts
    - backend/src/conversations/conversations.service.ts
    - backend/src/auth/guards/jwt-auth.guard.ts
    - backend/src/auth/decorators/current-user.decorator.ts
    - backend/src/config/env.schema.ts
    - backend/test/conversations-chat.e2e-spec.ts
    - .planning/phases/03-openrouter-streaming-chat/03-RESEARCH.md
  </read_first>
  <action>Create `backend/src/conversations/openrouter-chat.service.ts` that sends `POST https://openrouter.ai/api/v1/chat/completions` with `Authorization: Bearer ${OPENROUTER_API_KEY}`, `model: process.env.OPENROUTER_MODEL`, `messages` built from the current conversation history plus the new user message, and `stream: true`. Persist the user message before calling OpenRouter. Parse streamed SSE events, ignore comment frames like `: OPENROUTER PROCESSING`, forward only assistant text deltas to the client response, and detect top-level stream `error` payloads or `finish_reason: "error"`. On successful completion, persist exactly one assistant message with the accumulated final content. Do not expose the provider API key, do not write assistant rows per token, and do not allow chat sends for foreign conversations. Register the service in `ConversationsModule` and wire `POST /conversations/:id/chat` to it.</action>
  <acceptance_criteria>
    - `backend/src/conversations/openrouter-chat.service.ts` contains `https://openrouter.ai/api/v1/chat/completions`.
    - `backend/src/conversations/openrouter-chat.service.ts` contains `stream: true`.
    - `backend/src/conversations/openrouter-chat.service.ts` contains logic for `finish_reason` and top-level `error`.
    - `backend/src/conversations/conversations.controller.ts` contains `@Post(':id/chat')`.
    - `rg -n "OPENROUTER_API_KEY|OPENROUTER_MODEL|stream: true|finish_reason" backend/src/conversations backend/src/config/env.schema.ts` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir backend test:e2e -- --runInBand conversations-chat.e2e-spec.ts && rg -n "OPENROUTER_API_KEY|OPENROUTER_MODEL|stream: true|@Post\\(':id/chat'\\)" backend/src/conversations backend/src/config/env.schema.ts</automated>
  </verify>
  <done>The backend can securely proxy a chat request to OpenRouter, stream assistant deltas back to the browser, and persist the final assistant message once the stream completes.</done>
</task>

</tasks>

<verification>
Run the backend chat e2e suite. Confirm the chat endpoint is authenticated, uses backend-only OpenRouter env vars, persists the user message before upstream call, and saves exactly one assistant message after successful stream completion.
</verification>

<success_criteria>
Phase 3 backend groundwork is complete when the server can proxy an authenticated chat request to the fixed OpenRouter model, stream assistant chunks back to the client, and persist the final assistant output without breaking ownership boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/03-openrouter-streaming-chat/03-01-SUMMARY.md`
</output>
