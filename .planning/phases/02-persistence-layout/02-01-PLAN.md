---
phase: "02-persistence-layout"
plan: "01"
type: "execute"
wave: 1
depends_on:
  - "01-01"
files_modified:
  - src/db/index.ts
  - src/hooks/use-db.ts
  - src/stores/ui-store.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-02
must_haves:
  truths:
    - "IndexedDB의 conversations, messages 테이블을 liveQuery로 구독할 수 있다"
    - "Zustand UI store로 사이드바 열림/닫힘 상태를 관리할 수 있다"
    - "대화 생성, 메시지 추가, 대화 삭제가 가능하다"
  artifacts:
    - path: "src/hooks/use-db.ts"
      provides: "Dexie liveQuery reactive hooks"
      exports: ["useConversations", "useMessages", "createConversation", "addMessage", "deleteConversation", "useSetting"]
    - path: "src/stores/ui-store.ts"
      provides: "Zustand UI state (sidebarOpen, currentConversationId)"
      exports: ["useUIStore"]
  key_links:
    - from: "src/hooks/use-db.ts"
      to: "src/db/index.ts"
      via: "liveQuery() 구독"
    - from: "src/stores/ui-store.ts"
      to: "src/components/sidebar.tsx"
      via: "sidebarOpen 상태"
---

<objective>
Wave 1: IndexedDB Persistence - Dexie.js liveQuery reactive hooks와 UI state store를 구축한다.
Purpose: Phase 2의 모든后续计划가 의존하는 핵심 infrastructure 구축
Output: use-db.ts (liveQuery hooks), ui-store.ts (sidebar state)
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app.worktrees/front-chat-minimax/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app.worktrees/front-chat-minimax/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-01-PLAN.md
@.planning/phases/02-persistence-layout/02-RESEARCH.md
</context>

<interfaces>
From src/db/index.ts (Phase 1 artifact, depends on 01-01):
```typescript
export interface Setting {
  key: string
  value: string
}

export interface Conversation {
  id?: number
  title: string
  createdAt: number
  updatedAt: number
}

export interface Message {
  id?: number
  conversationId: number
  role: "user" | "assistant" | "system"
  content: string
  createdAt: number
}

export class ChatDatabase extends Dexie {
  settings!: Table<Setting>
  conversations!: Table<Conversation>
  messages!: Table<Message>
}

export const db: ChatDatabase
export async function getSetting(key: string): Promise<string | undefined>
export async function setSetting(key: string, value: string): Promise<void>
export async function deleteSetting(key: string): Promise<void>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1.1: Dexie.js liveQuery React Hooks</name>
  <files>
    src/hooks/use-db.ts
  </files>
  <read_first>
    src/db/index.ts
    src/stores/chat-store.ts
  </read_first>
  <action>
Dexie.js liveQuery를 React에서 reactive하게 구독하기 위한 hooks를 생성한다.

**`src/hooks/use-db.ts`** 생성:
```typescript
import { useSyncExternalStore } from "react"
import { liveQuery } from "dexie"
import { db, type Conversation, type Message } from "@/db"

// Generic liveQuery bridge for React
function useLiveQuery<T>(
  query: () => T | Promise<T>,
  deps: unknown[] = []
): T | undefined {
  return useSyncExternalStore(
    (onStoreChange) => {
      const subscription = liveQuery(query).subscribe({
        next: onStoreChange,
        error: (err) => console.error("LiveQuery error:", err),
      })
      return () => subscription.unsubscribe()
    },
    () => query(), // Client snapshot
    () => query()  // SSR snapshot (same in SPA)
  )
}

// Conversations list (sorted by updatedAt desc)
export function useConversations(): Conversation[] {
  return useLiveQuery(
    () => db.conversations.orderBy("updatedAt").reverse().toArray(),
    []
  ) ?? []
}

// Messages for a specific conversation (sorted by createdAt asc)
export function useMessages(conversationId: number | null): Message[] {
  return useLiveQuery(
    () =>
      conversationId
        ? db.messages
            .where("conversationId")
            .equals(conversationId)
            .sortBy("createdAt")
        : [],
    [conversationId]
  ) ?? []
}

// Single conversation by ID
export function useConversation(id: number | null): Conversation | undefined {
  return useLiveQuery(
    () => (id ? db.conversations.get(id) : undefined),
    [id]
  )
}

// Setting value by key
export function useSetting(key: string): string | undefined {
  return useLiveQuery(
    async () => {
      const setting = await db.settings.get(key)
      return setting?.value
    },
    [key]
  )
}

// Create new conversation
export async function createConversation(
  title: string = "새 대화"
): Promise<number> {
  const id = await db.conversations.add({
    title,
    createdAt: Date.now(),
    updatedAt: Date.now(),
  })
  return id as number
}

// Add message to conversation
export async function addMessage(
  conversationId: number,
  role: Message["role"],
  content: string
): Promise<void> {
  await db.messages.add({
    conversationId,
    role,
    content,
    createdAt: Date.now(),
  })
  // Update conversation's updatedAt for sorting
  await db.conversations.update(conversationId, { updatedAt: Date.now() })
}

// Delete conversation and its messages
export async function deleteConversation(id: number): Promise<void> {
  await db.transaction("rw", db.conversations, db.messages, async () => {
    await db.messages.where("conversationId").equals(id).delete()
    await db.conversations.delete(id)
  })
}

// Update conversation title
export async function updateConversationTitle(
  id: number,
  title: string
): Promise<void> {
  await db.conversations.update(id, { title, updatedAt: Date.now() })
}
```

**설명:**
- `useSyncExternalStore`는 Dexie의 TC39 Observable을 React state로 변환
- `liveQuery`는 DB 변경 시 자동으로 re-render 트리거
- `useConversations`는 updatedAt 역순으로 정렬 (최신 대화가 상단)
- `useMessages`는 conversationId로 필터링 후 createdAt 순서로 정렬
</action>
  <verify>
    <automated>cat src/hooks/use-db.ts | head -40</automated>
  </verify>
  <done>Dexie liveQuery hooks가 생성됨: useConversations, useMessages, useConversation, useSetting, createConversation, addMessage, deleteConversation, updateConversationTitle</done>
</task>

<task type="auto">
  <name>Task 1.2: Zustand UI Store (Sidebar State)</name>
  <files>
    src/stores/ui-store.ts
  </files>
  <read_first>
    src/stores/chat-store.ts (Phase 1 artifact, pattern reference)
  </read_first>
  <action>
Zustand를 사용한 UI 상태 관리 store를 생성한다. 사이드바 열림/닫힘 상태와 현재 선택된 대화를 관리한다.

**`src/stores/ui-store.ts`** 생성:
```typescript
import { create } from "zustand"

interface UIState {
  // Sidebar visibility (mobile)
  sidebarOpen: boolean
  // Current active conversation ID
  currentConversationId: number | null

  // Actions
  setSidebarOpen: (open: boolean) => void
  toggleSidebar: () => void
  setCurrentConversation: (id: number | null) => void
  closeSidebar: () => void
}

export const useUIStore = create<UIState>((set) => ({
  sidebarOpen: false,
  currentConversationId: null,

  setSidebarOpen: (open) => set({ sidebarOpen: open }),

  toggleSidebar: () =>
    set((state) => ({ sidebarOpen: !state.sidebarOpen })),

  setCurrentConversation: (id) =>
    set({ currentConversationId: id, sidebarOpen: false }),

  closeSidebar: () => set({ sidebarOpen: false }),
}))
```

**설명:**
- `sidebarOpen`: 모바일에서 사이드바 overlay 표시 여부
- `currentConversationId`: 현재 선택된 대화 ID (null = 새 대화)
- `setCurrentConversation`: 대화 선택 시 사이드바를 자동으로 닫음 (모바일 UX)
- `closeSidebar`: 사이드바를 닫는 편의 메서드
</action>
  <verify>
    <automated>cat src/stores/ui-store.ts | head -20</automated>
  </verify>
  <done>Zustand UI store가 생성됨: sidebarOpen, currentConversationId, setSidebarOpen, toggleSidebar, setCurrentConversation, closeSidebar</done>
</task>

</tasks>

<verification>
- Phase 1의 `pnpm typecheck`가 먼저 통과해야 함
- `cat src/hooks/use-db.ts | grep "useSyncExternalStore"` 확인
- `cat src/stores/ui-store.ts | grep "create"` 확인
</verification>

<success_criteria>
Wave 1 완료 시:
- use-db.ts의 liveQuery hooks가 conversations, messages 테이블을 reactive하게 구독
- ui-store.ts가 sidebarOpen, currentConversationId 상태를 관리
- Phase 2의后续计划(UI layout)에서 사용 가능
</success_criteria>

<output>
After completion, create `.planning/phases/02-persistence-layout/02-01-SUMMARY.md`
</output>
