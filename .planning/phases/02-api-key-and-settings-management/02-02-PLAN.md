---
phase: 02-api-key-and-settings-management
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/hooks/use-api-key-validation.ts
  - src/hooks/use-free-models-query.ts
  - src/hooks/use-settings-query.ts
  - src/lib/openrouter-client.ts
  - src/lib/settings-service.ts
autonomous: true
requirements:
  - SETT-03
  - SETT-04
  - DATA-02
must_haves:
  truths:
    - OpenRouter models endpoint 호출과 free model filtering 규칙이 재사용 가능한 service/hook으로 분리된다.
    - API 키 검증 결과는 `invalid_credentials`와 `transient` 두 가지 범주로만 매핑된다.
    - 저장된 default model이 무료 모델 목록과 맞지 않으면 자동으로 null 처리된다.
  artifacts:
    - src/lib/openrouter-client.ts
    - src/lib/settings-service.ts
    - src/hooks/use-api-key-validation.ts
    - src/hooks/use-free-models-query.ts
    - src/hooks/use-settings-query.ts
  key_links:
    - src/lib/openrouter-client.ts가 `/models` 호출과 무료 모델 필터를 담당한다.
    - src/lib/settings-service.ts가 Dexie settings read/write와 invalid default model cleanup을 담당한다.
    - hooks layer가 onboarding과 settings UI에서 동일한 정책을 공유한다.
---

<objective>
API 키 검증과 기본 설정 저장에 필요한 domain service/hook 계층을 만든다.

Purpose: onboarding과 settings 화면이 같은 정책과 같은 저장소를 사용하도록 business rule을 UI 밖으로 분리한다.
Output: OpenRouter client, settings service, validation/model/settings hooks.
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app.worktrees/frontend-codex/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app.worktrees/frontend-codex/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-api-key-and-settings-management/02-CONTEXT.md
@src/lib/app-db.ts
@src/components/chat/api-key-onboarding-card.tsx
@src/components/settings/settings-panel-placeholder.tsx
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: OpenRouter models client와 API 키 검증 mutation을 만든다</name>
  <files>src/lib/openrouter-client.ts, src/hooks/use-api-key-validation.ts</files>
  <read_first>src/lib/app-db.ts, .planning/phases/02-api-key-and-settings-management/02-CONTEXT.md, .planning/REQUIREMENTS.md, src/components/chat/api-key-onboarding-card.tsx</read_first>
  <action>`src/lib/openrouter-client.ts`에 `https://openrouter.ai/api/v1/models`를 호출하는 함수들을 추가한다. 헤더는 `Authorization: Bearer <key>`를 기본으로 넣고, direct-browser 사용을 위해 `HTTP-Referer`는 `window.location.origin`, `X-OpenRouter-Title`은 정확히 `OpenRouter Chat`으로 보낸다. 응답 `data[]`에서 무료 모델은 `pricing.prompt === "0"` && `pricing.completion === "0"` 규칙으로 필터링한다. `src/hooks/use-api-key-validation.ts`에는 validation mutation을 만들어 성공 시 free model 목록과 함께 결과를 반환하고, 실패는 HTTP 401/403 또는 empty data를 `invalid_credentials`, 나머지 fetch/network/5xx는 `transient`로만 매핑한다. 자동 debounce, background retry, optimistic save는 넣지 않는다.</action>
  <verify>pnpm build</verify>
  <acceptance_criteria>
    - src/lib/openrouter-client.ts contains `https://openrouter.ai/api/v1/models`
    - src/lib/openrouter-client.ts contains `HTTP-Referer`
    - src/lib/openrouter-client.ts contains `X-OpenRouter-Title`
    - src/lib/openrouter-client.ts contains `pricing.prompt === '0'`
    - src/lib/openrouter-client.ts contains `pricing.completion === '0'`
    - src/hooks/use-api-key-validation.ts contains `invalid_credentials`
    - src/hooks/use-api-key-validation.ts contains `transient`
  </acceptance_criteria>
  <done>UI가 호출할 수 있는 검증 mutation과 무료 모델 필터 로직이 완성된다.</done>
</task>

<task type="auto">
  <name>Task 2: settings read/write service와 free models query를 만든다</name>
  <files>src/lib/settings-service.ts, src/hooks/use-settings-query.ts, src/hooks/use-free-models-query.ts</files>
  <read_first>src/lib/app-db.ts, .planning/phases/02-api-key-and-settings-management/02-CONTEXT.md, src/providers/app-query-provider.tsx, src/routes/settings.tsx</read_first>
  <action>`src/lib/settings-service.ts`에는 Dexie 기반 `getSettings`, `saveApiKeyAfterValidation`, `replaceApiKeyAfterValidation`, `deleteApiKey`, `setDefaultModelId`, `setDefaultSystemPrompt`, `clearInvalidDefaultModel` helper를 추가한다. save helpers는 validation 성공 후에만 key를 저장하게 하고, delete는 settings row를 즉시 제거하도록 구현한다. `src/hooks/use-settings-query.ts`는 saved settings를 읽어 onboarding/settings route에서 공유할 수 있게 하고, `src/hooks/use-free-models-query.ts`는 saved key가 있을 때만 활성화되는 query로 구성하며 `staleTime: 600000`을 사용한다. stored `defaultModelId`가 현재 free model 목록에 없으면 service를 통해 null로 되돌리고 UI에는 '기본값 없음' 상태를 전달한다.</action>
  <verify>pnpm build</verify>
  <acceptance_criteria>
    - src/lib/settings-service.ts contains `saveApiKeyAfterValidation`
    - src/lib/settings-service.ts contains `replaceApiKeyAfterValidation`
    - src/lib/settings-service.ts contains `deleteApiKey`
    - src/lib/settings-service.ts contains `clearInvalidDefaultModel`
    - src/hooks/use-free-models-query.ts contains `staleTime: 600000`
    - src/hooks/use-free-models-query.ts contains `enabled:`
    - src/hooks/use-settings-query.ts contains `getSettings`
  </acceptance_criteria>
  <done>onboarding과 settings 화면이 동일한 settings/query 계층을 사용할 수 있다.</done>
</task>

</tasks>

<verification>
- `pnpm build`가 새 service/hook 계층 추가 후 통과한다.
- free model filtering 규칙과 validation error mapping이 코드에 명시된다.
- default model invalidation cleanup 경로가 service에 존재한다.
</verification>

<success_criteria>
- Phase 2의 핵심 정책이 UI 컴포넌트가 아니라 service/hook 계층에 고정된다.
- `/`와 `/settings`가 같은 validation/save/delete semantics를 재사용할 수 있다.
- Phase 3의 모델 선택 UI가 재사용할 free models query 기반을 미리 갖게 된다.
</success_criteria>

<output>
After completion, create `.planning/phases/02-api-key-and-settings-management/02-02-SUMMARY.md`
</output>
