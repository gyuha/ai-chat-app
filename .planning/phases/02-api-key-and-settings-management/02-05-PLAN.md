---
phase: 02-api-key-and-settings-management
plan: 05
type: execute
wave: 4
depends_on:
  - "03"
  - "04"
files_modified:
  - src/components/chat/api-key-onboarding-card.test.tsx
  - src/routes/index.test.tsx
  - src/routes/settings.test.tsx
  - src/test/test-utils.ts
autonomous: true
requirements:
  - SETT-01
  - SETT-02
  - SETT-03
  - SETT-04
  - DATA-02
must_haves:
  truths:
    - 성공 시에만 key가 저장되고 실패 시 draft만 남는 정책이 테스트로 고정된다.
    - `/`와 `/settings`의 상태 전환, 삭제 흐름, default model/system prompt persistence가 자동 검증된다.
    - Phase 2 구현이 build와 test를 함께 통과한다.
  artifacts:
    - src/components/chat/api-key-onboarding-card.test.tsx
    - src/routes/index.test.tsx
    - src/routes/settings.test.tsx
    - src/test/test-utils.ts
  key_links:
    - onboarding/settings route tests가 동일한 settings/query helpers를 사용해 phase policy를 검증한다.
    - test utils가 IndexedDB reset, fetch mock reset, route render helper를 제공한다.
---

<objective>
Phase 2 정책을 integration test로 고정한다.

Purpose: 실행 단계에서 검증 누락 없이 save/replace/delete/default persistence 흐름을 자동화하고, 이후 phase에서 regression을 막는다.
Output: onboarding/settings integration tests, reusable test helpers, passing verification commands.
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app.worktrees/frontend-codex/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app.worktrees/frontend-codex/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-api-key-and-settings-management/02-CONTEXT.md
@.planning/phases/01-app-shell-and-interface-foundation/01-VALIDATION.md
@src/test/setup.ts
@src/test/test-utils.ts
@src/routes/index.tsx
@src/routes/settings.tsx
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: onboarding과 settings route의 핵심 상태 전환을 integration test로 만든다</name>
  <files>src/components/chat/api-key-onboarding-card.test.tsx, src/routes/index.test.tsx, src/routes/settings.test.tsx, src/test/test-utils.ts</files>
  <read_first>src/test/setup.ts, src/test/test-utils.ts, src/routes/index.tsx, src/routes/settings.tsx, .planning/phases/02-api-key-and-settings-management/02-CONTEXT.md</read_first>
  <action>Vitest + Testing Library 기반으로 mock fetch와 fake IndexedDB를 사용하는 integration tests를 추가한다. onboarding 테스트는 성공 응답에서만 API key가 저장되고 same-route empty state로 전환되는지, invalid/transient 실패에서는 기존 saved key를 유지하고 input draft가 그대로 남는지 검증한다. settings route 테스트는 key replacement success-only 정책, delete confirmation 후 즉시 삭제, default model 선택 즉시 저장, stored invalid model fallback, system prompt onChange persistence를 모두 검증한다. `src/test/test-utils.ts`에는 IndexedDB reset helper, fetch mock helper, Router rendering helper를 추가하되 theme store reset도 계속 제공한다.</action>
  <verify>pnpm test</verify>
  <acceptance_criteria>
    - src/components/chat/api-key-onboarding-card.test.tsx contains `입력한 키로 모델 목록을 불러오지 못했습니다.`
    - src/routes/index.test.tsx contains `첫 대화를 시작해 보세요`
    - src/routes/settings.test.tsx contains `API 키 삭제`
    - src/routes/settings.test.tsx contains `기본값은 아직 선택되지 않았습니다.`
    - src/routes/settings.test.tsx contains `defaultSystemPrompt`
    - src/test/test-utils.ts contains `reset`
  </acceptance_criteria>
  <done>Phase 2의 주요 정책이 route-level integration tests로 재현 가능해진다.</done>
</task>

<task type="auto">
  <name>Task 2: build/lint/test verification을 묶어 Phase 2 완료 조건을 닫는다</name>
  <files>src/components/chat/api-key-onboarding-card.test.tsx, src/routes/index.test.tsx, src/routes/settings.test.tsx</files>
  <read_first>package.json, src/components/chat/api-key-onboarding-card.test.tsx, src/routes/index.test.tsx, src/routes/settings.test.tsx</read_first>
  <action>verification command는 정확히 `pnpm test`, `pnpm build`, `pnpm biome check .` 세 가지를 사용한다. 테스트는 network success/invalid/transient 세 경로와 delete/default prompt persistence를 모두 포함해야 하고, flaky timer dependency 없이 deterministic fetch mocks로 구성한다. build/lint가 통과하지 않으면 구현을 조정하되 Phase 1에서 이미 남아 있던 shadcn upstream warning 외에 새 warning/error를 추가하지 않는다.</action>
  <verify>pnpm test && pnpm build && pnpm biome check .</verify>
  <acceptance_criteria>
    - package.json contains `"test": "vitest run"`
    - src/routes/index.test.tsx contains `render`
    - src/routes/settings.test.tsx contains `waitFor`
    - src/components/chat/api-key-onboarding-card.test.tsx contains `userEvent`
  </acceptance_criteria>
  <done>Phase 2 실행 완료 시 테스트, 빌드, lint 명령으로 요구사항 충족을 반복 검증할 수 있다.</done>
</task>

</tasks>

<verification>
- `pnpm test`가 onboarding/settings integration tests를 포함해 통과한다.
- `pnpm build`가 settings persistence와 route branching 추가 후 통과한다.
- `pnpm biome check .`가 새 테스트/feature 코드까지 포함해 통과한다.
</verification>

<success_criteria>
- Phase 2 요구사항 전체가 자동 테스트와 build/lint 명령으로 다시 검증 가능하다.
- 저장/검증/삭제/복구 정책이 이후 phase 변경에도 쉽게 깨지지 않는다.
- executor는 Phase 2 완료 여부를 명령 실행 결과로 명확히 판단할 수 있다.
</success_criteria>

<output>
After completion, create `.planning/phases/02-api-key-and-settings-management/02-05-SUMMARY.md`
</output>
