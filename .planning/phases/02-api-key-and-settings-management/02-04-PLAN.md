---
phase: 02-api-key-and-settings-management
plan: 04
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - src/components/settings/settings-panel-placeholder.tsx
  - src/components/ui/alert-dialog.tsx
  - src/components/ui/select.tsx
  - src/routes/settings.tsx
autonomous: true
requirements:
  - SETT-02
  - SETT-04
  - DATA-02
must_haves:
  truths:
    - `/settings`에서 API 키 저장/교체/삭제와 기본 모델/시스템 프롬프트 저장을 모두 수행할 수 있다.
    - API 키 삭제는 확인 다이얼로그 후 즉시 적용되고, route는 그대로 유지된다.
    - default model 선택은 saved key가 있을 때만 가능한 free model 목록을 사용하고, invalid stored model은 기본값 없음으로 복귀한다.
  artifacts:
    - src/components/settings/settings-panel-placeholder.tsx
    - src/components/ui/alert-dialog.tsx
    - src/components/ui/select.tsx
    - src/routes/settings.tsx
  key_links:
    - settings route가 service/query 계층을 읽어 각 panel의 saved state를 렌더링한다.
    - alert dialog가 delete confirmation policy를 담당한다.
    - select UI가 Phase 3의 model selection에도 재사용 가능한 형태로 준비된다.
---

<objective>
설정 화면을 실제 API 키/기본 설정 관리 화면으로 전환한다.

Purpose: `/settings`에서 키 교체/삭제와 기본 모델/시스템 프롬프트 저장을 모두 처리해 Phase 2 요구사항을 닫는다.
Output: real settings panels, delete confirmation dialog, default model selector.
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app.worktrees/frontend-codex/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app.worktrees/frontend-codex/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-api-key-and-settings-management/02-CONTEXT.md
@.planning/phases/01-app-shell-and-interface-foundation/01-UI-SPEC.md
@src/components/settings/settings-panel-placeholder.tsx
@src/routes/settings.tsx
@src/hooks/use-settings-query.ts
@src/hooks/use-free-models-query.ts
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: API 키 관리 panel과 삭제 확인 다이얼로그를 구현한다</name>
  <files>src/components/settings/settings-panel-placeholder.tsx, src/components/ui/alert-dialog.tsx, src/routes/settings.tsx</files>
  <read_first>src/components/settings/settings-panel-placeholder.tsx, src/routes/settings.tsx, .planning/phases/02-api-key-and-settings-management/02-CONTEXT.md, .planning/phases/01-app-shell-and-interface-foundation/01-UI-SPEC.md, src/hooks/use-api-key-validation.ts, src/hooks/use-settings-query.ts</read_first>
  <action>placeholder settings panel을 실제 data-aware panel로 바꾸고, official shadcn `AlertDialog` primitive를 `src/components/ui/alert-dialog.tsx`로 추가한다. settings 화면에서는 저장된 key 전체를 다시 노출하지 말고 `현재 저장된 API 키가 있습니다.` 같은 saved-state helper와 교체용 password input을 사용한다. `키 확인하기`는 validation success 이후에만 key를 저장/교체하고, failure에서는 draft만 유지한다. 삭제 버튼은 title `API 키 삭제`, body `저장된 OpenRouter API 키를 삭제하면 채팅을 시작하기 전에 다시 입력해야 합니다. 삭제할까요?`를 가진 확인 다이얼로그를 연 뒤 확정 시 즉시 삭제하고 success toast를 띄운다. 삭제 후 `/settings` route에서 navigate를 호출하지 말고 같은 화면에 남아 saved-state helper만 비운다.</action>
  <verify>pnpm build</verify>
  <acceptance_criteria>
    - src/components/ui/alert-dialog.tsx contains `AlertDialog`
    - src/components/settings/settings-panel-placeholder.tsx contains `현재 저장된 API 키가 있습니다.`
    - src/components/settings/settings-panel-placeholder.tsx contains `API 키 삭제`
    - src/components/settings/settings-panel-placeholder.tsx contains `저장된 OpenRouter API 키를 삭제하면`
    - src/routes/settings.tsx contains `SettingsPanelPlaceholder`
  </acceptance_criteria>
  <done>settings route에서 key 교체와 delete confirmation 정책이 구현된다.</done>
</task>

<task type="auto">
  <name>Task 2: 기본 모델과 시스템 프롬프트 즉시 저장 UI를 구현한다</name>
  <files>src/components/settings/settings-panel-placeholder.tsx, src/components/ui/select.tsx</files>
  <read_first>src/components/settings/settings-panel-placeholder.tsx, .planning/phases/02-api-key-and-settings-management/02-CONTEXT.md, src/hooks/use-free-models-query.ts, src/hooks/use-settings-query.ts, .planning/phases/01-app-shell-and-interface-foundation/01-UI-SPEC.md</read_first>
  <action>`src/components/ui/select.tsx`에 official shadcn select primitive를 추가하고 settings 화면의 기본 모델 panel에서 이를 사용한다. saved API key가 없으면 model select는 disabled 상태와 안내 문구를 보여주고, saved key가 있으면 free models query 결과만 option으로 노출한다. model을 고르면 즉시 `defaultModelId`를 저장하고, query 결과에 없는 stored model은 `기본값은 아직 선택되지 않았습니다.` 상태로 되돌린다. 시스템 프롬프트 textarea는 `이 설정은 새 대화의 기본값으로만 사용됩니다.` helper를 두고 onChange마다 `defaultSystemPrompt`를 즉시 저장한다. theme panel은 기존 ui-store 동작을 유지하되 settings persistence와 충돌하지 않게 그대로 둔다.</action>
  <verify>pnpm build</verify>
  <acceptance_criteria>
    - src/components/ui/select.tsx contains `Select`
    - src/components/settings/settings-panel-placeholder.tsx contains `기본값은 아직 선택되지 않았습니다.`
    - src/components/settings/settings-panel-placeholder.tsx contains `이 설정은 새 대화의 기본값으로만 사용됩니다.`
    - src/components/settings/settings-panel-placeholder.tsx contains `disabled`
    - src/components/settings/settings-panel-placeholder.tsx contains `defaultSystemPrompt`
  </acceptance_criteria>
  <done>settings 화면에서 default model/system prompt를 저장 버튼 없이 즉시 저장할 수 있다.</done>
</task>

</tasks>

<verification>
- `pnpm build`가 settings route 데이터 연결 후 통과한다.
- `/settings`에서 key 저장/교체/삭제와 default model/system prompt 저장이 모두 가능한 구조가 된다.
- delete confirmation과 invalid default model fallback copy가 코드에 명시된다.
</verification>

<success_criteria>
- 사용자는 `/settings`에서 Phase 2의 모든 설정 요구사항을 수행할 수 있다.
- 강제 route 이동 없이 현재 설정 화면에서 상태만 갱신된다.
- 기본 모델과 시스템 프롬프트는 새 대화의 초기값으로만 저장되는 전제가 유지된다.
</success_criteria>

<output>
After completion, create `.planning/phases/02-api-key-and-settings-management/02-04-SUMMARY.md`
</output>
