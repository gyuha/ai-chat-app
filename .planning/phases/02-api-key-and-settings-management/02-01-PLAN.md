---
phase: 02-api-key-and-settings-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src/App.tsx
  - src/lib/app-db.ts
  - src/providers/app-query-provider.tsx
  - src/test/setup.ts
autonomous: true
requirements:
  - DATA-02
  - SETT-02
must_haves:
  truths:
    - 브라우저에서 설정 영속화를 담당하는 Dexie database와 React Query provider가 앱 엔트리에 연결된다.
    - 테스트 환경에서도 IndexedDB 기반 설정 로직을 실제와 가깝게 검증할 수 있다.
  artifacts:
    - package.json
    - src/lib/app-db.ts
    - src/providers/app-query-provider.tsx
    - src/App.tsx
    - src/test/setup.ts
  key_links:
    - src/lib/app-db.ts가 DB 이름 `openrouter-chat-db`와 settings store를 단일 source of truth로 정의한다.
    - src/App.tsx가 RouterProvider를 AppQueryProvider로 감싼다.
    - src/test/setup.ts가 fake IndexedDB를 등록해 이후 settings 테스트가 실제 Dexie 코드를 사용하게 만든다.
---

<objective>
Phase 2의 저장소/캐시 기반을 추가한다.

Purpose: API 키와 기본 설정을 로컬에 저장하고, 이후 plan들이 동일한 persistence/query 기반 위에서 검증 흐름을 누적 구현할 수 있게 한다.
Output: Dexie database, Query provider, 테스트용 IndexedDB bootstrap.
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app.worktrees/frontend-codex/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app.worktrees/frontend-codex/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-api-key-and-settings-management/02-CONTEXT.md
@AGENTS.md
@package.json
@src/App.tsx
@src/test/setup.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dexie settings 저장소와 의존성을 추가한다</name>
  <files>package.json, pnpm-lock.yaml, src/lib/app-db.ts</files>
  <read_first>package.json, .planning/phases/02-api-key-and-settings-management/02-CONTEXT.md, .planning/REQUIREMENTS.md</read_first>
  <action>package.json에 `dexie`, `@tanstack/react-query`, `fake-indexeddb`를 추가하고 lockfile을 갱신한다. `src/lib/app-db.ts`에는 Dexie subclass를 만들어 DB 이름을 정확히 `openrouter-chat-db`로 고정하고 `settings: 'key'` 스토어를 정의한다. settings key는 최소한 `openRouterApiKey`, `defaultModelId`, `defaultSystemPrompt`를 다룰 수 있는 typed helper를 export하고, Phase 2 executor가 이 파일을 기준으로 read/write helper를 확장할 수 있게 한다. IndexedDB 외 localStorage fallback을 새로 만들지 말고 settings persistence의 단일 source of truth는 Dexie로 유지한다.</action>
  <verify>pnpm install</verify>
  <acceptance_criteria>
    - package.json contains `"dexie"`
    - package.json contains `"@tanstack/react-query"`
    - package.json contains `"fake-indexeddb"`
    - src/lib/app-db.ts contains `openrouter-chat-db`
    - src/lib/app-db.ts contains `settings: 'key'`
    - src/lib/app-db.ts contains `openRouterApiKey`
    - src/lib/app-db.ts contains `defaultModelId`
    - src/lib/app-db.ts contains `defaultSystemPrompt`
  </acceptance_criteria>
  <done>앱 코드와 테스트 코드가 동일한 Dexie settings 저장소를 바라볼 수 있다.</done>
</task>

<task type="auto">
  <name>Task 2: Query provider와 test bootstrap을 앱 엔트리에 연결한다</name>
  <files>src/providers/app-query-provider.tsx, src/App.tsx, src/test/setup.ts</files>
  <read_first>src/App.tsx, src/test/setup.ts, src/router.tsx, .planning/phases/02-api-key-and-settings-management/02-CONTEXT.md</read_first>
  <action>`src/providers/app-query-provider.tsx`를 만들어 singleton QueryClient를 제공하고, src/App.tsx에서 RouterProvider를 이 provider로 감싼다. Query defaults는 free models cache 재사용을 고려해 query provider가 재사용 가능하도록 구성하되, Phase 2에서는 devtools를 추가하지 않는다. src/test/setup.ts에는 `fake-indexeddb/auto`와 필요한 IndexedDB globals를 등록해 Dexie가 JSDOM에서도 동작하게 하고, afterEach에서 DB 정리 helper를 호출할 수 있는 자리도 확보한다.</action>
  <verify>pnpm build</verify>
  <acceptance_criteria>
    - src/providers/app-query-provider.tsx contains `QueryClientProvider`
    - src/providers/app-query-provider.tsx contains `new QueryClient`
    - src/App.tsx contains `<AppQueryProvider>`
    - src/App.tsx contains `<RouterProvider router={router} />`
    - src/test/setup.ts contains `fake-indexeddb/auto`
  </acceptance_criteria>
  <done>Query provider와 IndexedDB test bootstrap이 이후 plan에서 바로 재사용 가능한 상태가 된다.</done>
</task>

</tasks>

<verification>
- `pnpm install`이 새 의존성을 포함해 완료된다.
- `pnpm build`가 provider 추가 후에도 통과한다.
- Dexie settings store와 Query provider가 엔트리에서 연결된다.
</verification>

<success_criteria>
- Phase 2 executor는 Dexie와 Query provider를 새로 설계하지 않고 바로 feature 로직에 집중할 수 있다.
- settings persistence는 `openrouter-chat-db`/`settings` store 하나로 고정된다.
- 테스트 환경이 실제 IndexedDB 기반 구현을 검증할 준비가 된다.
</success_criteria>

<output>
After completion, create `.planning/phases/02-api-key-and-settings-management/02-01-SUMMARY.md`
</output>
