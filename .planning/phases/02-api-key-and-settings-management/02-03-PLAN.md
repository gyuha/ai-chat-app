---
phase: 02-api-key-and-settings-management
plan: 03
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - src/components/chat/api-key-onboarding-card.tsx
  - src/components/chat/chat-empty-state.tsx
  - src/routes/index.tsx
autonomous: true
requirements:
  - SETT-01
  - SETT-03
  - DATA-02
must_haves:
  truths:
    - 저장된 API 키가 없을 때 `/`는 온보딩 카드를 렌더링하고, 저장된 키가 있으면 빈 상태 홈을 렌더링한다.
    - 온보딩 카드에서 검증 성공 시 현재 route를 유지한 채 빈 상태 홈으로 즉시 전환된다.
    - 검증 실패 시 draft 입력값은 남고, 오류는 인라인 메시지와 토스트로 동시에 전달된다.
  artifacts:
    - src/components/chat/api-key-onboarding-card.tsx
    - src/components/chat/chat-empty-state.tsx
    - src/routes/index.tsx
  key_links:
    - src/routes/index.tsx가 saved settings 존재 여부에 따라 onboarding/empty-state 분기를 담당한다.
    - api-key-onboarding-card가 validation mutation과 toast를 함께 사용한다.
    - chat-empty-state는 성공 후 redirect 없이 그대로 재사용된다.
---

<objective>
홈 route의 온보딩 검증/저장/전환 흐름을 구현한다.

Purpose: 사용자가 첫 진입에서 API 키를 검증하고 저장한 뒤 바로 빈 상태 홈으로 넘어가는 v1 시작 경험을 완성한다.
Output: controlled onboarding card, route branching, success/error transition.
</objective>

<execution_context>
@/Users/gyuha/workspace/ai-chat-app.worktrees/frontend-codex/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/workspace/ai-chat-app.worktrees/frontend-codex/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-api-key-and-settings-management/02-CONTEXT.md
@.planning/phases/01-app-shell-and-interface-foundation/01-UI-SPEC.md
@src/components/chat/api-key-onboarding-card.tsx
@src/components/chat/chat-empty-state.tsx
@src/routes/index.tsx
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: API 키 온보딩 카드를 controlled form과 상태 피드백으로 바꾼다</name>
  <files>src/components/chat/api-key-onboarding-card.tsx</files>
  <read_first>src/components/chat/api-key-onboarding-card.tsx, .planning/phases/02-api-key-and-settings-management/02-CONTEXT.md, .planning/phases/01-app-shell-and-interface-foundation/01-UI-SPEC.md, src/hooks/use-api-key-validation.ts</read_first>
  <action>온보딩 카드를 controlled password input, `모델 목록 확인` 버튼, aria-live status 영역을 가진 form으로 바꾼다. 버튼 클릭 시에만 validation mutation을 실행하고, pending 동안에는 input 값은 유지한 채 버튼만 disabled/loading 상태로 둔다. 성공 메시지는 인라인 status와 toast를 함께 띄우고, 실패 메시지는 정확히 두 갈래로만 노출한다. invalid/권한 문제에는 `입력한 키로 모델 목록을 불러오지 못했습니다. 키를 확인한 뒤 다시 시도해 주세요.`, transient 문제에는 `일시적인 문제로 모델 목록을 확인하지 못했습니다. 잠시 후 다시 시도해 주세요.`를 사용한다. 상태 표현은 색만으로 전달하지 말고 icon/text를 함께 사용한다.</action>
  <verify>pnpm build</verify>
  <acceptance_criteria>
    - src/components/chat/api-key-onboarding-card.tsx contains `모델 목록 확인`
    - src/components/chat/api-key-onboarding-card.tsx contains `aria-live="polite"`
    - src/components/chat/api-key-onboarding-card.tsx contains `입력한 키로 모델 목록을 불러오지 못했습니다.`
    - src/components/chat/api-key-onboarding-card.tsx contains `일시적인 문제로 모델 목록을 확인하지 못했습니다.`
    - src/components/chat/api-key-onboarding-card.tsx contains `disabled=`
  </acceptance_criteria>
  <done>온보딩 카드가 클릭 기반 validation과 인라인/토스트 피드백 정책을 그대로 따른다.</done>
</task>

<task type="auto">
  <name>Task 2: 홈 route를 saved settings 기반 onboarding/empty-state 분기로 연결한다</name>
  <files>src/routes/index.tsx, src/components/chat/chat-empty-state.tsx</files>
  <read_first>src/routes/index.tsx, src/components/chat/chat-empty-state.tsx, src/hooks/use-settings-query.ts, .planning/phases/02-api-key-and-settings-management/02-CONTEXT.md</read_first>
  <action>index route는 saved API key 유무를 읽어 없는 경우 ApiKeyOnboardingCard를, 있는 경우 ChatEmptyState를 렌더링하도록 바꾼다. 검증/저장 성공 후에는 navigate를 호출하지 말고 같은 `/` 화면에서 query invalidate 또는 local state 갱신으로 빈 상태 홈으로 즉시 전환한다. ChatEmptyState copy는 Phase 1 UI contract를 유지하되 saved key가 있을 때만 노출되게 한다. saved key 삭제 후 다시 `/`를 방문하면 자동으로 onboarding 카드로 복귀하는 구조를 route level에서 보장한다.</action>
  <verify>pnpm build</verify>
  <acceptance_criteria>
    - src/routes/index.tsx contains `ChatEmptyState`
    - src/routes/index.tsx contains `ApiKeyOnboardingCard`
    - src/routes/index.tsx contains `saved`
    - src/components/chat/chat-empty-state.tsx contains `첫 대화를 시작해 보세요`
    - src/components/chat/chat-empty-state.tsx contains `설정 보기`
  </acceptance_criteria>
  <done>`/` route가 API key 저장 상태에 따라 올바른 surface를 즉시 바꾼다.</done>
</task>

</tasks>

<verification>
- `pnpm build`가 홈 route 분기 추가 후 통과한다.
- 저장된 key 없음/있음 상태가 모두 `/`에서 렌더링 가능하다.
- 성공은 같은 route에서 empty state로 전환되고 실패는 draft를 남긴다.
</verification>

<success_criteria>
- 사용자는 첫 진입에서 API 키를 검증하고 저장할 수 있다.
- 사용자는 성공 직후 다른 페이지 이동 없이 바로 다음 빈 상태를 본다.
- 오류는 inline + toast 패턴과 접근성 조건을 유지한다.
</success_criteria>

<output>
After completion, create `.planning/phases/02-api-key-and-settings-management/02-03-SUMMARY.md`
</output>
