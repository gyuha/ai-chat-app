describe('AuthService contract', () => {
  it.todo('hashes passwords before persistence for auth/login and auth/signup flows')
  it.todo('issues JWT access and refresh credentials without leaking JWT_ACCESS_SECRET')
  it.todo('restores session state for auth/session without exposing JWT_REFRESH_SECRET')
})
